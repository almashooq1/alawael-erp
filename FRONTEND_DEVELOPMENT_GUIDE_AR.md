# تطوير الواجهة الأمامية - دليل شامل

## 📋 نظرة عامة

هذا الدليل يشرح كيفية استخدام وتطوير الواجهة الأمامية مع دعم كامل للغة العربية
وسهولة تعديل التبويبات والقوائم.

---

## 🎯 الميزات الرئيسية

### 1. دعم كامل للغة العربية (RTL)

- ✅ دعم RTL/LTR تلقائي
- ✅ تبديل بين العربية والإنجليزية بسهولة
- ✅ جميع النصوص محفوظة في configuration مركزية

### 2. تنقل سهل وقابل للتخصيص

- ✅ قائمة جانبية احترافية (Sidebar)
- ✅ شريط علوي محسّن (TopBar)
- ✅ تبويبات ديناميكية
- ✅ دعم الأجهزة المحمولة

### 3. إدارة أدوار المستخدمين (RBAC)

- ✅ عرض القوائم بناءً على دور المستخدم
- ✅ إخفاء/إظهار العناصر تلقائياً

### 4. تصميم حديث وسلس

- ✅ ألوان متناسقة
- ✅ animations محسنة
- ✅ استجابة كاملة للهواتف الذكية

---

## 📁 بنية المشروع

```
frontend/src/
├── config/
│   └── navigationConfig.js          # 🔧 ملف التكوين الرئيسي (تعديل القوائم من هنا)
├── components/
│   └── layout/
│       ├── Layout.jsx               # المخطط الرئيسي
│       ├── Sidebar.jsx              # القائمة الجانبية
│       ├── TopBar.jsx               # الشريط العلوي
│       ├── Layout.css
│       ├── Sidebar.css
│       └── TopBar.css
├── pages/
│   └── Dashboard.jsx                # لوحة التحكم مع التبويبات
└── i18n/
    └── config.js                    # إعدادات اللغة
```

---

## 🔧 كيفية تعديل القوائم والتبويبات

### أولاً: إضافة عنصر قائمة جديد

اذهب إلى `src/config/navigationConfig.js`:

```javascript
// إضافة عنصر جديد إلى MAIN_MENU_ITEMS
{
  id: 'myFeature',
  text: 'الميزة الجديدة',           // النص بالعربية
  textEn: 'My Feature',              // النص بالإنجليزية
  icon: MyIcon,                      // الأيقونة
  path: '/my-feature',               // المسار
  description: 'وصف الميزة',         // الوصف بالعربية
  descriptionEn: 'Feature Description', // الوصف بالإنجليزية
  order: 18,                         // الترتيب
  enabled: true,                     // هل مفعل؟
  roles: ['admin', 'manager'],       // الأدوار المسموحة
}
```

### ثانياً: تغيير ترتيب القوائم

عدّل حقل `order` في كل عنصر:

```javascript
// الترتيب الحالي:
// 1 = لوحة التحكم (الأول)
// 2 = المستخدمين (الثاني)
// ...إلخ
```

### ثالثاً: إخفاء عناصر القائمة

```javascript
// لتعطيل عنصر:
{
  ...item,
  enabled: false,  // سيتم إخفاؤه من القائمة
}
```

### رابعاً: تحديد الأدوار المسموحة

```javascript
{
  ...item,
  roles: ['admin'],           // فقط المديرين
  // أو
  roles: ['admin', 'manager'], // المديرين والمسؤولين
  // أو
  roles: ['admin', 'manager', 'user'], // الجميع
}
```

---

## 🎨 تخصيص التبويبات

### تعديل تبويبات Dashboard

في `src/config/navigationConfig.js`:

```javascript
export const DASHBOARD_TABS = [
  {
    id: 'overview',
    label: 'نظرة عامة',
    labelEn: 'Overview',
    value: 0,
    icon: DashboardIcon,
  },
  // أضف تبويبات جديدة هنا
  {
    id: 'myTab',
    label: 'تبويبي الجديد',
    labelEn: 'My Tab',
    value: 1,
    icon: MyTabIcon,
  },
];
```

### إضافة محتوى جديد للتبويبات

في `src/pages/Dashboard.jsx`:

```javascript
// ابحث عن DashboardTabsSection
// أضف TabPanel جديد:

<TabPanel value={activeTab} index={4}>
  <Card sx={{ p: 3 }}>
    <Typography variant="h6">محتوى التبويب الجديد</Typography>
    {/* محتواك هنا */}
  </Card>
</TabPanel>
```

---

## 🌐 إدارة اللغات

### تبديل اللغة تلقائياً

```javascript
// في أي مكون:
const { language } = useSelector(state => state.settings);

// استخدام:
<Typography>{language === 'en' ? 'English Text' : 'النص بالعربية'}</Typography>;
```

### استخدام النصوص من Configuration

```javascript
import { MAIN_MENU_ITEMS } from '../config/navigationConfig';

const items = MAIN_MENU_ITEMS.map(item => ({
  ...item,
  displayText: language === 'en' ? item.textEn : item.text,
}));
```

---

## 📱 الاستجابة للهواتف الذكية

جميع المكونات مستجيبة بالفعل:

```javascript
// الـ Sidebar يختفي تلقائياً على الهواتف
display: { xs: 'none', sm: 'block' }

// التبويبات تتحول إلى horizontal scroll على الهواتف
variant="scrollable"
scrollButtons="auto"
```

---

## 🎯 أمثلة عملية

### مثال 1: إضافة قائمة جديدة "إدارة المشاريع"

```javascript
// في navigationConfig.js
{
  id: 'projects',
  text: 'إدارة المشاريع',
  textEn: 'Projects',
  icon: FolderIcon,
  path: '/projects',
  description: 'إدارة جميع المشاريع',
  descriptionEn: 'Manage all projects',
  order: 5,
  enabled: true,
  roles: ['admin', 'manager', 'user'],
}
```

### مثال 2: عرض قائمة فقط للمديرين

```javascript
{
  id: 'reports',
  text: 'التقارير المتقدمة',
  textEn: 'Advanced Reports',
  icon: AnalyticsIcon,
  path: '/reports-advanced',
  order: 20,
  enabled: true,
  roles: ['admin', 'manager'],  // فقط المديرين
}
```

### مثال 3: إضافة تبويب جديد مع محتوى

```javascript
// 1. أضف في navigationConfig.js:
{
  id: 'custom',
  label: 'التخصيص',
  labelEn: 'Customization',
  value: 5,
  icon: SettingsIcon,
}

// 2. أضف في Dashboard.jsx:
<TabPanel value={activeTab} index={5}>
  <Grid container spacing={3}>
    {/* محتواك */}
  </Grid>
</TabPanel>
```

---

## 🎨 تخصيص الألوان والأنماط

### تغيير ألوان الـ TopBar

في `src/components/layout/TopBar.css`:

```css
.topbar {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  /* غيّر الألوان هنا */
}
```

### تغيير ألوان Sidebar

في `src/components/layout/Sidebar.css`:

```css
.menu-button.active {
  background-color: #e3f2fd;
  color: #1976d2;
}
```

---

## 🔍 استكشاف الأخطاء

### المشكلة: القوائم لا تظهر

**الحل:**

1. تحقق من `enabled: true` في navigationConfig.js
2. تحقق من `roles` هل تشمل دور المستخدم الحالي

### المشكلة: النصوص لم تترجم

**الحل:**

1. استخدم `language === 'en' ? textEn : text`
2. لا تنسَ حفظ الملف

### المشكلة: التبويبات لا تعمل

**الحل:**

1. تحقق من عدد TabPanel يطابق عدد Tabs
2. تحقق من قيم `value` و`index` متطابقة

---

## 📚 الملفات الرئيسية للتعديل

| الملف                 | الوصف            | التعديلات الشائعة       |
| --------------------- | ---------------- | ----------------------- |
| `navigationConfig.js` | ⭐ الأهم         | إضافة/حذف/ترتيب القوائم |
| `Layout.jsx`          | المخطط الرئيسي   | تغيير البنية العامة     |
| `Sidebar.jsx`         | القائمة الجانبية | تعديل المظهر            |
| `TopBar.jsx`          | الشريط العلوي    | البحث والإشعارات        |
| `Dashboard.jsx`       | لوحة التحكم      | التبويبات والمحتوى      |
| `*.css`               | الأنماط          | الألوان والتخطيط        |

---

## 🚀 نصائح للأداء الأفضل

1. **استخدم lazy loading للصفحات الثقيلة**

   ```javascript
   const HeavyComponent = React.lazy(() => import('./HeavyComponent'));
   ```

2. **استخدم memoization للمكونات الكبيرة**

   ```javascript
   export default React.memo(MyComponent);
   ```

3. **تجنب re-renders غير الضرورية**
   ```javascript
   const memoizedValue = useMemo(() => expensiveComputation(), [dep]);
   ```

---

## 🔐 الأمان والأفضليات

1. ✅ تحقق دائماً من الأدوار قبل عرض الميزات
2. ✅ استخدم environment variables للبيانات الحساسة
3. ✅ تجنب تخزين passwords في localStorage
4. ✅ استخدم HTTPS في الإنتاج

---

## 📞 الدعم والمساعدة

للمزيد من المعلومات:

- 📧 البريد: support@alawael.com
- 📱 الهاتف: +20 100 123 4567
- 🌐 الموقع: www.alawael.com

---

## ✅ Checklist للتطوير

- [ ] تحديث navigationConfig.js
- [ ] اختبار العنصر الجديد
- [ ] التحقق من الترجمة (العربية والإنجليزية)
- [ ] اختبار على الهاتف الذكي
- [ ] التحقق من الأدوار والصلاحيات
- [ ] حفظ التغييرات

---

**آخر تحديث:** يناير 22، 2026 **الإصدار:** 2.0.0 **الحالة:** ✅ جاهز للإنتاج
