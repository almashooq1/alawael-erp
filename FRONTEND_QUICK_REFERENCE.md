# ⚡ Frontend Quick Reference - مرجع سريع

## 🎯 أهم الملفات للتعديل

| الملف | المسار | الفائدة |
|------|--------|---------|
| 📋 navigationConfig.js | `src/config/` | ⭐ إضافة/تعديل القوائم والتبويبات |
| 🎨 Dashboard.jsx | `src/pages/` | تعديل محتوى لوحة التحكم |
| 🎨 Sidebar.jsx | `src/components/layout/` | تخصيص القائمة الجانبية |
| 🎨 TopBar.jsx | `src/components/layout/` | تخصيص الشريط العلوي |

---

## ⚙️ إضافة عنصر قائمة جديد (5 ثوان)

### الخطوة 1: افتح navigationConfig.js
```
src/config/navigationConfig.js
```

### الخطوة 2: جد MAIN_MENU_ITEMS
```javascript
export const MAIN_MENU_ITEMS = [
  // العناصر الموجودة
  ...
]
```

### الخطوة 3: أضف عنصرك
```javascript
{
  id: 'myfeature',
  text: 'ميزتي',
  textEn: 'My Feature',
  icon: MyIcon,
  path: '/myfeature',
  description: 'وصف',
  descriptionEn: 'Description',
  order: 18,
  enabled: true,
  roles: ['admin'],
}
```

### خلاص! ✅

---

## 🔄 تغيير ترتيب القوائم

```javascript
// الترتيب الحالي:
order: 1   // يظهر أولاً
order: 2   // يظهر ثانياً
order: 3   // يظهر ثالثاً

// لإعادة ترتيب: غيّر الأرقام!
```

---

## 🌐 إضافة لغة جديدة (إذا أردت)

### الخطوة 1: أضف النصوص بلغتك
```javascript
{
  ...item,
  textFr: 'Ma Fonctionnalité',  // فرنسي
  descriptionFr: 'Description française',
}
```

### الخطوة 2: استخدمها
```javascript
displayText: language === 'fr' ? item.textFr : item.text
```

---

## 👥 إخفاء عنصر عن دور معين

```javascript
// لعرض فقط للمديرين:
roles: ['admin', 'manager']

// لعرض الجميع:
roles: ['admin', 'manager', 'user']

// لعرض فقط للمسؤولين:
roles: ['admin']
```

---

## 🎚️ إضافة تبويب جديد

### الخطوة 1: في navigationConfig.js
```javascript
export const DASHBOARD_TABS = [
  {
    id: 'newtab',
    label: 'تبويبي',
    labelEn: 'My Tab',
    value: 4,  // رقم فريد
    icon: MyTabIcon,
  },
]
```

### الخطوة 2: في Dashboard.jsx
```javascript
<TabPanel value={activeTab} index={4}>
  {/* محتواك هنا */}
</TabPanel>
```

---

## 🔍 البحث عن مكان معين

| أبحث عن | ابحث في |
|---------|---------|
| تغيير الألوان | `*.css` و `theme/` |
| تغيير النصوص | `navigationConfig.js` و `i18n/` |
| تغيير التخطيط | `Layout.jsx` و `*.css` |
| إضافة مكون | `components/` |

---

## 🚀 الأوامر المهمة

```bash
# تطوير
npm start

# بناء الإنتاج
npm run build

# اختبارات
npm test

# تحليل الأخطاء
npm run lint
```

---

## 🎨 تخصيص الألوان

### غيّر في navigationConfig.js:
```javascript
// الألوان الحالية
#667eea (Primary Blue)
#764ba2 (Purple)
#4CAF50 (Green)
#FF9800 (Orange)

// استخدم ألونك في:
- statCard color props
- sx={{ color: 'yourColor' }}
```

---

## 🔐 السلامة والأفضليات

✅ استخدم roles دائماً
✅ تحقق من enabled قبل الاستخدام
✅ استخدم i18n للترجمة
✅ اختبر على الهواتف الذكية

---

## 📊 الأدوار المتاحة

| الدور | الصلاحيات |
|------|----------|
| admin | كل شيء |
| manager | إدارة وتقارير |
| user | عرض فقط |
| teacher | تعليم |
| driver | مركبات |
| support | دعم فني |
| finance | محاسبة |

---

## 💡 نصائح سريعة

1. **احفظ بعد التعديل** ⚡
2. **اختبر على الفور** 🧪
3. **استخدم order لترتيب سريع** 📊
4. **استخدم enabled لتعطيل مؤقت** ⏸️
5. **استخدم roles للسلامة** 🔐

---

## ❌ المشاكل الشائعة

### المشكلة: القائمة لا تظهر
**الحل:** تحقق من `enabled: true` و `roles`

### المشكلة: النص لم ينتقل
**الحل:** استخدم `language === 'en' ? textEn : text`

### المشكلة: التبويب فارغ
**الحل:** أضف TabPanel مع index صحيح

---

## 📞 هل تحتاج مساعدة؟

```
📧 support@alawael.com
📱 +20 100 123 4567
🌐 www.alawael.com
```

---

**نصيحة ذهبية:** 
عدّل navigationConfig.js فقط للـ 90% من التخصيصات! 🌟

---

**آخر تحديث:** 22 يناير 2026
**v2.1.0** ✅
