# خطوات سريعة لحل مشاكل GitHub Copilot

## 🔧 الإصلاحات السريعة (Quick Fixes)

### المشكلة 1: Copilot لا يعمل بعد التثبيت

**الوقت المتوقع**: 2 دقيقة

```
1. اضغط Ctrl+Shift+X (فتح Extensions)
2. ابحث عن "GitHub Copilot"
3. إذا كانت الحالة "Install"، اضغط عليها
4. بعد التثبيت، أغلق VS Code تماماً
5. أعد فتح VS Code
6. اضغط Ctrl+Shift+P وابحث عن "GitHub Copilot: Sign In"
7. متابعة عملية تسجيل الدخول
```

---

### المشكلة 2: لا يظهر أي اقتراحات (لكن الإضافة مثبتة)

**الوقت المتوقع**: 3 دقائق

```
الخطوة 1: تفعيل محرر التعليقات
├─ افتح ملف JavaScript أو Python
├─ اكتب تعليق واضح: // دالة تحسب مجموع الأرقام
├─ اضغط Enter
└─ اذا ظهر اقتراح، المشكلة محلولة

الخطوة 2: إعادة تحميل
├─ اضغط Ctrl+Shift+P
├─ اكتب "Reload Window"
├─ اضغط Enter
└─ انتظر 10 ثوان

الخطوة 3: فحص الإضافة
├─ اضغط Ctrl+Shift+U (فتح Output)
├─ في القائمة العلوية، اختر "GitHub Copilot"
├─ ابحث عن أخطاء (بيانات حمراء)
└─ إذا رأيت أخطاء، انتقل للمشكلة التالية
```

---

### المشكلة 3: رسائل خطأ في Output

**الوقت المتوقع**: 5 دقائق

**رسالة خطأ شائعة**: "Authentication failed"

```
الحل:
1. اضغط Ctrl+Shift+P
2. اشف عن "GitHub Copilot: Sign Out"
3. ثم "GitHub Copilot: Sign In"
4. متابعة عملية تسجيل الدخول مرة أخرى
5. انتظر 30 ثانية
```

**رسالة خطأ**: "Request timeout"

```
الحل:
1. تحقق من اتصال الإنترنت
2. أغلق VPN إن كانت مفعلة (قد تسبب مشاكل)
3. أعد تحميل النافذة (Ctrl+Shift+P → Reload Window)
4. انتظر دقيقة واحدة قبل المحاولة مرة أخرى
```

---

### المشكلة 4: الاقتراحات بطيئة جداً

**الوقت المتوقع**: 3 دقائق

```
الخطوة 1: تعطيل الإضافات غير الضرورية
├─ اضغط Ctrl+Shift+X
├─ ابحث عن إضافات غير مستخدمة (مثل old themes)
├─ اضغط "Disable" على كل منها
└─ أعد تحميل النافذة

الخطوة 2: تقليل عدد الملفات المراقبة
├─ اضغط Ctrl+Shift+P
├─ ابحث عن "Preferences: Open Settings (JSON)"
├─ أضف هذا:
│  {
│    "files.watcherExclude": {
│      "**/node_modules": true,
│      "**/.git": true,
│      "**/dist": true,
│      "**/.venv": true
│    }
│  }
└─ أعد التحميل

الخطوة 3: تنظيف الذاكرة المؤقتة
├─ أغلق VS Code
├─ اضغط Windows + R
├─ اكتب: %APPDATA%\Code\Cache
├─ احذف جميع الملفات
└─ أعد فتح VS Code
```

---

### المشكلة 5: "Copilot غير متوفر" في بلدك

**الوقت المتوقع**: يعتمد على الحل

**الحل الأول**: استخدام VPN (يمكن أن يقلل الأداء)

```
1. اشترك في خدمة VPN موثوقة
2. قم بالاتصال بخادم في الولايات المتحدة أو أوروبا
3. أعد فتح VS Code
4. حاول تسجيل الدخول مرة أخرى
```

**الحل الثاني**: استخدام GitHub Copilot من خلال خادم وكيل

```
أضف في settings.json:
{
  "http.proxy": "http://proxy-address:port"
}
```

---

### المشكلة 6: "License not valid" أو "Subscription expired"

**الوقت المتوقع**: 2 دقيقة

```
الحل:
1. تسجيل الخروج والدخول من جديد
   ├─ Ctrl+Shift+P
   ├─ GitHub Copilot: Sign Out
   ├─ GitHub Copilot: Sign In
   
2. تحقق من اشتراك GitHub
   ├─ اذهب إلى https://github.com/settings/copilot
   ├─ تأكد من أن الاشتراك نشط
   ├─ أعد تحميل الصفحة
   
3. انتظر 5 دقائق ثم أعد المحاولة
```

---

### المشكلة 7: Copilot يقترح أشياء خاطئة أو غير صحيحة

**الوقت المتوقع**: لا يتطلب إصلاح - نصائح استخدام

```
هذا طبيعي! Copilot ليس مثالياً.

الحل:
1. استخدم Alt+] و Alt+[ للانتقال لاقتراحات أخرى
2. اضغط Escape لرفض الاقتراح
3. اكتب التعليق بوضوح أكثر
4. جرب إضافة سطر واحد من الكود الصحيح قبل التعليق

مثال:
// ✗ سيء - غير واضح
// معالجة البيانات
function process(data) {
  // الكود الذي يقترحه قد يكون خاطئ

// ✓ جيد - واضح جداً
// دالة للتحقق من صحة عنوان البريد الإلكتروني وإرجاع boolean
function validateEmail(email) {
  // الآن الاقتراح سيكون أفضل بكثير
}
```

---

## 🚨 الإصلاح الشامل (عندما لا شيء يعمل)

**الوقت المتوقع**: 15 دقيقة

### الخطوة 1: حفظ الإعدادات الحالية
```powershell
# افتح PowerShell كمسؤول
Copy-Item "$env:APPDATA\Code\User\settings.json" -Destination "settings.json.backup"
Copy-Item "$env:APPDATA\Code\User\keybindings.json" -Destination "keybindings.json.backup"
```

### الخطوة 2: حذف Copilot بالكامل
```powershell
# أغلق VS Code أولاً!

# حذف مجلد الإضافة
Remove-Item "$env:USERPROFILE\.vscode\extensions\github.copilot*" -Force -Recurse

# حذف البيانات المخزنة
Remove-Item "$env:APPDATA\Code\User\globalStorage\GitHub.copilot" -Force -Recurse

# تنظيف الذاكرة المؤقتة
Remove-Item "$env:APPDATA\Code\Cache" -Force -Recurse
```

### الخطوة 3: إعادة التثبيت
```
1. افتح VS Code
2. اضغط Ctrl+Shift+X
3. ابحث عن "GitHub Copilot"
4. اضغط Install
5. اضغط Sign In
6. انتظر دقيقة واحدة على الأقل
7. اختبر مع تعليق بسيط
```

---

## 📋 قائمة التحقق النهائية

قبل طلب المساعدة:

```
☐ تحديث VS Code إلى الإصدار الأخير
☐ تحديث GitHub Copilot إلى الإصدار الأخير
☐ تسجيل الدخول مرة أخرى (Sign Out → Sign In)
☐ إعادة تحميل النافذة (Ctrl+Shift+P → Reload Window)
☐ حذف وإعادة تثبيت الإضافة
☐ التحقق من اتصال الإنترنت
☐ التحقق من حالة الاشتراك (GitHub.com/settings/copilot)
☐ تنظيف الذاكرة المؤقتة
☐ تعطيل الإضافات الأخرى مؤقتاً
☐ محاولة الاتصال بـ GitHub Support
```

---

## 📞 الدعم الإضافي

إذا لم تحل أي من الحلول أعلاه المشكلة:

1. **زيارة GitHub Issues**: [github.com/github/copilot-docs/issues](https://github.com/github/copilot-docs/issues)

2. **توثيق GitHub الرسمي**: [docs.github.com/en/copilot](https://docs.github.com/en/copilot)

3. **فتح تذكرة دعم**: [support.github.com](https://support.github.com)

4. **مجتمع VS Code**: [Stack Overflow بعلامة copilot](https://stackoverflow.com/questions/tagged/copilot)

---

**تم التحديث**: فبراير 20, 2026
**آخر اختبار**: تم اختبار جميع الحلول على Windows 11 + VS Code 1.85.0
