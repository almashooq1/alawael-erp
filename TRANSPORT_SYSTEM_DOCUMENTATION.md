# نظام النقل والمواصلات للطلاب 🚌

## Student Transport and Shuttle Management System

---

## 📋 نظرة عامة (Overview)

نظام شامل واحترافي لإدارة النقل والمواصلات للطلاب يتضمن:

- ✅ إدارة الحافلات والسائقين
- ✅ إدارة المسارات والمحطات
- ✅ تسجيل الطلاب ومتابعة الحضور
- ✅ نظام الدفعات والفواتير
- ✅ تتبع GPS في الوقت الفعلي
- ✅ نظام الشكاوى والملاحظات
- ✅ التنبيهات والإشعارات الفورية
- ✅ التقارير والإحصائيات المتقدمة

---

## 🏗️ البنية المعمارية (Architecture)

### Backend Components

#### 1. **النماذج (Models)** - `transport.models.js`

```
Bus Model
├── معلومات الحافلة الأساسية
├── نظام GPS والتتبع
├── جدول الصيانة
└── الوثائق الرسمية

Driver Model
├── البيانات الشخصية
├── معلومات الرخصة
├── تسجيل الحضور
├── الانتهاكات والمخالفات
└── المؤهلات والشهادات

BusAssistant Model
├── بيانات المساعد
├── شهادات السلامة
└── حالة الفحص الأمني

Route Model
├── المسارات والمحطات
├── جداول التشغيل
├── معلومات التأجير
└── هندسة الطريق

StudentTransport Model
├── تسجيل الطالب
├── حالة الموافقة
├── معلومات الدفع
└── السجلات الطبية

TransportAttendance Model
├── تسجيل الحضور اليومي
├── أوقات الصعود والنزول
└── ملاحظات الحضور

TransportPayment Model
├── تسجيل الدفعات
├── طرق الدفع
├── حالة الدفع
└── الفواتير

TransportComplaint Model
├── تسجيل الشكاوى
├── الحالة والمتابعة
└── الحل والإجراءات

TripReport Model
├── تقارير الرحلات
├── استهلاك الوقود والمسافة
├── الحوادث والمشاكل
└── الفحوصات الأمنية

TransportNotification Model
├── التنبيهات والإشعارات
├── حالة القراءة
└── الأولويات
```

#### 2. **الخدمات (Services)** - `transport.services.js`

```
BusService
├── createBus() - إضافة حافلة
├── getAllBuses() - جلب الحافلات
├── getBusById() - حافلة واحدة
├── updateBus() - تحديث
├── deleteBus() - حذف
├── updateBusLocation() - تحديث GPS
└── addMaintenanceSchedule() - جدول صيانة

DriverService
├── createDriver() - إضافة سائق
├── getAllDrivers() - جلب السائقين
├── getDriverById() - سائق واحد
├── updateDriver() - تحديث
├── deleteDriver() - حذف
├── recordAttendance() - تسجيل الحضور
├── recordViolation() - تسجيل المخالفات
└── verifyLicenseValidity() - التحقق من الرخصة

RouteService
├── createRoute() - إضافة مسار
├── getAllRoutes() - جلب المسارات
├── getRouteById() - مسار واحد
├── updateRoute() - تحديث
├── calculateRouteFee() - حساب الرسوم
└── getActiveStops() - المحطات النشطة

StudentTransportService
├── registerStudent() - تسجيل الطالب
├── getStudentRegistrations() - جلب التسجيلات
├── approveRegistration() - الموافقة
├── cancelRegistration() - الإلغاء
├── updateStudentFee() - تحديث الرسوم
└── getStudentsInRoute() - الطلاب في المسار

AttendanceService
├── recordAttendance() - تسجيل الحضور
├── getAttendanceRecords() - سجل الحضور
└── calculateAttendanceRate() - معدل الحضور

PaymentService
├── recordPayment() - تسجيل الدفعة
├── getPaymentRecords() - سجل الدفعات
├── getOverdueAccounts() - الحسابات المتأخرة
└── getRevenueReport() - تقرير الإيرادات

ComplaintService
├── createComplaint() - إضافة شكوى
├── getComplaints() - جلب الشكاوى
├── updateComplaintStatus() - تحديث الحالة
└── getComplaintStatistics() - الإحصائيات

NotificationService
├── sendNotification() - إرسال تنبيه
├── getNotifications() - جلب التنبيهات
├── markAsRead() - تحديد كمقروء
└── sendBusDelayNotification() - تنبيه التأخير
```

#### 3. **APIs** - `transport.routes.js`

**15 API Endpoint**:

```
GET    /transport/buses - جلب جميع الحافلات
POST   /transport/buses - إضافة حافلة
GET    /transport/buses/:busId - حافلة واحدة
PUT    /transport/buses/:busId - تحديث الحافلة
DELETE /transport/buses/:busId - حذف الحافلة
POST   /transport/buses/:busId/location - تحديث GPS

GET    /transport/drivers - جلب جميع السائقين
POST   /transport/drivers - إضافة سائق
GET    /transport/drivers/:driverId - سائق واحد
PUT    /transport/drivers/:driverId - تحديث السائق
DELETE /transport/drivers/:driverId - حذف السائق
GET    /transport/drivers/:driverId/verify-license - التحقق من الرخصة

GET    /transport/routes - جلب المسارات
POST   /transport/routes - إضافة مسار
GET    /transport/routes/:routeId - مسار واحد
PUT    /transport/routes/:routeId - تحديث المسار

POST   /transport/student-registration - تسجيل الطالب
GET    /transport/student-registration - جلب التسجيلات
POST   /transport/student-registration/:id/approve - الموافقة
POST   /transport/student-registration/:id/cancel - الإلغاء

POST   /transport/attendance - تسجيل الحضور
GET    /transport/attendance/:studentTransportId - سجل الحضور
GET    /transport/attendance/:studentTransportId/rate - معدل الحضور

POST   /transport/payments - تسجيل الدفعة
GET    /transport/payments/:studentTransportId - سجل الدفعات
GET    /transport/payments/overdue/all - الحسابات المتأخرة
GET    /transport/payments/report/:month/:year - تقرير الإيرادات

POST   /transport/complaints - إضافة شكوى
GET    /transport/complaints - جلب الشكاوى
PUT    /transport/complaints/:id - تحديث الشكوى
GET    /transport/complaints/stats - إحصائيات

GET    /transport/notifications - جلب التنبيهات
POST   /transport/notifications/:id/read - تحديد كمقروء

GET    /transport/dashboard - لوحة التحكم
```

---

### Frontend Components

#### 1. **StudentTransportPage.js** - واجهة الطالب

```
✨ الميزات:
├── تسجيل في النقل
├── عرض حالة التسجيل
├── سجل الحضور والغياب
├── سجل الدفعات
├── معلومات المسار
├── بيانات السائق والحافلة
└── تحديثات الموقع GPS

📊 الجداول:
├── جدول التسجيلات
├── جدول الحضور
└── جدول الدفعات
```

#### 2. **ParentTransportPage.js** - واجهة ولي الأمر

```
✨ الميزات:
├── عرض تسجيل الطالب
├── متابعة حضور الطالب
├── عرض سجل الدفعات
├── تنبيهات تأخير الحافلة
├── معلومات السائق والحافلة
├── تقييم خدمة النقل
├── طلب المساعدة
└── الاتصال بالإدارة

📱 الإحصائيات السريعة:
├── التسجيلات النشطة
├── الدفعات المتأخرة
├── الحافلات المتاحة
└── التنبيهات الجديدة
```

#### 3. **DriverTransportPage.js** - واجهة السائق

```
✨ الميزات:
├── عرض الحافلة المخصصة
├── معلومات المسار
├── قائمة الطلاب
├── تسجيل الحضور والغياب
├── تقرير الرحلة
├── فحص الحافلة اليومي
├── تتبع GPS
├── الملاحة
└── التنبيهات

🔧 الإجراءات السريعة:
├── فحص الحافلة
├── قائمة الطلاب
├── تقرير الرحلة
└── الملاحة
```

#### 4. **AdminTransportPage.js** - لوحة التحكم الإدارية

```
✨ الميزات:
├── إدارة الحافلات (CRUD)
├── إدارة السائقين (CRUD)
├── إدارة المسارات (CRUD)
├── إدارة تسجيلات الطلاب
├── معالجة الشكاوى
├── متابعة الدفعات
└── التقارير والإحصائيات

📊 لوحة التحكم:
├── إجمالي الحافلات
├── إجمالي السائقين
├── إجمالي المسارات
├── الشكاوى المفتوحة
└── الإحصائيات التفصيلية
```

---

## 🚀 الميزات الرئيسية

### 1. **إدارة الحافلات**

- ✅ تسجيل وتتبع الحافلات
- ✅ نظام GPS في الوقت الفعلي
- ✅ جداول الصيانة الدورية
- ✅ معلومات التأمين والترخيص
- ✅ تتبع حالة الحافلة

### 2. **إدارة السائقين**

- ✅ تسجيل بيانات السائقين
- ✅ التحقق من صلاحية الرخصة
- ✅ تسجيل الحضور والغياب
- ✅ تسجيل المخالفات والانتهاكات
- ✅ نظام التقييم والأداء
- ✅ شهادات السلامة والتدريب

### 3. **إدارة المسارات**

- ✅ إنشاء وتحديث المسارات
- ✅ تحديد المحطات والفترات
- ✅ حساب الرسوم بناءً على المسافة
- ✅ جداول التشغيل المرنة
- ✅ حساب الوقت المتوقع

### 4. **تسجيل الطلاب**

- ✅ تسجيل الطالب في النقل
- ✅ نظام الموافقة المرحلي
- ✅ إدارة البيانات الطبية
- ✅ معلومات جهات الاتصال
- ✅ إدارة الرسوم والدفعات

### 5. **نظام الحضور**

- ✅ تسجيل الحضور اليومي
- ✅ حساب معدل الحضور
- ✅ تقارير الحضور الشهرية
- ✅ تتبع أوقات الصعود والنزول

### 6. **نظام الدفعات**

- ✅ تسجيل الدفعات
- ✅ حساب الأرصدة المتبقية
- ✅ طرق دفع متعددة
- ✅ الفواتير والإيصالات
- ✅ تقارير الإيرادات

### 7. **نظام الشكاوى**

- ✅ تقديم الشكاوى من الطلاب والآباء
- ✅ تصنيفات الشكاوى المختلفة
- ✅ متابعة الحل والإجراءات
- ✅ إحصائيات الشكاوى

### 8. **التنبيهات والإشعارات**

- ✅ تنبيهات تأخير الحافلة
- ✅ تنبيهات وصول الحافلة
- ✅ تنبيهات الدفعات المتأخرة
- ✅ إشعارات تسجيل الحضور
- ✅ إشعارات الطوارئ

### 9. **تقارير وإحصائيات**

- ✅ تقارير الحضور
- ✅ تقارير الدفعات والإيرادات
- ✅ تقارير الشكاوى
- ✅ إحصائيات الأداء
- ✅ تحليل استخدام الموارد

---

## 📱 واجهات المستخدم

### الأدوار المدعومة:

1. **الطالب** - StudentTransportPage
2. **ولي الأمر** - ParentTransportPage
3. **السائق** - DriverTransportPage
4. **المسؤول** - AdminTransportPage

### مزايا الواجهات:

- ✅ تصميم متجاوب (Responsive)
- ✅ دعم اللغة العربية كاملاً
- ✅ رموز بصرية واضحة
- ✅ جداول وقوائم منظمة
- ✅ نماذج سهلة التعبئة
- ✅ إشعارات فورية

---

## 🔗 التكامل مع النظام الرئيسي

### يتم إضافة المسارات التالية إلى `App.js`:

```javascript
// Transport Routes
import StudentTransportPage from './pages/StudentTransportPage';
import ParentTransportPage from './pages/ParentTransportPage';
import DriverTransportPage from './pages/DriverTransportPage';
import AdminTransportPage from './pages/AdminTransportPage';

// في App.js
<Route path="/student-transport" element={<StudentTransportPage />} />
<Route path="/parent-transport" element={<ParentTransportPage />} />
<Route path="/driver-transport" element={<DriverTransportPage />} />
<Route path="/admin-transport" element={<AdminTransportPage />} />
```

### يتم تسجيل API في `server.js`:

```javascript
const transportRoutes = require('./api/routes/transport.routes');
app.use('/api/transport', transportRoutes);
```

---

## 📊 نموذج البيانات

### العلاقات بين النماذج:

```
Bus ← Driver
  ↓
Route ← StudentTransport → TransportAttendance
           ↓
       TransportPayment

TransportComplaint → StudentTransport
TransportNotification → StudentTransport
TripReport → Bus, Route, Driver
```

---

## 🔐 الأمان والتحقق

### المميزات الأمنية:

- ✅ المصادقة عبر JWT
- ✅ التحكم في الوصول حسب الأدوار
- ✅ تشفير كلمات المرور
- ✅ التحقق من صلاحيات العمليات
- ✅ تسجيل الأنشطة والعمليات

---

## 💾 إدارة البيانات

### أنواع البيانات المخزنة:

**1. بيانات الحافلات:**

- معلومات الحافلة الأساسية
- موقع GPS الحالي
- سجل الصيانة
- الوثائق

**2. بيانات السائقين:**

- البيانات الشخصية
- معلومات الرخصة
- سجل الحضور
- المخالفات

**3. بيانات المسارات:**

- معلومات المسار
- المحطات
- الجداول
- الرسوم

**4. بيانات الطلاب:**

- معلومات التسجيل
- البيانات الطبية
- سجل الحضور
- سجل الدفعات

---

## 🛠️ التثبيت والإعداد

### 1. تثبيت الحزم المطلوبة:

```bash
npm install mongoose uuid express
```

### 2. تفعيل الـ Routes:

- قم بإضافة `import` لـ transport routes في `server.js`
- قم بتسجيل الـ routes في الـ Express app

### 3. إنشاء النماذج:

```javascript
const {
  Bus,
  Driver,
  BusAssistant,
  Route,
  StudentTransport,
  // ... باقي النماذج
} = require('./models/transport.models');
```

### 4. إضافة الصفحات إلى الـ Frontend:

- انسخ الملفات الأربعة إلى `frontend/src/pages/`
- أضف المسارات إلى `App.js`

---

## 📈 الإحصائيات والتقارير

### التقارير المتاحة:

1. **تقرير الحضور الشهري:**
   - معدل الحضور
   - الغيابات
   - الأعذار

2. **تقرير الدفعات:**
   - إجمالي الإيرادات
   - الحسابات المتأخرة
   - طرق الدفع

3. **تقرير الأداء:**
   - أداء السائقين
   - تقييمات الطلاب
   - الشكاوى والملاحظات

4. **تقرير الموارد:**
   - استخدام الحافلات
   - أيام التشغيل
   - معدل الامتلاء

---

## 🎯 الخطوات التالية

### تحسينات مستقبلية:

- [ ] تطبيق الجوال للطلاب وأولياء الأمور
- [ ] نظام الحجوزات المسبقة
- [ ] تكامل مع خرائط Google
- [ ] نظام الدفع الإلكترونية المتقدم
- [ ] تحليلات البيانات الموسعة
- [ ] نظام الذكاء الاصطناعي للتنبؤات
- [ ] تطبيق الواقع المعزز لمتابعة الحافلة
- [ ] نظام الصيانة الوقائية الذكية

---

## 📞 الدعم والمساعدة

للمزيد من المعلومات والدعم:

- 📧 البريد الإلكتروني: support@alawael.com
- 📱 الهاتف: +966-xxxxxxxxx
- 🌐 الموقع: https://alawael.com

---

**آخر تحديث:** 13 يناير 2026
**الإصدار:** 1.0.0
**الحالة:** ✅ جاهز للاستخدام الكامل

---

## 📄 الملفات المُنشأة:

✅ `/backend/models/transport.models.js` - النماذج الكاملة
✅ `/backend/services/transport.services.js` - الخدمات الكاملة
✅ `/backend/api/routes/transport.routes.js` - جميع الـ APIs
✅ `/frontend/src/pages/StudentTransportPage.js` - واجهة الطالب
✅ `/frontend/src/pages/ParentTransportPage.js` - واجهة ولي الأمر
✅ `/frontend/src/pages/DriverTransportPage.js` - واجهة السائق
✅ `/frontend/src/pages/AdminTransportPage.js` - لوحة التحكم
✅ `/TRANSPORT_SYSTEM_DOCUMENTATION.md` - هذا الملف

---

**🎉 نظام النقل والمواصلات جاهز بالكامل!**
