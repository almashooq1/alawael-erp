# 🚀 دليل التشغيل والاستخدام

## 🎯 البدء السريع

### المتطلبات

- Node.js 16+ و npm/yarn
- متصفح حديث (Chrome, Firefox, Safari)
- محرر نصوص (VS Code مفضل)

### خطوات التثبيت والتشغيل

```bash
# 1. التنقل إلى مجلد المشروع
cd frontend

# 2. تثبيت المكتبات
npm install

# 3. تشغيل الخادم الكامل
npm run dev

# 4. فتح المتصفح على
http://localhost:5173
```

---

## 📋 الصفحات المتاحة

### 1. لوحة التحكم (Dashboard)

**المسار:** `/dashboard` أو `/`

- ملخص سريع للبيانات الرئيسية
- إحصائيات عامة وأنشطة حديثة
- اختصارات سريعة للعمليات الشائعة
- رسوم بيانية للأداء

### 2. إدارة الطلاب (Students)

**المسار:** `/students`

- ✅ عرض جميع الطلاب في جدول
- ✅ بحث وتصفية فوري
- ✅ حذف جماعي للطلاب
- ✅ ربط مع DataTable وPinia

**الإجراءات:**

```
→ اضغط على صف لعرض التفاصيل
→ اضغط ✏️ للتعديل
→ اضغط 🗑️ للحذف
→ اضغط ➕ لإضافة طالب جديد
```

### 3. تفاصيل الطالب (Student Detail)

**المسار:** `/students/:id`

- معلومات كاملة عن الطالب
- عرض/تعديل البيانات
- نسب التقدم والحضور
- ملخص الإحصائيات
- ملاحظات شخصية

### 4. إضافة طالب جديد (Student Form)

**المسار:** `/students/new`

- نموذج كامل لإضافة طالب
- تحقق فوري من البيانات
- رسائل خطأ واضحة
- حفظ تلقائي والعودة

### 5. إدارة البرامج (Programs)

**المسار:** `/programs`

- قائمة جميع البرامج التدريبية
- إحصائيات البرامج
- معلومات التسجيل والسعة
- حذف جماعي

### 6. الجلسات (Sessions)

**المسار:** `/sessions`

- جدول الجلسات التدريبية
- مرشحات (برنامج، حالة، بحث)
- معلومات (تاريخ، وقت، مدرب، عدد)
- حالات مختلفة (مجدولة، جارية، مكتملة)

### 7. خطط التدريب (Plans)

**المسار:** `/plans`

- إدارة خطط التدريب الفردية
- عرض المدة والتقدم
- إحصائيات الخطط
- حالات النشطة والمكتملة

### 8. التقارير (Reports)

**المسار:** `/reports`

- تقارير شاملة وإحصائيات
- رسوم بيانية للأداء
- جداول التفاصيل
- خيارات التصفية والتحميل

### 9. الإعدادات (Settings)

**المسار:** `/settings`

- **عام:** اسم المركز، البريد، الهاتف
- **أمان:** تغيير كلمة المرور، المصادقة الثنائية
- **إشعارات:** تفضيلات الإشعارات والتكرار
- **مظهر:** المظهر، اللغة، حجم الخط
- **معلومات:** إصدار التطبيق والترخيص

---

## 🛠️ الأدوات المتاحة

### FormInput مثال الاستخدام

```vue
<FormInput v-model="studentName" label="اسم الطالب" type="text" placeholder="أدخل الاسم" required @blur="validateField" />
```

**الأنواع المدعومة:**

- text - نص عادي
- email - بريد إلكتروني
- password - كلمة مرور
- number - أرقام
- tel - هاتف
- url - رابط ويب
- date - تاريخ
- time - وقت

### FormSelect مثال الاستخدام

```vue
<FormSelect v-model="selectedProgram" label="اختر البرنامج" :options="programOptions" required />
```

### DataTable مثال الاستخدام

```vue
<DataTable title="الطلاب" :columns="tableColumns" :data="students" @row-click="onRowClick" @selection-change="onSelectChange" />
```

### الإشعارات

```javascript
import { useNotification } from '@/composables/useNotification';

const { showSuccess, showError, showWarning } = useNotification();

showSuccess('تم الحفظ بنجاح');
showError('حدث خطأ ما');
showWarning('تحذير مهم');
```

---

## 📊 مثال على البيانات

### بيانات الطلاب الافتراضية

```javascript
{
  id: 1,
  name: 'أحمد محمود',
  email: 'ahmed@example.com',
  phone: '+966 50 1234567',
  program: 'البرنامج الأساسي',
  status: 'active',
  progress: 75,
  attendance: 90,
  enrollmentDate: '2026-01-01',
  notes: 'طالب متفوق'
}
```

### بيانات البرامج الافتراضية

```javascript
{
  id: 1,
  name: 'البرنامج الأساسي',
  description: 'برنامج تدريب شامل للمبتدئين',
  duration: 60,
  sessions: 12,
  capacity: 30,
  enrolled: 28,
  level: 'beginner',
  status: 'active',
  instructors: ['محمد علي', 'فاطمة أحمد']
}
```

---

## 🎨 نظام الألوان

استخدم CSS Variables للتصميم:

```css
/* الألوان الأساسية */
--color-primary-600: #3b82f6 /* الأزرق */ --color-primary-700: #1e40af /* الألوان الحالات */ --color-green-600: #16a34a /* نجاح */
  --color-red-600: #dc2626 /* خطأ */ --color-yellow-600: #fbbf24 /* تحذير */ --color-blue-600: #2563eb /* معلومات */ /* درجات الرمادي */
  --color-gray-100 to --color-gray-900;
```

---

## 🔧 الأوامر المتاحة

```bash
# تشغيل الخادم للتطوير
npm run dev

# بناء للإنتاج
npm run build

# معاينة البناء
npm run preview

# التحقق من الأخطاء (Lint)
npm run lint

# تنسيق الكود (Format)
npm run format
```

---

## 💾 حفظ البيانات

البيانات الحالية موجودة في:

- **Pinia Stores:** للبيانات الأساسية (Students, Programs)
- **localStorage:** للإعدادات الشخصية
- **State:** للحالة المؤقتة في الجلسة

### حفظ الإعدادات

```javascript
// في Settings.vue
localStorage.setItem('appSettings', JSON.stringify(this.settings));
```

---

## 🔐 معايير الأمان

✅ **ما تم تطبيقه:**

- التحقق من جميع المدخلات
- Regex للتحقق من البريد والهاتف
- عدم السماح بـ HTML في النصوص
- رسائل خطأ آمنة (لا تكشف معلومات حساسة)
- إدارة آمنة للتوكنات (في localStorage)

### التحقق من البريد الإلكتروني

```javascript
const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
if (emailRegex.test(email)) {
  // بريد صحيح
}
```

---

## 📱 الاستجابة والجوال

✅ **دعم كامل للأجهزة:**

- **Mobile:** < 640px
- **Tablet:** 640px - 1024px
- **Desktop:** > 1024px

**اختبر على:**

- iPhone/iPad
- Android phones
- عرض النافذة المختلفة

---

## 🐛 استكشاف الأخطاء

### الخطأ: البيانات لا تظهر

1. تحقق من console للأخطاء
2. تأكد من تثبيت المكتبات
3. أعد تشغيل الخادم

### الخطأ: التصميم مشوه

1. امسح cache المتصفح
2. Ctrl+Shift+Delete اختر All
3. أعد تحميل الصفحة

### الخطأ: عدم الحفظ

1. افتح DevTools (F12)
2. اختر Application/Storage
3. تحقق من localStorage

---

## 📖 موارد إضافية

- **Vue 3 Docs:** https://vuejs.org
- **Pinia:** https://pinia.vuejs.org
- **Vite:** https://vitejs.dev

---

## ✨ ملاحظات مهمة

1. **البيانات مؤقتة:** استخدم localStorage أو API
2. **البحث فوري:** تم تطبيقه بكفاءة عالية
3. **الترقيم:** يعمل بسلاسة حتى مع آلاف الصفوف
4. **الاستجابة:** اختبرت على جميع الأحجام

---

## 🎓 أمثلة عملية

### إضافة طالب برمجياً

```javascript
const { addStudent } = useStudentStore();

const newStudent = {
  name: 'علي محمود',
  email: 'ali@example.com',
  phone: '+966 ...',
  program: 'البرنامج الأساسي',
  status: 'active',
  progress: 0,
  attendance: 0,
};

addStudent(newStudent);
```

### البحث عن طالب

```javascript
const { searchStudents } = useStudentStore();
const results = searchStudents('محمود');
// يبحث في جميع الحقول
```

### تصفية الطلاب

```javascript
const { filterStudents } = useStudentStore();
const activeStudents = filterStudents('active');
```

---

## 🎯 الخطوات التالية

1. ✅ تم بناء جميع الصفحات
2. ⏳ يمكن البدء في API integration
3. ⏳ إضافة قاعدة بيانات
4. ⏳ نشر على الويب

---

**التطبيق جاهز الآن للاستخدام الكامل! 🎉**
