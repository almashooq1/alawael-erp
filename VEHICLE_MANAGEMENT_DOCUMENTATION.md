# 📋 نظام إدارة المركبات والأسطول - وثائق شاملة

## 🎯 نظرة عامة

نظام إدارة المركبات والأسطول المتقدم يوفر حلاً شاملاً لإدارة الأسطول بكفاءة عالية مع تتبع فوري، إدارة الصيانة، ومراقبة الأداء.

---

## 📊 المميزات الرئيسية

### 1. **إدارة المركبات** 🚗

- تسجيل المركبات بجميع البيانات الكاملة
- إدارة التسجيل والترخيص
- تتبع التأمين والفحص الدوري
- سجل الصيانة والإصلاحات
- تتبع الأداء والمسافة

### 2. **إدارة السائقين** 👨‍🦰

- بيانات شاملة للسائقين
- إدارة الترخيص والمستندات
- نظام نقاط المخالفات
- تقييم الأداء والكفاءة
- سجل التدريب والشهادات

### 3. **تتبع فوري** 📍

- تتبع GPS للمركبات في الوقت الفعلي
- عرض على خريطة تفاعلية
- سجل المواقع التاريخية
- تنبيهات الحدود الجغرافية (Geofences)
- كشف السلوك غير الآمن

### 4. **إدارة الرحلات** 🛣️

- بدء وإنهاء الرحلات تلقائياً
- تسجيل المسافة والوقت
- حساب استهلاك الوقود
- تحليل جودة القيادة
- حساب التكاليف والإيرادات

### 5. **إدارة الصيانة** 🔧

- جدول صيانة منتظم
- تسجيل سجلات الصيانة
- إنذارات الصيانة المستحقة
- حساب تكاليف الصيانة
- سجل الإصلاحات

### 6. **الامتثال والفحص** ✅

- تتبع الفحص الدوري
- تنبيهات انتهاء الصلاحية
- تقارير الامتثال
- إدارة المستندات
- سجل الفحوصات

### 7. **التحليلات والتقارير** 📈

- إحصائيات الأسطول
- تقارير الأداء
- تحليل التكاليف
- رسوم بيانية وتصورات
- تصدير البيانات

---

## 🗄️ البيانات والنماذج

### نموذج المركبة (Vehicle)

```javascript
{
  // بيانات أساسية
  registrationNumber: "س ق أ 1234",
  plateNumber: "ق أ 1234",
  vin: "VINNUMBER123",

  // معلومات المالك
  owner: ObjectId,
  ownerName: "محمد العتيبي",

  // البيانات التقنية
  basicInfo: {
    make: "Toyota",
    model: "Hilux",
    year: 2023,
    type: "سيارة نقل",
    fuelType: "ديزل"
  },

  // التسجيل والملكية
  registration: {
    status: "نشط",
    expiryDate: Date,
    category: "تجاري"
  },

  // التأمين
  insurance: {
    provider: "سامبا",
    policyNumber: "POLICY123",
    insured: true,
    policyExpiryDate: Date
  },

  // الفحص الدوري
  inspection: {
    lastInspectionDate: Date,
    nextInspectionDate: Date,
    status: "معايير"
  },

  // الصيانة
  maintenance: {
    lastMaintenanceDate: Date,
    nextMaintenanceDate: Date,
    maintenanceHistory: [...]
  },

  // السائق المعين
  assignedDriver: ObjectId,

  // التتبع GPS
  tracking: {
    gpsEnabled: true,
    lastLocation: {
      latitude: 24.7136,
      longitude: 46.6753,
      timestamp: Date
    },
    speed: 85
  },

  // المخالفات
  violations: [...],
  totalViolations: 2,
  totalFines: 500
}
```

### نموذج السائق (Driver)

```javascript
{
  // بيانات شخصية
  personalInfo: {
    firstName: "أحمد",
    lastName: "السالم",
    nationalId: "1234567890",
    phone: "0501234567"
  },

  // الترخيص
  license: {
    licenseNumber: "LIC123",
    licenseClass: "ركوب",
    expiryDate: Date,
    status: "نشطة"
  },

  // نقاط المخالفات
  violationPoints: {
    currentPoints: 5,
    status: "آمن"
  },

  // المخالفات والحوادث
  violations: [...],
  accidents: [...],

  // الأداء
  performance: {
    rating: 4.5,
    totalTrips: 150,
    safetyScore: 95
  },

  // التدريب
  training: {
    defensiveDriving: { completed: true },
    safetyTraining: { completed: true },
    firstAid: { completed: true }
  }
}
```

### نموذج الرحلة (Trip)

```javascript
{
  tripId: "TRIP-123456",
  vehicle: ObjectId,
  driver: ObjectId,

  // البيانات الزمنية
  startTime: Date,
  endTime: Date,
  duration: 120, // بالدقائق
  status: "اكتملت",

  // الموقع والمسار
  startLocation: {
    latitude: 24.7136,
    longitude: 46.6753,
    address: "الرياض"
  },
  endLocation: {
    latitude: 24.7241,
    longitude: 46.6844,
    address: "جدة"
  },
  route: [...], // سجل المواقع

  // المسافة والسرعة
  distance: 450, // كم
  averageSpeed: 95,
  maxSpeed: 140,

  // استهلاك الوقود
  fuelConsumption: {
    startFuelLevel: 100,
    endFuelLevel: 75,
    fuelUsed: 25,
    efficiency: 18 // كم/لتر
  },

  // التكاليف
  costs: {
    fuelCost: 150,
    tolls: 50,
    parkingFees: 20,
    total: 220
  },

  // جودة القيادة
  drivingQuality: {
    safetyScore: 98,
    smoothnessScore: 95,
    efficiencyScore: 92
  }
}
```

---

## 🔌 واجهات البرمجة (API)

### المركبات

#### جلب جميع المركبات

```
GET /api/vehicles
Query: status, category, owner, assignedDriver
Response: { success, count, vehicles[] }
```

#### إضافة مركبة

```
POST /api/vehicles
Body: { vehicleData }
Response: { success, message, vehicle }
```

#### تحديث مركبة

```
PUT /api/vehicles/:id
Body: { updateData }
Response: { success, message, vehicle }
```

#### إضافة صيانة

```
POST /api/vehicles/:id/maintenance
Body: { type, description, cost, provider, mileage }
Response: { success, message, vehicle }
```

#### تسجيل فحص

```
POST /api/vehicles/:id/inspection
Body: { result, notes, inspector }
Response: { success, message, vehicle }
```

#### تسجيل مخالفة

```
POST /api/vehicles/:id/violations
Body: { type, description, fine }
Response: { success, message, vehicle }
```

### السائقين

#### جلب جميع السائقين

```
GET /api/drivers
Query: status, isActive
Response: { success, count, drivers[] }
```

#### إضافة سائق

```
POST /api/drivers
Body: { driverData }
Response: { success, message, driver }
```

#### تسجيل مخالفة سائق

```
POST /api/drivers/:id/violations
Body: { type, description, fine, points }
Response: { success, message, driver }
```

#### الحصول على السائقين الممنوعين

```
GET /api/drivers/violations/suspended/list
Response: { success, count, drivers[] }
```

### التتبع والرحلات

#### تحديث موقع المركبة

```
POST /api/tracking/update-location
Body: { vehicleId, latitude, longitude, speed, address }
Response: { success, message }
```

#### الحصول على الموقع الحالي

```
GET /api/tracking/:vehicleId/location
Response: { success, location }
```

#### بدء رحلة

```
POST /api/trips/start
Body: { vehicleId, driverId, startLocation }
Response: { success, message, trip }
```

#### إنهاء رحلة

```
POST /api/trips/:tripId/end
Body: { endLocation, endOdometer }
Response: { success, message, trip }
```

### الإحصائيات

#### إحصائيات الأسطول

```
GET /api/vehicles/stats/fleet/overview
Response: {
  success,
  statistics: {
    totalVehicles,
    totalDrivers,
    totalDistance,
    totalCosts,
    profit,
    ...
  }
}
```

#### تقرير الامتثال

```
GET /api/vehicles/reports/compliance/status
Response: {
  success,
  report: {
    compliant,
    noncompliant,
    complianceRate,
    issues
  }
}
```

---

## 💻 المكونات الواجهة الأمامية

### 1. **لوحة تحكم الأسطول** (FleetDashboard)

- إحصائيات شاملة
- رسوم بيانية الأداء
- قائمة المركبات
- تقارير الامتثال

### 2. **خريطة المركبات** (VehicleMap)

- عرض فوري للمركبات
- معلومات السرعة والموقع
- تنبيهات الحدود الجغرافية
- تفاصيل المركبة عند النقر

### 3. **إدارة المركبات** (VehicleManager)

- إضافة/تحديث المركبات
- إدارة الصيانة
- تسجيل الفحص
- تتبع المستندات

### 4. **إدارة السائقين** (DriverManager)

- إضافة/تحديث السائقين
- إدارة الترخيص
- تتبع المخالفات
- تقييم الأداء

### 5. **تقارير المركبات** (VehicleReports)

- تقارير الأداء
- تقارير التكاليف
- تقارير الامتثال
- تصدير البيانات

---

## 🔐 الأمان والصلاحيات

### أدوار المستخدمين

```javascript
{
  admin: ['إدارة كاملة', 'الوصول لكل الميزات'],
  fleetManager: ['إدارة الأسطول', 'التقارير'],
  driver: ['عرض البيانات الخاصة به'],
  inspector: ['تسجيل الفحص'],
  trafficOfficer: ['تسجيل المخالفات']
}
```

### الحماية

- JWT Token للمصادقة
- تشفير البيانات الحساسة
- تسجيل جميع العمليات (Audit Log)
- التحقق من الصلاحيات في كل طلب

---

## 📱 التطبيق المحمول

### المميزات

- عرض المركبات المعينة للسائق
- تتبع الرحلة الحالية
- تنبيهات المخالفات والصيانة
- سجل الرحلات والأداء
- إرسال مستندات الفحص

---

## 🚀 النشر والتشغيل

### المتطلبات

- Node.js 16+
- MongoDB 5.0+
- Redis 6.0+ (للـ Cache)
- خادم GPS خارجي (Teltonika, Siyata, إلخ)

### التثبيت

```bash
# تثبيت الحزم
npm install

# تشغيل قاعدة البيانات
npm run seed:vehicles

# بدء الخادم
npm start
```

---

## 📈 الإحصائيات المدعومة

- المركبات النشطة والمتوقفة
- إجمالي المسافة المقطوعة
- استهلاك الوقود والتكاليف
- الإيرادات والأرباح
- معدل الامتثال
- أداء السائقين
- المخالفات والحوادث

---

## ⚙️ التخصيص والإضافات

يمكن تخصيص النظام لإضافة:

- تكامل مع أنظمة الدفع
- تكامل مع أنظمة الحجز
- تكامل مع أنظمة ERP
- تطبيقات ذكية للصيانة
- تحليلات متقدمة باستخدام AI

---

## 📞 الدعم والمساعدة

للحصول على الدعم الفني والمساعدة:

- البريد الإلكتروني: support@fleet-management.sa
- الخط الساخن: 800-1234-567
- الموقع: www.fleet-management.sa

---

## 📝 ملاحظات أخيرة

- النظام قيد التطوير المستمر
- يتم إضافة ميزات جديدة بانتظام
- الأمان والخصوصية أولويتنا الأولى

**صُنع بـ ❤️ في المملكة العربية السعودية**
