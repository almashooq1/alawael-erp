# 🚀 دليل التشغيل على Windows - AlAwael ERP

## ⚠️ المتطلبات المسبقة

تأكد من تثبيت هذه البرامج:

### 1. Node.js و npm

```
تحميل من: https://nodejs.org
اختر: LTS Version (الإصدار الثابت)
```

**للتحقق:**

```powershell
node --version
npm --version
```

### 2. MongoDB

اختر **واحداً من الخيارات التالية**:

#### ✅ الخيار A: MongoDB محلي على Windows (مستحسن)

```powershell
# إذا كان Chocolatey مثبتاً
choco install mongodb-community

# أو قم بالتحميل اليدوي من:
https://www.mongodb.com/try/download/community-windows
```

#### ✅ الخيار B: MongoDB Atlas (سحابة - بدون تثبيت)

```
1. اذهب إلى: https://www.mongodb.com/cloud/atlas
2. أنشئ حساب مجاني
3. انسخ رابط الاتصال
4. أضفه في backend/.env
   MONGODB_URI=<رابط الاتصال>
```

---

## 🎯 الطريقة الأولى: استخدام البرنامج التلقائي (الأسهل)

### الخطوة 1: افتح PowerShell

```
اضغط: Windows Key + R
اكتب: powershell
اضغط: Enter
```

### الخطوة 2: شغل البرنامج التلقائي

```powershell
# انسخ والصق هذا الأمر:
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser;
cd "c:\Users\x-be\OneDrive\المستندات\04-10-2025\66666";
.\🚀_START_SYSTEM.ps1
```

### الخطوة 3: انتظر النتائج

سيتم فتح 3 نوافذ تلقائياً:

- 🗄️ **نافذة 1**: MongoDB (قاعدة البيانات)
- 🔧 **نافذة 2**: Backend (الخادم الخلفي)
- ⚛️ **نافذة 3**: Frontend (الواجهة الأمامية)

### الخطوة 4: افتح المتصفح

عندما ترى رسالة "Server Started"، افتح:

```
http://localhost:5173
أو
http://localhost:3000
```

---

## 🎯 الطريقة الثانية: التشغيل اليدوي (متقدم)

### في 3 نوافذ PowerShell منفصلة:

#### **نافذة 1: MongoDB**

```powershell
mongod
```

#### **نافذة 2: Backend**

```powershell
cd "c:\Users\x-be\OneDrive\المستندات\04-10-2025\66666\backend"
npm start
```

#### **نافذة 3: Frontend**

```powershell
cd "c:\Users\x-be\OneDrive\المستندات\04-10-2025\66666\frontend"
npm run dev
```

---

## 🔐 تسجيل الدخول

عندما يفتح المتصفح:

```
📧 البريد الإلكتروني: admin@alawael.com
🔑 كلمة المرور:      Admin@123456
```

---

## ❌ حل المشاكل الشائعة

### المشكلة: "mongod is not recognized"

**الحل:**

- MongoDB لم يتم تثبيته
- تثبيت من: https://www.mongodb.com/try/download/community-windows
- أو استخدم `choco install mongodb-community`
- أو استخدم MongoDB Atlas (سحابة)

### المشكلة: "Port 3001 already in use"

**الحل:**

```powershell
# إيقاف جميع عمليات Node.js
Get-Process node -ErrorAction SilentlyContinue | Stop-Process -Force

# ثم حاول مرة أخرى
```

### المشكلة: "Cannot find path"

**الحل:**

- تأكد من أن المسار صحيح
- استخدم علامات الاقتباس كما هو موضح أعلاه

### المشكلة: "npm start فشل"

**الحل:**

```powershell
# أعد تثبيت المكتبات
cd backend
npm install
npm start
```

### المشكلة: "Login failed / Connection refused"

**الحل:**

1. تأكد من تشغيل MongoDB (نافذة 1)
2. تأكد من تشغيل Backend (نافذة 2)
3. تأكد من وجود صحافة على المتصفح Ctrl+Shift+Delete لمسح Cookies
4. حاول في نافذة Incognito/Private

---

## 📊 علامات النجاح

### ✅ MongoDB

```
2026-01-13T10:00:00.000+0200 I  - "msg":"Waiting for connections"
```

### ✅ Backend

```
🚀 AlAwael ERP Backend Server Started
✅ MongoDB connected
✅ Admin user already exists
```

### ✅ Frontend

```
VITE v5.x.x ready in xxx ms

  ➜  Local:   http://localhost:5173
  ➜  press h + enter to show help
```

---

## 🎓 نصائح مهمة

### 💡 نصيحة 1: استخدم نوافذ منفصلة

- لا تحاول تشغيل 3 أشياء في نفس النافذة
- استخدم 3 نوافذ منفصلة

### 💡 نصيحة 2: افتح DevTools

```
اضغط: F12
انظر إلى: Console tab
ستشاهد رسائل مفصلة عن أي خطأ
```

### 💡 نصيحة 3: استخدم Incognito

```
إذا واجهت مشكلة في تسجيل الدخول:
- افتح نافذة Incognito (Ctrl+Shift+N)
- جرب مرة أخرى
```

### 💡 نصيحة 4: تحقق من الاتصال

```
http://localhost:3001/health
يجب أن يظهر: {"status":"OK"}
```

---

## 🆘 إذا لم ينجح أي شيء

اتبع هذه الخطوات:

### 1. تحقق من المتطلبات

```powershell
node --version    # يجب أن يظهر v18+
npm --version     # يجب أن يظهر 9+
```

### 2. أعد تثبيت المكتبات

```powershell
cd backend
npm install

cd ../frontend
npm install
```

### 3. امسح البيانات القديمة

```powershell
# احذف مجلد node_modules والملفات المؤقتة
rmdir node_modules -Recurse -Force
npm cache clean --force
npm install
```

### 4. ابدأ من جديد

```powershell
# أعد تشغيل جميع الخدمات
```

---

## 📞 احصل على المساعدة

### قراءة إضافية:

- [LOGIN_FIX_REPORT.md](./LOGIN_FIX_REPORT.md)
- [دليل*حل*مشكلة_التسجيل.md](./دليل_حل_مشكلة_التسجيل.md)
- [00_READ_ME_FIRST.md](./00_READ_ME_FIRST.md)

---

## ✅ الحالة الحالية

```
✅ المشروع جاهز للتشغيل
✅ جميع المكتبات مثبتة
✅ قاعدة البيانات معدة
✅ Admin user موجود
✅ Backend API يعمل
✅ Frontend جاهز
```

---

**تاريخ الإنشاء:** January 13, 2026  
**الحالة:** ✅ **جاهز للاستخدام**

🎉 **استمتع باستخدام AlAwael ERP!**
