# نظام التدقيق الداخلي الشامل

# Internal Audit Management System

## 📋 نظرة عامة

نظام متكامل لإدارة عمليات التدقيق الداخلي مع دعم كامل لخطط التدقيق السنوية،
عمليات التدقيق المفاجئة، تقارير عدم المطابقة، والخطط التصحيحية والوقائية،
بالإضافة إلى متابعة الإغلاق.

---

## ✨ المميزات الرئيسية

### 1. خطط التدقيق السنوية (Annual Audit Plans)

- إنشاء وتخطيط عمليات التدقيق للسنة بأكملها
- تحديد الأقسام والعمليات المراد تدقيقها
- تخصيص موارد (أشخاص، ميزانية، أيام)
- جدولة مراحل التنفيذ بالفترات
- تعيين فريق التدقيق والمتخصصين
- تتبع الموافقات

#### البيانات المرتبطة:

```
- معرف الخطة (planId)
- السنة المالية
- الأقسام والعمليات المراد تدقيقها
- الموارد المخصصة
- جدول التنفيذ بالمراحل
- فريق التدقيق
- الحالة (مسودة، موافقة عليها، نشطة، مكتملة، مؤرشفة)
- المعايير المطبقة
```

### 2. عمليات التدقيق المفاجئة (Surprise Audits)

- تنفيذ عمليات تدقيق مفاجئة
- تسجيل سبب التدقيق والعامل المحفز
- جمع الأدلة والمستندات
- تسجيل الملاحظات المبدئية
- معالجة المخاطر والنتائج الأولية

#### البيانات المرتبطة:

```
- معرف التدقيق (auditId)
- نوع التدقيق (مفاجئ، مخطط، متابعة)
- معلومات التدقيق الأساسية
- نطاق التدقيق (القسم، العملية)
- فريق التدقيق والمسؤوليات
- جدول التدقيق والمدة
- معايير التدقيق
- الأدلة المجمعة
- الملاحظات والنتائج الأولية
```

### 3. تقارير عدم المطابقة (Non-Conformance Reports)

- تسجيل عدم المطابقة والانحرافات
- تصنيف بناءً على الخطورة (حرجة، رئيسية، بسيطة)
- تحديد التأثير المحتمل
- تعيين الملكية
- متابعة الحالة والإجراءات المؤقتة
- إدارة التصعيد

#### البيانات المرتبطة:

```
- معرف التقرير (ncrId)
- نوع التقرير (تدقيق خارجي، تدقيق داخلي، شكوى، مراقبة)
- التصنيف والخطورة
- تفاصيل عدم المطابقة
- الأدلة والمستندات
- التأثير المحدث
- المالك والمسؤول
- الإجراءات المؤقتة
- الحالة والتسلسل الزمني
```

### 4. الخطط التصحيحية والوقائية (Corrective/Preventive Actions)

- تحليل الأسباب الجذرية
- تطوير خطط الإجراءات
- تتبع التنفيذ والتقدم
- المراقبة والمؤشرات
- التحقق من الفعالية
- الإغلاق والدروس المستفادة

#### البيانات المرتبطة:

```
- معرف الإجراء (actionId)
- النوع (تصحيحي، وقائي)
- الارتباط بعدم المطابقة
- تحليل الأسباب الجذرية
- الإجراءات المقترحة
- تفاصيل التنفيذ
- المراحل والتقدم
- الموارد المخصصة
- المخاطر والعقبات
- المراقبة والمؤشرات
- التحقق من الفعالية
- الإغلاق والدروس المستفادة
```

### 5. متابعة الإغلاق (Closure Follow-up)

- التحقق من معايير الإغلاق
- إجراء الاختبارات والعينات
- المراجعات الدورية
- الموافقة النهائية
- الإغلاق النهائي
- إدارة إعادة الفتح

#### البيانات المرتبطة:

```
- معرف المتابعة (followUpId)
- الارتباط بالمشكلة الأصلية
- معايير الإغلاق
- التحقق من الإغلاق
- الاختبارات والعينات
- المراجعات الدورية
- الموافقة النهائية
- الإغلاق النهائي
- إعادة الفتح والمسؤولية
- الجدول الزمني والمحاسبية
```

---

## 🎯 التكامل مع النظام

### قاعدة البيانات

- نموذج AnnualAuditPlan
- نموذج SurpriseAudit
- نموذج NonConformanceReport
- نموذج CorrectivePreventiveAction
- نموذج ClosureFollowUp

### API Endpoints

#### خطط التدقيق السنوية

```
POST   /api/internal-audits/audit-plans
GET    /api/internal-audits/audit-plans
GET    /api/internal-audits/audit-plans/:planId
PUT    /api/internal-audits/audit-plans/:planId
```

#### عمليات التدقيق المفاجئة

```
POST   /api/internal-audits/surprise-audits
GET    /api/internal-audits/surprise-audits
GET    /api/internal-audits/surprise-audits/:auditId
PUT    /api/internal-audits/surprise-audits/:auditId
POST   /api/internal-audits/surprise-audits/:auditId/observations
```

#### تقارير عدم المطابقة

```
POST   /api/internal-audits/non-conformance-reports
GET    /api/internal-audits/non-conformance-reports
GET    /api/internal-audits/non-conformance-reports/:ncrId
PUT    /api/internal-audits/non-conformance-reports/:ncrId
PUT    /api/internal-audits/non-conformance-reports/:ncrId/assign
PUT    /api/internal-audits/non-conformance-reports/:ncrId/close
```

#### الإجراءات التصحيحية والوقائية

```
POST   /api/internal-audits/corrective-preventive-actions
GET    /api/internal-audits/corrective-preventive-actions
GET    /api/internal-audits/corrective-preventive-actions/:actionId
PUT    /api/internal-audits/corrective-preventive-actions/:actionId
PUT    /api/internal-audits/corrective-preventive-actions/:actionId/progress
PUT    /api/internal-audits/corrective-preventive-actions/:actionId/verify-effectiveness
PUT    /api/internal-audits/corrective-preventive-actions/:actionId/close
```

#### متابعات الإغلاق

```
POST   /api/internal-audits/closure-followups
GET    /api/internal-audits/closure-followups
GET    /api/internal-audits/closure-followups/:followUpId
PUT    /api/internal-audits/closure-followups/:followUpId
PUT    /api/internal-audits/closure-followups/:followUpId/verify-closure
PUT    /api/internal-audits/closure-followups/:followUpId/final-closure
```

#### لوحات التقارير

```
GET    /api/internal-audits/internal-audit-dashboard
GET    /api/internal-audits/reports/compliance-by-department
GET    /api/internal-audits/reports/actions-status
```

---

## 👥 الأدوار والصلاحيات

| الدور              | خطط التدقيق | التدقيق المفاجئ | التقارير | الإجراءات | الإغلاق  |
| ------------------ | ----------- | --------------- | -------- | --------- | -------- |
| Admin              | ✅ كامل     | ✅ كامل         | ✅ كامل  | ✅ كامل   | ✅ كامل  |
| Audit Manager      | ✅ كامل     | ✅ كامل         | ✅ كامل  | ✅ كامل   | ✅ كامل  |
| Quality Manager    | ⚠️ عرض      | ⚠️ عرض          | ✅ إنشاء | ✅ إنشاء  | ⚠️ عرض   |
| Department Manager | ⚠️ عرض      | ⚠️ عرض          | ⚠️ عرض   | ✅ تنفيذ  | ✅ تنفيذ |
| User               | ⚠️ عرض      | ⚠️ عرض          | ⚠️ عرض   | ⚠️ عرض    | ⚠️ عرض   |

---

## 📊 لوحات التحكم

### لوحة التحكم الرئيسية

- عدد خطط التدقيق الإجمالي والنشط
- عدد عمليات التدقيق المفاجئة والمكتملة
- عدد تقارير عدم المطابقة (الإجمالي والحرجة والمفتوحة)
- عدد الإجراءات والمكتملة منها
- متابعات الإغلاق المكتملة

### تقرير الامتثال حسب القسم

- عدد عدم المطابقة لكل قسم
- توزيع الخطورة (حرجة، رئيسية، بسيطة)

### تقرير حالة الإجراءات

- توزيع الإجراءات حسب الحالة
- متوسط التقدم لكل حالة

---

## 🔄 تدفق العملية

### 1. خطة التدقيق السنوية

```
إنشاء الخطة → الموافقة → التنشيط → المراقبة → الإكمال
```

### 2. التدقيق المفاجئ

```
الإشعار → التنفيذ → جمع الأدلة → الملاحظات → النتائج → الإبلاغ
```

### 3. عدم المطابقة

```
التقرير → الاعتراف → التحقيق → التصعيد → الإجراء → الإغلاق
```

### 4. الإجراء التصحيحي

```
التحليل → التخطيط → الموافقة → التنفيذ → المراقبة → التحقق → الإغلاق
```

### 5. متابعة الإغلاق

```
الإنشاء → التحقق من المعايير → الاختبارات → المراجعات → الموافقة → الإغلاق النهائي
```

---

## 📈 مؤشرات الأداء

- نسبة إكمال خطط التدقيق
- نسبة إغلاق تقارير عدم المطابقة
- متوسط وقت الإغلاق
- عدد الإجراءات الفعالة
- نسبة الامتثال حسب القسم
- عدد التدقيقات المفاجئة المنفذة

---

## 🔐 الأمان والموثوقية

- مصادقة JWT
- التحكم في الوصول بناءً على الأدوار
- تسجيل النشاط
- النسخ الاحتياطية الآلية
- تتبع التاريخ والتعديلات

---

## 📝 ملاحظات إضافية

- جميع التقارير قابلة للتصدير
- دعم اللغة العربية كاملة
- واجهة المستخدم متجاوبة
- تكامل سلس مع باقي النظام
