# دليل استخدام نظام التدقيق الداخلي الشامل

# Internal Audit Management System - User Guide

---

## 📌 القائمة السريعة

### الملفات المضافة:

1. **Backend Models**: `erp_new_system/backend/models/internalAudit.js`
2. **Backend Routes**: `erp_new_system/backend/routes/internalAudit.js`
3. **Frontend Component**:
   `erp_new_system/frontend/src/components/InternalAudit/InternalAuditDashboard.jsx`
4. **Integration**: تم تحديث `app.js` لتسجيل الـ Routes

---

## 🚀 البدء السريع

### 1. بدء تشغيل النظام

```bash
# في مجلد Backend
cd erp_new_system/backend
npm start

# في مجلد Frontend (في نافذة طرفية منفصلة)
cd erp_new_system/frontend
npm start
```

### 2. الوصول إلى النظام

- **URL**: http://localhost:3002
- **البيانات الافتراضية**:
  - البريد: `admin@alawael.com`
  - كلمة السر: `Admin@123456`

### 3. الوصول إلى Dashboard التدقيق الداخلي

- من القائمة الجانبية: اختر "التدقيق الداخلي"
- أو اذهب إلى: http://localhost:3002/internal-audit

---

## 📚 شرح كل قسم

### 🎯 التبويب الأول: نظرة عامة (Overview)

عرض شامل لجميع معدلات الإنجاز والإحصائيات:

| المقياس                 | الوصف                       |
| ----------------------- | --------------------------- |
| **خطط التدقيق**         | إجمالي الخطط والخطط النشطة  |
| **عمليات التدقيق**      | عدد العمليات والمكتملة منها |
| **تقارير عدم المطابقة** | الإجمالي والحرجة والمفتوحة  |
| **الإجراءات التصحيحية** | الإجمالي والمكتملة          |
| **متابعات الإغلاق**     | الإجمالي والمغلقة           |

**الإجراء**: عرض النسب المئوية والتقدم فقط - لا يمكن التعديل من هنا

---

### 📋 التبويب الثاني: خطط التدقيق السنوية

#### إنشاء خطة جديدة:

```bash
# اضغط على "خطة جديدة"
# ملء البيانات:
- معرف الخطة (مثل: PLAN-2026-001)
- السنة (2026)
- العنوان والوصف
- الأقسام المراد تدقيقها
- الموارد المخصصة
- جدول المراحل
- فريق التدقيق
```

#### الحالات:

- **Draft**: مسودة أولية
- **Approved**: تمت الموافقة
- **Active**: قيد التنفيذ
- **Completed**: مكتملة
- **Archived**: مؤرشفة

#### العمليات:

- عرض تفاصيل الخطة
- تعديل بيانات الخطة
- تغيير الحالة
- إضافة مراحل جديدة

---

### 🔍 التبويب الثالث: عمليات التدقيق المفاجئة

#### إنشاء تدقيق مفاجئ:

```bash
# اضغط على "تدقيق جديد"
# ملء البيانات:
- معرف التدقيق (مثل: AUD-2026-001)
- سبب التدقيق المفاجئ
- القسم المراد تدقيقه
- فريق التدقيق
- جدول التدقيق
- معايير الفحص
- جمع الأدلة
- تسجيل الملاحظات
```

#### مراحل التدقيق:

```
مخطط → قيد التنفيذ → اكتمل العمل الميداني → إعداد التقرير → مكتمل → مغلق
```

#### إضافة ملاحظات:

- **نوع**: نقطة قوة / ضعف / مخاطر / فرص
- **الخطورة**: حرجة / رئيسية / بسيطة
- **التفاصيل**: وصف كامل للملاحظة

---

### ⚠️ التبويب الرابع: تقارير عدم المطابقة

#### إنشاء تقرير:

```bash
# اضغط على "تقرير جديد"
# ملء البيانات:
- معرف التقرير (مثل: NCR-2026-001)
- النوع (تدقيق خارجي / داخلي / شكوى / مراقبة)
- الفئة (حرجة / رئيسية / بسيطة)
- القسم المتأثر
- وصف عدم المطابقة
- المعيار الذي لم يتم الامتثال له
- السبب الجذري
- التأثير المحتمل
```

#### الحالات:

```
مفتوح → معترف به → قيد التحقيق → تم تحديد خطة الإجراء →
قيد التنفيذ → في انتظار التحقق → مغلق / مرفوض
```

#### تعيين المالك:

```bash
# اضغط على التقرير → "تعيين"
# اختر مالك القسم المسؤول عن الإجراء
```

#### إغلاق التقرير:

```bash
# بعد اكتمال الإجراء
# اضغط على "إغلاق"
# أضف طريقة التحقق والتعليقات
```

---

### ✅ التبويب الخامس: الإجراءات التصحيحية والوقائية

#### إنشاء إجراء:

```bash
# اضغط على "إجراء جديد"
# ملء البيانات:
- معرف الإجراء (مثل: CA-2026-001)
- النوع: تصحيحي / وقائي
- ارتباط بتقرير عدم المطابقة (اختياري)
- تحليل الأسباب الجذرية (5-Why / Fishbone / Fault Tree)
- الإجراءات المقترحة
- المالك والمسؤول
- تاريخ الانتهاء المستهدف
- الموارد المخصصة
```

#### تحديث التقدم:

```bash
# اضغط على الإجراء → "تحديث التقدم"
# أضف:
- النسبة المئوية الحالية
- الحالة: التخطيط / موافقة / قيد التنفيذ / مكتمل / متأخر / معلق
```

#### التحقق من الفعالية:

```bash
# بعد إكمال الإجراء
# اضغط على "التحقق من الفعالية"
# أضف:
- طريقة التحقق
- النتائج
- هل الإجراء فعال؟ (نعم / لا)
- درجة الفعالية (0-100)
- الأدلة
```

#### إغلاق الإجراء:

```bash
# بعد التحقق من الفعالية
# اضغط على "إغلاق"
# أضف التعليقات والدروس المستفادة
```

---

### 🔐 التبويب السادس: متابعات الإغلاق

#### إنشاء متابعة:

```bash
# اضغط على "متابعة جديدة"
# ملء البيانات:
- معرف المتابعة
- المرتبطة بـ: (اختر التقرير أو الإجراء)
- معايير الإغلاق
- طريقة التحقق
- الجدول الزمني
```

#### التحقق من المعايير:

```bash
# اضغط على "التحقق من الإغلاق"
# تحقق من:
- هل تم استيفاء جميع المعايير؟
- الأدلة المرفقة
- الملاحظات
```

#### الموافقة النهائية:

```bash
# اضغط على "الموافقة النهائية"
# اختر:
- الموافقة: موافق / رفض / شرطي
- إذا كان رفض: السبب
- إذا كان شرطي: المتطلبات الإضافية
```

#### الإغلاق النهائي:

```bash
# بعد الموافقة
# النظام يغلق تلقائياً المتابعة
# وتسجل في قاعدة البيانات كمغلقة
```

---

## 📊 الفحوصات والتقارير

### 1. لوحة التحكم الرئيسية

تظهر:

- نسب الإكمال لجميع العمليات
- عدد المشاكل المعلقة
- الأولويات والمخاطر العالية

### 2. تقرير الامتثال حسب القسم

```bash
# اضغط على "التقارير"
# اختر "الامتثال حسب القسم"
# يعرض:
- عدد عدم المطابقة لكل قسم
- توزيع الخطورة
- نسب الامتثال
```

### 3. تقرير حالة الإجراءات

```bash
# اضغط على "التقارير"
# اختر "حالة الإجراءات"
# يعرض:
- توزيع الإجراءات حسب الحالة
- متوسط التقدم
- التاريخ المتوقع للإكمال
```

---

## 🔄 سيناريوهات واقعية

### السيناريو 1: عملية تدقيق مفاجئة من البداية إلى النهاية

```
1. إنشاء تدقيق مفاجئ
   ↓
2. إضافة فريق التدقيق وجدول العمل
   ↓
3. جمع الأدلة والملاحظات
   ↓
4. إذا وجدت مشكلة: إنشاء تقرير عدم مطابقة
   ↓
5. تعيين مالك للتقرير
   ↓
6. المالك ينشئ إجراء تصحيحي
   ↓
7. متابعة التنفيذ والتقدم
   ↓
8. التحقق من الفعالية
   ↓
9. إنشاء متابعة إغلاق
   ↓
10. التحقق والموافقة النهائية والإغلاق
```

### السيناريو 2: خطة تدقيق سنوية

```
1. إنشاء خطة التدقيق للسنة
   ↓
2. إضافة الأقسام والعمليات والموارد
   ↓
3. تحديد جدول المراحل
   ↓
4. تعيين فريق التدقيق
   ↓
5. الموافقة على الخطة
   ↓
6. تنشيط الخطة
   ↓
7. تنفيذ التدقيقات المخطط لها
   ↓
8. متابعة التقدم
   ↓
9. إكمال الخطة
```

---

## 🆘 استكشاف الأخطاء

### مشكلة: لا يظهر التبويب

**الحل**:

1. تحقق من الصلاحيات (هل أنت Admin أو Audit Manager؟)
2. تحقق من أن app.js يحتوي على routes التدقيق الداخلي
3. أعد تحميل الصفحة

### مشكلة: الإجراء لا يحفظ

**الحل**:

1. تحقق من ملء جميع الحقول المطلوبة
2. تحقق من اتصال Backend
3. افتح DevTools (F12) وتحقق من الأخطاء

### مشكلة: لا أستطيع إغلاق التقرير

**الحل**:

1. تأكد من أن جميع الإجراءات المرتبطة اكتملت
2. تأكد من التحقق من الفعالية
3. تأكد من أن لديك صلاحية الإغلاق (Admin فقط)

---

## 📞 التواصل والدعم

- **للمساعدة التقنية**: اتصل بفريق تطوير النظام
- **للأسئلة عن السياسات**: اتصل بفريق جودة الخدمات
- **للشكاوى**: استخدم نموذج الشكوى في النظام

---

## 📝 ملخص النقاط المهمة

✅ **افعل**:

- حفظ جميع الأدلة والمستندات
- تحديث التقدم بانتظام
- الإبلاغ عن المشاكل فوراً
- توثيق الدروس المستفادة
- متابعة المواعيد النهائية

❌ **لا تفعل**:

- إغلاق التقارير بدون تحقق
- تجاهل التصعيدات
- حذف البيانات التاريخية
- تخطي خطوات التحقق
- التأخير عن المواعيد

---

**تم إنشاء النظام بتاريخ**: يناير 22، 2026 **الإصدار**: 1.0 **الحالة**: جاهز
للاستخدام الإنتاجي
