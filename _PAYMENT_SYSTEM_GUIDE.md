# 💳 دليل نظام الدفع والمحاسبة

هذا المستند يشرح كيفية استخدام نظام الدفع، الفواتير، والاشتراكات في نظام "الأوائل".

## 1. نظرة عامة

يحتوي النظام على وحدة مالية متكاملة تتيح:

- دفع الرسوم (بطاقات، PayPal، تقسيط).
- إدارة الاشتراكات (Create, Upgrade).
- إصدار الفواتير ومتابعة حالتها.
- لوحة تحكم للمسؤولين لمراقبة الدخل والمدفوعات.

## 2. للمستخدمين (الآباء/الطلاب)

يمكن الوصول للوحة التحكم المالية عبر الرابط `/parent-portal/payments-history` أو `/payment-dashboard` (حسب الصلاحية).

### الميزات:

- **نظرة عامة:** عرض إجمالي المدفوعات والاشتراك الحالي.
- **إجراء دفع:**
  - اختيار المبلغ والعملة.
  - اختيار الطريقة: البطاقة (Stripe)، PayPal، أو التقسيط.
- **سجل المدفوعات:** عرض جدول زمني بجميع العمليات السابقة وحالتها.
- **الاشتراكات:** الترقية بين الباقات (Basic, Professional, Enterprise).

## 3. للمسؤولين (Admin)

يمكن الوصول للوحة تحكم المحاسبة عبر `/admin-portal/payments`.

### الميزات:

- **الإحصائيات:** إجمالي الإيرادات، المبالغ المعلقة، والمتأخرة.
- **إدارة الفواتير:**
  - إنشاء فاتورة جديدة لعميل معين.
  - عرض جميع الفواتير والمدفوعات في النظام.
  - تصدير البيانات (قيد التطوير).

## 4. التكامل التقني (API)

### Endpoints الرئيسية:

- `POST /api/payments/stripe`: إنشاء عملية دفع Stripe.
- `POST /api/payments/paypal`: بدء عملية دفع PayPal.
- `GET /api/payments/history`: سجل مدفوعات المستخدم الحالي.
- `GET /api/payments/all`: (Admin Only) عرض كل المدفوعات.
- `GET /api/payments/invoices/all`: (Admin Only) عرض كل الفواتير.
- `POST /api/payments/invoices/create`: (Admin Only) إنشاء فاتورة.

## 5. ملاحظات التطوير

- النظام يدعم `Mock Mode` عند عدم توفر مفاتيح API حقيقية لـ Stripe/PayPal.
- يتم تخزين البيانات في مجموعات `Payment` و `Invoice` و `Subscription` في قاعدة البيانات.
