# 📅 جدول العمل اليومي - هجرة قاعدة البيانات
## ALAWAEL ERP - 26 و 28 فبراير 2026

---

## ☀️ الأربعاء 26 فبراير - يوم التحضير

### 10:00 - 10:15 صباحاً | ⚡ اجتماع البدء والتوضيح (15 دقيقة)

**الحضور:**
- [ ] مدير قاعدة البيانات (DBA)
- [ ] مدير النظام الخلفي (Backend Lead)
- [ ] مدير المشروع (VP Engineering)

**جدول الاجتماع:**
1. **10:00-10:05**: استعراض الخطة الشاملة
   - [ ] قراءة ملف MIGRATION_EXECUTION_GUIDE.md
   - [ ] فهم كل خطوة بوضوح
   
2. **10:05-10:10**: الأسئلة والنقاش
   - [ ] هل جميع أدوات قاعدة البيانات جاهزة?
   - [ ] هل كلمات المرور والاتصالات جاهزة?
   - [ ] هل 28 فبراير في 6:00 صباحاً محجوزة من الجميع?
   - [ ] هل أرقام الاتصال الحرجة محفوظة?
   
3. **10:10-10:15**: الموافقة والتحضير الذهني
   - [ ] الجميع يشعرون بالثقة والجهوزية
   - [ ] لا توجد أسئلة معلقة
   - [ ] الاتفاق على الجدول الزمني

**ملاحظات الاجتماع:**
```
_________________________________________________________________
_________________________________________________________________
_________________________________________________________________
```

---

### 10:15 - 11:30 صباحاً | 💻 تحضير وتعديل Scripts (75 دقيقة)

**المسؤول:** مدير قاعدة البيانات (DBA)  
**المكان:** مكتب DBA / محطة العمل الموثوقة

**قائمة التحقق:**

#### 10:15 - 10:25: فتح وفحص الملفات (10 دقائق)
- [ ] فتح ملف: `migration-script-main.js`
- [ ] فتح ملف: `MIGRATION_EXECUTION_GUIDE.md`
- [ ] قراءة سريعة للـ comments في الملف
- [ ] فهم آلية العمل بشكل عام

#### 10:25 - 10:35: إدخال بيانات الاتصال (10 دقائق)
في `migration-script-main.js`, عدّل `OLD_DB_CONFIG`:
```javascript
const OLD_DB_CONFIG = {
  host: '[IP القديك الفعلي]',           // ✓ تحقق مرة أخرى
  user: '[اسم المستخدم الفعلي]',       // ✓ صيغة صحيحة
  password: '[كلمة المرور]',           // ✓ بدون مسافات زائدة
  database: '[اسم القاعدة القديمة]'    // ✓ اكتب بشكل دقيق
};
```

- [ ] البيانات صحيحة 100%
- [ ] لا توجد مسافات إضافية
- [ ] أسماء قواعد البيانات تطابق الواقع

وأيضاً عدّل `NEW_DB_CONFIG`:
```javascript
const NEW_DB_CONFIG = {
  host: '[IP الجديد الفعلي]',
  user: '[اسم المستخدم الفعلي]',
  password: '[كلمة المرور]',
  database: '[اسم القاعدة الجديدة]'
};
```

- [ ] البيانات صحيحة 100%
- [ ] لا توجد مسافات إضافية
- [ ] أسماء قواعد البيانات جديدة وفارغة

#### 10:35 - 10:45: التحقق من أسماء الجداول (10 دقائق)
- [ ] افتح قاعدة البيانات القديمة
- [ ] تحقق من أسماء الجداول الفعلية
- [ ] تأكد أن الأسماء مطابقة 100% للـ script

**الجداول المتوقعة:**
- [ ] `users` أو نسخة أخرى? → اكتب الاسم الفعلي: `_______`
- [ ] `products` أو نسخة أخرى? → اكتب الاسم الفعلي: `_______`
- [ ] `orders` أو نسخة أخرى? → اكتب الاسم الفعلي: `_______`

#### 10:45 - 11:05: اختبار الاتصال (20 دقيقة)
```bash
# Test 1: الاتصال بقاعدة البيانات القديمة
mysql -h [old-host] -u [user] -p[password] -e "SELECT COUNT(*) FROM users;"
→ يجب أن تشاهد: رقم (عدد المستخدمين)

# Test 2: الاتصال بقاعدة البيانات الجديدة
mysql -h [new-host] -u [user] -p[password] -e "SHOW TABLES;"
→ يجب أن تكون القاعدة فارغة (أو بجداول موجودة)
```

- [ ] الاتصال بقاعدة البيانات القديمة ✓ نجح
- [ ] الاتصال بقاعدة البيانات الجديدة ✓ نجح
- [ ] كلا الاتصالين يعملان بدون مشاكل

#### 11:05 - 11:30: إنشاء نسخ احتياطية للـ scripts (25 دقيقة)
```bash
# انسخ الملفات الأصلية للأمان
cp migration-script-main.js migration-script-main.js.BACKUP
cp migration-script.sql migration-script.sql.BACKUP
```

- [ ] تم حفظ نسخة احتياطية من migration-script-main.js
- [ ] تم حفظ نسخة احتياطية من migration-script.sql
- [ ] جميع النسخ الاحتياطية موجودة في مكان آمن

**خلاصة التحضير:**
- [ ] جميع المعلومات صحيحة ومؤكدة
- [ ] جميع الاتصالات تعمل بنجاح
- [ ] Scripts جاهزة للاختبار

---

### 11:30 - 12:30 ظهراً | 🧪 الاختبار على بيانات التجربة (60 دقيقة)

**المسؤول:** مدير قاعدة البيانات (DBA) + مدير النظام الخلفي

**البيئة:** بيانات اختبار فقط (ليس الإنتاج!)

#### 11:30 - 11:35: تحضير بيانات الاختبار (5 دقائق)
```bash
node test-data-setup.js
```

- [ ] تم تشغيل الأمر بنجاح
- [ ] تم إنشاء `test_alawael_old` بـ 5 مستخدمين و 5 منتجات
- [ ] تم إنشاء `test_alawael_new` قاعدة فارغة
- [ ] لا توجد رسائل خطأ

#### 11:35 - 11:45: تعديل Scripts للاختبار (10 دقائق)
في `migration-script-main.js`:
```javascript
// غير اسم قاعدة البيانات مؤقتاً:
database: 'test_alawael_old'  // بدلاً من alawael_old
database: 'test_alawael_new'  // بدلاً من alawael_new
```

- [ ] تم التعديل بنجاح
- [ ] احفظ الملف

#### 11:45 - 12:00: تشغيل الهجرة على بيانات الاختبار (15 دقيقة)
```bash
node migration-script-main.js
```

**المراقبة الحية:**
```
أثناء التنفيذ، انتظر الرسائل التالية:
- [ ] "🔌 جاري الاتصال بـ قواعد البيانات..."
- [ ] "✓ متصل بقاعدة البيانات القديمة"
- [ ] "✓ متصل بقاعدة البيانات الجديدة"
- [ ] "📋 بدء هجرة بيانات المستخدمين..."
- [ ] "✅ انتهى نقل المستخدمين: النجح: 5"
- [ ] "📦 بدء هجرة بيانات المنتجات..."
- [ ] "✅ انتهى نقل المنتجات: النجح: 5"
```

**النتيجة المتوقعة:**
```
✅ انتهت الهجرة بنجاح في X.XX دقيقة
الإجمالي الناجح: 10
الإجمالي الفاشل: 0
```

- [ ] الهجرة انتهت بنجاح بدون أخطاء
- [ ] جميع البيانات انتقلت (5 مستخدمين + 5 منتجات)
- [ ] لا توجد رسائل خطأ

#### 12:00 - 12:15: التحقق من البيانات المنقولة (15 دقيقة)
```bash
node migration-verification.js
```

**المراقبة الحية:**
```
انتظر الرسائل التالية:
- [ ] "✓ جدول users موجود"
- [ ] "✓ جدول products موجود"
- [ ] "✓ لا توجد قيم مفقودة في البريد الإلكتروني"
- [ ] "✓ جميع كلمات المرور مشفرة بـ bcrypt"
- [ ] "✅ جميع الفحوصات نجحت!"
```

**النتيجة المتوقعة:**
- [ ] جميع الفحوصات نجحت ✅
- [ ] لا توجد تحذيرات حمراء ❌
- [ ] البيانات سليمة وجاهزة

#### 12:15 - 12:30: توثيق النتائج (15 دقيقة)
احفظ النتائج في ملف نصي:
```
ملف: test-results-2026-02-26.txt

التاريخ: 26 فبراير 2026
الوقت: 11:30-12:30
مدير قاعدة البيانات: ________________

النتائج:
✓ بيانات الاختبار: تم الإنشاء بنجاح
✓ الهجرة: تم بنجاح (10 سجلات)
✓ التحقق: نجح (بدون أخطاء)
✓ كلمات المرور: مشفرة بـ bcrypt
✓ الجداول: صحيحة

الخلاصة: جاهزة للإنتاج ✅
```

- [ ] تم حفظ النتائج في ملف
- [ ] تم طباعة النتائج للمراجعة
- [ ] استعد للاجتماع التالي

---

### 12:30 - 13:00 ظهراً | ✅ عرض النتائج والموافقة (30 دقيقة)

**الحضور:**
- [ ] مدير قاعدة البيانات (DBA)
- [ ] مدير النظام الخلفي (Backend Lead)
- [ ] مدير المشروع (VP Engineering)

**جدول الاجتماع:**

#### 12:30-12:40: عرض النتائج (10 دقائق)
DBA يعرض:
- [ ] نتائج الهجرة على بيانات الاختبار
- [ ] رسائل النجاح والتحقق
- [ ] لقطات الشاشة من التنفيذ

**الأسئلة المتوقعة من VP:**
- [ ] هل جميع البيانات انتقلت بنجاح?
  → نعم، ✅ 5 مستخدمين و 5 منتجات
  
- [ ] هل كلمات المرور محمية?
  → نعم، ✅ جميعها مشفرة بـ bcrypt
  
- [ ] هل الهجرة تستغرق وقتاً طويلاً?
  → لا، ✅ استغرقت ~5 دقائق على بيانات الاختبار

#### 12:40-12:50: النقاش والمخاوف (10 دقائق)
- [ ] هل توجد أي مخاوف متبقية?
- [ ] هل يحتاج أي شخص لأسئلة إضافية?
- [ ] هل الخطة واضحة للجميع?

#### 12:50-13:00: الموافقة الرسمية (10 دقائق)

**القرار النهائي من VP Engineering:**
- [ ] ✅ **موافق** - يمكن المتابعة إلى 28 فبراير 6:00 صباحاً

**التوقيع على الموافقة:**
```
Name: ___________________________
Date: 26 فبراير 2026
Time: 12:50 ظهراً
Signature: ______________________

"أوافق على تنفيذ الهجرة في 28 فبراير 6:00 صباحاً"
```

- [ ] تم الحصول على الموافقة الرسمية
- [ ] تم توثيق الموافقة في بريد إلكتروني أو رسالة
- [ ] الفريق كله يعرفون أن الموافقة حصلت

---

### 13:00 - 17:00 بعد الظهر | 🌙 الراحة والتحضير الذهني

- [ ] خذ فترة استراحة جيدة
- [ ] تناول غداء صحي
- [ ] لا تقلق بشأن الأعمال
- [ ] نم جيداً في الليل!

**ملاحظات للمساء:**
- [ ] أعد بيئة عمل نظيفة
- [ ] تأكد أن كل الملفات محفوظة
- [ ] تحقق من درجة حرارة الخادم إن أمكن

---

---

## ⭐ الجمعة 28 فبراير - يوم الهجرة الحقيقي (THE BIG DAY!)

### 05:45 - 06:00 صباحاً | 🌅 التحضير النهائي (15 دقيقة)

**المسؤول:** مدير قاعدة البيانات (DBA)

**قبل الذهاب للعمل:**
- [ ] نمت جيداً البارحة
- [ ] تناولت فطوراً صحياً
- [ ] شربت قهوة/شاي
- [ ] ارتديت ملابس مريحة

**في الطريق للعمل:**
- [ ] شغل الموسيقى الهادئة
- [ ] خذ نفس عميق عدة مرات
- [ ] تذكر: أنت مستعد! ✅

**عند الوصول للمكتب (05:50):**
- [ ] تحقق من تعقيم محطة العمل
- [ ] افتح محطة العمل الرئيسية
- [ ] تحقق من الإضاءة والمروحة/التهوية
- [ ] ضع الهاتف قرب يدك (للاتصالات الحرجة)

**قبل 06:00 بـ 5 دقائق:**
- [ ] افتح ملف MIGRATION_QUICK_REFERENCE.md
- [ ] اقرأ الخطوات السريعة مرة أخرى (2 دقيقة)
- [ ] استعد عقلياً (3 دقائق)

---

### 06:00 - 06:30 صباحاً | 🔐 النسخ الاحتياطي والإيقاف (30 دقيقة)

**البدء الرسمي: 06:00 صباحاً بالضبط**

#### 06:00-06:05: اتصال إداري (5 دقائق)
```bash
# اتصل بـ VP Engineering
phone_call: "مرحباً، بدأت عملية الهجرة الآن في 06:00 صباحاً. كل شيء يسير كالمخطط له"

# ابقِ على الهاتف أو أبقِ الـ Zoom مفتوحاً
zoom_room: "جاهز للبث المباشر"
```

- [ ] اتصال مع VP Engineering ✓
- [ ] تأكيد: "كل شيء يسير بحسب الخطة"

#### 06:05-06:15: احسب الصفوف المرجعية (10 دقائق)
**هذه أرقام مهمة جداً - ستقارن معها لاحقاً**

```bash
# أمر 1: عد المستخدمين في النظام القديم
mysql -h [old-host] -u [user] -p[password] alawael_old -e "SELECT COUNT(*) as user_count FROM users;"
→ النتيجة: ____________  (اكتب الرقم هنا)

# أمر 2: عد المنتجات في النظام القديم
mysql -h [old-host] -u [user] -p[password] alawael_old -e "SELECT COUNT(*) as product_count FROM products;"
→ النتيجة: ____________  (اكتب الرقم هنا)

# أمر 3: عد الطلبات في النظام القديم
mysql -h [old-host] -u [user] -p[password] alawael_old -e "SELECT COUNT(*) as order_count FROM orders;"
→ النتيجة: ____________  (اكتب الرقم هنا)
```

- [ ] تم حساب جميع الأرقام المرجعية
- [ ] تم كتابة الأرقام على ورقة (للمقارنة لـاحقاً)

#### 06:15-06:25: أنشئ نسخة احتياطية كاملة (10 دقائق)
**تحذير حرج:** هذا هو آخر خط دفاع إذا حدث شيء!

```bash
# أمر النسخ الاحتياطي الكامل
date_time=$(date +%Y%m%d_%H%M%S)
mysqldump -h [old-host] -u [user] -p[password] alawael_old \
  > "backup_final_FEB28_${date_time}.sql"

# تحقق من حجم الملف
ls -lh backup_final_FEB28_*.sql
→ يجب أن يكون > 100 MB (إذا كانت البيانات كثيرة)

# انسخ النسخة الاحتياطية إلى مكان آمن (Cloud/Drive خارجي)
cp backup_final_FEB28_*.sql "D:\Backup\Database Backups\"
```

- [ ] تم إنشاء ملف النسخة الاحتياطية بنجاح
- [ ] حجم الملف معقول (> 100 MB)
- [ ] تم نسخ الملف إلى مكان آمن

#### 06:25-06:30: إيقاف التطبيق القديم (5 دقائق)
```bash
# أيقف التطبيق القديم تماماً
# (الطريقة تختلف حسب النظام)

# إذا كان Node.js:
pm2 stop all  # أو pkill node

# إذا كان Docker:
docker-compose down

# تحقق من الإيقاف
# (لا يجب أن يكون هناك اتصالات)
netstat -an | grep :3000  # يجب أن تكون النتيجة فارغة
```

- [ ] تم إيقاف التطبيق القديم بنجاح
- [ ] لا توجد اتصالات نشطة مع قاعدة البيانات القديمة
- [ ] الخادم فارغ وجاهز

---

### 06:30 - 08:00 صباحاً | 🚀 تنفيذ Scripts الهجرة بالترتيب (90 دقيقة)

**الخط الزمني الحرج! كل دقيقة مهمة!**

#### 06:30-06:45 صباحاً: تنفيذ الهجرة الفعلية (15 دقيقة)

**تأكد أن النسخ الاحتياطية جاهزة قبل البدء!**

```bash
# عد تعيين بيانات الاتصال بالإنتاج (إذا ما زالت في testing):
# في migration-script-main.js:
# database: 'alawael_old'  ← الإنتاج الحقيقي
# database: 'alawael_new'  ← الإنتاج الجديد

# شغل النص الرئيسي:
node migration-script-main.js

# راقب الـ output:
# 06:30 - ✅ اتصال بقاعدة البيانات
# 06:32 - 📋 بدء نقل المستخدمين
# 06:38 - ✅ انتهى نقل المستخدمين: النجح: 1523
# 06:40 - 📦 بدء نقل المنتجات
# 06:45 - ✅ انتهى نقل المنتجات: النجح: 456

# احفظ السجل:
node migration-script-main.js | tee migration_run_FEB28_0630.log
```

**إذا حدث خطأ في أي نقطة:**
- [ ] اقرأ الرسالة بعناية
- [ ] تذكر الخطأ
- [ ] اتصل بـ VP Engineering فوراً
- [ ] أبلغ عن الخطأ الدقيق
- [ ] قرر: إصلاح سريع أم rollback?

**عند النجاح:**
- [ ] رسالة النجاح ظهرت ✅
- [ ] عدد الصفوف يطابق الأرقام المرجعية
- [ ] لا توجد رسائل خطأ حمراء

---

### 08:00 - 08:30 صباحاً | 🔍 التحقق الشامل (30 دقيقة)

#### 08:00-08:15: تشغيل نص التحقق (15 دقيقة)

```bash
# شغل نص التحقق الشامل
node migration-verification.js

# توقع الرسائل التالية:
# ✓ جدول users موجود
# ✓ جدول products موجود
# 📊 عدد الصفوف:
#    المستخدمين: 1523
#    المنتجات: 456
# ✓ لا توجد قيم مفقودة
# ✓ جميع كلمات المرور مشفرة
# ✅ جميع الفحوصات نجحت!
```

**كل الفحوصات يجب أن تكون خضراء ✅**

- [ ] جميع الفحوصات نجحت
- [ ] لا توجد رسائل خطأ حمراء
- [ ] عدد الصفوف يطابق الأرقام المرجعية (أعلاه)

#### 08:15-08:30: اختبار يدوي إضافي (15 دقيقة)

```bash
# اختبر 1: لاحظ البيانات المنقولة
mysql -h [new-host] -u [user] -p[password] alawael_new \
  -e "SELECT * FROM users LIMIT 5\G"
# يجب أن ترى: 5 مستخدمين بـ عناوين بريد، أسماء، إلخ

# اختبر 2: تحقق من صيغة كلمات المرور
mysql -h [new-host] -u [user] -p[password] alawael_new \
  -e "SELECT email, password FROM users LIMIT 1\G"
# يجب أن تبدأ كلمة المرور بـ: $2b$

# اختبر 3: عد الصفوف النهائي
mysql -h [new-host] -u [user] -p[password] alawael_new -e \
  "SELECT 'users' as table_name, COUNT(*) as count FROM users
   UNION ALL
   SELECT 'products', COUNT(*) FROM products
   UNION ALL
   SELECT 'orders', COUNT(*) FROM orders;"
# يجب أن تطابق الأرقام المرجعية أعلاه
```

- [ ] اختبار 1: البيانات موجودة وصحيحة
- [ ] اختبار 2: كلمات المرور مشفرة بـ bcrypt
- [ ] اختبار 3: عدد الصفوف يساوي الأرقام المرجعية

**الخلاصة:**
```
☑ جميع الفحوصات نجحت
☑ البيانات سليمة 100%
☑ جاهز للتفعيل
```

---

### 08:30 - 09:00 صباحاً | ✨ التفعيل والإعلان (30 دقيقة)

#### 08:30-08:35: بدء التطبيق الجديد (5 دقائق)

**مسؤول النظام الخلفي (Backend Lead):**

```bash
# ابدأ التطبيق الجديد
cd /path/to/erp_new_system/backend
npm start

# تحقق من البدء
# (انتظر حتى تشاهد "Server running on port 3000" أو ما شابه)

# تحقق من الاتصال بقاعدة البيانات:
# يجب أن تشاهد في السجلات: ✓ Connected to database
```

- [ ] التطبيق بدأ بنجاح
- [ ] لا توجد رسائل خطأ في السجلات
- [ ] الاتصال بقاعدة البيانات الجديدة نجح

#### 08:35-08:45: اختبار وظيفي سريع (10 دقائق)

**المسؤول: مدير النظام الخلفي**

```bash
# اختبر 1: تسجيل الدخول بحساب قديم
# (استخدم حساب من الأرقام المرجعية أعلاه)
curl -X POST http://localhost:3000/api/login \
  -d "email=ali@test.com&password=TestPass123"

# الاستجابة المتوقعة: token أو .json بـ معلومات المستخدم

# اختبر 2: استرجاع قائمة المستخدمين
curl http://localhost:3000/api/users

# الاستجابة يجب أن تحتوي: [ { email, firstName, lastName, ... } ]

# اختبر 3: استرجاع قائمة المنتجات
curl http://localhost:3000/api/products

# الاستجابة يجب أن تحتوي: [ { name, sku, pricing, ... } ]
```

- [ ] تسجيل الدخول بحساب قديم نجح ✅
- [ ] استرجاع المستخدمين نجح ✅
- [ ] استرجاع المنتجات نجح ✅

#### 08:45-09:00: الإعلان الرسمي (15 دقيقة)

```bash
# أرسل الإعلان في Slack / Teams
@channel @here
✅ تم انتقال النظام بنجاح إلى نسخة جديدة!
- جميع بياناتك القديمة هنا
- قد تكون هناك تأخير طفيف
- لا تعيد تحديث الصفحة كثيراً
- أي مشاكل؟ اتصل بنا: +966...

Time: 2026-02-28 09:00 AM
```

**اتصالات إدارية:**
- [ ] أرسل رسالة نجاح إلى عميل رئيسي
- [ ] أخبر الفريق: "الهجرة انتهت بنجاح!"
- [ ] ابقَ متيقظاً لأي استفسارات

**المراقبة تبدأ الآن:**
- [ ] راقب سجلات الخطأ كل 5 دقائق
- [ ] راقب الأداء (CPU, Memory, Disk)
- [ ] صحح أي مشاكل فوراً

---

### 09:00 - 09:30 صباحاً | 📊 المراقبة الأولية (30 دقيقة)

```
الفترات الزمنية:
09:00-09:05 - فحص سريع
09:05-09:10 - استراحة قصيرة
09:10-09:15 - فحص ثاني
09:15-09:20 - استراحة قصيرة
09:20-09:25 - فحص ثالث
09:25-09:30 - تقرير أولي
```

**كل 5 دقائق، تحقق من:**
- [ ] هل هناك رسائل خطأ؟
- [ ] هل استجابة الخادم سريعة؟
- [ ] هل المستخدمون يستطيعون الدخول؟

---

### 09:30 - 18:00 (باقي اليوم) | 🛡️ المراقبة المستمرة

**أول 8 ساعات:**
- [ ] فحص كل 5 دقائق
- [ ] سجل أي مشاكل
- [ ] صحح الأخطاء فوراً

---

---

## 📋 ملاحظات عامة

### في حالة الفشل (نعوذ بالله):
```
1. اتصل بـ VP Engineering فوراً
2. أبلغ عن الخطأ الدقيق
3. استخدم: node migration-rollback.js
4. استعد لمحاولة جديدة غداً أو الأسبوع القادم
```

### للنجاح:
```
1. أريح نفسك - أنت فعلت شيء عظيم! 🎉
2. احفظ جميع السجلات للتوثيق
3. ركز على ملاحظات الدرس المستفادة
```

---

**تاريخ الإعداد:** 25 فبراير 2026  
**النسخة:** 1.0.0  
**آخر تحديث:** 25 فبراير 2026  
**الحالة:** ✅ جاهز للطباعة والاستخدام
