# 📱 React Native Driver Tracking Mobile App - Phase 32

## نظرة عامة
تطبيق موبايل متكامل لتتبع السائقين والأسطول باستخدام React Native. يوفر تجربة مستخدم سلسة لتتبع الموقع في الوقت الفعلي، والإشعارات الذكية، وتحليل الأداء.

---

## 🎯 الميزات الرئيسية

### 🔐 المصادقة والأمان
- تسجيل دخول آمن بالبريد والكلمة
- تخزين توكن آمن (AsyncStorage)
- استعادة الجلسة تلقائية
- تحديث التوكن (Refresh Token)
- تغيير كلمة المرور
- إعادة تعيين كلمة المرور

### 📍 تتبع الموقع الحقيقي
- تتبع GPS تلقائي كل 10 ثواني
- رفع الموقع إلى الخادم كل 30 ثانية
- حفظ المسار على الخريطة
- دقة الموقع العالية (High Accuracy)
- التكيف مع وضع الخلفية

### 🗺️ الخريطة التفاعلية
- عرض الموقع الحالي على الخريطة
- رسم المسار المسجل
- أزرار تحكم (تكبير/تصغير/توسيط)
- البحث عن السائقين القريبين
- مسح سجل المسار

### 📊 لوحة التحكم
- عرض السرعة الحالية
- درجة الأداء
- عدد الإشعارات الجديدة
- إحصائيات اليوم (المسافة، الوقت، الانتهاكات)
- حالة التتبع (مفعل/معطل)

### 🔔 نظام الإشعارات الذكي
- إشعارات غير محدودة
- فلترة (الكل، غير المقروءة، الانتهاكات، التنبيهات)
- تنبيهات الانتهاكات (سرعة، فرملة، تسارع)
- تقارير الأداء
- تذكيرات الصيانة
- رموز الشارات

### 👤 الملف الشخصي
- عرض بيانات السائق الكاملة
- معلومات الرخصة
- الإحصائيات الشخصية
- تحديث ملف البيانات
- تغيير كلمة المرور
- خيارات الإشعارات
- تسجيل الخروج الآمن

---

## 📁 هيكل المشروع

```
mobile/
├── App.jsx                           # التطبيق الرئيسي
├── package.json                      # الحزم والتبعيات
│
├── screens/                          # شاشات التطبيق
│   ├── SplashScreen.jsx             # شاشة البداية
│   ├── auth/
│   │   ├── LoginScreen.jsx          # تسجيل الدخول
│   │   ├── RegisterScreen.jsx       # التسجيل الجديد (قريباً)
│   │   └── ForgotPasswordScreen.jsx # استعادة كلمة المرور (قريباً)
│   └── app/
│       ├── DashboardScreen.jsx      # لوحة التحكم الرئيسية
│       ├── MapScreen.jsx            # الخريطة والتتبع
│       ├── NotificationsScreen.jsx  # الإشعارات
│       ├── ProfileScreen.jsx        # الملف الشخصي
│       └── SettingsScreen.jsx       # الإعدادات (قريباً)
│
├── services/                         # خدمات التطبيق
│   ├── AuthService.js               # خدمة المصادقة (385 سطر)
│   ├── GPSService.js                # خدمة GPS (190 سطر)
│   └── NotificationService.js       # خدمة الإشعارات (220 سطر)
│
├── navigation/
│   └── RootNavigator.js             # إدارة التنقل (210 سطر)
│
├── constants/
│   ├── colors.js                    # الألوان الموحدة
│   ├── strings.js                   # النصوص والرسائل
│   └── config.js                    # إعدادات التطبيق
│
├── assets/
│   └── default-avatar.png           # الصورة الافتراضية
│
└── utils/                            # دوال مساعدة
    ├── validators.js                # التحقق من البيانات
    └── formatters.js                # تنسيق البيانات

```

---

## 🚀 البدء السريع

### المتطلبات
- Node.js 16+
- React Native CLI
- Android SDK أو Xcode (iOS)
- Expo CLI (اختياري)

### التثبيت

```bash
# 1. استنسخ المشروع
git clone <repo-url>
cd mobile

# 2. ثبت الحزم
npm install
# أو
yarn install

# 3. ثبت الحزم الأصلية (Native)
npx react-native link

# 4. شغل التطبيق على Android
npm run android
# أو iOS
npm run ios
```

### الإعدادات الأولية

1. **تحديث API URL**: عدّل `API_BASE_URL` في الملفات:
   ```javascript
   const API_BASE_URL = 'http://YOUR_SERVER_IP:3001/api';
   ```

2. **Firebase Project**: للإشعارات
   - أضفر `google-services.json` إلى `android/app/`
   - وأضفر `GoogleService-Info.plist` إلى `ios/`

3. **الأذونات**: ستُطلب من المستخدم الآن:
   - الموقع الدقيق
   - الكاميرا
   - التخزين

---

## 📱 شاشات التطبيق

### شاشة تسجيل الدخول (LoginScreen)
```typescript
// المدخلات
- email: string
- password: string

// المخرجات
- تسجيل دخول ناجح → لوحة التحكم
- خطأ في المصادقة → رسالة خطأ
```

**الميزات:**
- التحقق من صحة البيانات
- إظهار/إخفاء كلمة المرور
- رابط نسيان كلمة المرور
- رابط التسجيل الجديد
- شروط الخدمة

### شاشة لوحة التحكم (DashboardScreen)
```
┌─────────────────────────────────┐
│  مرحباً بك               ⚙️      │
│  محمد أحمد الشهري              │
├─────────────────────────────────┤
│  ✅ التتبع مفعل    [إيقاف]     │
├─────────────────────────────────┤
│  ⚡ السرعة: 85 كم/س  📊 أداء  │
│  ✅ سرعة آمنة        5.0/5     │
├─────────────────────────────────┤
│  📍 الموقع الحالي               │
│  25.2048° N, 55.2708° E        │
├─────────────────────────────────┤
│  📊 إحصائيات اليوم              │
│  المسافة: 125 كم               │
│  الوقت: 3.5 ساعات             │
│  الانتهاكات: 0                 │
├─────────────────────────────────┤
│  [الخريطة] [الدعم] [التقارير]   │
└─────────────────────────────────┘
```

### شاشة الخريطة (MapScreen)
```
الميزات:
+ تتبع GPS مباشر
+ رسم المسار
+ أزرار التحكم:
  - تكبير / تصغير
  - توسيط (GPS)
  - البحث عن سائقين قريبين
  - مسح المسار
- معلومات الموقع الحالي في الأسفل
```

### شاشة الإشعارات (NotificationsScreen)
```
الفلاتر:
- الكل (جميع الإشعارات)
- غير المقروءة
- الانتهاكات
- التنبيهات العاجلة

لكل إشعار:
- أيقونة + عنوان + رسالة
- الوقت
- زر تحديث كمقروء
- زر حذف
```

### شاشة الملف الشخصي (ProfileScreen)
```
الأقسام:
1. صورة الملف + الاسم + الدور
2. البيانات الشخصية
   - البريد الإلكتروني
   - الهاتف
   - رقم الهوية
   - تاريخ التوظيف
3. معلومات الترخيص
   - رقم الرخصة
   - صلاحية الرخصة
   - التصنيفات
4. الإحصائيات
   - درجة الأداء
   - الانتهاكات
   - عدد الرحلات
5. الخيارات
   - تغيير كلمة المرور
   - إعدادات الإشعارات
   - حول التطبيق
   - شروط الخدمة
6. زر تسجيل الخروج
```

---

## 🔧 الخدمات (Services)

### AuthService (385 سطر)
```javascript
// الدوال الرئيسية
- login(email, password): Promise
- logout(): Promise
- refreshAccessToken(): Promise
- loadStoredData(): Promise
- getCurrentUser(): Promise
- updateProfile(data): Promise
- changePassword(old, new): Promise
- requestPasswordReset(email): Promise
- resetPassword(token, newPassword): Promise
- checkConnectivity(): Promise
```

### GPSService (190 سطر)
```javascript
// الدوال الرئيسية
- startLocationTracking(callback, interval): Promise
- stopLocationTracking(): void
- uploadLocation(location, driverId): Promise
- getCurrentSpeed(): Promise
- getDistance(lat1, lon1, lat2, lon2): number
- getLocationHistory(driverId, hours): Promise
- getDrivingBehavior(driverId): Promise
- getActiveAlerts(driverId): Promise
- acknowledgeAlert(locationId, alertIndex): Promise
```

### NotificationService (220 سطر)
```javascript
// الدوال الرئيسية
- getUnreadNotifications(userId, limit): Promise
- getAllNotifications(userId, page, limit): Promise
- getUnreadCount(userId): Promise
- markAsRead(notificationId): Promise
- deleteNotification(notificationId): Promise
- sendQuickNotification(userId, title, msg, priority): Promise
- sendViolationAlert(driverId, type, details): Promise
- sendPerformanceReport(driverId, period): Promise
- getNotificationStats(userId): Promise
- showLocalNotification(title, msg, priority): void
- scheduleNotification(userId, title, msg, time): void
- sendBulkNotifications(driverIds, title, msg): Promise
```

---

## 🧭 إدارة التنقل

```javascript
Navigation Structure:
├── Splash (Loading)
├── Auth Stack
│   ├── Login
│   ├── Register
│   └── ForgotPassword
└── App Stack
    └── App Tabs
        ├── Dashboard
        ├── Map
        ├── Notifications
        └── Profile
    └── Modal Stack
        └── Settings
```

---

## 🔐 أمان البيانات

1. **التخزين المحلي**:
   ```javascript
   AsyncStorage.setItem('token', jwtToken);     // مشفر
   AsyncStorage.setItem('user', userData);      // مشفر
   AsyncStorage.setItem('refreshToken', token); // مشفر
   ```

2. **Headers عند الطلب**:
   ```javascript
   Authorization: Bearer <JWT_TOKEN>
   Content-Type: application/json
   User-Agent: DriverTrackingApp/1.0.0
   ```

3. **HTTPS فقط** (في الإنتاج):
   ```javascript
   const API_BASE_URL = 'https://api.yourdomain.com/api';
   ```

---

## 🎨 التصميم UI

### الألوان الرئيسية
- 🟦 الأزرق: `#4ECDC4` (Primary)
- 🟥 الأحمر: `#FF6B6B` (Danger)
- 🟩 الأخضر: `#51CF66` (Success)
- 🟨 البرتقالي: `#FFB647` (Warning)
- ⬜ الرمادي: `#F5F5F5` (Background)

### الخطوط
- الرئيسية: `Rubik` / System Font
- الحجم: 12-28px
- الوزن: 400-700

---

## 📊 الإحصائيات والبيانات

### الوقت الفعلي
- **تحديث الموقع**: كل 10 ثواني
- **رفع الموقع**: كل 30 ثانية
- **تحديث الإشعارات**: كل 30 ثانية
- **تحديث الأداء**: عند الطلب

### استهلاك البيانات
- تحديث موقع واحد: ~500 B
- إشعار واحد: ~300 B
- يومياً (نشط): ~15-20 MB

### استهلاك البطارية
- GPS مفعل: 25-30% / ساعة
- الشاشة: 40-50% / ساعة
- Wi-Fi/4G: 5-10% / ساعة

---

## 🧪 الاختبار

```bash
# اختبار الملفات
npm test

# اختبار مع Coverage
npm test -- --coverage

# اختبار بوضع الساعة
npm test -- --watch
```

### حالات الاختبار
- ✅ تسجيل دخول صحيح/خاطئ
- ✅ تحديث التوكن
- ✅ تتبع GPS
- ✅ إرسال الموقع
- ✅ الإشعارات المحلية والبعيدة
- ✅ الملف الشخصي والتحديثات

---

## 📦 الحزم المستخدمة

```json
{
  "react": "18.2.0",
  "react-native": "0.72.0",
  "@react-navigation/native": "6.1.0",
  "@react-navigation/bottom-tabs": "6.5.0",
  "react-native-maps": "1.8.0",
  "@react-native-community/geolocation": "3.2.1",
  "react-native-vector-icons": "10.0.0",
  "axios": "1.6.0",
  "react-native-push-notification": "8.1.1",
  "@react-native-async-storage/async-storage": "1.18.2"
}
```

---

## 🚀 النشر والإنتاج

### Android
```bash
# بناء Release APK
npm run build:android

# إصدار محقق
./android/gradlew assembleRelease -p android

# التوقيع
jarsigner -verbose -sigalg SHA256withRSA -digestalg SHA-256 \
  -keystore keystore.jks app-release-unsigned.apk alias_name

# التوازن
zipalign -v 4 app-release-unsigned.apk app-release.apk
```

### iOS
```bash
# بناء Release
npm run build:ios

# التوقيع والنشر
xcodebuild -workspace DriverTracking.xcworkspace \
  -scheme DriverTracking -configuration Release \
  -derivedDataPath build
```

---

## 🐛 استكشاف الأخطاء

### GPS لا يعمل
```javascript
1. تحقق من الأذونات:
   - Android: Settings → Apps → Permissions → Location
   - iOS: Settings → Privacy → Location Services

2. تفعيل الموقع في الإعدادات
3. استخدم جهاز واقعي (المحاكاة قد لا تعمل)
```

### الإشعارات لا تظهر
```javascript
1. تحقق من Firebase Configuration
2. تأكد من تفعيل الإشعارات في الإعدادات
3. تحقق من صحة البيانات المرسلة
4. عدّل أولويات الإشعارات
```

### الأداء بطيء
```javascript
1. قلل تحديثات GPS (من 10 إلى 20 ثانية)
2. قلل حجم البيانات المُرسلة
3. استخدم React.memo للتحسينات
4. عطّل تشخيصات التطوير
```

---

## 📞 الدعم والمساعدة

- 📧 البريد: support@company.com
- 📱 الهاتف: +966-XX-XXXX-XXXX
- 💬 الدردشة: https://chat.company.com
- 🐛 الأخطاء: https://github.com/repo/issues

---

## 📄 الترخيص

هذا المشروع مرخص تحت [MIT License](LICENSE)

---

## ✨ الإصدارات القادمة

- [ ] إشعارات الصوت والاهتزاز
- [ ] الكاميرا الخلفية للتنبيهات
- [ ] دعم Offline Mode
- [ ] الترجمة متعددة اللغات
- [ ] التيمات المظلمة
- [ ] اتصال Bluetooth للأجهزة الإضافية

---

**تم إنشاؤه بحب من قبل فريق التطوير** ❤️
