# 🚀 دليل البدء العملي للمشروع
**التاريخ:** 19 فبراية 2026
**الحالة:** جاهز للتطوير

---

## 📋 الحالة الحالية

✅ **التنظيف:** اكتمل بنجاح
✅ **Git:** مع آخر commit  
⚠️ **الأمان:** 51 ثغرة أمنية (معظمها في المكتبات القديمة)
✅ **Scripts:** تم إضافة scripts في الجذر

---

## 🚀 خطوات البدء السريعة

### 1️⃣ **بدء المشروع كاملاً** (Frontend + Backend معاً)

```bash
# الطريقة الأولى: استخدام npm scripts
npm run start

# أو الطريقة الثانية: يدويّ من مجلدات منفصلة
# في terminal واحد:
npm run start:backend

# وفي terminal آخر:
npm run start:frontend
```

### 2️⃣ **بدء Backend فقط**

```bash
npm run start:backend

# أو بوضع التطوير مع auto-reload
npm run dev:backend
```

### 3️⃣ **بدء Frontend فقط**

```bash
npm run start:frontend
```

### 4️⃣ **تشغيل الاختبارات**

```bash
# اختبارات Backend
npm test

# أو
npm run test:backend

# اختبارات Frontend
npm run test:frontend
```

### 5️⃣ **بناء المشروع**

```bash
npm run build:frontend
```

---

## 📚 الـ Scripts المتاحة

### تشغيل المشروع
- `npm start` - بدء المشروع الكامل
- `npm run start:erp` - بدء المشروع الكامل (نفس الأعلى)
- `npm run start:backend` - بدء الخادم فقط
- `npm run start:frontend` - بدء الواجهة الأمامية فقط

### التطوير
- `npm run dev` - وضع التطوير للـ Backend
- `npm run dev:backend` - وضع التطوير مع auto-reload

### الاختبارات
- `npm test` - اختبارات Backend
- `npm run test:backend` - اختبارات Backend
- `npm run test:frontend` - اختبارات Frontend

### البناء والتجميع
- `npm run build` - بناء المشروع
- `npm run build:frontend` - بناء Frontend

### فحص الجودة
- `npm run lint` - فحص Backend (إن وجد)
- `npm run lint:backend` - فحص Backend
- `npm run lint:frontend` - فحص Frontend

### الأمان
- `npm run audit` - فحص الثغرات الأمنية
- `npm run audit-fix` - إصلاح الثغرات (بدون breaking changes)
- `npm run audit-fix-force` - إصلاح جميع الثغرات (قد تحتوي breaking changes)

### التثبيت
- `npm run install-all` - تثبيت جميع المكتبات (الجذر + backend + frontend)

---

## ⚠️ ملاحظات حول الأمان

### الثغرات الحالية
```
51 vulnerabilities (2 low, 4 moderate, 42 high, 3 critical)
```

### الثغرات التي تحتاج منصات خارجية
- **xlsx** - لا يوجد إصلاح متاح (شيت جي على مسؤوليتك)
- **quill** - مختبر XSS (قد يحتاج تحديث يدوي)

### الحل
1. **للثغرات العادية:**
   ```bash
   npm run audit-fix
   ```

2. **للثغرات الخطيرة:**
   ```bash
   npm run audit-fix-force
   ```
   ⚠️ تحذير: قد يسبب breaking changes

3. **للمتقدمين:**
   ```bash
   npm audit 2>&1 | less
   # ثم يمكنك اختيار الإصلاحات يدويّ
   ```

---

## 📂 هيكل المشروع

```
66666 (الجذر)
├── erp_new_system/        [المشروع الأساسي]
│   ├── backend/           [الخادم - Node.js/Express]
│   │   ├── server.js      [نقطة الدخول]
│   │   ├── routes/
│   │   ├── controllers/
│   │   └── package.json
│   ├── frontend/          [الواجهة - React]
│   │   ├── src/
│   │   ├── public/
│   │   └── package.json
│   └── mobile/            [التطبيق المحمول]
├── supply-chain-management/
├── secretary_ai/
├── intelligent-agent/
├── docker/
├── k8s/
├── devops/
├── scripts/
└── package.json           [الجذر]
```

---

## 🔌 المنافذ الافتراضية

بعد بدء المشروع:

| الخدمة | المنفذ | الرابط |
|:---|:---:|:---|
| Backend Server | 3000 | http://localhost:3000 |
| Frontend App | 3000 أو 5173 | http://localhost:3000 أو http://localhost:5173 |
| Database | يعتمد على التكوين | - |

---

## 📖 الملفات المرجعية

- **[erp_new_system/START_HERE.md](erp_new_system/START_HERE.md)** - دليل المشروع الأساسي
- **[erp_new_system/backend/README.md](erp_new_system/backend/README.md)** - توثيق Backend
- **[erp_new_system/frontend/README.md](erp_new_system/frontend/README.md)** - توثيق Frontend
- **[CLEANUP_INDEX_FEB19_2026.md](CLEANUP_INDEX_FEB19_2026.md)** - فهرس التنظيف

---

## 🆘 حل المشاكل الشائعة

### "Cannot find module..."
```bash
# أعد تثبيت المكتبات
npm run install-all
```

### "Port 3000 is already in use"
```bash
# غيّر المنفذ في متغيرات البيئة
# أو استخدم منفذ مختلف
PORT=3001 npm run start:backend
```

### الـ Frontend لا يتصل بـ Backend
```bash
# تأكد من:
# 1. أن Backend يعمل (اسمع على المنفذ 3000)
# 2. أن CORS مفعّل في Backend
# 3. أن المتغيرات البيئية صحيحة
```

### "npm: command not found"
```bash
# تأكد من تثبيت Node و npm
node --version
npm --version

# إذا لم تكن مثبتة:
# حمّل من https://nodejs.org
```

---

## 💡 نصائح للتطوير

### 1. **أثناء التطوير:**
استخدم وضع `dev` للتطوير السريع مع auto-reload:
```bash
npm run dev:backend
```

### 2. **الاختبار الدوري:**
شغّل الاختبارات قبل كل push:
```bash
npm test
npm run test:frontend
```

### 3. **فحص الجودة:**
قبل النشر، افحص الجودة:
```bash
npm run lint:backend
npm run lint:frontend
```

### 4. **مراقبة الأمان:**
افحص الثغرات بشكل دوري:
```bash
npm audit
```

---

## 🎯 المراحل التالية

### قصيرة الأجل (اليوم)
- [ ] اختبار بدء المشروع
- [ ] تشغيل الاختبارات
- [ ] التحقق من الاتصال بين Frontend و Backend

### متوسطة الأجل (الأسبوع)
- [ ] إصلاح الثغرات الأمنية الحرجة
- [ ] تحديث المكتبات القديمة
- [ ] توثيق الـ API

### طويلة الأجل (الشهر)
- [ ] إعادة بناء المشروع باستخدام أحدث الأدوات
- [ ] تحسين الأداء
- [ ] نشر الإنتاج

---

## 📞 الدعم والتواصل

في حالة واجهت أي مشكلة:

1. **تحقق من الملفات المرجعية** أولاً
2. **اقرأ رسالة الخطأ بعناية** - غالباً تحتوي على الحل
3. **ابحث عن المشكلة في GitHub Issues**
4. **أنشئ issue جديد** إذا لم تجد الحل

---

**🎉 أنت الآن جاهز للبدء! ابدأ بتشغيل المشروع الآن.**

```bash
npm start
```

أو

```bash
npm run start:backend  # Terminal 1
npm run start:frontend # Terminal 2
```

---

*تم التحديث:** 19 فبراير 2026*
