╔═══════════════════════════════════════════════════════════════════════════╗
║                    🐳 AL-AWAEL DOCKER DEPLOYMENT CARD                    ║
║                          Quick Reference v1.0                            ║
╚═══════════════════════════════════════════════════════════════════════════╝

┌─────────────────────────────────────────────────────────────────────────────┐
│ 📋 PHASE 1: LOCAL TESTING (Your Windows Machine)                           │
└─────────────────────────────────────────────────────────────────────────────┘

1️⃣ Navigate to backend:
   cd "C:\Users\x-be\OneDrive\المستندات\04-10-2025\66666\backend"

2️⃣ Build image:
   docker build -t alawael-backend:v1 .

3️⃣ Start with compose (5 seconds):
   docker-compose up -d
   Start-Sleep -Seconds 5

4️⃣ Test endpoints:
   curl http://localhost:3001/health
   curl http://localhost:3001/test-first
   curl http://localhost:3001/phases-29-33

5️⃣ Stop:
   docker-compose down

Status: ✅ When you see 200 OK responses

┌─────────────────────────────────────────────────────────────────────────────┐
│ 🌐 PHASE 2: DOCKER HUB REGISTRY (Push Image)                               │
└─────────────────────────────────────────────────────────────────────────────┘

1️⃣ Create account at https://hub.docker.com (if needed)

2️⃣ Create private repository: "alawael-backend"

3️⃣ Generate access token (Security settings)

4️⃣ Login locally:
   docker login
   # Username: your-username
   # Password: paste-access-token

5️⃣ Tag & push:
   docker tag alawael-backend:v1 <your-username>/alawael-backend:v1
   docker push <your-username>/alawael-backend:v1

Status: ✅ When you see "Successfully pushed" message

┌─────────────────────────────────────────────────────────────────────────────┐
│ 🖥️  PHASE 3: HOSTINGER VPS SETUP (Ubuntu 22.04)                            │
└─────────────────────────────────────────────────────────────────────────────┘

1️⃣ SSH into VPS (from any terminal):
   ssh root@<your-vps-ip>

2️⃣ Update system:
   apt update && apt upgrade -y

3️⃣ Install Docker:
   curl -fsSL https://get.docker.com -o get-docker.sh
   sudo sh get-docker.sh

4️⃣ Add to docker group:
   sudo usermod -aG docker $USER
   newgrp docker

5️⃣ Install Docker Compose:
   sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
   sudo chmod +x /usr/local/bin/docker-compose

6️⃣ Verify:
   docker --version && docker-compose --version

Status: ✅ When you see version numbers

┌─────────────────────────────────────────────────────────────────────────────┐
│ 🚀 PHASE 4: DEPLOY ON VPS                                                  │
└─────────────────────────────────────────────────────────────────────────────┘

1️⃣ Login to Docker Hub (on VPS):
   docker login

2️⃣ Pull image:
   docker pull <your-username>/alawael-backend:v1

3️⃣ Run container:
   docker run -d \
     --name alawael-app \
     -p 3001:3001 \
     -e PORT=3001 \
     -e USE_MOCK_DB=true \
     -e SKIP_SOCKET_IO=true \
     -e DISABLE_REDIS=true \
     -e SKIP_PHASE17=true \
     --restart=unless-stopped \
     <your-username>/alawael-backend:v1

4️⃣ Check status:
   docker ps
   docker logs alawael-app

5️⃣ Test locally (on VPS):
   curl http://localhost:3001/health

Status: ✅ When you see {"status":"ok",...}

┌─────────────────────────────────────────────────────────────────────────────┐
│ 🔐 PHASE 5: SETUP NGINX + SSL (Optional but Recommended)                    │
└─────────────────────────────────────────────────────────────────────────────┘

1️⃣ Install Nginx & certbot:
   apt install -y nginx certbot python3-certbot-nginx

2️⃣ Create config /etc/nginx/sites-available/default:
   server {
       listen 80;
       server_name your-domain.com www.your-domain.com;
       location / {
           proxy_pass http://127.0.0.1:3001;
           proxy_set_header Host $host;
           proxy_set_header X-Real-IP $remote_addr;
       }
   }

3️⃣ Test & reload:
   sudo nginx -t
   sudo systemctl reload nginx

4️⃣ Setup SSL:
   sudo certbot --nginx -d your-domain.com

5️⃣ Auto-renewal enabled:
   sudo systemctl status certbot.timer

Status: ✅ When HTTPS works at https://your-domain.com

┌─────────────────────────────────────────────────────────────────────────────┐
│ 📊 VERIFICATION CHECKLIST                                                   │
└─────────────────────────────────────────────────────────────────────────────┘

LOCAL TESTS:
  ☐ docker build completes without errors
  ☐ docker-compose up starts successfully
  ☐ Health check returns 200 OK
  ☐ Other endpoints respond properly
  ☐ docker-compose down stops cleanly

REGISTRY:
  ☐ Docker Hub account created
  ☐ Repository "alawael-backend" created
  ☐ Access token generated
  ☐ Image pushed successfully

VPS DEPLOYMENT:
  ☐ SSH access confirmed
  ☐ Docker installed on VPS
  ☐ Image pulled to VPS
  ☐ Container running with `docker ps`
  ☐ Health endpoint responds from VPS
  ☐ Nginx configured (if using)
  ☐ SSL certificate installed (if using)
  ☐ Domain resolves to VPS IP

┌─────────────────────────────────────────────────────────────────────────────┐
│ 🆘 QUICK TROUBLESHOOTING                                                    │
└─────────────────────────────────────────────────────────────────────────────┘

Container won't start:
  $ docker logs alawael-app

Port already in use (locally):
  $ docker ps (find the container)
  $ docker stop <container-id>

Can't push to Docker Hub:
  $ docker login (re-authenticate)
  $ docker logout (clear & retry)

VPS connection refused:
  $ Check firewall: ufw status
  $ Open port: ufw allow 3001/tcp

High CPU/Memory:
  $ docker stats alawael-app

┌─────────────────────────────────────────────────────────────────────────────┐
│ 🔄 MAINTENANCE COMMANDS                                                     │
└─────────────────────────────────────────────────────────────────────────────┘

View logs:
  docker logs -f alawael-app

Update image:
  docker pull <user>/alawael-backend:v1
  docker stop alawael-app
  docker rm alawael-app
  docker run -d ... (repeat run command from Phase 4)

Stop/Start/Restart:
  docker stop alawael-app
  docker start alawael-app
  docker restart alawael-app

Cleanup:
  docker system prune (remove unused images)
  docker volume prune (remove unused volumes)

┌─────────────────────────────────────────────────────────────────────────────┐
│ 📞 SUPPORT FILES                                                            │
└─────────────────────────────────────────────────────────────────────────────┘

✓ DOCKER_DEPLOYMENT_GUIDE.md     (Complete reference - all steps)
✓ docker-compose.yml              (Local testing config)
✓ Dockerfile                       (Production-ready image)
✓ .dockerignore                    (Optimized build)
✓ docker-deploy.ps1               (Automated script)
✓ DOCKER_SETUP_SUMMARY.md         (This card's details)

┌─────────────────────────────────────────────────────────────────────────────┐
│ ✅ EXPECTED RESULTS                                                         │
└─────────────────────────────────────────────────────────────────────────────┘

Health Endpoint Response:
  {
    "status": "ok",
    "message": "Server is running",
    ...
  }

Endpoints Available:
  /health                           ✓ 200 OK
  /test-first                       ✓ 200 OK
  /phases-29-33                     ✓ 200 OK
  /api/phases-29-33/*               ✓ Routed
  /api/phases-21-28/*               ✓ Routed

┌─────────────────────────────────────────────────────────────────────────────┐
│ 📅 TIMELINE                                                                 │
└─────────────────────────────────────────────────────────────────────────────┘

TODAY:
  ✓ Build & test locally (30 min)

TOMORROW:
  ✓ Push to Docker Hub (5 min)

THIS WEEK:
  ✓ Deploy on Hostinger VPS (15 min)
  ✓ Setup Nginx + SSL (20 min)

ONGOING:
  ✓ Monitor application
  ✓ Update image when needed
  ✓ Backup regularly

╔═══════════════════════════════════════════════════════════════════════════╗
║  Date: January 25, 2026 | Version: 1.0 | Status: Ready for Deployment   ║
╚═══════════════════════════════════════════════════════════════════════════╝
