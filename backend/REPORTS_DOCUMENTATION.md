# 📊 نظام التقارير البسيط والمرن

## Simple & Flexible Reporting System

**الحالة:** ✅ جاهز  
**النسخة:** 1.0.0  
**التاريخ:** 1 فبراير 2026

---

## ✨ الميزات الرئيسية

### 1️⃣ تقارير مخصصة مرنة ✅

```javascript
// إنشاء تقرير مخصص بسهولة
{
  "templateType": "PAYMENT_SUMMARY",
  "filters": {
    "startDate": "2026-01-01",
    "endDate": "2026-02-01",
    "groupBy": "daily"
  }
}
```

### 2️⃣ Export متعدد الصيغ ✅

- 📄 **CSV** - للاستيراد والمعالجة
- 📊 **Excel** - للتحليل المتقدم
- 📃 **PDF** - للطباعة والمشاركة

### 3️⃣ جدولة التقارير ✅

```javascript
// تقرير يومي تلقائياً
{
  "schedule": "0 9 * * *",  // كل يوم الساعة 9 صباحاً
  "templateType": "PAYMENT_SUMMARY",
  "format": "excel",
  "email": "manager@company.com"
}
```

### 4️⃣ تحليلات متقدمة ✅

- 📈 اتجاهات الأداء
- 🎯 رؤى ذكية
- 📊 مقارنة التقارير

---

## 📋 أنماط التقارير المتاحة

### 1. ملخص الدفع (PAYMENT_SUMMARY)

```
الحقول:
- date: التاريخ
- totalAmount: إجمالي المبلغ
- successRate: معدل النجاح (%)
- failedCount: عدد الفشل
- refunds: عدد المبالغ المسترجعة

مثال الإخراج:
{
  "date": "2026-02-01",
  "totalAmount": 45000,
  "successRate": 98,
  "failedCount": 42,
  "refunds": 15
}
```

### 2. تقرير البريد الإلكتروني (EMAIL_REPORT)

```
الحقول:
- date: التاريخ
- sent: المرسلة
- delivered: الموصلة
- bounced: المرتجعة
- opened: المفتوحة
- clicked: المضغوطة

يوفر: معدل الفتح، معدل النقر، جودة القائمة
```

### 3. تقرير الرسائل النصية (SMS_REPORT)

```
الحقول:
- date: التاريخ
- sent: المرسلة
- delivered: الموصلة
- failed: الفاشلة
- cost: التكلفة

يوفر: معدل التسليم، التكلفة لكل رسالة
```

### 4. نشاط المستخدمين (USER_ACTIVITY)

```
الحقول:
- date: التاريخ
- activeUsers: المستخدمون النشطون
- newUsers: المستخدمون الجدد
- actions: الإجراءات
- engagementRate: معدل التفاعل

يوفر: اتجاهات النمو، مؤشرات التفاعل
```

### 5. صحة النظام (SYSTEM_HEALTH)

```
الحقول:
- date: التاريخ
- uptime: وقت التشغيل (%)
- errorRate: معدل الأخطاء
- avgResponseTime: متوسط وقت الاستجابة
- activeConnections: الاتصالات النشطة

يوفر: مؤشرات الأداء الرئيسية
```

### 6. الإيرادات (REVENUE)

```
الحقول:
- date: التاريخ
- revenue: الإيرادات
- expenses: المصاريف
- profit: الأرباح
- transactionCount: عدد العمليات
- avgTransaction: متوسط العملية

يوفر: تحليل الربحية
```

---

## 🚀 استخدام النظام

### 1. عرض القوالب المتاحة

```bash
GET /api/v1/reports/templates

Response:
{
  "success": true,
  "templates": [
    {
      "id": "PAYMENT_SUMMARY",
      "name": "ملخص الدفع",
      "description": "تقرير ملخص معاملات الدفع",
      "fields": ["date", "totalAmount", "successRate", ...]
    }
  ]
}
```

### 2. إنشاء تقرير مخصص

```bash
POST /api/v1/reports/generate
Content-Type: application/json

{
  "templateType": "PAYMENT_SUMMARY",
  "filters": {
    "startDate": "2026-01-01",
    "endDate": "2026-02-01"
  }
}

Response:
{
  "success": true,
  "report": {
    "template": "ملخص الدفع",
    "description": "تقرير ملخص معاملات الدفع",
    "generatedAt": "2026-02-01T10:30:00Z",
    "period": { "startDate": "2026-01-01", "endDate": "2026-02-01" },
    "data": [...],
    "summary": {
      "totalAmount": {
        "total": 1350000,
        "average": 45000,
        "min": 10000,
        "max": 50000
      }
    }
  }
}
```

### 3. تصدير التقرير

```bash
POST /api/v1/reports/export
Content-Type: application/json

{
  "templateType": "PAYMENT_SUMMARY",
  "format": "pdf",
  "filters": { "days": 30 }
}

Response: PDF file download
```

**الصيغ المدعومة:**

- `csv` - ملف نصي
- `excel` أو `xlsx` - جدول بيانات
- `pdf` - مستند قابل للطباعة

### 4. جدولة تقرير

```bash
POST /api/v1/reports/schedule
Content-Type: application/json

{
  "templateType": "PAYMENT_SUMMARY",
  "schedule": "0 9 * * *",
  "format": "excel",
  "email": "manager@company.com"
}

Response:
{
  "success": true,
  "message": "Report scheduled successfully",
  "schedule": {
    "templateType": "PAYMENT_SUMMARY",
    "schedule": "0 9 * * *",
    "nextRun": "Check system logs"
  }
}
```

**صيغة الجدولة (Cron):**

```
*    *    *    *    *
└─── └─── └─── └─── └─────── يوم الأسبوع (0-7)
      │    │    │    └────────── الشهر (1-12)
      │    │    └────────────── يوم الشهر (1-31)
      │    └────────────────── الساعة (0-23)
      └──────────────────────── الدقيقة (0-59)

أمثلة:
- "0 9 * * *"      = كل يوم الساعة 9:00
- "0 9 * * 1"      = كل الاثنين الساعة 9:00
- "0 0 1 * *"      = أول كل شهر منتصف الليل
- "*/30 * * * *"   = كل 30 دقيقة
- "0 */6 * * *"    = كل 6 ساعات
```

### 5. الحصول على التحليلات

```bash
GET /api/v1/reports/analytics?reportType=PAYMENT_SUMMARY&days=30

Response:
{
  "success": true,
  "report": "ملخص الدفع",
  "period": "آخر 30 يوم",
  "summary": {
    "totalAmount": {
      "total": 1350000,
      "average": 45000,
      "min": 10000,
      "max": 50000
    }
  },
  "trends": {
    "totalAmount": {
      "first": 45000,
      "last": 48000,
      "change": "6.67%",
      "trend": "📈 صاعد"
    }
  },
  "insights": [
    "✅ جميع المؤشرات طبيعية"
  ]
}
```

### 6. مقارنة التقارير

```bash
POST /api/v1/reports/compare
Content-Type: application/json

{
  "templates": ["PAYMENT_SUMMARY", "EMAIL_REPORT", "SMS_REPORT"],
  "filters": { "days": 7 }
}

Response:
{
  "success": true,
  "comparison": [
    {
      "template": "PAYMENT_SUMMARY",
      "data": { ... }
    },
    {
      "template": "EMAIL_REPORT",
      "data": { ... }
    }
  ]
}
```

---

## 📊 الملخصات الإحصائية

جميع التقارير توفر ملخص إحصائي تلقائي:

```javascript
{
  "fieldName": {
    "total": 1000,        // المجموع
    "average": 33.33,     // المتوسط
    "min": 10,            // الحد الأدنى
    "max": 100            // الحد الأقصى
  }
}
```

---

## 🎯 حالات الاستخدام

### استخدام 1: تقرير يومي للمديرين

```bash
# تقرير الدفع كل صباح
POST /api/v1/reports/schedule
{
  "templateType": "PAYMENT_SUMMARY",
  "schedule": "0 8 * * *",
  "format": "pdf",
  "email": "director@company.com"
}
```

### استخدام 2: فحص صحة النظام

```bash
# فحص أداء النظام كل ساعة
GET /api/v1/reports/analytics?reportType=SYSTEM_HEALTH&days=1
```

### استخدام 3: تحليل الحملات البريدية

```bash
# مقارنة أداء البريد الإلكتروني
POST /api/v1/reports/export
{
  "templateType": "EMAIL_REPORT",
  "format": "excel",
  "filters": { "startDate": "2026-01-01", "endDate": "2026-02-01" }
}
```

### استخدام 4: تحليل الأرباح

```bash
# تقرير الإيرادات الشهري
POST /api/v1/reports/export
{
  "templateType": "REVENUE",
  "format": "pdf",
  "filters": { "days": 30 }
}
```

---

## 🔌 التكامل مع النظام

### إضافة المسار للتطبيق الرئيسي

```javascript
// app.js
const reportsRoutes = require('./routes/reports');

app.use('/api/v1/reports', authenticate, reportsRoutes);
```

### المتطلبات المثبتة

```bash
npm install pdfkit exceljs node-cron
```

---

## 📈 المؤشرات الرئيسية (KPIs)

### الدفع

- ✅ معدل نجاح العمليات
- ✅ إجمالي الإيرادات
- ✅ عدد المبالغ المسترجعة

### البريد الإلكتروني

- ✅ معدل التسليم
- ✅ معدل الفتح
- ✅ معدل النقر

### الرسائل النصية

- ✅ معدل التسليم
- ✅ التكلفة الإجمالية
- ✅ عدد الفشل

### النظام

- ✅ وقت التشغيل (Uptime)
- ✅ معدل الأخطاء
- ✅ وقت الاستجابة

---

## ⚙️ الإعدادات المتقدمة

### تحديد صيغة التاريخ

```javascript
// ISO Format (افتراضي)
"2026-02-01T10:30:00Z"

// يمكن تخصيصه في الفلترات
filters: {
  startDate: new Date("2026-01-01"),
  endDate: new Date("2026-02-01")
}
```

### تجميع البيانات

```javascript
filters: {
  groupBy: 'daily'; // يومي (افتراضي)
  // يمكن إضافة: weekly, monthly, yearly
}
```

---

## 🔒 الأمان

✅ **المصادقة المطلوبة:** JWT token لجميع الطلبات  
✅ **التحقق من الصلاحيات:** بناءً على دور المستخدم  
✅ **الخصوصية:** لا يتم حفظ البيانات الحساسة  
✅ **التشفير:** HTTPS للنقل

---

## 📝 أمثلة عملية

### مثال 1: تقرير يومي

```bash
curl -X GET http://localhost:3001/api/v1/reports/PAYMENT_SUMMARY \
  -H "Authorization: Bearer YOUR_TOKEN"
```

### مثال 2: تصدير إلى Excel

```bash
curl -X POST http://localhost:3001/api/v1/reports/export \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "templateType": "PAYMENT_SUMMARY",
    "format": "excel"
  }' \
  > report.xlsx
```

### مثال 3: جدولة تقرير أسبوعي

```bash
curl -X POST http://localhost:3001/api/v1/reports/schedule \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "templateType": "REVENUE",
    "schedule": "0 9 * * 1",
    "format": "pdf",
    "email": "cfo@company.com"
  }'
```

---

## ✅ قائمة التدقيق

- [x] تقارير مخصصة مرنة
- [x] صادرات متعددة الصيغ (CSV, Excel, PDF)
- [x] جدولة التقارير التلقائية
- [x] تحليلات متقدمة واتجاهات
- [x] مقارنة التقارير
- [x] رؤى ذكية وتنبيهات
- [x] أمان وتصادقة
- [x] توثيق كامل

---

## 🎉 النتيجة

**نظام تقارير متكامل:**

- ✅ بسيط وسهل الاستخدام
- ✅ مرن وقابل للتوسع
- ✅ آمن وموثوق
- ✅ متعدد الصيغ
- ✅ ذكي وتحليلي

**جاهز للإنتاج!**

---

**التاريخ:** 1 فبراير 2026  
**الحالة:** ✅ Production Ready  
**الإصدار:** 1.0.0
