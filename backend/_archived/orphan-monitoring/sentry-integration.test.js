/**
 * Auto-generated tests for errors/sentry-integration.js
 * @generated by _gen_remaining_tests.js
 */
'use strict';

/* ── Mocks ── */
jest.mock('../../utils/logger', () => ({
  info: jest.fn(),
  error: jest.fn(),
  warn: jest.fn(),
  debug: jest.fn(),
  verbose: jest.fn(),
  silly: jest.fn(),
  child: jest.fn(() => ({ info: jest.fn(), error: jest.fn(), warn: jest.fn(), debug: jest.fn() })),
  stream: { write: jest.fn() },
}));

jest.mock('@sentry/node', () => ({
  init: jest.fn(),
  captureException: jest.fn(),
  captureMessage: jest.fn(),
  setUser: jest.fn(),
  setTag: jest.fn(),
  setContext: jest.fn(),
  startTransaction: jest.fn(() => ({ finish: jest.fn(), setTag: jest.fn(), startChild: jest.fn(() => ({ finish: jest.fn() })) })),
  Handlers: { requestHandler: jest.fn(() => (req, res, next) => next()), errorHandler: jest.fn(() => (err, req, res, next) => next(err)), tracingHandler: jest.fn(() => (req, res, next) => next()) },
  Integrations: {},
}), { virtual: true });

jest.mock('@sentry/profiling-node', () => ({}), { virtual: true });

jest.mock('@sentry/tracing', () => ({}), { virtual: true });

/* ── Module under test ── */
const mod = require('../../errors/sentry-integration');

describe('errors/sentry-integration', () => {
  test('should export a valid module', () => {
    expect(mod).toBeDefined();
    expect(typeof mod).toBe('object');
  });

  test('should export config', () => {
    expect(mod.config).toBeDefined();
  });

  test('should export AppError as a constructor', () => {
    expect(mod.AppError).toBeDefined();
    expect(typeof mod.AppError).toBe('function');
  });

  test('should export ValidationError as a constructor', () => {
    expect(mod.ValidationError).toBeDefined();
    expect(typeof mod.ValidationError).toBe('function');
  });

  test('should export UnauthorizedError as a constructor', () => {
    expect(mod.UnauthorizedError).toBeDefined();
    expect(typeof mod.UnauthorizedError).toBe('function');
  });

  test('should export ForbiddenError as a constructor', () => {
    expect(mod.ForbiddenError).toBeDefined();
    expect(typeof mod.ForbiddenError).toBe('function');
  });

  test('should export NotFoundError as a constructor', () => {
    expect(mod.NotFoundError).toBeDefined();
    expect(typeof mod.NotFoundError).toBe('function');
  });

  test('should export ConflictError as a constructor', () => {
    expect(mod.ConflictError).toBeDefined();
    expect(typeof mod.ConflictError).toBe('function');
  });

  test('should export RateLimitError as a constructor', () => {
    expect(mod.RateLimitError).toBeDefined();
    expect(typeof mod.RateLimitError).toBe('function');
  });

  test('should export initializeSentry', () => {
    expect(mod.initializeSentry).toBeDefined();
    expect(typeof mod.initializeSentry).toBe('function');
  });

  test('should export closeSentry', () => {
    expect(mod.closeSentry).toBeDefined();
    expect(typeof mod.closeSentry).toBe('function');
  });

  test('should export setUser', () => {
    expect(mod.setUser).toBeDefined();
    expect(typeof mod.setUser).toBe('function');
  });

  test('should export getUser', () => {
    expect(mod.getUser).toBeDefined();
    expect(typeof mod.getUser).toBe('function');
  });

  test('should export captureException', () => {
    expect(mod.captureException).toBeDefined();
    expect(typeof mod.captureException).toBe('function');
  });

  test('should export captureMessage', () => {
    expect(mod.captureMessage).toBeDefined();
    expect(typeof mod.captureMessage).toBe('function');
  });

  test('should export addBreadcrumb', () => {
    expect(mod.addBreadcrumb).toBeDefined();
    expect(typeof mod.addBreadcrumb).toBe('function');
  });

  test('should export startTransaction', () => {
    expect(mod.startTransaction).toBeDefined();
    expect(typeof mod.startTransaction).toBe('function');
  });

  test('should export createSpan', () => {
    expect(mod.createSpan).toBeDefined();
    expect(typeof mod.createSpan).toBe('function');
  });

  test('should export requestHandler', () => {
    expect(mod.requestHandler).toBeDefined();
    expect(typeof mod.requestHandler).toBe('function');
  });

  test('should export errorHandler', () => {
    expect(mod.errorHandler).toBeDefined();
    expect(typeof mod.errorHandler).toBe('function');
  });

  test('should export tracingMiddleware', () => {
    expect(mod.tracingMiddleware).toBeDefined();
    expect(typeof mod.tracingMiddleware).toBe('function');
  });

  test('should export wrapAsync', () => {
    expect(mod.wrapAsync).toBeDefined();
    expect(typeof mod.wrapAsync).toBe('function');
  });

  test('should export serializeError', () => {
    expect(mod.serializeError).toBeDefined();
    expect(typeof mod.serializeError).toBe('function');
  });

  describe('AppError', () => {
    test('should create instance with message', () => {
      const err = new mod.AppError('test error');
      expect(err).toBeInstanceOf(Error);
      expect(err.message).toBe('test error');
    });

    test('should have statusCode', () => {
      const err = new mod.AppError('test');
      expect(err.statusCode).toBeDefined();
    });

  });

  describe('ValidationError', () => {
    test('should create instance with message', () => {
      const err = new mod.ValidationError('test error');
      expect(err).toBeInstanceOf(Error);
      expect(err.message).toBe('test error');
    });

    test('should have statusCode', () => {
      const err = new mod.ValidationError('test');
      expect(err.statusCode).toBeDefined();
    });

  });

  describe('UnauthorizedError', () => {
    test('should create instance with message', () => {
      const err = new mod.UnauthorizedError('test error');
      expect(err).toBeInstanceOf(Error);
      expect(err.message).toBe('test error');
    });

    test('should have statusCode', () => {
      const err = new mod.UnauthorizedError('test');
      expect(err.statusCode).toBeDefined();
    });

  });

  describe('ForbiddenError', () => {
    test('should create instance with message', () => {
      const err = new mod.ForbiddenError('test error');
      expect(err).toBeInstanceOf(Error);
      expect(err.message).toBe('test error');
    });

    test('should have statusCode', () => {
      const err = new mod.ForbiddenError('test');
      expect(err.statusCode).toBeDefined();
    });

  });

  describe('NotFoundError', () => {
    test('should create instance with message', () => {
      const err = new mod.NotFoundError('test error');
      expect(err).toBeInstanceOf(Error);
      expect(err.message).toBe('test error');
    });

    test('should have statusCode', () => {
      const err = new mod.NotFoundError('test');
      expect(err.statusCode).toBeDefined();
    });

  });

  describe('ConflictError', () => {
    test('should create instance with message', () => {
      const err = new mod.ConflictError('test error');
      expect(err).toBeInstanceOf(Error);
      expect(err.message).toBe('test error');
    });

    test('should have statusCode', () => {
      const err = new mod.ConflictError('test');
      expect(err.statusCode).toBeDefined();
    });

  });

  describe('RateLimitError', () => {
    test('should create instance with message', () => {
      const err = new mod.RateLimitError('test error');
      expect(err).toBeInstanceOf(Error);
      expect(err.message).toBe('test error');
    });

    test('should have statusCode', () => {
      const err = new mod.RateLimitError('test');
      expect(err.statusCode).toBeDefined();
    });

  });

});
