/**
 * Auto-generated tests for config/notificationTemplates.js
 * @generated by _gen_config_tests.js
 */
'use strict';

/* ── Mocks ── */
jest.mock('../../services/notificationService', () => new Proxy({}, {
  get: (_, p) => {
    if (p === '__esModule') return false;
    if (p === 'default') return {};
    return jest.fn(() => Promise.resolve({}));
  },
}));

/* ── Module under test ── */
const mod = require('../../config/notificationTemplates');

describe('config/notificationTemplates', () => {
  test('should export a valid module', () => {
    expect(mod).toBeDefined();
    expect(typeof mod).toBe('object');
  });

  test('should export emailTemplates', () => {
    expect(mod.emailTemplates).toBeDefined();
  });

  test('should export smsTemplates', () => {
    expect(mod.smsTemplates).toBeDefined();
  });

  test('should export pushTemplates', () => {
    expect(mod.pushTemplates).toBeDefined();
  });

  test('should export initializeTemplates', () => {
    expect(mod.initializeTemplates).toBeDefined();
    expect(typeof mod.initializeTemplates).toBe('function');
  });

});
