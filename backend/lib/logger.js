/**
 * Logger — alias module
 * Re-exports the main logger from ../utils/logger (backend/utils/logger.js)
 * to support imports via:
 *   require('../lib/logger')
 */
module.exports = require('../utils/logger');
