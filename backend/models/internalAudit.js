'use strict';
/**
 * @deprecated — Split into backend/models/internal-audit/ (5 individual model files).
 * Kept as a backward-compatible barrel export so existing requires keep working.
 */
module.exports = require('./internal-audit/index');
