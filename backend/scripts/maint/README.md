# maint/

Maintenance helpers used for one-off safe refactors.

## Scripts

- `rename-unused-catch.ps1` — rename unused catch variables in a controlled way
- `rename-unused-fast.ps1` — faster bulk rename helper for the same pattern

## Notes

- Use carefully and always validate with lint/tests after running.
