'use strict';
/**
 * @deprecated — Stub replaced by gosi-full.service.js (Phase 4 refactor).
 * Kept as a backward-compatible proxy so existing requires keep working.
 * Migrate callers to: const gosiService = require('./gosi-full.service');
 */
module.exports = require('./gosi-full.service');
