/**
 * Auto-generated tests for models/DddAssetTracker.js
 * Validates syntax, structure, and patterns (model — 249L)
 * @generated by P#106 universal generator
 */
'use strict';

const fs = require('fs');
const path = require('path');
const vm = require('vm');

const SRC = path.resolve(__dirname, '../../models/DddAssetTracker.js');

describe('models/DddAssetTracker.js', () => {
  let source;
  beforeAll(() => {
    source = fs.readFileSync(SRC, 'utf8');
  });

  test('file exists', () => {
    expect(fs.existsSync(SRC)).toBe(true);
  });

  test('is syntactically valid JavaScript', () => {
    expect(() => new vm.Script(source, { filename: 'DddAssetTracker.js' })).not.toThrow();
  });

  test('defines a Mongoose model', () => {
    expect(source).toMatch(/mongoose\.model\s*\(/);
  });

  test('uses timestamps', () => {
    expect(source).toMatch(/timestamps\s*:\s*true/);
  });

  test('has required fields', () => {
    expect(source).toMatch(/required\s*:\s*true/);
  });

  test('defines indexes', () => {
    expect(source).toMatch(/index/);
  });

  test('has npm dependencies (1)', () => {
    const npms = (source.match(/require\s*\(\s*['"](?![./])[^'"]+['"]\s*\)/g) || []);
    expect(npms.length).toBe(1);
  });

  test('has module.exports', () => {
    expect(source).toMatch(/module\.exports/);
  });

  test('exports ASSET_CATEGORIES', () => {
    expect(source).toMatch(/ASSET_CATEGORIES/);
  });

  test('exports ASSET_STATUSES', () => {
    expect(source).toMatch(/ASSET_STATUSES/);
  });

  test('exports MAINTENANCE_TYPES', () => {
    expect(source).toMatch(/MAINTENANCE_TYPES/);
  });

  test('exports CONDITION_GRADES', () => {
    expect(source).toMatch(/CONDITION_GRADES/);
  });

  test('exports BUILTIN_ASSET_TYPES', () => {
    expect(source).toMatch(/BUILTIN_ASSET_TYPES/);
  });
});
