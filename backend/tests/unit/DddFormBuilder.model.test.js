/**
 * Auto-generated tests for models/DddFormBuilder.js
 * Validates syntax, structure, and patterns (model — 312L)
 * @generated by P#106 universal generator
 */
'use strict';

const fs = require('fs');
const path = require('path');
const vm = require('vm');

const SRC = path.resolve(__dirname, '../../models/DddFormBuilder.js');

describe('models/DddFormBuilder.js', () => {
  let source;
  beforeAll(() => {
    source = fs.readFileSync(SRC, 'utf8');
  });

  test('file exists', () => {
    expect(fs.existsSync(SRC)).toBe(true);
  });

  test('is syntactically valid JavaScript', () => {
    expect(() => new vm.Script(source, { filename: 'DddFormBuilder.js' })).not.toThrow();
  });

  test('defines a Mongoose model', () => {
    expect(source).toMatch(/mongoose\.model\s*\(/);
  });

  test('uses timestamps', () => {
    expect(source).toMatch(/timestamps\s*:\s*true/);
  });

  test('has required fields', () => {
    expect(source).toMatch(/required\s*:\s*true/);
  });

  test('defines indexes', () => {
    expect(source).toMatch(/index/);
  });

  test('has npm dependencies (1)', () => {
    const npms = (source.match(/require\s*\(\s*['"](?![./])[^'"]+['"]\s*\)/g) || []);
    expect(npms.length).toBe(1);
  });

  test('has module.exports', () => {
    expect(source).toMatch(/module\.exports/);
  });

  test('exports FIELD_TYPES', () => {
    expect(source).toMatch(/FIELD_TYPES/);
  });

  test('exports FORM_CATEGORIES', () => {
    expect(source).toMatch(/FORM_CATEGORIES/);
  });

  test('exports VALIDATION_RULES', () => {
    expect(source).toMatch(/VALIDATION_RULES/);
  });

  test('exports BUILTIN_FORM_TEMPLATES', () => {
    expect(source).toMatch(/BUILTIN_FORM_TEMPLATES/);
  });
});
