/**
 * Auto-generated tests for support/Money.js
 * Validates syntax, structure, and patterns (module — 332L)
 * @generated by P#106 universal generator
 */
'use strict';

const fs = require('fs');
const path = require('path');
const vm = require('vm');

const SRC = path.resolve(__dirname, '../../support/Money.js');

describe('support/Money.js', () => {
  let source;
  beforeAll(() => {
    source = fs.readFileSync(SRC, 'utf8');
  });

  test('file exists', () => {
    expect(fs.existsSync(SRC)).toBe(true);
  });

  test('is syntactically valid JavaScript', () => {
    expect(() => new vm.Script(source, { filename: 'Money.js' })).not.toThrow();
  });

  test('has module.exports', () => {
    expect(source).toMatch(/module\.exports/);
  });
});
