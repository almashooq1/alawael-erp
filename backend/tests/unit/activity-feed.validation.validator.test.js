/**
 * Auto-generated tests for validations/activity-feed.validation.js
 * Validates syntax, structure, and patterns (validator — 98L)
 * @generated by P#106 universal generator
 */
'use strict';

const fs = require('fs');
const path = require('path');
const vm = require('vm');

const SRC = path.resolve(__dirname, '../../validations/activity-feed.validation.js');

describe('validations/activity-feed.validation.js', () => {
  let source;
  beforeAll(() => {
    source = fs.readFileSync(SRC, 'utf8');
  });

  test('file exists', () => {
    expect(fs.existsSync(SRC)).toBe(true);
  });

  test('is syntactically valid JavaScript', () => {
    expect(() => new vm.Script(source, { filename: 'activity-feed.validation.js' })).not.toThrow();
  });

  test('defines validation rules', () => {
    expect(source).toMatch(/(?:body|param|query)\s*\(/);
  });

  test('has npm dependencies (1)', () => {
    const npms = (source.match(/require\s*\(\s*['"](?![./])[^'"]+['"]\s*\)/g) || []);
    expect(npms.length).toBe(1);
  });

  test('has module.exports', () => {
    expect(source).toMatch(/module\.exports/);
  });

  test('exports createActivity', () => {
    expect(source).toMatch(/createActivity/);
  });

  test('exports updateActivity', () => {
    expect(source).toMatch(/updateActivity/);
  });

  test('exports createSubscription', () => {
    expect(source).toMatch(/createSubscription/);
  });

  test('exports updateSubscription', () => {
    expect(source).toMatch(/updateSubscription/);
  });

  test('exports createDigest', () => {
    expect(source).toMatch(/createDigest/);
  });
});
