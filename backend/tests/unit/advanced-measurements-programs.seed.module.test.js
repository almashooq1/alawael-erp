/**
 * Auto-generated tests for seeds/advanced-measurements-programs.seed.js
 * Validates syntax, structure, and patterns (module — 619L)
 * @generated by P#106 universal generator
 */
'use strict';

const fs = require('fs');
const path = require('path');
const vm = require('vm');

const SRC = path.resolve(__dirname, '../../seeds/advanced-measurements-programs.seed.js');

describe('seeds/advanced-measurements-programs.seed.js', () => {
  let source;
  beforeAll(() => {
    source = fs.readFileSync(SRC, 'utf8');
  });

  test('file exists', () => {
    expect(fs.existsSync(SRC)).toBe(true);
  });

  test('is syntactically valid JavaScript', () => {
    expect(() => new vm.Script(source, { filename: 'advanced-measurements-programs.seed.js' })).not.toThrow();
  });

  test('has module.exports', () => {
    expect(source).toMatch(/module\.exports/);
  });

  test('exports advancedMeasurementTypes', () => {
    expect(source).toMatch(/advancedMeasurementTypes/);
  });

  test('exports advancedRehabilitationPrograms', () => {
    expect(source).toMatch(/advancedRehabilitationPrograms/);
  });

  test('exports newProgramCategories', () => {
    expect(source).toMatch(/newProgramCategories/);
  });
});
