/**
 * Auto-generated tests for rehabilitation-services/advanced-speech-therapy-service.js
 * Validates syntax, structure, and patterns (service — 381L)
 * @generated by P#106 universal generator
 */
'use strict';

const fs = require('fs');
const path = require('path');
const vm = require('vm');

const SRC = path.resolve(__dirname, '../../rehabilitation-services/advanced-speech-therapy-service.js');

describe('rehabilitation-services/advanced-speech-therapy-service.js', () => {
  let source;
  beforeAll(() => {
    source = fs.readFileSync(SRC, 'utf8');
  });

  test('file exists', () => {
    expect(fs.existsSync(SRC)).toBe(true);
  });

  test('is syntactically valid JavaScript', () => {
    expect(() => new vm.Script(source, { filename: 'advanced-speech-therapy-service.js' })).not.toThrow();
  });

  test('defines class AdvancedSpeechTherapyService', () => {
    expect(source).toMatch(/class\s+AdvancedSpeechTherapyService/);
  });

  test('has async methods (4)', () => {
    const matches = source.match(/async\s+\w+\s*\(/g) || [];
    expect(matches.length).toBeGreaterThanOrEqual(1);
  });

  test('has module.exports', () => {
    expect(source).toMatch(/module\.exports/);
  });

  test('exports AdvancedSpeechTherapyService', () => {
    expect(source).toMatch(/AdvancedSpeechTherapyService/);
  });
});
