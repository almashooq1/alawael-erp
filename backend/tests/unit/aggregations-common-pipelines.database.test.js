/**
 * Auto-generated tests for database/aggregations/common-pipelines.js
 * Validates syntax, structure, and patterns (module — 1029L)
 * @generated by P#106 universal generator
 */
'use strict';

const fs = require('fs');
const path = require('path');
const vm = require('vm');

const SRC = path.resolve(__dirname, '../../database/aggregations/common-pipelines.js');

describe('database/aggregations/common-pipelines.js', () => {
  let source;
  beforeAll(() => {
    source = fs.readFileSync(SRC, 'utf8');
  });

  test('file exists', () => {
    expect(fs.existsSync(SRC)).toBe(true);
  });

  test('is syntactically valid JavaScript', () => {
    expect(() => new vm.Script(source, { filename: 'common-pipelines.js' })).not.toThrow();
  });

  test('has npm dependencies (1)', () => {
    const npms = (source.match(/require\s*\(\s*['"](?![./])[^'"]+['"]\s*\)/g) || []);
    expect(npms.length).toBe(1);
  });

  test('has module.exports', () => {
    expect(source).toMatch(/module\.exports/);
  });

  test('exports stages', () => {
    expect(source).toMatch(/stages/);
  });

  test('exports beneficiaryPipelines', () => {
    expect(source).toMatch(/beneficiaryPipelines/);
  });

  test('exports hrPipelines', () => {
    expect(source).toMatch(/hrPipelines/);
  });

  test('exports financialPipelines', () => {
    expect(source).toMatch(/financialPipelines/);
  });

  test('exports rehabPipelines', () => {
    expect(source).toMatch(/rehabPipelines/);
  });
});
