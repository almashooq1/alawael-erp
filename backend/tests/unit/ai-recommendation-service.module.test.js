/**
 * Auto-generated tests for rehabilitation-ai/ai-recommendation-service.js
 * Validates syntax, structure, and patterns (model — 627L)
 * @generated by P#106 universal generator
 */
'use strict';

const fs = require('fs');
const path = require('path');
const vm = require('vm');

const SRC = path.resolve(__dirname, '../../rehabilitation-ai/ai-recommendation-service.js');

describe('rehabilitation-ai/ai-recommendation-service.js', () => {
  let source;
  beforeAll(() => {
    source = fs.readFileSync(SRC, 'utf8');
  });

  test('file exists', () => {
    expect(fs.existsSync(SRC)).toBe(true);
  });

  test('is syntactically valid JavaScript', () => {
    expect(() => new vm.Script(source, { filename: 'ai-recommendation-service.js' })).not.toThrow();
  });

  test('defines a Mongoose model', () => {
    expect(source).toMatch(/mongoose\.model\s*\(/);
  });

  test('creates a Schema', () => {
    expect(source).toMatch(/new\s+Schema\s*\(/);
  });

  test('has required fields', () => {
    expect(source).toMatch(/required\s*:\s*true/);
  });

  test('has npm dependencies (1)', () => {
    const npms = (source.match(/require\s*\(\s*['"](?![./])[^'"]+['"]\s*\)/g) || []);
    expect(npms.length).toBe(1);
  });

  test('has local dependencies (1)', () => {
    const locals = (source.match(/require\s*\(\s*['"]\.[^'"]+['"]\s*\)/g) || []);
    expect(locals.length).toBe(1);
  });

  test('has module.exports', () => {
    expect(source).toMatch(/module\.exports/);
  });

  test('exports AIRecommendationService', () => {
    expect(source).toMatch(/AIRecommendationService/);
  });

  test('exports Recommendation', () => {
    expect(source).toMatch(/Recommendation/);
  });

  test('exports ProgressAnalysis', () => {
    expect(source).toMatch(/ProgressAnalysis/);
  });

  test('exports recommendationSchema', () => {
    expect(source).toMatch(/recommendationSchema/);
  });

  test('exports progressAnalysisSchema', () => {
    expect(source).toMatch(/progressAnalysisSchema/);
  });
});
