/**
 * Auto-generated tests for validations/announcement-manager.validation.js
 * Validates syntax, structure, and patterns (validator — 140L)
 * @generated by P#106 universal generator
 */
'use strict';

const fs = require('fs');
const path = require('path');
const vm = require('vm');

const SRC = path.resolve(__dirname, '../../validations/announcement-manager.validation.js');

describe('validations/announcement-manager.validation.js', () => {
  let source;
  beforeAll(() => {
    source = fs.readFileSync(SRC, 'utf8');
  });

  test('file exists', () => {
    expect(fs.existsSync(SRC)).toBe(true);
  });

  test('is syntactically valid JavaScript', () => {
    expect(() => new vm.Script(source, { filename: 'announcement-manager.validation.js' })).not.toThrow();
  });

  test('defines validation rules', () => {
    expect(source).toMatch(/(?:body|param|query)\s*\(/);
  });

  test('has npm dependencies (1)', () => {
    const npms = (source.match(/require\s*\(\s*['"](?![./])[^'"]+['"]\s*\)/g) || []);
    expect(npms.length).toBe(1);
  });

  test('has module.exports', () => {
    expect(source).toMatch(/module\.exports/);
  });

  test('exports createAnnouncement', () => {
    expect(source).toMatch(/createAnnouncement/);
  });

  test('exports updateAnnouncement', () => {
    expect(source).toMatch(/updateAnnouncement/);
  });

  test('exports createBulletinBoard', () => {
    expect(source).toMatch(/createBulletinBoard/);
  });

  test('exports updateBulletinBoard', () => {
    expect(source).toMatch(/updateBulletinBoard/);
  });

  test('exports createAnnouncementCategory', () => {
    expect(source).toMatch(/createAnnouncementCategory/);
  });
});
