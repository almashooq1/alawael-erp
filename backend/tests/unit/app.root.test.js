/**
 * Tests for app.js (Express application orchestrator)
 * Uses syntax + structure validation — the file has 23+ deps
 * making full mock-based loading impractical.
 * @generated by P#105
 */
'use strict';

const fs = require('fs');
const path = require('path');
const vm = require('vm');

const SRC = path.resolve(__dirname, '../../app.js');

describe('app.js', () => {
  let source;
  beforeAll(() => {
    source = fs.readFileSync(SRC, 'utf8');
  });

  test('file exists', () => {
    expect(fs.existsSync(SRC)).toBe(true);
  });

  test('is syntactically valid JavaScript', () => {
    expect(() => new vm.Script(source, { filename: 'app.js' })).not.toThrow();
  });

  test('creates Express app via express()', () => {
    expect(source).toMatch(/express\(\)/);
  });

  test('requires dotenv', () => {
    expect(source).toMatch(/require\s*\(\s*['"]dotenv['"]\s*\)/);
  });

  test('requires express', () => {
    expect(source).toMatch(/require\s*\(\s*['"]express['"]\s*\)/);
  });

  test('requires mongoose', () => {
    expect(source).toMatch(/require\s*\(\s*['"]mongoose['"]\s*\)/);
  });

  test('calls validateEnv()', () => {
    expect(source).toMatch(/validateEnv\(\)/);
  });

  test('sets up middleware', () => {
    expect(source).toMatch(/setupMiddleware\s*\(/);
  });

  test('sets up health probes', () => {
    expect(source).toMatch(/setupHealthProbes\s*\(/);
  });

  test('mounts routes', () => {
    expect(source).toMatch(/mountAllRoutes\s*\(/);
  });

  test('sets up integration bus', () => {
    expect(source).toMatch(/setupIntegrationBus\s*\(/);
  });

  test('sets up schedulers', () => {
    expect(source).toMatch(/setupSchedulers\s*\(/);
  });

  test('registers error handlers', () => {
    expect(source).toMatch(/errorHandler/);
    expect(source).toMatch(/notFoundHandler/);
  });

  test('exports the app', () => {
    expect(source).toMatch(/module\.exports\s*=\s*app/);
  });

  test('exports PORT', () => {
    expect(source).toMatch(/module\.exports\.PORT/);
  });
});
