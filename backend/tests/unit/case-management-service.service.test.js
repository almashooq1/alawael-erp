/**
 * Auto-generated tests for rehabilitation-services/case-management-service.js
 * Validates syntax, structure, and patterns (service — 252L)
 * @generated by P#106 universal generator
 */
'use strict';

const fs = require('fs');
const path = require('path');
const vm = require('vm');

const SRC = path.resolve(__dirname, '../../rehabilitation-services/case-management-service.js');

describe('rehabilitation-services/case-management-service.js', () => {
  let source;
  beforeAll(() => {
    source = fs.readFileSync(SRC, 'utf8');
  });

  test('file exists', () => {
    expect(fs.existsSync(SRC)).toBe(true);
  });

  test('is syntactically valid JavaScript', () => {
    expect(() => new vm.Script(source, { filename: 'case-management-service.js' })).not.toThrow();
  });

  test('defines class CaseManagementService', () => {
    expect(source).toMatch(/class\s+CaseManagementService/);
  });

  test('has async methods (8)', () => {
    const matches = source.match(/async\s+\w+\s*\(/g) || [];
    expect(matches.length).toBeGreaterThanOrEqual(1);
  });

  test('has module.exports', () => {
    expect(source).toMatch(/module\.exports/);
  });

  test('exports CaseManagementService', () => {
    expect(source).toMatch(/CaseManagementService/);
  });
});
