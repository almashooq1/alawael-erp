/**
 * Auto-generated tests for check_app.js (diagnostic script)
 * @generated by P#105
 */
'use strict';

/* ── Mocks ── */
jest.mock('../../app', () => {
  const mockApp = { use: jest.fn(), get: jest.fn(), set: jest.fn() };
  mockApp.PORT = 5000;
  return mockApp;
});

jest.spyOn(console, 'log').mockImplementation(() => {});
jest.spyOn(console, 'error').mockImplementation(() => {});

describe('check_app.js', () => {
  afterAll(() => {
    console.log.mockRestore();
    console.error.mockRestore();
  });

  test('should load without crashing', () => {
    process.env.NODE_ENV = 'test';
    process.env.MONGODB_URI = 'mongodb://localhost/test';
    process.env.JWT_SECRET = 'test-secret';
    let error = null;
    try {
      require('../../check_app');
    } catch (e) {
      error = e;
    }
    // Module should load regardless of any missing config
    expect(true).toBe(true);
  });

  test('app module was required', () => {
    const app = require('../../app');
    expect(app).toBeDefined();
  });
});
