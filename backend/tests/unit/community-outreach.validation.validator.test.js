/**
 * Auto-generated tests for validations/community-outreach.validation.js
 * Validates syntax, structure, and patterns (validator — 142L)
 * @generated by P#106 universal generator
 */
'use strict';

const fs = require('fs');
const path = require('path');
const vm = require('vm');

const SRC = path.resolve(__dirname, '../../validations/community-outreach.validation.js');

describe('validations/community-outreach.validation.js', () => {
  let source;
  beforeAll(() => {
    source = fs.readFileSync(SRC, 'utf8');
  });

  test('file exists', () => {
    expect(fs.existsSync(SRC)).toBe(true);
  });

  test('is syntactically valid JavaScript', () => {
    expect(() => new vm.Script(source, { filename: 'community-outreach.validation.js' })).not.toThrow();
  });

  test('defines validation rules', () => {
    expect(source).toMatch(/(?:body|param|query)\s*\(/);
  });

  test('has npm dependencies (1)', () => {
    const npms = (source.match(/require\s*\(\s*['"](?![./])[^'"]+['"]\s*\)/g) || []);
    expect(npms.length).toBe(1);
  });

  test('has module.exports', () => {
    expect(source).toMatch(/module\.exports/);
  });

  test('exports createOutreachProgram', () => {
    expect(source).toMatch(/createOutreachProgram/);
  });

  test('exports updateOutreachProgram', () => {
    expect(source).toMatch(/updateOutreachProgram/);
  });

  test('exports createCommunityPartner', () => {
    expect(source).toMatch(/createCommunityPartner/);
  });

  test('exports updateCommunityPartner', () => {
    expect(source).toMatch(/updateCommunityPartner/);
  });

  test('exports createOutreachEvent', () => {
    expect(source).toMatch(/createOutreachEvent/);
  });
});
