/**
 * Auto-generated tests for config/database.js
 * @generated by _gen_config_tests.js
 */
'use strict';

/* ── Mocks ── */
jest.mock('../../utils/logger', () => ({
  info: jest.fn(),
  error: jest.fn(),
  warn: jest.fn(),
  debug: jest.fn(),
  verbose: jest.fn(),
  silly: jest.fn(),
  child: jest.fn(() => ({ info: jest.fn(), error: jest.fn(), warn: jest.fn(), debug: jest.fn() })),
  stream: { write: jest.fn() },
}));

const mockSchema = { index: jest.fn().mockReturnThis(), virtual: jest.fn(() => ({ get: jest.fn() })), pre: jest.fn(), post: jest.fn(), plugin: jest.fn(), statics: {}, methods: {}, set: jest.fn() };
jest.mock('mongoose', () => ({
  Schema: jest.fn(() => mockSchema),
  model: jest.fn(() => ({ find: jest.fn(), findOne: jest.fn(), create: jest.fn() })),
  connect: jest.fn(() => Promise.resolve()),
  disconnect: jest.fn(() => Promise.resolve()),
  connection: { readyState: 1, on: jest.fn(), close: jest.fn(), db: { admin: jest.fn(() => ({ ping: jest.fn(() => Promise.resolve({ ok: 1 })), serverStatus: jest.fn(() => Promise.resolve({ connections: {} })) })), stats: jest.fn(() => Promise.resolve({})), command: jest.fn(() => Promise.resolve({})) } },
  plugin: jest.fn(),
  Types: { ObjectId: { isValid: jest.fn(() => true) } },
  set: jest.fn(),
}));

jest.mock('mongodb-memory-server', () => ({
  MongoMemoryServer: { create: jest.fn(() => Promise.resolve({ getUri: jest.fn(() => 'mongodb://mock'), stop: jest.fn() })) },
}), { virtual: true });

/* ── Module under test (loaded after mocks) ── */
let mod;
beforeAll(() => {
  mod = require('../../config/database');
});

describe('config/database', () => {
  test('should export a valid module', () => {
    expect(mod).toBeDefined();
    expect(typeof mod).toBe('object');
  });

  test('should export RETRY_CONFIG', () => {
    expect(mod.RETRY_CONFIG).toBeDefined();
  });

  test('should export connectDB', () => {
    expect(mod.connectDB).toBeDefined();
    expect(typeof mod.connectDB).toBe('function');
  });

  test('should export disconnectDB', () => {
    expect(mod.disconnectDB).toBeDefined();
    expect(typeof mod.disconnectDB).toBe('function');
  });

  test('should export checkConnection', () => {
    expect(mod.checkConnection).toBeDefined();
    expect(typeof mod.checkConnection).toBe('function');
  });

  test('should export getConnectionHealth', () => {
    expect(mod.getConnectionHealth).toBeDefined();
    expect(typeof mod.getConnectionHealth).toBe('function');
  });

  test('should export pingDatabase', () => {
    expect(mod.pingDatabase).toBeDefined();
    expect(typeof mod.pingDatabase).toBe('function');
  });

  test('should export getPoolMetrics', () => {
    expect(mod.getPoolMetrics).toBeDefined();
    expect(typeof mod.getPoolMetrics).toBe('function');
  });

  test('should export enableSlowQueryProfiling', () => {
    expect(mod.enableSlowQueryProfiling).toBeDefined();
    expect(typeof mod.enableSlowQueryProfiling).toBe('function');
  });

});
