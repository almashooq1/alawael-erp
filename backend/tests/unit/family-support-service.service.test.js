/**
 * Auto-generated tests for rehabilitation-services/family-support-service.js
 * Validates syntax, structure, and patterns (service — 320L)
 * @generated by P#106 universal generator
 */
'use strict';

const fs = require('fs');
const path = require('path');
const vm = require('vm');

const SRC = path.resolve(__dirname, '../../rehabilitation-services/family-support-service.js');

describe('rehabilitation-services/family-support-service.js', () => {
  let source;
  beforeAll(() => {
    source = fs.readFileSync(SRC, 'utf8');
  });

  test('file exists', () => {
    expect(fs.existsSync(SRC)).toBe(true);
  });

  test('is syntactically valid JavaScript', () => {
    expect(() => new vm.Script(source, { filename: 'family-support-service.js' })).not.toThrow();
  });

  test('defines class FamilySupportService', () => {
    expect(source).toMatch(/class\s+FamilySupportService/);
  });

  test('has async methods (8)', () => {
    const matches = source.match(/async\s+\w+\s*\(/g) || [];
    expect(matches.length).toBeGreaterThanOrEqual(1);
  });

  test('has module.exports', () => {
    expect(source).toMatch(/module\.exports/);
  });

  test('exports FamilySupportService', () => {
    expect(source).toMatch(/FamilySupportService/);
  });
});
