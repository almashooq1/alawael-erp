/**
 * Auto-generated tests for validations/feedback-manager.validation.js
 * Validates syntax, structure, and patterns (validator — 140L)
 * @generated by P#106 universal generator
 */
'use strict';

const fs = require('fs');
const path = require('path');
const vm = require('vm');

const SRC = path.resolve(__dirname, '../../validations/feedback-manager.validation.js');

describe('validations/feedback-manager.validation.js', () => {
  let source;
  beforeAll(() => {
    source = fs.readFileSync(SRC, 'utf8');
  });

  test('file exists', () => {
    expect(fs.existsSync(SRC)).toBe(true);
  });

  test('is syntactically valid JavaScript', () => {
    expect(() => new vm.Script(source, { filename: 'feedback-manager.validation.js' })).not.toThrow();
  });

  test('defines validation rules', () => {
    expect(source).toMatch(/(?:body|param|query)\s*\(/);
  });

  test('has npm dependencies (1)', () => {
    const npms = (source.match(/require\s*\(\s*['"](?![./])[^'"]+['"]\s*\)/g) || []);
    expect(npms.length).toBe(1);
  });

  test('has module.exports', () => {
    expect(source).toMatch(/module\.exports/);
  });

  test('exports createFeedback', () => {
    expect(source).toMatch(/createFeedback/);
  });

  test('exports updateFeedback', () => {
    expect(source).toMatch(/updateFeedback/);
  });

  test('exports createSurvey', () => {
    expect(source).toMatch(/createSurvey/);
  });

  test('exports updateSurvey', () => {
    expect(source).toMatch(/updateSurvey/);
  });

  test('exports createSurveyResponse', () => {
    expect(source).toMatch(/createSurveyResponse/);
  });
});
