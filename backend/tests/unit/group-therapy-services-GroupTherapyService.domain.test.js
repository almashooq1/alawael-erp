/**
 * Auto-generated tests for domains/group-therapy/services/GroupTherapyService.js
 * Validates syntax, structure, and patterns (model — 171L)
 * @generated by P#106 universal generator
 */
'use strict';

const fs = require('fs');
const path = require('path');
const vm = require('vm');

const SRC = path.resolve(__dirname, '../../domains/group-therapy/services/GroupTherapyService.js');

describe('domains/group-therapy/services/GroupTherapyService.js', () => {
  let source;
  beforeAll(() => {
    source = fs.readFileSync(SRC, 'utf8');
  });

  test('file exists', () => {
    expect(fs.existsSync(SRC)).toBe(true);
  });

  test('is syntactically valid JavaScript', () => {
    expect(() => new vm.Script(source, { filename: 'GroupTherapyService.js' })).not.toThrow();
  });

  test('defines a Mongoose model', () => {
    expect(source).toMatch(/mongoose\.model\s*\(/);
  });

  test('has npm dependencies (1)', () => {
    const npms = (source.match(/require\s*\(\s*['"](?![./])[^'"]+['"]\s*\)/g) || []);
    expect(npms.length).toBe(1);
  });

  test('has module.exports', () => {
    expect(source).toMatch(/module\.exports/);
  });

  test('exports groupTherapyService', () => {
    expect(source).toMatch(/groupTherapyService/);
  });

  test('exports GroupTherapyService', () => {
    expect(source).toMatch(/GroupTherapyService/);
  });
});
