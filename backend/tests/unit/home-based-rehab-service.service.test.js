/**
 * Auto-generated tests for rehabilitation-services/home-based-rehab-service.js
 * Validates syntax, structure, and patterns (service — 185L)
 * @generated by P#106 universal generator
 */
'use strict';

const fs = require('fs');
const path = require('path');
const vm = require('vm');

const SRC = path.resolve(__dirname, '../../rehabilitation-services/home-based-rehab-service.js');

describe('rehabilitation-services/home-based-rehab-service.js', () => {
  let source;
  beforeAll(() => {
    source = fs.readFileSync(SRC, 'utf8');
  });

  test('file exists', () => {
    expect(fs.existsSync(SRC)).toBe(true);
  });

  test('is syntactically valid JavaScript', () => {
    expect(() => new vm.Script(source, { filename: 'home-based-rehab-service.js' })).not.toThrow();
  });

  test('defines class HomeBasedRehabService', () => {
    expect(source).toMatch(/class\s+HomeBasedRehabService/);
  });

  test('has async methods (6)', () => {
    const matches = source.match(/async\s+\w+\s*\(/g) || [];
    expect(matches.length).toBeGreaterThanOrEqual(1);
  });

  test('has module.exports', () => {
    expect(source).toMatch(/module\.exports/);
  });

  test('exports HomeBasedRehabService', () => {
    expect(source).toMatch(/HomeBasedRehabService/);
  });
});
