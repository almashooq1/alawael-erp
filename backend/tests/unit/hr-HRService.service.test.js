/**
 * Auto-generated tests for services/hr/HRService.js
 * Validates syntax, structure, and patterns (service — 291L)
 * @generated by P#106 universal generator
 */
'use strict';

const fs = require('fs');
const path = require('path');
const vm = require('vm');

const SRC = path.resolve(__dirname, '../../services/hr/HRService.js');

describe('services/hr/HRService.js', () => {
  let source;
  beforeAll(() => {
    source = fs.readFileSync(SRC, 'utf8');
  });

  test('file exists', () => {
    expect(fs.existsSync(SRC)).toBe(true);
  });

  test('is syntactically valid JavaScript', () => {
    expect(() => new vm.Script(source, { filename: 'HRService.js' })).not.toThrow();
  });

  test('defines class PayrollCalculationService', () => {
    expect(source).toMatch(/class\s+PayrollCalculationService/);
  });

  test('defines class EndOfServiceService', () => {
    expect(source).toMatch(/class\s+EndOfServiceService/);
  });

  test('defines class LeaveService', () => {
    expect(source).toMatch(/class\s+LeaveService/);
  });

  test('has async methods (8)', () => {
    const matches = source.match(/async\s+\w+\s*\(/g) || [];
    expect(matches.length).toBeGreaterThanOrEqual(1);
  });

  test('uses error handling', () => {
    expect(source).toMatch(/try\s*\{/);
  });

  test('has local dependencies (7)', () => {
    const locals = (source.match(/require\s*\(\s*['"]\.[^'"]+['"]\s*\)/g) || []);
    expect(locals.length).toBe(7);
  });

  test('has module.exports', () => {
    expect(source).toMatch(/module\.exports/);
  });

  test('exports PayrollCalculationService', () => {
    expect(source).toMatch(/PayrollCalculationService/);
  });

  test('exports EndOfServiceService', () => {
    expect(source).toMatch(/EndOfServiceService/);
  });

  test('exports LeaveService', () => {
    expect(source).toMatch(/LeaveService/);
  });

  test('exports HRAlertService', () => {
    expect(source).toMatch(/HRAlertService/);
  });
});
