/**
 * Auto-generated tests for seeds/initDatabase.js
 * Validates syntax, structure, and patterns (module — 383L)
 * @generated by P#106 universal generator
 */
'use strict';

const fs = require('fs');
const path = require('path');
const vm = require('vm');

const SRC = path.resolve(__dirname, '../../seeds/initDatabase.js');

describe('seeds/initDatabase.js', () => {
  let source;
  beforeAll(() => {
    source = fs.readFileSync(SRC, 'utf8');
  });

  test('file exists', () => {
    expect(fs.existsSync(SRC)).toBe(true);
  });

  test('is syntactically valid JavaScript', () => {
    expect(() => new vm.Script(source, { filename: 'initDatabase.js' })).not.toThrow();
  });

  test('has local dependencies (2)', () => {
    const locals = (source.match(/require\s*\(\s*['"]\.[^'"]+['"]\s*\)/g) || []);
    expect(locals.length).toBe(2);
  });

  test('has module.exports', () => {
    expect(source).toMatch(/module\.exports/);
  });

  test('exports seedData', () => {
    expect(source).toMatch(/seedData/);
  });

  test('exports clearDatabase', () => {
    expect(source).toMatch(/clearDatabase/);
  });

  test('exports resetDatabase', () => {
    expect(source).toMatch(/resetDatabase/);
  });
});
