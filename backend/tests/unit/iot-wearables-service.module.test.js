/**
 * Auto-generated tests for rehabilitation-gamification/iot-wearables-service.js
 * Validates syntax, structure, and patterns (model — 782L)
 * @generated by P#106 universal generator
 */
'use strict';

const fs = require('fs');
const path = require('path');
const vm = require('vm');

const SRC = path.resolve(__dirname, '../../rehabilitation-gamification/iot-wearables-service.js');

describe('rehabilitation-gamification/iot-wearables-service.js', () => {
  let source;
  beforeAll(() => {
    source = fs.readFileSync(SRC, 'utf8');
  });

  test('file exists', () => {
    expect(fs.existsSync(SRC)).toBe(true);
  });

  test('is syntactically valid JavaScript', () => {
    expect(() => new vm.Script(source, { filename: 'iot-wearables-service.js' })).not.toThrow();
  });

  test('defines a Mongoose model', () => {
    expect(source).toMatch(/mongoose\.model\s*\(/);
  });

  test('creates a Schema', () => {
    expect(source).toMatch(/new\s+Schema\s*\(/);
  });

  test('uses timestamps', () => {
    expect(source).toMatch(/timestamps\s*:\s*true/);
  });

  test('has required fields', () => {
    expect(source).toMatch(/required\s*:\s*true/);
  });

  test('defines indexes', () => {
    expect(source).toMatch(/index/);
  });

  test('has npm dependencies (14)', () => {
    const npms = (source.match(/require\s*\(\s*['"](?![./])[^'"]+['"]\s*\)/g) || []);
    expect(npms.length).toBe(14);
  });

  test('has module.exports', () => {
    expect(source).toMatch(/module\.exports/);
  });

  test('exports IotWearablesService', () => {
    expect(source).toMatch(/IotWearablesService/);
  });

  test('exports IotDeviceType', () => {
    expect(source).toMatch(/IotDeviceType/);
  });

  test('exports IotDevice', () => {
    expect(source).toMatch(/IotDevice/);
  });

  test('exports IotDeviceAssignment', () => {
    expect(source).toMatch(/IotDeviceAssignment/);
  });

  test('exports IotReading', () => {
    expect(source).toMatch(/IotReading/);
  });
});
