/**
 * Auto-generated tests for scheduler/kpi-attendance.scheduler.js
 * Validates syntax, structure, and patterns (validator — 636L)
 * @generated by P#106 universal generator
 */
'use strict';

const fs = require('fs');
const path = require('path');
const vm = require('vm');

const SRC = path.resolve(__dirname, '../../scheduler/kpi-attendance.scheduler.js');

describe('scheduler/kpi-attendance.scheduler.js', () => {
  let source;
  beforeAll(() => {
    source = fs.readFileSync(SRC, 'utf8');
  });

  test('file exists', () => {
    expect(fs.existsSync(SRC)).toBe(true);
  });

  test('is syntactically valid JavaScript', () => {
    expect(() => new vm.Script(source, { filename: 'kpi-attendance.scheduler.js' })).not.toThrow();
  });

  test('has npm dependencies (1)', () => {
    const npms = (source.match(/require\s*\(\s*['"](?![./])[^'"]+['"]\s*\)/g) || []);
    expect(npms.length).toBe(1);
  });

  test('has local dependencies (19)', () => {
    const locals = (source.match(/require\s*\(\s*['"]\.[^'"]+['"]\s*\)/g) || []);
    expect(locals.length).toBe(19);
  });

  test('has module.exports', () => {
    expect(source).toMatch(/module\.exports/);
  });

  test('exports startKpiAttendanceScheduler', () => {
    expect(source).toMatch(/startKpiAttendanceScheduler/);
  });

  test('exports stopKpiAttendanceScheduler', () => {
    expect(source).toMatch(/stopKpiAttendanceScheduler/);
  });

  test('exports runJobManually', () => {
    expect(source).toMatch(/runJobManually/);
  });

  test('exports generateMonthlyKpiReports', () => {
    expect(source).toMatch(/generateMonthlyKpiReports/);
  });

  test('exports sendKpiAlertDigest', () => {
    expect(source).toMatch(/sendKpiAlertDigest/);
  });
});
