/**
 * Auto-generated tests for config/logging.advanced.js
 * @generated by _gen_config_tests.js
 */
'use strict';

/* ── Mocks ── */
jest.mock('winston', () => ({
  createLogger: jest.fn(() => ({
    info: jest.fn(), error: jest.fn(), warn: jest.fn(), debug: jest.fn(),
    add: jest.fn(), stream: { write: jest.fn() },
  })),
  format: {
    combine: jest.fn(), timestamp: jest.fn(), printf: jest.fn(() => jest.fn()),
    colorize: jest.fn(), simple: jest.fn(), json: jest.fn(),
    errors: jest.fn(), metadata: jest.fn(), label: jest.fn(),
  },
  transports: { Console: jest.fn(), File: jest.fn(), DailyRotateFile: jest.fn() },
  addColors: jest.fn(),
}));

jest.mock('fs', () => ({
  existsSync: jest.fn(() => true),
  mkdirSync: jest.fn(),
  readFileSync: jest.fn(() => '{}'),
  writeFileSync: jest.fn(),
  readdirSync: jest.fn(() => []),
  unlinkSync: jest.fn(),
  statSync: jest.fn(() => ({ mtimeMs: Date.now() })),
  createReadStream: jest.fn(() => ({ pipe: jest.fn() })),
  createWriteStream: jest.fn(() => ({ on: jest.fn(), end: jest.fn() })),
}));

jest.mock('../../package.json', () => new Proxy({}, {
  get: (_, p) => {
    if (p === '__esModule') return false;
    if (p === 'default') return {};
    return jest.fn(() => Promise.resolve({}));
  },
}), { virtual: true });

/* ── Module under test ── */
const mod = require('../../config/logging.advanced');

describe('config/logging.advanced', () => {
  test('should export a valid module', () => {
    expect(mod).toBeDefined();
    expect(typeof mod).toBe('object');
  });

  test('should export logger', () => {
    expect(mod.logger).toBeDefined();
  });

  test('should export getLogger', () => {
    expect(mod.getLogger).toBeDefined();
    expect(typeof mod.getLogger).toBe('function');
  });

  test('should export createChildLogger', () => {
    expect(mod.createChildLogger).toBeDefined();
    expect(typeof mod.createChildLogger).toBe('function');
  });

  test('should export sanitizeLogData', () => {
    expect(mod.sanitizeLogData).toBeDefined();
    expect(typeof mod.sanitizeLogData).toBe('function');
  });

  test('should export requestLoggingMiddleware', () => {
    expect(mod.requestLoggingMiddleware).toBeDefined();
    expect(typeof mod.requestLoggingMiddleware).toBe('function');
  });

  test('should export logError', () => {
    expect(mod.logError).toBeDefined();
    expect(typeof mod.logError).toBe('function');
  });

  test('should export logPerformance', () => {
    expect(mod.logPerformance).toBeDefined();
    expect(typeof mod.logPerformance).toBe('function');
  });

  test('should export logSecurityEvent', () => {
    expect(mod.logSecurityEvent).toBeDefined();
    expect(typeof mod.logSecurityEvent).toBe('function');
  });

  test('should export logDatabaseOperation', () => {
    expect(mod.logDatabaseOperation).toBeDefined();
    expect(typeof mod.logDatabaseOperation).toBe('function');
  });

  test('should export cleanupOldLogs', () => {
    expect(mod.cleanupOldLogs).toBeDefined();
    expect(typeof mod.cleanupOldLogs).toBe('function');
  });

});
