/**
 * Auto-generated tests for services/messagingService.js
 * @generated by _gen_remaining_tests.js
 */
'use strict';

/* ── Mocks ── */
jest.mock('../../utils/logger', () => ({
  info: jest.fn(),
  error: jest.fn(),
  warn: jest.fn(),
  debug: jest.fn(),
  verbose: jest.fn(),
  silly: jest.fn(),
  child: jest.fn(() => ({ info: jest.fn(), error: jest.fn(), warn: jest.fn(), debug: jest.fn() })),
  stream: { write: jest.fn() },
}));

jest.mock('crypto', () => ({
  ...jest.requireActual('crypto'),
  randomBytes: jest.fn(() => Buffer.from('abcdef1234567890abcdef1234567890', 'hex')),
  createHash: jest.fn(() => ({ update: jest.fn().mockReturnThis(), digest: jest.fn(() => 'mockhash') })),
  createHmac: jest.fn(() => ({ update: jest.fn().mockReturnThis(), digest: jest.fn(() => 'mockhmac') })),
  createCipheriv: jest.fn(() => ({ update: jest.fn(() => 'enc'), final: jest.fn(() => ''), getAuthTag: jest.fn(() => Buffer.from('tag')) })),
  createDecipheriv: jest.fn(() => ({ update: jest.fn(() => 'dec'), final: jest.fn(() => ''), setAuthTag: jest.fn() })),
  pbkdf2Sync: jest.fn(() => Buffer.from('derived-key-32-bytes-long!!!!!!!')),
}));

jest.mock('nodemailer', () => ({
  createTransport: jest.fn(() => ({
    sendMail: jest.fn(() => Promise.resolve({ messageId: 'mock-id' })),
    verify: jest.fn(() => Promise.resolve(true)),
  })),
}), { virtual: true });

jest.mock('twilio', () => jest.fn(() => ({
  messages: { create: jest.fn(() => Promise.resolve({ sid: 'mock-sid' })) },
})), { virtual: true });

jest.mock('../../config/secrets', () => new Proxy({}, {
  get: (_, p) => {
    if (p === '__esModule') return false;
    if (p === 'default') return {};
    return jest.fn(() => Promise.resolve({}));
  },
}));

/* ── Module under test ── */
const mod = require('../../services/messagingService');

describe('services/messagingService', () => {
  test('should export a valid module', () => {
    expect(mod).toBeDefined();
    expect(typeof mod).toBe('object');
  });

  test('should export MessageEncryptionService as a constructor', () => {
    expect(mod.MessageEncryptionService).toBeDefined();
    expect(typeof mod.MessageEncryptionService).toBe('function');
  });

  test('should export NotificationService as a constructor', () => {
    expect(mod.NotificationService).toBeDefined();
    expect(typeof mod.NotificationService).toBe('function');
  });

  test('should export MessageValidator as a constructor', () => {
    expect(mod.MessageValidator).toBeDefined();
    expect(typeof mod.MessageValidator).toBe('function');
  });

  test('should export MessageAccessLogger as a constructor', () => {
    expect(mod.MessageAccessLogger).toBeDefined();
    expect(typeof mod.MessageAccessLogger).toBe('function');
  });

  test('should export AbuseDetectionService as a constructor', () => {
    expect(mod.AbuseDetectionService).toBeDefined();
    expect(typeof mod.AbuseDetectionService).toBe('function');
  });

});
