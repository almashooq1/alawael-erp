/**
 * Auto-generated tests for config/metrics.js
 * @generated by _gen_config_tests.js
 */
'use strict';

/* ── Mocks ── */
jest.mock('../../utils/logger', () => ({
  info: jest.fn(),
  error: jest.fn(),
  warn: jest.fn(),
  debug: jest.fn(),
  verbose: jest.fn(),
  silly: jest.fn(),
  child: jest.fn(() => ({ info: jest.fn(), error: jest.fn(), warn: jest.fn(), debug: jest.fn() })),
  stream: { write: jest.fn() },
}));

jest.mock('prom-client', () => ({
  Counter: jest.fn(() => ({ inc: jest.fn(), get: jest.fn(() => ({ values: [] })) })),
  Histogram: jest.fn(() => ({ observe: jest.fn(), startTimer: jest.fn(() => jest.fn()), get: jest.fn(() => ({ values: [] })) })),
  Gauge: jest.fn(() => ({ set: jest.fn(), inc: jest.fn(), dec: jest.fn(), get: jest.fn(() => ({ values: [] })) })),
  Summary: jest.fn(() => ({ observe: jest.fn() })),
  register: { metrics: jest.fn(() => ''), contentType: 'text/plain', getSingleMetric: jest.fn() },
  collectDefaultMetrics: jest.fn(),
}), { virtual: true });

/* ── Module under test ── */
const mod = require('../../config/metrics');

describe('config/metrics', () => {
  test('should export a valid module', () => {
    expect(mod).toBeDefined();
    expect(typeof mod).toBe('object');
  });

  test('should export httpRequestDuration', () => {
    expect(mod.httpRequestDuration).toBeDefined();
  });

  test('should export cacheCounter', () => {
    expect(mod.cacheCounter).toBeDefined();
  });

  test('should export activeUsers', () => {
    expect(mod.activeUsers).toBeDefined();
  });

  test('should export dbQueryDuration', () => {
    expect(mod.dbQueryDuration).toBeDefined();
  });

  test('should export apiErrors', () => {
    expect(mod.apiErrors).toBeDefined();
  });

  test('should export redisOperations', () => {
    expect(mod.redisOperations).toBeDefined();
  });

  test('should export memoryUsage', () => {
    expect(mod.memoryUsage).toBeDefined();
  });

  test('should export socketConnections', () => {
    expect(mod.socketConnections).toBeDefined();
  });

  test('should export requestQueueLength', () => {
    expect(mod.requestQueueLength).toBeDefined();
  });

  test('should export businessMetrics', () => {
    expect(mod.businessMetrics).toBeDefined();
  });

  test('should export metricsMiddleware', () => {
    expect(mod.metricsMiddleware).toBeDefined();
    expect(typeof mod.metricsMiddleware).toBe('function');
  });

  test('should export initMetricsServer', () => {
    expect(mod.initMetricsServer).toBeDefined();
    expect(typeof mod.initMetricsServer).toBe('function');
  });

  test('should export trackCacheHit', () => {
    expect(mod.trackCacheHit).toBeDefined();
    expect(typeof mod.trackCacheHit).toBe('function');
  });

  test('should export trackCacheMiss', () => {
    expect(mod.trackCacheMiss).toBeDefined();
    expect(typeof mod.trackCacheMiss).toBe('function');
  });

  test('should export trackSocketEvent', () => {
    expect(mod.trackSocketEvent).toBeDefined();
    expect(typeof mod.trackSocketEvent).toBe('function');
  });

  test('should export trackDbQuery', () => {
    expect(mod.trackDbQuery).toBeDefined();
    expect(typeof mod.trackDbQuery).toBe('function');
  });

  test('should export trackRedisOperation', () => {
    expect(mod.trackRedisOperation).toBeDefined();
    expect(typeof mod.trackRedisOperation).toBe('function');
  });

  test('should export trackTransaction', () => {
    expect(mod.trackTransaction).toBeDefined();
    expect(typeof mod.trackTransaction).toBe('function');
  });

  test('should export updateMemoryMetrics', () => {
    expect(mod.updateMemoryMetrics).toBeDefined();
    expect(typeof mod.updateMemoryMetrics).toBe('function');
  });

  test('should export getMetricsSnapshot', () => {
    expect(mod.getMetricsSnapshot).toBeDefined();
    expect(typeof mod.getMetricsSnapshot).toBe('function');
  });

});
