/**
 * Auto-generated tests for config/mongoose.plugins.js
 * @generated by _gen_config_tests.js
 */
'use strict';

/* ── Mocks ── */
jest.mock('../../utils/logger', () => ({
  info: jest.fn(),
  error: jest.fn(),
  warn: jest.fn(),
  debug: jest.fn(),
  verbose: jest.fn(),
  silly: jest.fn(),
  child: jest.fn(() => ({ info: jest.fn(), error: jest.fn(), warn: jest.fn(), debug: jest.fn() })),
  stream: { write: jest.fn() },
}));

const mockSchema = { index: jest.fn().mockReturnThis(), virtual: jest.fn(() => ({ get: jest.fn() })), pre: jest.fn(), post: jest.fn(), plugin: jest.fn(), statics: {}, methods: {}, set: jest.fn() };
jest.mock('mongoose', () => ({
  Schema: jest.fn(() => mockSchema),
  model: jest.fn(() => ({ find: jest.fn(), findOne: jest.fn(), create: jest.fn() })),
  connect: jest.fn(() => Promise.resolve()),
  disconnect: jest.fn(() => Promise.resolve()),
  connection: { readyState: 1, on: jest.fn(), close: jest.fn(), db: { admin: jest.fn(() => ({ ping: jest.fn(() => Promise.resolve({ ok: 1 })), serverStatus: jest.fn(() => Promise.resolve({ connections: {} })) })), stats: jest.fn(() => Promise.resolve({})), command: jest.fn(() => Promise.resolve({})) } },
  plugin: jest.fn(),
  Types: { ObjectId: { isValid: jest.fn(() => true) } },
  set: jest.fn(),
}));

/* ── Module under test ── */
const mod = require('../../config/mongoose.plugins');

describe('config/mongoose.plugins', () => {
  test('should export a valid module', () => {
    expect(mod).toBeDefined();
    expect(typeof mod).toBe('object');
  });

  test('should export slowQueryPlugin', () => {
    expect(mod.slowQueryPlugin).toBeDefined();
    expect(typeof mod.slowQueryPlugin).toBe('function');
  });

  test('should export toJSONPlugin', () => {
    expect(mod.toJSONPlugin).toBeDefined();
    expect(typeof mod.toJSONPlugin).toBe('function');
  });

  test('should export registerGlobalPlugins', () => {
    expect(mod.registerGlobalPlugins).toBeDefined();
    expect(typeof mod.registerGlobalPlugins).toBe('function');
  });

  test('should export getConnectionPoolHealth', () => {
    expect(mod.getConnectionPoolHealth).toBeDefined();
    expect(typeof mod.getConnectionPoolHealth).toBe('function');
  });

});
