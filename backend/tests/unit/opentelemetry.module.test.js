/**
 * Auto-generated tests for observability/opentelemetry.js
 * Validates syntax, structure, and patterns (middleware — 309L)
 * @generated by P#106 universal generator
 */
'use strict';

const fs = require('fs');
const path = require('path');
const vm = require('vm');

const SRC = path.resolve(__dirname, '../../observability/opentelemetry.js');

describe('observability/opentelemetry.js', () => {
  let source;
  beforeAll(() => {
    source = fs.readFileSync(SRC, 'utf8');
  });

  test('file exists', () => {
    expect(fs.existsSync(SRC)).toBe(true);
  });

  test('is syntactically valid JavaScript', () => {
    expect(() => new vm.Script(source, { filename: 'opentelemetry.js' })).not.toThrow();
  });

  test('exports a middleware function', () => {
    expect(source).toMatch(/(?:req,\s*res,\s*next|module\.exports)/);
  });

  test('has npm dependencies (12)', () => {
    const npms = (source.match(/require\s*\(\s*['"](?![./])[^'"]+['"]\s*\)/g) || []);
    expect(npms.length).toBe(12);
  });

  test('has local dependencies (1)', () => {
    const locals = (source.match(/require\s*\(\s*['"]\.[^'"]+['"]\s*\)/g) || []);
    expect(locals.length).toBe(1);
  });

  test('has module.exports', () => {
    expect(source).toMatch(/module\.exports/);
  });

  test('exports initializeOpenTelemetry', () => {
    expect(source).toMatch(/initializeOpenTelemetry/);
  });

  test('exports shutdownOpenTelemetry', () => {
    expect(source).toMatch(/shutdownOpenTelemetry/);
  });

  test('exports getTracer', () => {
    expect(source).toMatch(/getTracer/);
  });

  test('exports getMeter', () => {
    expect(source).toMatch(/getMeter/);
  });

  test('exports createCustomMetrics', () => {
    expect(source).toMatch(/createCustomMetrics/);
  });
});
