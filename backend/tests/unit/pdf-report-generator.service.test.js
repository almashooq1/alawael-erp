/**
 * Auto-generated tests for rehabilitation-services/pdf-report-generator.js
 * Validates syntax, structure, and patterns (service — 718L)
 * @generated by P#106 universal generator
 */
'use strict';

const fs = require('fs');
const path = require('path');
const vm = require('vm');

const SRC = path.resolve(__dirname, '../../rehabilitation-services/pdf-report-generator.js');

describe('rehabilitation-services/pdf-report-generator.js', () => {
  let source;
  beforeAll(() => {
    source = fs.readFileSync(SRC, 'utf8');
  });

  test('file exists', () => {
    expect(fs.existsSync(SRC)).toBe(true);
  });

  test('is syntactically valid JavaScript', () => {
    expect(() => new vm.Script(source, { filename: 'pdf-report-generator.js' })).not.toThrow();
  });

  test('defines class PDFReportGenerator', () => {
    expect(source).toMatch(/class\s+PDFReportGenerator/);
  });

  test('has async methods (7)', () => {
    const matches = source.match(/async\s+\w+\s*\(/g) || [];
    expect(matches.length).toBeGreaterThanOrEqual(1);
  });

  test('has npm dependencies (3)', () => {
    const npms = (source.match(/require\s*\(\s*['"](?![./])[^'"]+['"]\s*\)/g) || []);
    expect(npms.length).toBe(3);
  });

  test('has module.exports', () => {
    expect(source).toMatch(/module\.exports/);
  });

  test('exports PDFReportGenerator', () => {
    expect(source).toMatch(/PDFReportGenerator/);
  });

  test('exports REPORT_CONFIG', () => {
    expect(source).toMatch(/REPORT_CONFIG/);
  });
});
