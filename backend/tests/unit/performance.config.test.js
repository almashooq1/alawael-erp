/**
 * Auto-generated tests for config/performance.js
 * @generated by _gen_config_tests.js
 */
'use strict';

/* ── Mocks ── */
jest.mock('../../utils/logger', () => ({
  info: jest.fn(),
  error: jest.fn(),
  warn: jest.fn(),
  debug: jest.fn(),
  verbose: jest.fn(),
  silly: jest.fn(),
  child: jest.fn(() => ({ info: jest.fn(), error: jest.fn(), warn: jest.fn(), debug: jest.fn() })),
  stream: { write: jest.fn() },
}));

jest.mock('ioredis', () => {
  const RedisMock = jest.fn(() => ({
    get: jest.fn(() => Promise.resolve(null)),
    set: jest.fn(() => Promise.resolve('OK')),
    del: jest.fn(() => Promise.resolve(1)),
    setex: jest.fn(() => Promise.resolve('OK')),
    expire: jest.fn(() => Promise.resolve(1)),
    keys: jest.fn(() => Promise.resolve([])),
    info: jest.fn(() => Promise.resolve('# Server\nredis_version:7.0.0')),
    ping: jest.fn(() => Promise.resolve('PONG')),
    on: jest.fn().mockReturnThis(),
    connect: jest.fn(() => Promise.resolve()),
    disconnect: jest.fn(),
    quit: jest.fn(() => Promise.resolve()),
    status: 'ready',
    pipeline: jest.fn(() => ({ exec: jest.fn(() => Promise.resolve([])) })),
  }));
  RedisMock.Cluster = jest.fn(() => ({}));
  return RedisMock;
}, { virtual: true });

jest.mock('compression', () => jest.fn(() => jest.fn((req, res, next) => next && next())), { virtual: true });

jest.mock('crypto', () => ({
  ...jest.requireActual('crypto'),
  randomBytes: jest.fn(() => Buffer.from('abcdef1234567890', 'hex')),
  createHash: jest.fn(() => ({ update: jest.fn().mockReturnThis(), digest: jest.fn(() => 'mockhash') })),
  createHmac: jest.fn(() => ({ update: jest.fn().mockReturnThis(), digest: jest.fn(() => 'mockhmac') })),
}));

/* ── Module under test ── */
const mod = require('../../config/performance');

describe('config/performance', () => {
  test('should export a valid module', () => {
    expect(mod).toBeDefined();
    expect(typeof mod).toBe('object');
  });

  test('should export compressionMiddleware', () => {
    expect(mod.compressionMiddleware).toBeDefined();
  });

  test('should export queryOptimizationHints', () => {
    expect(mod.queryOptimizationHints).toBeDefined();
  });

  test('should export performanceMonitor', () => {
    expect(mod.performanceMonitor).toBeDefined();
  });

  test('should export initializeRedis', () => {
    expect(mod.initializeRedis).toBeDefined();
    expect(typeof mod.initializeRedis).toBe('function');
  });

  test('should export cacheMiddleware', () => {
    expect(mod.cacheMiddleware).toBeDefined();
    expect(typeof mod.cacheMiddleware).toBe('function');
  });

  test('should export clearCache', () => {
    expect(mod.clearCache).toBeDefined();
    expect(typeof mod.clearCache).toBe('function');
  });

  test('should export getCacheStats', () => {
    expect(mod.getCacheStats).toBeDefined();
    expect(typeof mod.getCacheStats).toBe('function');
  });

  test('should export requestTimerMiddleware', () => {
    expect(mod.requestTimerMiddleware).toBeDefined();
    expect(typeof mod.requestTimerMiddleware).toBe('function');
  });

  test('should export etagMiddleware', () => {
    expect(mod.etagMiddleware).toBeDefined();
    expect(typeof mod.etagMiddleware).toBe('function');
  });

  test('should export getResponseTimePercentiles', () => {
    expect(mod.getResponseTimePercentiles).toBeDefined();
    expect(typeof mod.getResponseTimePercentiles).toBe('function');
  });

  test('should export getMemoryPressure', () => {
    expect(mod.getMemoryPressure).toBeDefined();
    expect(typeof mod.getMemoryPressure).toBe('function');
  });

});
