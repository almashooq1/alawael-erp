/**
 * Auto-generated tests for models/prediction.model.js
 * @generated by _gen_remaining_tests.js
 */
'use strict';

/* ── Mocks ── */
const mockSchema = {
  index: jest.fn().mockReturnThis(),
  virtual: jest.fn(() => ({ get: jest.fn() })),
  pre: jest.fn(),
  post: jest.fn(),
  plugin: jest.fn(),
  statics: {},
  methods: {},
  set: jest.fn(),
};
jest.mock('mongoose', () => ({
  Schema: jest.fn(() => mockSchema),
  model: jest.fn(() => ({
    find: jest.fn(),
    findOne: jest.fn(),
    create: jest.fn(),
    findById: jest.fn(),
    findByIdAndUpdate: jest.fn(),
    deleteMany: jest.fn(),
  })),
  connect: jest.fn(() => Promise.resolve()),
  disconnect: jest.fn(() => Promise.resolve()),
  connection: { readyState: 1, on: jest.fn(), close: jest.fn() },
  Types: { ObjectId: { isValid: jest.fn(() => true) } },
  plugin: jest.fn(),
  set: jest.fn(),
}));

jest.mock('uuid', () => ({ v4: jest.fn(() => 'mock-uuid-v4') }));

/* ── Module under test ── */
const mod = require('../../models/prediction.model');

describe('models/prediction.model', () => {
  test('should export a valid module', () => {
    expect(mod).toBeDefined();
    expect(typeof mod).toBe('function');
  });

  test('should export isMockModel', () => {
    expect(mod.isMockModel).toBeDefined();
  });

  test('should export _mockStore', () => {
    expect(mod._mockStore).toBeDefined();
  });

  test('should be instantiable', () => {
    const instance = new mod();
    expect(instance).toBeDefined();
  });

  test('instance should have method save', () => {
    const instance = new mod();
    expect(typeof instance.save).toBe('function');
  });
});
