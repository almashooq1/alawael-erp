/**
 * Auto-generated tests for config/rehab-roles.js
 * @generated by _gen_config_tests.js
 */
'use strict';

/* ── Module under test ── */
const mod = require('../../config/rehab-roles');

describe('config/rehab-roles', () => {
  test('should export a valid module', () => {
    expect(mod).toBeDefined();
    expect(typeof mod).toBe('object');
  });

  test('should export REHAB_ROLES', () => {
    expect(mod.REHAB_ROLES).toBeDefined();
  });

  test('should export REHAB_PERMISSIONS', () => {
    expect(mod.REHAB_PERMISSIONS).toBeDefined();
  });

  test('should export ROLE_PERMISSIONS_MAP', () => {
    expect(mod.ROLE_PERMISSIONS_MAP).toBeDefined();
  });

  test('should export REHAB_CROSS_BRANCH_ROLES', () => {
    expect(mod.REHAB_CROSS_BRANCH_ROLES).toBeDefined();
  });

  test('should export rehabHasPermission', () => {
    expect(mod.rehabHasPermission).toBeDefined();
    expect(typeof mod.rehabHasPermission).toBe('function');
  });

  test('should export getRolePermissions', () => {
    expect(mod.getRolePermissions).toBeDefined();
    expect(typeof mod.getRolePermissions).toBe('function');
  });

  test('should export getPermissionsByModule', () => {
    expect(mod.getPermissionsByModule).toBeDefined();
    expect(typeof mod.getPermissionsByModule).toBe('function');
  });

  test('should export requireRehabPermission', () => {
    expect(mod.requireRehabPermission).toBeDefined();
    expect(typeof mod.requireRehabPermission).toBe('function');
  });

});
