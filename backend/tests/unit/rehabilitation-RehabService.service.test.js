/**
 * Auto-generated tests for services/rehabilitation/RehabService.js
 * Validates syntax, structure, and patterns (service — 626L)
 * @generated by P#106 universal generator
 */
'use strict';

const fs = require('fs');
const path = require('path');
const vm = require('vm');

const SRC = path.resolve(__dirname, '../../services/rehabilitation/RehabService.js');

describe('services/rehabilitation/RehabService.js', () => {
  let source;
  beforeAll(() => {
    source = fs.readFileSync(SRC, 'utf8');
  });

  test('file exists', () => {
    expect(fs.existsSync(SRC)).toBe(true);
  });

  test('is syntactically valid JavaScript', () => {
    expect(() => new vm.Script(source, { filename: 'RehabService.js' })).not.toThrow();
  });

  test('defines class ProgramService', () => {
    expect(source).toMatch(/class\s+ProgramService/);
  });

  test('defines class EnrollmentService', () => {
    expect(source).toMatch(/class\s+EnrollmentService/);
  });

  test('defines class RehabPlanService', () => {
    expect(source).toMatch(/class\s+RehabPlanService/);
  });

  test('has async methods (16)', () => {
    const matches = source.match(/async\s+\w+\s*\(/g) || [];
    expect(matches.length).toBeGreaterThanOrEqual(1);
  });

  test('uses error handling', () => {
    expect(source).toMatch(/try\s*\{/);
  });

  test('has local dependencies (5)', () => {
    const locals = (source.match(/require\s*\(\s*['"]\.[^'"]+['"]\s*\)/g) || []);
    expect(locals.length).toBe(5);
  });

  test('has module.exports', () => {
    expect(source).toMatch(/module\.exports/);
  });

  test('exports ProgramService', () => {
    expect(source).toMatch(/ProgramService/);
  });

  test('exports EnrollmentService', () => {
    expect(source).toMatch(/EnrollmentService/);
  });

  test('exports RehabPlanService', () => {
    expect(source).toMatch(/RehabPlanService/);
  });

  test('exports SessionService', () => {
    expect(source).toMatch(/SessionService/);
  });
});
