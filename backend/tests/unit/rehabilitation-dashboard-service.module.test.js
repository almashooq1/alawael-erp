/**
 * Auto-generated tests for dashboard/rehabilitation-dashboard-service.js
 * Validates syntax, structure, and patterns (service — 371L)
 * @generated by P#106 universal generator
 */
'use strict';

const fs = require('fs');
const path = require('path');
const vm = require('vm');

const SRC = path.resolve(__dirname, '../../dashboard/rehabilitation-dashboard-service.js');

describe('dashboard/rehabilitation-dashboard-service.js', () => {
  let source;
  beforeAll(() => {
    source = fs.readFileSync(SRC, 'utf8');
  });

  test('file exists', () => {
    expect(fs.existsSync(SRC)).toBe(true);
  });

  test('is syntactically valid JavaScript', () => {
    expect(() => new vm.Script(source, { filename: 'rehabilitation-dashboard-service.js' })).not.toThrow();
  });

  test('defines class RehabilitationDashboardService', () => {
    expect(source).toMatch(/class\s+RehabilitationDashboardService/);
  });

  test('has async methods (14)', () => {
    const matches = source.match(/async\s+\w+\s*\(/g) || [];
    expect(matches.length).toBeGreaterThanOrEqual(1);
  });

  test('has npm dependencies (2)', () => {
    const npms = (source.match(/require\s*\(\s*['"](?![./])[^'"]+['"]\s*\)/g) || []);
    expect(npms.length).toBe(2);
  });

  test('has local dependencies (1)', () => {
    const locals = (source.match(/require\s*\(\s*['"]\.[^'"]+['"]\s*\)/g) || []);
    expect(locals.length).toBe(1);
  });

  test('has module.exports', () => {
    expect(source).toMatch(/module\.exports/);
  });

  test('exports RehabilitationDashboardService', () => {
    expect(source).toMatch(/RehabilitationDashboardService/);
  });

  test('exports rehabilitationDashboardService', () => {
    expect(source).toMatch(/rehabilitationDashboardService/);
  });

  test('exports dashboardConfig', () => {
    expect(source).toMatch(/dashboardConfig/);
  });
});
