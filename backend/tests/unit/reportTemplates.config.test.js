/**
 * Auto-generated tests for config/reportTemplates.js
 * @generated by _gen_config_tests.js
 */
'use strict';

/* ── Mocks ── */
jest.mock('../../services/ReportingService', () => new Proxy({}, {
  get: (_, p) => {
    if (p === '__esModule') return false;
    if (p === 'default') return {};
    return jest.fn(() => Promise.resolve({}));
  },
}));

/* ── Module under test ── */
const mod = require('../../config/reportTemplates');

describe('config/reportTemplates', () => {
  test('should export a valid module', () => {
    expect(mod).toBeDefined();
    expect(typeof mod).toBe('object');
  });

  test('should export salesReport', () => {
    expect(mod.salesReport).toBeDefined();
  });

  test('should export inventoryReport', () => {
    expect(mod.inventoryReport).toBeDefined();
  });

  test('should export financialReport', () => {
    expect(mod.financialReport).toBeDefined();
  });

  test('should export userActivityReport', () => {
    expect(mod.userActivityReport).toBeDefined();
  });

  test('should export customerReport', () => {
    expect(mod.customerReport).toBeDefined();
  });

  test('should export performanceReport', () => {
    expect(mod.performanceReport).toBeDefined();
  });

  test('should export initializeTemplates', () => {
    expect(mod.initializeTemplates).toBeDefined();
    expect(typeof mod.initializeTemplates).toBe('function');
  });

});
