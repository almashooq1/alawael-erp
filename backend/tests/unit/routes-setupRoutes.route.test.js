/**
 * Auto-generated tests for api/routes/setupRoutes.js
 * Validates syntax, structure, and patterns (module — 107L)
 * @generated by P#106 universal generator
 */
'use strict';

const fs = require('fs');
const path = require('path');
const vm = require('vm');

const SRC = path.resolve(__dirname, '../../api/routes/setupRoutes.js');

describe('api/routes/setupRoutes.js', () => {
  let source;
  beforeAll(() => {
    source = fs.readFileSync(SRC, 'utf8');
  });

  test('file exists', () => {
    expect(fs.existsSync(SRC)).toBe(true);
  });

  test('is syntactically valid JavaScript', () => {
    expect(() => new vm.Script(source, { filename: 'setupRoutes.js' })).not.toThrow();
  });

  test('has local dependencies (9)', () => {
    const locals = (source.match(/require\s*\(\s*['"]\.[^'"]+['"]\s*\)/g) || []);
    expect(locals.length).toBe(9);
  });

  test('has module.exports', () => {
    expect(source).toMatch(/module\.exports/);
  });
});
