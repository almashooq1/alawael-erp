/**
 * Auto-generated tests for hr/saudi-hr-service.js
 * Validates syntax, structure, and patterns (model — 962L)
 * @generated by P#106 universal generator
 */
'use strict';

const fs = require('fs');
const path = require('path');
const vm = require('vm');

const SRC = path.resolve(__dirname, '../../hr/saudi-hr-service.js');

describe('hr/saudi-hr-service.js', () => {
  let source;
  beforeAll(() => {
    source = fs.readFileSync(SRC, 'utf8');
  });

  test('file exists', () => {
    expect(fs.existsSync(SRC)).toBe(true);
  });

  test('is syntactically valid JavaScript', () => {
    expect(() => new vm.Script(source, { filename: 'saudi-hr-service.js' })).not.toThrow();
  });

  test('defines a Mongoose model', () => {
    expect(source).toMatch(/mongoose\.model\s*\(/);
  });

  test('creates a Schema', () => {
    expect(source).toMatch(/new\s+Schema\s*\(/);
  });

  test('uses timestamps', () => {
    expect(source).toMatch(/timestamps\s*:\s*true/);
  });

  test('has required fields', () => {
    expect(source).toMatch(/required\s*:\s*true/);
  });

  test('has npm dependencies (1)', () => {
    const npms = (source.match(/require\s*\(\s*['"](?![./])[^'"]+['"]\s*\)/g) || []);
    expect(npms.length).toBe(1);
  });

  test('has module.exports', () => {
    expect(source).toMatch(/module\.exports/);
  });

  test('exports SaudiHRService', () => {
    expect(source).toMatch(/SaudiHRService/);
  });

  test('exports Employee', () => {
    expect(source).toMatch(/Employee/);
  });

  test('exports LeaveRequest', () => {
    expect(source).toMatch(/LeaveRequest/);
  });

  test('exports Attendance', () => {
    expect(source).toMatch(/Attendance/);
  });

  test('exports Payroll', () => {
    expect(source).toMatch(/Payroll/);
  });
});
