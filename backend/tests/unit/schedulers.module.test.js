/**
 * Auto-generated tests for startup/schedulers.js
 * Validates syntax, structure, and patterns (module — 90L)
 * @generated by P#106 universal generator
 */
'use strict';

const fs = require('fs');
const path = require('path');
const vm = require('vm');

const SRC = path.resolve(__dirname, '../../startup/schedulers.js');

describe('startup/schedulers.js', () => {
  let source;
  beforeAll(() => {
    source = fs.readFileSync(SRC, 'utf8');
  });

  test('file exists', () => {
    expect(fs.existsSync(SRC)).toBe(true);
  });

  test('is syntactically valid JavaScript', () => {
    expect(() => new vm.Script(source, { filename: 'schedulers.js' })).not.toThrow();
  });

  test('has local dependencies (16)', () => {
    const locals = (source.match(/require\s*\(\s*['"]\.[^'"]+['"]\s*\)/g) || []);
    expect(locals.length).toBe(16);
  });

  test('has module.exports', () => {
    expect(source).toMatch(/module\.exports/);
  });

  test('exports setupSchedulers', () => {
    expect(source).toMatch(/setupSchedulers/);
  });
});
