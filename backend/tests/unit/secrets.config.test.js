/**
 * Auto-generated tests for config/secrets.js
 * @generated by _gen_config_tests.js
 */
'use strict';

/* ── Mocks ── */
jest.mock('../../config/secrets', () => new Proxy({}, {
  get: (_, p) => {
    if (p === '__esModule') return false;
    if (p === 'default') return {};
    return jest.fn(() => Promise.resolve({}));
  },
}));

/* ── Module under test (loaded after mocks) ── */
let mod;
beforeAll(() => {
  process.env.JWT_SECRET = 'test-jwt-secret';
  process.env.JWT_REFRESH_SECRET = 'test-jwt-refresh';
  process.env.ENCRYPTION_KEY = 'test-encryption-key-32chars!!!!!';
  process.env.NODE_ENV = 'test';
  mod = require('../../config/secrets');
});

describe('config/secrets', () => {
  test('should export a valid module', () => {
    expect(mod).toBeDefined();
    expect(typeof mod).toBe('object');
  });

  test('should export jwtSecret', () => {
    expect(mod.jwtSecret).toBeDefined();
  });

  test('should export jwtRefreshSecret', () => {
    expect(mod.jwtRefreshSecret).toBeDefined();
  });

  test('should export notificationJwtSecret', () => {
    expect(mod.notificationJwtSecret).toBeDefined();
  });

  test('should export encryptionKey', () => {
    expect(mod.encryptionKey).toBeDefined();
  });

  test('should export hmacKey', () => {
    expect(mod.hmacKey).toBeDefined();
  });

  test('should export backupEncryptionPassword', () => {
    expect(mod.backupEncryptionPassword).toBeDefined();
  });

  test('should export gpsEncryptionKey', () => {
    expect(mod.gpsEncryptionKey).toBeDefined();
  });

  test('should export sessionSecret', () => {
    expect(mod.sessionSecret).toBeDefined();
  });

  test('should export fcmServerKey', () => {
    expect(mod.fcmServerKey).toBeDefined();
  });

});
