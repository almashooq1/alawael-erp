/**
 * Auto-generated tests for rehabilitation-services/sleep-therapy-service.js
 * Validates syntax, structure, and patterns (service — 239L)
 * @generated by P#106 universal generator
 */
'use strict';

const fs = require('fs');
const path = require('path');
const vm = require('vm');

const SRC = path.resolve(__dirname, '../../rehabilitation-services/sleep-therapy-service.js');

describe('rehabilitation-services/sleep-therapy-service.js', () => {
  let source;
  beforeAll(() => {
    source = fs.readFileSync(SRC, 'utf8');
  });

  test('file exists', () => {
    expect(fs.existsSync(SRC)).toBe(true);
  });

  test('is syntactically valid JavaScript', () => {
    expect(() => new vm.Script(source, { filename: 'sleep-therapy-service.js' })).not.toThrow();
  });

  test('defines class SleepTherapyService', () => {
    expect(source).toMatch(/class\s+SleepTherapyService/);
  });

  test('has async methods (5)', () => {
    const matches = source.match(/async\s+\w+\s*\(/g) || [];
    expect(matches.length).toBeGreaterThanOrEqual(1);
  });

  test('has module.exports', () => {
    expect(source).toMatch(/module\.exports/);
  });

  test('exports SleepTherapyService', () => {
    expect(source).toMatch(/SleepTherapyService/);
  });
});
