/**
 * Auto-generated tests for rehabilitation-services/smart-iep-routes.js
 * Validates syntax, structure, and patterns (route — 547L)
 * @generated by P#106 universal generator
 */
'use strict';

const fs = require('fs');
const path = require('path');
const vm = require('vm');

const SRC = path.resolve(__dirname, '../../rehabilitation-services/smart-iep-routes.js');

describe('rehabilitation-services/smart-iep-routes.js', () => {
  let source;
  beforeAll(() => {
    source = fs.readFileSync(SRC, 'utf8');
  });

  test('file exists', () => {
    expect(fs.existsSync(SRC)).toBe(true);
  });

  test('is syntactically valid JavaScript', () => {
    expect(() => new vm.Script(source, { filename: 'smart-iep-routes.js' })).not.toThrow();
  });

  test('uses express Router', () => {
    expect(source).toMatch(/Router\s*\(/);
  });

  test('defines GET routes (12)', () => {
    const matches = source.match(/router\.get\s*\(/gi) || [];
    expect(matches.length).toBeGreaterThanOrEqual(1);
  });

  test('defines POST routes (7)', () => {
    const matches = source.match(/router\.post\s*\(/gi) || [];
    expect(matches.length).toBeGreaterThanOrEqual(1);
  });

  test('defines PATCH routes (2)', () => {
    const matches = source.match(/router\.patch\s*\(/gi) || [];
    expect(matches.length).toBeGreaterThanOrEqual(1);
  });

  test('exports the router', () => {
    expect(source).toMatch(/module\.exports/);
  });

  test('has npm dependencies (2)', () => {
    const npms = (source.match(/require\s*\(\s*['"](?![./])[^'"]+['"]\s*\)/g) || []);
    expect(npms.length).toBe(2);
  });

  test('has local dependencies (3)', () => {
    const locals = (source.match(/require\s*\(\s*['"]\.[^'"]+['"]\s*\)/g) || []);
    expect(locals.length).toBe(3);
  });

  test('has module.exports', () => {
    expect(source).toMatch(/module\.exports/);
  });
});
