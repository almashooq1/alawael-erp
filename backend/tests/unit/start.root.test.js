/**
 * Auto-generated tests for start.js (entry-point script)
 * Validates structure and syntax without executing side effects.
 * @generated by P#105
 */
'use strict';

const fs = require('fs');
const path = require('path');

const SRC = path.resolve(__dirname, '../../start.js');

describe('start.js', () => {
  test('file exists', () => {
    expect(fs.existsSync(SRC)).toBe(true);
  });

  test('is syntactically valid JavaScript', () => {
    const code = fs.readFileSync(SRC, 'utf8');
    const vm = require('vm');
    expect(() => new vm.Script(code, { filename: 'start.js' })).not.toThrow();
  });

  test('calls process.chdir', () => {
    const code = fs.readFileSync(SRC, 'utf8');
    expect(code).toMatch(/process\.chdir/);
  });

  test('requires server.js', () => {
    const code = fs.readFileSync(SRC, 'utf8');
    expect(code).toMatch(/require\(.+server/);
  });
});
