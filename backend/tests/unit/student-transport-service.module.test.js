/**
 * Auto-generated tests for vehicles/student-transport-service.js
 * Validates syntax, structure, and patterns (service — 775L)
 * @generated by P#106 universal generator
 */
'use strict';

const fs = require('fs');
const path = require('path');
const vm = require('vm');

const SRC = path.resolve(__dirname, '../../vehicles/student-transport-service.js');

describe('vehicles/student-transport-service.js', () => {
  let source;
  beforeAll(() => {
    source = fs.readFileSync(SRC, 'utf8');
  });

  test('file exists', () => {
    expect(fs.existsSync(SRC)).toBe(true);
  });

  test('is syntactically valid JavaScript', () => {
    expect(() => new vm.Script(source, { filename: 'student-transport-service.js' })).not.toThrow();
  });

  test('defines class StudentTransportService', () => {
    expect(source).toMatch(/class\s+StudentTransportService/);
  });

  test('has async methods (25)', () => {
    const matches = source.match(/async\s+\w+\s*\(/g) || [];
    expect(matches.length).toBeGreaterThanOrEqual(1);
  });

  test('has npm dependencies (2)', () => {
    const npms = (source.match(/require\s*\(\s*['"](?![./])[^'"]+['"]\s*\)/g) || []);
    expect(npms.length).toBe(2);
  });

  test('has local dependencies (1)', () => {
    const locals = (source.match(/require\s*\(\s*['"]\.[^'"]+['"]\s*\)/g) || []);
    expect(locals.length).toBe(1);
  });

  test('has module.exports', () => {
    expect(source).toMatch(/module\.exports/);
  });

  test('exports StudentTransportService', () => {
    expect(source).toMatch(/StudentTransportService/);
  });

  test('exports studentTransportService', () => {
    expect(source).toMatch(/studentTransportService/);
  });

  test('exports transportConfig', () => {
    expect(source).toMatch(/transportConfig/);
  });
});
