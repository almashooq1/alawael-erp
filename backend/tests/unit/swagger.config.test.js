/**
 * Auto-generated tests for config/swagger.config.js
 * @generated by _gen_config_tests.js
 */
'use strict';

/* ── Mocks ── */
jest.mock('../../utils/logger', () => ({
  info: jest.fn(),
  error: jest.fn(),
  warn: jest.fn(),
  debug: jest.fn(),
  verbose: jest.fn(),
  silly: jest.fn(),
  child: jest.fn(() => ({ info: jest.fn(), error: jest.fn(), warn: jest.fn(), debug: jest.fn() })),
  stream: { write: jest.fn() },
}));

jest.mock('swagger-jsdoc', () => jest.fn(() => ({
  openapi: '3.0.0',
  info: { title: 'Test', version: '1.0.0' },
  paths: {},
})), { virtual: true });

jest.mock('swagger-ui-express', () => ({
  serve: [jest.fn()],
  setup: jest.fn(() => jest.fn()),
}), { virtual: true });

jest.mock('../../config/swagger-ddd.config', () => new Proxy({}, {
  get: (_, p) => {
    if (p === '__esModule') return false;
    if (p === 'default') return {};
    return jest.fn(() => Promise.resolve({}));
  },
}));

/* ── Module under test ── */
const mod = require('../../config/swagger.config');

describe('config/swagger.config', () => {
  test('should export a valid module', () => {
    expect(mod).toBeDefined();
    expect(typeof mod).toBe('object');
  });

  test('should export swaggerSpec', () => {
    expect(mod.swaggerSpec).toBeDefined();
  });

  test('should export setupSwagger', () => {
    expect(mod.setupSwagger).toBeDefined();
    expect(typeof mod.setupSwagger).toBe('function');
  });

});
