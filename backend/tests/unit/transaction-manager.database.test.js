/**
 * Auto-generated tests for database/transaction-manager.js
 * Validates syntax, structure, and patterns (module — 270L)
 * @generated by P#106 universal generator
 */
'use strict';

const fs = require('fs');
const path = require('path');
const vm = require('vm');

const SRC = path.resolve(__dirname, '../../database/transaction-manager.js');

describe('database/transaction-manager.js', () => {
  let source;
  beforeAll(() => {
    source = fs.readFileSync(SRC, 'utf8');
  });

  test('file exists', () => {
    expect(fs.existsSync(SRC)).toBe(true);
  });

  test('is syntactically valid JavaScript', () => {
    expect(() => new vm.Script(source, { filename: 'transaction-manager.js' })).not.toThrow();
  });

  test('has npm dependencies (1)', () => {
    const npms = (source.match(/require\s*\(\s*['"](?![./])[^'"]+['"]\s*\)/g) || []);
    expect(npms.length).toBe(1);
  });

  test('has local dependencies (1)', () => {
    const locals = (source.match(/require\s*\(\s*['"]\.[^'"]+['"]\s*\)/g) || []);
    expect(locals.length).toBe(1);
  });

  test('has module.exports', () => {
    expect(source).toMatch(/module\.exports/);
  });

  test('exports TransactionManager', () => {
    expect(source).toMatch(/TransactionManager/);
  });

  test('exports transactionManager', () => {
    expect(source).toMatch(/transactionManager/);
  });

  test('exports withTransaction', () => {
    expect(source).toMatch(/withTransaction/);
  });
});
