/**
 * Auto-generated tests for config/validateEnv.js
 * @generated by _gen_config_tests.js
 */
'use strict';

/* ── Mocks ── */
jest.mock('../../utils/logger', () => ({
  info: jest.fn(),
  error: jest.fn(),
  warn: jest.fn(),
  debug: jest.fn(),
  verbose: jest.fn(),
  silly: jest.fn(),
  child: jest.fn(() => ({ info: jest.fn(), error: jest.fn(), warn: jest.fn(), debug: jest.fn() })),
  stream: { write: jest.fn() },
}));

jest.mock('joi', () => {
  const chain = () => new Proxy({}, { get: (_, p) => typeof p === 'string' ? jest.fn(() => chain()) : undefined });
  return {
    object: jest.fn(() => ({ keys: jest.fn(() => ({ validate: jest.fn(() => ({ value: {}, error: null })), unknown: jest.fn().mockReturnThis(), options: jest.fn().mockReturnThis() })), validate: jest.fn(() => ({ value: {}, error: null })), unknown: jest.fn().mockReturnThis(), options: jest.fn().mockReturnThis(), append: jest.fn().mockReturnThis() })),
    string: jest.fn(() => chain()),
    number: jest.fn(() => chain()),
    boolean: jest.fn(() => chain()),
    array: jest.fn(() => chain()),
    date: jest.fn(() => chain()),
    alternatives: jest.fn(() => chain()),
    any: jest.fn(() => chain()),
    ref: jest.fn(() => 'ref'),
  };
});

/* ── Module under test ── */
const mod = require('../../config/validateEnv');

describe('config/validateEnv', () => {
  test('should export a valid module', () => {
    expect(mod).toBeDefined();
    expect(typeof mod).toBe('object');
  });

  test('should have expected structure', () => {
    // NODE_ENV may exist on module
    // PORT may exist on module
    // MONGODB_URI may exist on module
    // USE_MOCK_DB may exist on module
    // JWT_SECRET may exist on module
    // JWT_REFRESH_SECRET may exist on module
    // JWT_EXPIRES_IN may exist on module
    // ENCRYPTION_KEY may exist on module
    // SESSION_SECRET may exist on module
    // REDIS_URL may exist on module
    // REDIS_HOST may exist on module
    // REDIS_PORT may exist on module
    // REDIS_PASSWORD may exist on module
    // REDIS_DB may exist on module
    // REDIS_ENABLED may exist on module
    const keys = Object.keys(mod);
    expect(keys.length).toBeGreaterThan(0);
  });

  test('module should be non-empty', () => {
    if (typeof mod === 'object' && mod !== null) {
      expect(Object.keys(mod).length).toBeGreaterThanOrEqual(0);
    } else {
      expect(mod).toBeTruthy();
    }
  });

});
