/**
 * Auto-generated tests for api/versionRouter.js
 * Validates syntax, structure, and patterns (route — 158L)
 * @generated by P#106 universal generator
 */
'use strict';

const fs = require('fs');
const path = require('path');
const vm = require('vm');

const SRC = path.resolve(__dirname, '../../api/versionRouter.js');

describe('api/versionRouter.js', () => {
  let source;
  beforeAll(() => {
    source = fs.readFileSync(SRC, 'utf8');
  });

  test('file exists', () => {
    expect(fs.existsSync(SRC)).toBe(true);
  });

  test('is syntactically valid JavaScript', () => {
    expect(() => new vm.Script(source, { filename: 'versionRouter.js' })).not.toThrow();
  });

  test('uses express Router', () => {
    expect(source).toMatch(/Router\s*\(/);
  });

  test('exports the router', () => {
    expect(source).toMatch(/module\.exports/);
  });

  test('has npm dependencies (1)', () => {
    const npms = (source.match(/require\s*\(\s*['"](?![./])[^'"]+['"]\s*\)/g) || []);
    expect(npms.length).toBe(1);
  });

  test('has local dependencies (1)', () => {
    const locals = (source.match(/require\s*\(\s*['"]\.[^'"]+['"]\s*\)/g) || []);
    expect(locals.length).toBe(1);
  });

  test('has module.exports', () => {
    expect(source).toMatch(/module\.exports/);
  });

  test('exports VERSION_CONFIG', () => {
    expect(source).toMatch(/VERSION_CONFIG/);
  });

  test('exports CURRENT_VERSION', () => {
    expect(source).toMatch(/CURRENT_VERSION/);
  });

  test('exports getVersionRouter', () => {
    expect(source).toMatch(/getVersionRouter/);
  });

  test('exports mountOnVersions', () => {
    expect(source).toMatch(/mountOnVersions/);
  });

  test('exports mountVersionRouters', () => {
    expect(source).toMatch(/mountVersionRouters/);
  });
});
