/**
 * Auto-generated tests for validations/volunteer-manager.validation.js
 * Validates syntax, structure, and patterns (validator — 154L)
 * @generated by P#106 universal generator
 */
'use strict';

const fs = require('fs');
const path = require('path');
const vm = require('vm');

const SRC = path.resolve(__dirname, '../../validations/volunteer-manager.validation.js');

describe('validations/volunteer-manager.validation.js', () => {
  let source;
  beforeAll(() => {
    source = fs.readFileSync(SRC, 'utf8');
  });

  test('file exists', () => {
    expect(fs.existsSync(SRC)).toBe(true);
  });

  test('is syntactically valid JavaScript', () => {
    expect(() => new vm.Script(source, { filename: 'volunteer-manager.validation.js' })).not.toThrow();
  });

  test('defines validation rules', () => {
    expect(source).toMatch(/(?:body|param|query)\s*\(/);
  });

  test('has npm dependencies (1)', () => {
    const npms = (source.match(/require\s*\(\s*['"](?![./])[^'"]+['"]\s*\)/g) || []);
    expect(npms.length).toBe(1);
  });

  test('has module.exports', () => {
    expect(source).toMatch(/module\.exports/);
  });

  test('exports createVolunteer', () => {
    expect(source).toMatch(/createVolunteer/);
  });

  test('exports updateVolunteer', () => {
    expect(source).toMatch(/updateVolunteer/);
  });

  test('exports createVolunteerShift', () => {
    expect(source).toMatch(/createVolunteerShift/);
  });

  test('exports updateVolunteerShift', () => {
    expect(source).toMatch(/updateVolunteerShift/);
  });

  test('exports createVolunteerSkill', () => {
    expect(source).toMatch(/createVolunteerSkill/);
  });
});
