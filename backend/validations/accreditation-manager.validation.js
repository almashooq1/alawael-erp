'use strict';
/**
 * AccreditationManager — Auto-Generated Validation Schemas
 * Generated by: scripts/gen-validation-from-model.js
 */

const { body, param } = require('express-validator');

const isObjectId = (value) => /^[a-f\d]{24}$/i.test(value);

/* ═══ AccreditationCycle: Create ═══ */
const createAccreditationCycle = [
  body('bodyCode').trim().notEmpty().withMessage('bodyCode مطلوب'),
  body('bodyName').trim().notEmpty().withMessage('bodyName مطلوب'),
  body('type').isIn(["jci","cbahi","carf","iso_9001","iso_45001","cap","magnet","planetree","national_body","specialty_specific","program_level","international"]).withMessage('type غير صالح'),
  body('status').optional().isIn(["preparing","self_assessment","submitted","under_review","survey_scheduled","survey_complete","awarded","conditional","denied","expired"]).withMessage('status غير صالح'),
  body('cycleStartDate').isISO8601().withMessage('cycleStartDate يجب أن يكون تاريخ ISO صالح'),
  body('cycleEndDate').optional().isISO8601().withMessage('cycleEndDate يجب أن يكون تاريخ ISO صالح'),
  body('surveyDate').optional().isISO8601().withMessage('surveyDate يجب أن يكون تاريخ ISO صالح'),
  body('awardedDate').optional().isISO8601().withMessage('awardedDate يجب أن يكون تاريخ ISO صالح'),
  body('expiryDate').optional().isISO8601().withMessage('expiryDate يجب أن يكون تاريخ ISO صالح'),
  body('leadCoordinatorId').optional().custom(isObjectId).withMessage('leadCoordinatorId غير صالح'),
  body('teamMembers').optional().isArray().withMessage('teamMembers يجب أن يكون مصفوفة'),
  body('overallScore').optional().isFloat().withMessage('overallScore يجب أن يكون رقماً'),
  body('certificateUrl').optional().trim().isString().withMessage('certificateUrl يجب أن يكون نصاً'),
  body('notes').optional().trim().isString().withMessage('notes يجب أن يكون نصاً'),
  body('createdBy').optional().custom(isObjectId).withMessage('createdBy غير صالح'),
];

/* ═══ AccreditationCycle: Update ═══ */
const updateAccreditationCycle = [
  body('bodyCode').optional().trim().isString().withMessage('bodyCode يجب أن يكون نصاً'),
  body('bodyName').optional().trim().isString().withMessage('bodyName يجب أن يكون نصاً'),
  body('type').optional().isIn(["jci","cbahi","carf","iso_9001","iso_45001","cap","magnet","planetree","national_body","specialty_specific","program_level","international"]).withMessage('type غير صالح'),
  body('status').optional().isIn(["preparing","self_assessment","submitted","under_review","survey_scheduled","survey_complete","awarded","conditional","denied","expired"]).withMessage('status غير صالح'),
  body('cycleStartDate').optional().isISO8601().withMessage('cycleStartDate يجب أن يكون تاريخ ISO صالح'),
  body('cycleEndDate').optional().isISO8601().withMessage('cycleEndDate يجب أن يكون تاريخ ISO صالح'),
  body('surveyDate').optional().isISO8601().withMessage('surveyDate يجب أن يكون تاريخ ISO صالح'),
  body('awardedDate').optional().isISO8601().withMessage('awardedDate يجب أن يكون تاريخ ISO صالح'),
  body('expiryDate').optional().isISO8601().withMessage('expiryDate يجب أن يكون تاريخ ISO صالح'),
  body('leadCoordinatorId').optional().custom(isObjectId).withMessage('leadCoordinatorId غير صالح'),
  body('teamMembers').optional().isArray().withMessage('teamMembers يجب أن يكون مصفوفة'),
  body('overallScore').optional().isFloat().withMessage('overallScore يجب أن يكون رقماً'),
  body('certificateUrl').optional().trim().isString().withMessage('certificateUrl يجب أن يكون نصاً'),
  body('notes').optional().trim().isString().withMessage('notes يجب أن يكون نصاً'),
  body('createdBy').optional().custom(isObjectId).withMessage('createdBy غير صالح'),
];

/* ═══ SelfAssessment: Create ═══ */
const createSelfAssessment = [
  body('cycleId').custom(isObjectId).withMessage('cycleId غير صالح'),
  body('chapter').isIn(["patient_safety","infection_control","medication_management","patient_rights","governance","staff_qualifications","facility_management","information_management","performance_improvement","education"]).withMessage('chapter غير صالح'),
  body('standardRef').optional().trim().isString().withMessage('standardRef يجب أن يكون نصاً'),
  body('assessorId').optional().custom(isObjectId).withMessage('assessorId غير صالح'),
  body('assessmentDate').optional().isISO8601().withMessage('assessmentDate يجب أن يكون تاريخ ISO صالح'),
  body('complianceScore').optional().isFloat({ min: 0, max: 100 }).withMessage('complianceScore يجب أن يكون بين 0 و 100'),
  body('finding').optional().isIn(["critical","major","minor","observation","opportunity","commendation","not_applicable","partially_met","not_met","fully_met"]).withMessage('finding غير صالح'),
  body('evidence').optional().trim().isString().withMessage('evidence يجب أن يكون نصاً'),
  body('gaps').optional().isArray().withMessage('gaps يجب أن يكون مصفوفة'),
  body('documentUrls').optional().isArray().withMessage('documentUrls يجب أن يكون مصفوفة'),
  body('createdBy').optional().custom(isObjectId).withMessage('createdBy غير صالح'),
];

/* ═══ SelfAssessment: Update ═══ */
const updateSelfAssessment = [
  body('cycleId').optional().custom(isObjectId).withMessage('cycleId غير صالح'),
  body('chapter').optional().isIn(["patient_safety","infection_control","medication_management","patient_rights","governance","staff_qualifications","facility_management","information_management","performance_improvement","education"]).withMessage('chapter غير صالح'),
  body('standardRef').optional().trim().isString().withMessage('standardRef يجب أن يكون نصاً'),
  body('assessorId').optional().custom(isObjectId).withMessage('assessorId غير صالح'),
  body('assessmentDate').optional().isISO8601().withMessage('assessmentDate يجب أن يكون تاريخ ISO صالح'),
  body('complianceScore').optional().isFloat({ min: 0, max: 100 }).withMessage('complianceScore يجب أن يكون بين 0 و 100'),
  body('finding').optional().isIn(["critical","major","minor","observation","opportunity","commendation","not_applicable","partially_met","not_met","fully_met"]).withMessage('finding غير صالح'),
  body('evidence').optional().trim().isString().withMessage('evidence يجب أن يكون نصاً'),
  body('gaps').optional().isArray().withMessage('gaps يجب أن يكون مصفوفة'),
  body('documentUrls').optional().isArray().withMessage('documentUrls يجب أن يكون مصفوفة'),
  body('createdBy').optional().custom(isObjectId).withMessage('createdBy غير صالح'),
];

/* ═══ SurveyFinding: Create ═══ */
const createSurveyFinding = [
  body('cycleId').custom(isObjectId).withMessage('cycleId غير صالح'),
  body('surveyType').isIn(["initial","triennial","focused","unannounced","follow_up","validation","extension","complaint_driven","mock_survey","desk_review"]).withMessage('surveyType غير صالح'),
  body('chapter').optional().isIn(["patient_safety","infection_control","medication_management","patient_rights","governance","staff_qualifications","facility_management","information_management","performance_improvement","education"]).withMessage('chapter غير صالح'),
  body('standardRef').optional().trim().isString().withMessage('standardRef يجب أن يكون نصاً'),
  body('severity').isIn(["critical","major","minor","observation","opportunity","commendation","not_applicable","partially_met","not_met","fully_met"]).withMessage('severity غير صالح'),
  body('description').trim().notEmpty().withMessage('description مطلوب'),
  body('surveyorNotes').optional().trim().isString().withMessage('surveyorNotes يجب أن يكون نصاً'),
  body('department').optional().trim().isString().withMessage('department يجب أن يكون نصاً'),
  body('responsibleId').optional().custom(isObjectId).withMessage('responsibleId غير صالح'),
  body('dueDate').optional().isISO8601().withMessage('dueDate يجب أن يكون تاريخ ISO صالح'),
  body('createdBy').optional().custom(isObjectId).withMessage('createdBy غير صالح'),
];

/* ═══ SurveyFinding: Update ═══ */
const updateSurveyFinding = [
  body('cycleId').optional().custom(isObjectId).withMessage('cycleId غير صالح'),
  body('surveyType').optional().isIn(["initial","triennial","focused","unannounced","follow_up","validation","extension","complaint_driven","mock_survey","desk_review"]).withMessage('surveyType غير صالح'),
  body('chapter').optional().isIn(["patient_safety","infection_control","medication_management","patient_rights","governance","staff_qualifications","facility_management","information_management","performance_improvement","education"]).withMessage('chapter غير صالح'),
  body('standardRef').optional().trim().isString().withMessage('standardRef يجب أن يكون نصاً'),
  body('severity').optional().isIn(["critical","major","minor","observation","opportunity","commendation","not_applicable","partially_met","not_met","fully_met"]).withMessage('severity غير صالح'),
  body('description').optional().trim().isString().withMessage('description يجب أن يكون نصاً'),
  body('surveyorNotes').optional().trim().isString().withMessage('surveyorNotes يجب أن يكون نصاً'),
  body('department').optional().trim().isString().withMessage('department يجب أن يكون نصاً'),
  body('responsibleId').optional().custom(isObjectId).withMessage('responsibleId غير صالح'),
  body('dueDate').optional().isISO8601().withMessage('dueDate يجب أن يكون تاريخ ISO صالح'),
  body('createdBy').optional().custom(isObjectId).withMessage('createdBy غير صالح'),
];

/* ═══ CorrectiveAction: Create ═══ */
const createCorrectiveAction = [
  body('findingId').custom(isObjectId).withMessage('findingId غير صالح'),
  body('cycleId').optional().custom(isObjectId).withMessage('cycleId غير صالح'),
  body('status').optional().isIn(["identified","planned","in_progress","implemented","verified","closed","overdue","escalated","deferred","cancelled"]).withMessage('status غير صالح'),
  body('description').trim().notEmpty().withMessage('description مطلوب'),
  body('rootCause').optional().trim().isString().withMessage('rootCause يجب أن يكون نصاً'),
  body('actionPlan').optional().trim().isString().withMessage('actionPlan يجب أن يكون نصاً'),
  body('responsibleId').optional().custom(isObjectId).withMessage('responsibleId غير صالح'),
  body('targetDate').optional().isISO8601().withMessage('targetDate يجب أن يكون تاريخ ISO صالح'),
  body('verifiedBy').optional().custom(isObjectId).withMessage('verifiedBy غير صالح'),
  body('evidence').optional().isArray().withMessage('evidence يجب أن يكون مصفوفة'),
  body('createdBy').optional().custom(isObjectId).withMessage('createdBy غير صالح'),
];

/* ═══ CorrectiveAction: Update ═══ */
const updateCorrectiveAction = [
  body('findingId').optional().custom(isObjectId).withMessage('findingId غير صالح'),
  body('cycleId').optional().custom(isObjectId).withMessage('cycleId غير صالح'),
  body('status').optional().isIn(["identified","planned","in_progress","implemented","verified","closed","overdue","escalated","deferred","cancelled"]).withMessage('status غير صالح'),
  body('description').optional().trim().isString().withMessage('description يجب أن يكون نصاً'),
  body('rootCause').optional().trim().isString().withMessage('rootCause يجب أن يكون نصاً'),
  body('actionPlan').optional().trim().isString().withMessage('actionPlan يجب أن يكون نصاً'),
  body('responsibleId').optional().custom(isObjectId).withMessage('responsibleId غير صالح'),
  body('targetDate').optional().isISO8601().withMessage('targetDate يجب أن يكون تاريخ ISO صالح'),
  body('verifiedBy').optional().custom(isObjectId).withMessage('verifiedBy غير صالح'),
  body('evidence').optional().isArray().withMessage('evidence يجب أن يكون مصفوفة'),
  body('createdBy').optional().custom(isObjectId).withMessage('createdBy غير صالح'),
];

module.exports = {
  createAccreditationCycle,
  updateAccreditationCycle,
  createSelfAssessment,
  updateSelfAssessment,
  createSurveyFinding,
  updateSurveyFinding,
  createCorrectiveAction,
  updateCorrectiveAction,
};