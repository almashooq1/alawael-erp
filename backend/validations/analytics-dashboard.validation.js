'use strict';
/**
 * AnalyticsDashboard — Auto-Generated Validation Schemas
 * Generated by: scripts/gen-validation-from-model.js
 */

const { body, param } = require('express-validator');

const isObjectId = (value) => /^[a-f\d]{24}$/i.test(value);

/* ═══ Widget: Create ═══ */
const createWidget = [
  body('widgetId').trim().notEmpty().withMessage('widgetId مطلوب'),
  body('title').trim().notEmpty().withMessage('title مطلوب'),
  body('titleAr').optional().trim().isString().withMessage('titleAr يجب أن يكون نصاً'),
  body('type').isIn(["kpi","chart","table","heatmap","gauge","funnel","map","timeline","treemap","scatter"]).withMessage('type غير صالح'),
  body('domain').optional().trim().isString().withMessage('domain يجب أن يكون نصاً'),
  body('dataSource').optional().trim().isString().withMessage('dataSource يجب أن يكون نصاً'),
  body('config').optional().isFloat().withMessage('config يجب أن يكون رقماً'),
  body('layout').optional().isFloat().withMessage('layout يجب أن يكون رقماً'),
  body('isActive').optional().isBoolean().withMessage('isActive يجب أن يكون قيمة منطقية'),
  body('createdBy').optional().custom(isObjectId).withMessage('createdBy غير صالح'),
];

/* ═══ Widget: Update ═══ */
const updateWidget = [
  body('widgetId').optional().trim().isString().withMessage('widgetId يجب أن يكون نصاً'),
  body('title').optional().trim().isString().withMessage('title يجب أن يكون نصاً'),
  body('titleAr').optional().trim().isString().withMessage('titleAr يجب أن يكون نصاً'),
  body('type').optional().isIn(["kpi","chart","table","heatmap","gauge","funnel","map","timeline","treemap","scatter"]).withMessage('type غير صالح'),
  body('domain').optional().trim().isString().withMessage('domain يجب أن يكون نصاً'),
  body('dataSource').optional().trim().isString().withMessage('dataSource يجب أن يكون نصاً'),
  body('config').optional().isFloat().withMessage('config يجب أن يكون رقماً'),
  body('layout').optional().isFloat().withMessage('layout يجب أن يكون رقماً'),
  body('isActive').optional().isBoolean().withMessage('isActive يجب أن يكون قيمة منطقية'),
  body('createdBy').optional().custom(isObjectId).withMessage('createdBy غير صالح'),
];

/* ═══ DashboardLayout: Create ═══ */
const createDashboardLayout = [
  body('layoutId').trim().notEmpty().withMessage('layoutId مطلوب'),
  body('name').trim().notEmpty().withMessage('name مطلوب'),
  body('nameAr').optional().trim().isString().withMessage('nameAr يجب أن يكون نصاً'),
  body('description').optional().trim().isString().withMessage('description يجب أن يكون نصاً'),
  body('widgets').optional().isArray().withMessage('widgets يجب أن يكون مصفوفة'),
  body('owner').optional().custom(isObjectId).withMessage('owner غير صالح'),
  body('isDefault').optional().isBoolean().withMessage('isDefault يجب أن يكون قيمة منطقية'),
  body('role').optional().trim().isString().withMessage('role يجب أن يكون نصاً'),
  body('refreshRate').optional().isFloat().withMessage('refreshRate يجب أن يكون رقماً'),
  body('theme').optional().isIn(["light","dark","clinical","executive"]).withMessage('theme غير صالح'),
  body('filters').optional().isArray().withMessage('filters يجب أن يكون مصفوفة'),
  body('isActive').optional().isBoolean().withMessage('isActive يجب أن يكون قيمة منطقية'),
];

/* ═══ DashboardLayout: Update ═══ */
const updateDashboardLayout = [
  body('layoutId').optional().trim().isString().withMessage('layoutId يجب أن يكون نصاً'),
  body('name').optional().trim().isString().withMessage('name يجب أن يكون نصاً'),
  body('nameAr').optional().trim().isString().withMessage('nameAr يجب أن يكون نصاً'),
  body('description').optional().trim().isString().withMessage('description يجب أن يكون نصاً'),
  body('widgets').optional().isArray().withMessage('widgets يجب أن يكون مصفوفة'),
  body('owner').optional().custom(isObjectId).withMessage('owner غير صالح'),
  body('isDefault').optional().isBoolean().withMessage('isDefault يجب أن يكون قيمة منطقية'),
  body('role').optional().trim().isString().withMessage('role يجب أن يكون نصاً'),
  body('refreshRate').optional().isFloat().withMessage('refreshRate يجب أن يكون رقماً'),
  body('theme').optional().isIn(["light","dark","clinical","executive"]).withMessage('theme غير صالح'),
  body('filters').optional().isArray().withMessage('filters يجب أن يكون مصفوفة'),
  body('isActive').optional().isBoolean().withMessage('isActive يجب أن يكون قيمة منطقية'),
];

/* ═══ AnalyticsSnapshot: Create ═══ */
const createAnalyticsSnapshot = [
  body('snapshotId').trim().notEmpty().withMessage('snapshotId مطلوب'),
  body('domain').trim().notEmpty().withMessage('domain مطلوب'),
  body('metricKey').trim().notEmpty().withMessage('metricKey مطلوب'),
  body('value').trim().notEmpty().withMessage('value مطلوب'),
  body('period').optional().isIn(["hourly","daily","weekly","monthly","quarterly","yearly"]).withMessage('period غير صالح'),
  body('timestamp').optional().isISO8601().withMessage('timestamp يجب أن يكون تاريخ ISO صالح'),
];

/* ═══ AnalyticsSnapshot: Update ═══ */
const updateAnalyticsSnapshot = [
  body('snapshotId').optional().trim().isString().withMessage('snapshotId يجب أن يكون نصاً'),
  body('domain').optional().trim().isString().withMessage('domain يجب أن يكون نصاً'),
  body('metricKey').optional().trim().isString().withMessage('metricKey يجب أن يكون نصاً'),
  body('value').optional().trim().isString().withMessage('value يجب أن يكون نصاً'),
  body('period').optional().isIn(["hourly","daily","weekly","monthly","quarterly","yearly"]).withMessage('period غير صالح'),
  body('timestamp').optional().isISO8601().withMessage('timestamp يجب أن يكون تاريخ ISO صالح'),
];

module.exports = {
  createWidget,
  updateWidget,
  createDashboardLayout,
  updateDashboardLayout,
  createAnalyticsSnapshot,
  updateAnalyticsSnapshot,
};