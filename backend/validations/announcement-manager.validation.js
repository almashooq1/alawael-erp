'use strict';
/**
 * AnnouncementManager — Auto-Generated Validation Schemas
 * Generated by: scripts/gen-validation-from-model.js
 */

const { body, param } = require('express-validator');

const isObjectId = (value) => /^[a-f\d]{24}$/i.test(value);

/* ═══ Announcement: Create ═══ */
const createAnnouncement = [
  body('announcementCode').trim().notEmpty().withMessage('announcementCode مطلوب'),
  body('title').trim().notEmpty().withMessage('title مطلوب'),
  body('titleAr').optional().trim().isString().withMessage('titleAr يجب أن يكون نصاً'),
  body('type').isIn(["news","policy_update","event","training","maintenance","emergency","recognition","vacancy","research","compliance","social","general"]).withMessage('type غير صالح'),
  body('status').optional().isIn(["draft","pending_approval","approved","published","pinned","archived","expired","recalled","rejected","scheduled"]).withMessage('status غير صالح'),
  body('categoryId').optional().custom(isObjectId).withMessage('categoryId غير صالح'),
  body('bulletinId').optional().custom(isObjectId).withMessage('bulletinId غير صالح'),
  body('content').trim().notEmpty().withMessage('content مطلوب'),
  body('contentAr').optional().trim().isString().withMessage('contentAr يجب أن يكون نصاً'),
  body('summary').optional().trim().isString().withMessage('summary يجب أن يكون نصاً'),
  body('authorId').custom(isObjectId).withMessage('authorId غير صالح'),
  body('audienceScope').optional().isIn(["all_staff","department","role","position_level","building","clinical_team","specific_users","patients","families","external"]).withMessage('audienceScope غير صالح'),
  body('targetDepartments').optional().isArray().withMessage('targetDepartments يجب أن يكون مصفوفة'),
  body('targetRoles').optional().isArray().withMessage('targetRoles يجب أن يكون مصفوفة'),
  body('targetUsers').optional().isArray().withMessage('targetUsers يجب أن يكون مصفوفة'),
  body('priority').optional().isIn(["low","normal","high","urgent","pinned","banner"]).withMessage('priority غير صالح'),
  body('publishDate').optional().isISO8601().withMessage('publishDate يجب أن يكون تاريخ ISO صالح'),
  body('expiryDate').optional().isISO8601().withMessage('expiryDate يجب أن يكون تاريخ ISO صالح'),
  body('attachments').optional().isArray().withMessage('attachments يجب أن يكون مصفوفة'),
  body('acknowledgedCount').optional().isFloat().withMessage('acknowledgedCount يجب أن يكون رقماً'),
  body('requiresAcknowledgement').optional().isBoolean().withMessage('requiresAcknowledgement يجب أن يكون قيمة منطقية'),
];

/* ═══ Announcement: Update ═══ */
const updateAnnouncement = [
  body('announcementCode').optional().trim().isString().withMessage('announcementCode يجب أن يكون نصاً'),
  body('title').optional().trim().isString().withMessage('title يجب أن يكون نصاً'),
  body('titleAr').optional().trim().isString().withMessage('titleAr يجب أن يكون نصاً'),
  body('type').optional().isIn(["news","policy_update","event","training","maintenance","emergency","recognition","vacancy","research","compliance","social","general"]).withMessage('type غير صالح'),
  body('status').optional().isIn(["draft","pending_approval","approved","published","pinned","archived","expired","recalled","rejected","scheduled"]).withMessage('status غير صالح'),
  body('categoryId').optional().custom(isObjectId).withMessage('categoryId غير صالح'),
  body('bulletinId').optional().custom(isObjectId).withMessage('bulletinId غير صالح'),
  body('content').optional().trim().isString().withMessage('content يجب أن يكون نصاً'),
  body('contentAr').optional().trim().isString().withMessage('contentAr يجب أن يكون نصاً'),
  body('summary').optional().trim().isString().withMessage('summary يجب أن يكون نصاً'),
  body('authorId').optional().custom(isObjectId).withMessage('authorId غير صالح'),
  body('audienceScope').optional().isIn(["all_staff","department","role","position_level","building","clinical_team","specific_users","patients","families","external"]).withMessage('audienceScope غير صالح'),
  body('targetDepartments').optional().isArray().withMessage('targetDepartments يجب أن يكون مصفوفة'),
  body('targetRoles').optional().isArray().withMessage('targetRoles يجب أن يكون مصفوفة'),
  body('targetUsers').optional().isArray().withMessage('targetUsers يجب أن يكون مصفوفة'),
  body('priority').optional().isIn(["low","normal","high","urgent","pinned","banner"]).withMessage('priority غير صالح'),
  body('publishDate').optional().isISO8601().withMessage('publishDate يجب أن يكون تاريخ ISO صالح'),
  body('expiryDate').optional().isISO8601().withMessage('expiryDate يجب أن يكون تاريخ ISO صالح'),
  body('attachments').optional().isArray().withMessage('attachments يجب أن يكون مصفوفة'),
  body('acknowledgedCount').optional().isFloat().withMessage('acknowledgedCount يجب أن يكون رقماً'),
  body('requiresAcknowledgement').optional().isBoolean().withMessage('requiresAcknowledgement يجب أن يكون قيمة منطقية'),
];

/* ═══ BulletinBoard: Create ═══ */
const createBulletinBoard = [
  body('code').trim().notEmpty().withMessage('code مطلوب'),
  body('name').trim().notEmpty().withMessage('name مطلوب'),
  body('nameAr').optional().trim().isString().withMessage('nameAr يجب أن يكون نصاً'),
  body('type').isIn(["general","clinical_updates","policy","training","events","recognition","safety","research","community","technology"]).withMessage('type غير صالح'),
  body('description').optional().trim().isString().withMessage('description يجب أن يكون نصاً'),
  body('departmentId').optional().custom(isObjectId).withMessage('departmentId غير صالح'),
  body('moderators').optional().isArray().withMessage('moderators يجب أن يكون مصفوفة'),
  body('isPublic').optional().isBoolean().withMessage('isPublic يجب أن يكون قيمة منطقية'),
  body('isActive').optional().isBoolean().withMessage('isActive يجب أن يكون قيمة منطقية'),
  body('postCount').optional().isFloat().withMessage('postCount يجب أن يكون رقماً'),
];

/* ═══ BulletinBoard: Update ═══ */
const updateBulletinBoard = [
  body('code').optional().trim().isString().withMessage('code يجب أن يكون نصاً'),
  body('name').optional().trim().isString().withMessage('name يجب أن يكون نصاً'),
  body('nameAr').optional().trim().isString().withMessage('nameAr يجب أن يكون نصاً'),
  body('type').optional().isIn(["general","clinical_updates","policy","training","events","recognition","safety","research","community","technology"]).withMessage('type غير صالح'),
  body('description').optional().trim().isString().withMessage('description يجب أن يكون نصاً'),
  body('departmentId').optional().custom(isObjectId).withMessage('departmentId غير صالح'),
  body('moderators').optional().isArray().withMessage('moderators يجب أن يكون مصفوفة'),
  body('isPublic').optional().isBoolean().withMessage('isPublic يجب أن يكون قيمة منطقية'),
  body('isActive').optional().isBoolean().withMessage('isActive يجب أن يكون قيمة منطقية'),
  body('postCount').optional().isFloat().withMessage('postCount يجب أن يكون رقماً'),
];

/* ═══ AnnouncementCategory: Create ═══ */
const createAnnouncementCategory = [
  body('code').trim().notEmpty().withMessage('code مطلوب'),
  body('name').trim().notEmpty().withMessage('name مطلوب'),
  body('nameAr').optional().trim().isString().withMessage('nameAr يجب أن يكون نصاً'),
  body('icon').optional().trim().isString().withMessage('icon يجب أن يكون نصاً'),
  body('color').optional().trim().isString().withMessage('color يجب أن يكون نصاً'),
  body('parentId').optional().custom(isObjectId).withMessage('parentId غير صالح'),
  body('isActive').optional().isBoolean().withMessage('isActive يجب أن يكون قيمة منطقية'),
  body('sortOrder').optional().isFloat().withMessage('sortOrder يجب أن يكون رقماً'),
];

/* ═══ AnnouncementCategory: Update ═══ */
const updateAnnouncementCategory = [
  body('code').optional().trim().isString().withMessage('code يجب أن يكون نصاً'),
  body('name').optional().trim().isString().withMessage('name يجب أن يكون نصاً'),
  body('nameAr').optional().trim().isString().withMessage('nameAr يجب أن يكون نصاً'),
  body('icon').optional().trim().isString().withMessage('icon يجب أن يكون نصاً'),
  body('color').optional().trim().isString().withMessage('color يجب أن يكون نصاً'),
  body('parentId').optional().custom(isObjectId).withMessage('parentId غير صالح'),
  body('isActive').optional().isBoolean().withMessage('isActive يجب أن يكون قيمة منطقية'),
  body('sortOrder').optional().isFloat().withMessage('sortOrder يجب أن يكون رقماً'),
];

/* ═══ AnnouncementReaction: Create ═══ */
const createAnnouncementReaction = [
  body('announcementId').custom(isObjectId).withMessage('announcementId غير صالح'),
  body('userId').custom(isObjectId).withMessage('userId غير صالح'),
  body('type').isIn(["acknowledged","liked","helpful","important","question","disagree","celebrate","support"]).withMessage('type غير صالح'),
  body('comment').optional().trim().isString().withMessage('comment يجب أن يكون نصاً'),
  body('reactedAt').optional().isISO8601().withMessage('reactedAt يجب أن يكون تاريخ ISO صالح'),
];

/* ═══ AnnouncementReaction: Update ═══ */
const updateAnnouncementReaction = [
  body('announcementId').optional().custom(isObjectId).withMessage('announcementId غير صالح'),
  body('userId').optional().custom(isObjectId).withMessage('userId غير صالح'),
  body('type').optional().isIn(["acknowledged","liked","helpful","important","question","disagree","celebrate","support"]).withMessage('type غير صالح'),
  body('comment').optional().trim().isString().withMessage('comment يجب أن يكون نصاً'),
  body('reactedAt').optional().isISO8601().withMessage('reactedAt يجب أن يكون تاريخ ISO صالح'),
];

module.exports = {
  createAnnouncement,
  updateAnnouncement,
  createBulletinBoard,
  updateBulletinBoard,
  createAnnouncementCategory,
  updateAnnouncementCategory,
  createAnnouncementReaction,
  updateAnnouncementReaction,
};