'use strict';
/**
 * BusinessIntelligence — Auto-Generated Validation Schemas
 * Generated by: scripts/gen-validation-from-model.js
 */

const { body, param } = require('express-validator');

const isObjectId = (value) => /^[a-f\d]{24}$/i.test(value);

/* ═══ BIReport: Create ═══ */
const createBIReport = [
  body('reportId').trim().notEmpty().withMessage('reportId مطلوب'),
  body('name').trim().notEmpty().withMessage('name مطلوب'),
  body('nameAr').optional().trim().isString().withMessage('nameAr يجب أن يكون نصاً'),
  body('description').optional().trim().isString().withMessage('description يجب أن يكون نصاً'),
  body('category').isIn(["clinical","operational","financial","compliance","executive","research","quality","population","performance","custom"]).withMessage('category غير صالح'),
  body('domain').optional().trim().isString().withMessage('domain يجب أن يكون نصاً'),
  body('query').optional().trim().isString().withMessage('query يجب أن يكون نصاً'),
  body('schedule').optional().isBoolean().withMessage('schedule يجب أن يكون قيمة منطقية'),
  body('owner').optional().custom(isObjectId).withMessage('owner غير صالح'),
  body('isActive').optional().isBoolean().withMessage('isActive يجب أن يكون قيمة منطقية'),
];

/* ═══ BIReport: Update ═══ */
const updateBIReport = [
  body('reportId').optional().trim().isString().withMessage('reportId يجب أن يكون نصاً'),
  body('name').optional().trim().isString().withMessage('name يجب أن يكون نصاً'),
  body('nameAr').optional().trim().isString().withMessage('nameAr يجب أن يكون نصاً'),
  body('description').optional().trim().isString().withMessage('description يجب أن يكون نصاً'),
  body('category').optional().isIn(["clinical","operational","financial","compliance","executive","research","quality","population","performance","custom"]).withMessage('category غير صالح'),
  body('domain').optional().trim().isString().withMessage('domain يجب أن يكون نصاً'),
  body('query').optional().trim().isString().withMessage('query يجب أن يكون نصاً'),
  body('schedule').optional().isBoolean().withMessage('schedule يجب أن يكون قيمة منطقية'),
  body('owner').optional().custom(isObjectId).withMessage('owner غير صالح'),
  body('isActive').optional().isBoolean().withMessage('isActive يجب أن يكون قيمة منطقية'),
];

/* ═══ Scorecard: Create ═══ */
const createScorecard = [
  body('scorecardId').trim().notEmpty().withMessage('scorecardId مطلوب'),
  body('name').trim().notEmpty().withMessage('name مطلوب'),
  body('nameAr').optional().trim().isString().withMessage('nameAr يجب أن يكون نصاً'),
  body('perspective').isIn(["clinical","operational","financial","learning","patient","stakeholder"]).withMessage('perspective غير صالح'),
  body('objectiveId').optional().trim().isString().withMessage('objectiveId يجب أن يكون نصاً'),
  body('name').optional().trim().isString().withMessage('name يجب أن يكون نصاً'),
  body('nameAr').optional().trim().isString().withMessage('nameAr يجب أن يكون نصاً'),
  body('kpiId').optional().trim().isString().withMessage('kpiId يجب أن يكون نصاً'),
  body('target').optional().isFloat().withMessage('target يجب أن يكون رقماً'),
  body('actual').optional().isFloat().withMessage('actual يجب أن يكون رقماً'),
  body('weight').optional().isFloat().withMessage('weight يجب أن يكون رقماً'),
  body('status').optional().isIn(["on-track","at-risk","behind","exceeded"]).withMessage('status غير صالح'),
  body('trend').optional().isIn(["improving","stable","declining"]).withMessage('trend غير صالح'),
  body('period').optional().isIn(["monthly","quarterly","yearly"]).withMessage('period غير صالح'),
  body('overallScore').optional().isFloat({ min: 0, max: 100 }).withMessage('overallScore يجب أن يكون بين 0 و 100'),
  body('lastUpdated').optional().isISO8601().withMessage('lastUpdated يجب أن يكون تاريخ ISO صالح'),
  body('isActive').optional().isBoolean().withMessage('isActive يجب أن يكون قيمة منطقية'),
];

/* ═══ Scorecard: Update ═══ */
const updateScorecard = [
  body('scorecardId').optional().trim().isString().withMessage('scorecardId يجب أن يكون نصاً'),
  body('name').optional().trim().isString().withMessage('name يجب أن يكون نصاً'),
  body('nameAr').optional().trim().isString().withMessage('nameAr يجب أن يكون نصاً'),
  body('perspective').optional().isIn(["clinical","operational","financial","learning","patient","stakeholder"]).withMessage('perspective غير صالح'),
  body('objectiveId').optional().trim().isString().withMessage('objectiveId يجب أن يكون نصاً'),
  body('name').optional().trim().isString().withMessage('name يجب أن يكون نصاً'),
  body('nameAr').optional().trim().isString().withMessage('nameAr يجب أن يكون نصاً'),
  body('kpiId').optional().trim().isString().withMessage('kpiId يجب أن يكون نصاً'),
  body('target').optional().isFloat().withMessage('target يجب أن يكون رقماً'),
  body('actual').optional().isFloat().withMessage('actual يجب أن يكون رقماً'),
  body('weight').optional().isFloat().withMessage('weight يجب أن يكون رقماً'),
  body('status').optional().isIn(["on-track","at-risk","behind","exceeded"]).withMessage('status غير صالح'),
  body('trend').optional().isIn(["improving","stable","declining"]).withMessage('trend غير صالح'),
  body('period').optional().isIn(["monthly","quarterly","yearly"]).withMessage('period غير صالح'),
  body('overallScore').optional().isFloat({ min: 0, max: 100 }).withMessage('overallScore يجب أن يكون بين 0 و 100'),
  body('lastUpdated').optional().isISO8601().withMessage('lastUpdated يجب أن يكون تاريخ ISO صالح'),
  body('isActive').optional().isBoolean().withMessage('isActive يجب أن يكون قيمة منطقية'),
];

/* ═══ Benchmark: Create ═══ */
const createBenchmark = [
  body('benchmarkId').trim().notEmpty().withMessage('benchmarkId مطلوب'),
  body('name').trim().notEmpty().withMessage('name مطلوب'),
  body('nameAr').optional().trim().isString().withMessage('nameAr يجب أن يكون نصاً'),
  body('domain').trim().notEmpty().withMessage('domain مطلوب'),
  body('metricKey').trim().notEmpty().withMessage('metricKey مطلوب'),
  body('internalValue').optional().isFloat().withMessage('internalValue يجب أن يكون رقماً'),
  body('benchmarkValue').optional().isFloat().withMessage('benchmarkValue يجب أن يكون رقماً'),
  body('source').optional().trim().isString().withMessage('source يجب أن يكون نصاً'),
  body('percentile').optional().isFloat({ min: 0, max: 100 }).withMessage('percentile يجب أن يكون بين 0 و 100'),
  body('gap').optional().isFloat().withMessage('gap يجب أن يكون رقماً'),
  body('trend').optional().isIn(["improving","stable","declining"]).withMessage('trend غير صالح'),
  body('period').optional().trim().isString().withMessage('period يجب أن يكون نصاً'),
  body('lastUpdated').optional().isISO8601().withMessage('lastUpdated يجب أن يكون تاريخ ISO صالح'),
];

/* ═══ Benchmark: Update ═══ */
const updateBenchmark = [
  body('benchmarkId').optional().trim().isString().withMessage('benchmarkId يجب أن يكون نصاً'),
  body('name').optional().trim().isString().withMessage('name يجب أن يكون نصاً'),
  body('nameAr').optional().trim().isString().withMessage('nameAr يجب أن يكون نصاً'),
  body('domain').optional().trim().isString().withMessage('domain يجب أن يكون نصاً'),
  body('metricKey').optional().trim().isString().withMessage('metricKey يجب أن يكون نصاً'),
  body('internalValue').optional().isFloat().withMessage('internalValue يجب أن يكون رقماً'),
  body('benchmarkValue').optional().isFloat().withMessage('benchmarkValue يجب أن يكون رقماً'),
  body('source').optional().trim().isString().withMessage('source يجب أن يكون نصاً'),
  body('percentile').optional().isFloat({ min: 0, max: 100 }).withMessage('percentile يجب أن يكون بين 0 و 100'),
  body('gap').optional().isFloat().withMessage('gap يجب أن يكون رقماً'),
  body('trend').optional().isIn(["improving","stable","declining"]).withMessage('trend غير صالح'),
  body('period').optional().trim().isString().withMessage('period يجب أن يكون نصاً'),
  body('lastUpdated').optional().isISO8601().withMessage('lastUpdated يجب أن يكون تاريخ ISO صالح'),
];

module.exports = {
  createBIReport,
  updateBIReport,
  createScorecard,
  updateScorecard,
  createBenchmark,
  updateBenchmark,
};