'use strict';
/**
 * ClinicalTrial — Auto-Generated Validation Schemas
 * Generated by: scripts/gen-validation-from-model.js
 */

const { body, param } = require('express-validator');

const isObjectId = (value) => /^[a-f\d]{24}$/i.test(value);

/* ═══ ClinicalTrial: Create ═══ */
const createClinicalTrial = [
  body('trialId').trim().notEmpty().withMessage('trialId مطلوب'),
  body('protocolId').optional().custom(isObjectId).withMessage('protocolId غير صالح'),
  body('title').trim().notEmpty().withMessage('title مطلوب'),
  body('type').isIn(["interventional","observational","expanded_access","registry","prevention","diagnostic","treatment","supportive_care","screening","health_services","basic_science","device"]).withMessage('type غير صالح'),
  body('status').optional().isIn(["planning","not_yet_recruiting","recruiting","active_not_recruiting","completed","suspended","terminated","withdrawn","enrolling_by_invitation","approved_for_marketing"]).withMessage('status غير صالح'),
  body('phase').optional().trim().isString().withMessage('phase يجب أن يكون نصاً'),
  body('sponsor').optional().trim().isString().withMessage('sponsor يجب أن يكون نصاً'),
  body('registrationNumber').optional().trim().isString().withMessage('registrationNumber يجب أن يكون نصاً'),
  body('targetEnrollment').optional().isFloat().withMessage('targetEnrollment يجب أن يكون رقماً'),
  body('actualEnrollment').optional().isFloat().withMessage('actualEnrollment يجب أن يكون رقماً'),
  body('sites').optional().isArray().withMessage('sites يجب أن يكون مصفوفة'),
  body('arms').optional().isArray().withMessage('arms يجب أن يكون مصفوفة'),
  body('startDate').optional().isISO8601().withMessage('startDate يجب أن يكون تاريخ ISO صالح'),
  body('endDate').optional().isISO8601().withMessage('endDate يجب أن يكون تاريخ ISO صالح'),
  body('randomization').optional().isIn(["simple","block","stratified","cluster","adaptive","minimization","biased_coin","urn","response_adaptive","bayesian_adaptive","covariate_adaptive","sequential"]).withMessage('randomization غير صالح'),
];

/* ═══ ClinicalTrial: Update ═══ */
const updateClinicalTrial = [
  body('trialId').optional().trim().isString().withMessage('trialId يجب أن يكون نصاً'),
  body('protocolId').optional().custom(isObjectId).withMessage('protocolId غير صالح'),
  body('title').optional().trim().isString().withMessage('title يجب أن يكون نصاً'),
  body('type').optional().isIn(["interventional","observational","expanded_access","registry","prevention","diagnostic","treatment","supportive_care","screening","health_services","basic_science","device"]).withMessage('type غير صالح'),
  body('status').optional().isIn(["planning","not_yet_recruiting","recruiting","active_not_recruiting","completed","suspended","terminated","withdrawn","enrolling_by_invitation","approved_for_marketing"]).withMessage('status غير صالح'),
  body('phase').optional().trim().isString().withMessage('phase يجب أن يكون نصاً'),
  body('sponsor').optional().trim().isString().withMessage('sponsor يجب أن يكون نصاً'),
  body('registrationNumber').optional().trim().isString().withMessage('registrationNumber يجب أن يكون نصاً'),
  body('targetEnrollment').optional().isFloat().withMessage('targetEnrollment يجب أن يكون رقماً'),
  body('actualEnrollment').optional().isFloat().withMessage('actualEnrollment يجب أن يكون رقماً'),
  body('sites').optional().isArray().withMessage('sites يجب أن يكون مصفوفة'),
  body('arms').optional().isArray().withMessage('arms يجب أن يكون مصفوفة'),
  body('startDate').optional().isISO8601().withMessage('startDate يجب أن يكون تاريخ ISO صالح'),
  body('endDate').optional().isISO8601().withMessage('endDate يجب أن يكون تاريخ ISO صالح'),
  body('randomization').optional().isIn(["simple","block","stratified","cluster","adaptive","minimization","biased_coin","urn","response_adaptive","bayesian_adaptive","covariate_adaptive","sequential"]).withMessage('randomization غير صالح'),
];

/* ═══ TrialParticipant: Create ═══ */
const createTrialParticipant = [
  body('participantId').trim().notEmpty().withMessage('participantId مطلوب'),
  body('trialId').custom(isObjectId).withMessage('trialId غير صالح'),
  body('beneficiaryId').optional().custom(isObjectId).withMessage('beneficiaryId غير صالح'),
  body('status').optional().isIn(["screened","eligible","consented","enrolled","randomized","active","completed","withdrawn","lost_to_followup","screen_failed"]).withMessage('status غير صالح'),
  body('arm').optional().trim().isString().withMessage('arm يجب أن يكون نصاً'),
  body('consentDate').optional().isISO8601().withMessage('consentDate يجب أن يكون تاريخ ISO صالح'),
  body('enrollmentDate').optional().isISO8601().withMessage('enrollmentDate يجب أن يكون تاريخ ISO صالح'),
  body('completionDate').optional().isISO8601().withMessage('completionDate يجب أن يكون تاريخ ISO صالح'),
  body('withdrawalReason').optional().trim().isString().withMessage('withdrawalReason يجب أن يكون نصاً'),
  body('visits').optional().isArray().withMessage('visits يجب أن يكون مصفوفة'),
];

/* ═══ TrialParticipant: Update ═══ */
const updateTrialParticipant = [
  body('participantId').optional().trim().isString().withMessage('participantId يجب أن يكون نصاً'),
  body('trialId').optional().custom(isObjectId).withMessage('trialId غير صالح'),
  body('beneficiaryId').optional().custom(isObjectId).withMessage('beneficiaryId غير صالح'),
  body('status').optional().isIn(["screened","eligible","consented","enrolled","randomized","active","completed","withdrawn","lost_to_followup","screen_failed"]).withMessage('status غير صالح'),
  body('arm').optional().trim().isString().withMessage('arm يجب أن يكون نصاً'),
  body('consentDate').optional().isISO8601().withMessage('consentDate يجب أن يكون تاريخ ISO صالح'),
  body('enrollmentDate').optional().isISO8601().withMessage('enrollmentDate يجب أن يكون تاريخ ISO صالح'),
  body('completionDate').optional().isISO8601().withMessage('completionDate يجب أن يكون تاريخ ISO صالح'),
  body('withdrawalReason').optional().trim().isString().withMessage('withdrawalReason يجب أن يكون نصاً'),
  body('visits').optional().isArray().withMessage('visits يجب أن يكون مصفوفة'),
];

/* ═══ MonitoringEvent: Create ═══ */
const createMonitoringEvent = [
  body('eventId').trim().notEmpty().withMessage('eventId مطلوب'),
  body('trialId').custom(isObjectId).withMessage('trialId غير صالح'),
  body('type').isIn(["site_visit","remote_monitoring","central_monitoring","risk_based","triggered_visit","interim_analysis","data_review","safety_review","protocol_deviation","adverse_event"]).withMessage('type غير صالح'),
  body('date').optional().isISO8601().withMessage('date يجب أن يكون تاريخ ISO صالح'),
  body('monitor').optional().trim().isString().withMessage('monitor يجب أن يكون نصاً'),
  body('findings').optional().isArray().withMessage('findings يجب أن يكون مصفوفة'),
  body('actionItems').optional().isArray().withMessage('actionItems يجب أن يكون مصفوفة'),
  body('outcome').optional().trim().isString().withMessage('outcome يجب أن يكون نصاً'),
];

/* ═══ MonitoringEvent: Update ═══ */
const updateMonitoringEvent = [
  body('eventId').optional().trim().isString().withMessage('eventId يجب أن يكون نصاً'),
  body('trialId').optional().custom(isObjectId).withMessage('trialId غير صالح'),
  body('type').optional().isIn(["site_visit","remote_monitoring","central_monitoring","risk_based","triggered_visit","interim_analysis","data_review","safety_review","protocol_deviation","adverse_event"]).withMessage('type غير صالح'),
  body('date').optional().isISO8601().withMessage('date يجب أن يكون تاريخ ISO صالح'),
  body('monitor').optional().trim().isString().withMessage('monitor يجب أن يكون نصاً'),
  body('findings').optional().isArray().withMessage('findings يجب أن يكون مصفوفة'),
  body('actionItems').optional().isArray().withMessage('actionItems يجب أن يكون مصفوفة'),
  body('outcome').optional().trim().isString().withMessage('outcome يجب أن يكون نصاً'),
];

/* ═══ AdverseEvent: Create ═══ */
const createAdverseEvent = [
  body('aeId').trim().notEmpty().withMessage('aeId مطلوب'),
  body('trialId').custom(isObjectId).withMessage('trialId غير صالح'),
  body('participantId').optional().custom(isObjectId).withMessage('participantId غير صالح'),
  body('grade').isIn(["grade_1_mild","grade_2_moderate","grade_3_severe","grade_4_life_threatening","grade_5_death","not_applicable","expected","unexpected","related","unrelated"]).withMessage('grade غير صالح'),
  body('description').trim().notEmpty().withMessage('description مطلوب'),
  body('onsetDate').optional().isISO8601().withMessage('onsetDate يجب أن يكون تاريخ ISO صالح'),
  body('resolvedDate').optional().isISO8601().withMessage('resolvedDate يجب أن يكون تاريخ ISO صالح'),
  body('serious').optional().isBoolean().withMessage('serious يجب أن يكون قيمة منطقية'),
  body('causality').optional().trim().isString().withMessage('causality يجب أن يكون نصاً'),
  body('action').optional().trim().isString().withMessage('action يجب أن يكون نصاً'),
  body('outcome').optional().trim().isString().withMessage('outcome يجب أن يكون نصاً'),
  body('reportedBy').optional().custom(isObjectId).withMessage('reportedBy غير صالح'),
  body('reportedAt').optional().isISO8601().withMessage('reportedAt يجب أن يكون تاريخ ISO صالح'),
];

/* ═══ AdverseEvent: Update ═══ */
const updateAdverseEvent = [
  body('aeId').optional().trim().isString().withMessage('aeId يجب أن يكون نصاً'),
  body('trialId').optional().custom(isObjectId).withMessage('trialId غير صالح'),
  body('participantId').optional().custom(isObjectId).withMessage('participantId غير صالح'),
  body('grade').optional().isIn(["grade_1_mild","grade_2_moderate","grade_3_severe","grade_4_life_threatening","grade_5_death","not_applicable","expected","unexpected","related","unrelated"]).withMessage('grade غير صالح'),
  body('description').optional().trim().isString().withMessage('description يجب أن يكون نصاً'),
  body('onsetDate').optional().isISO8601().withMessage('onsetDate يجب أن يكون تاريخ ISO صالح'),
  body('resolvedDate').optional().isISO8601().withMessage('resolvedDate يجب أن يكون تاريخ ISO صالح'),
  body('serious').optional().isBoolean().withMessage('serious يجب أن يكون قيمة منطقية'),
  body('causality').optional().trim().isString().withMessage('causality يجب أن يكون نصاً'),
  body('action').optional().trim().isString().withMessage('action يجب أن يكون نصاً'),
  body('outcome').optional().trim().isString().withMessage('outcome يجب أن يكون نصاً'),
  body('reportedBy').optional().custom(isObjectId).withMessage('reportedBy غير صالح'),
  body('reportedAt').optional().isISO8601().withMessage('reportedAt يجب أن يكون تاريخ ISO صالح'),
];

module.exports = {
  createClinicalTrial,
  updateClinicalTrial,
  createTrialParticipant,
  updateTrialParticipant,
  createMonitoringEvent,
  updateMonitoringEvent,
  createAdverseEvent,
  updateAdverseEvent,
};