'use strict';
/**
 * CollaborationHub — Auto-Generated Validation Schemas
 * Generated by: scripts/gen-validation-from-model.js
 */

const { body, param } = require('express-validator');

const isObjectId = (value) => /^[a-f\d]{24}$/i.test(value);

/* ═══ Channel: Create ═══ */
const createChannel = [
  body('channelId').trim().notEmpty().withMessage('channelId مطلوب'),
  body('name').trim().notEmpty().withMessage('name مطلوب'),
  body('nameAr').optional().trim().isString().withMessage('nameAr يجب أن يكون نصاً'),
  body('type').optional().isIn(["direct","group","team","department","case","announcement","support"]).withMessage('type غير صالح'),
  body('domain').optional().trim().isString().withMessage('domain يجب أن يكون نصاً'),
  body('entityRef').optional().custom(isObjectId).withMessage('entityRef غير صالح'),
  body('userId').optional().custom(isObjectId).withMessage('userId غير صالح'),
  body('role').optional().isIn(["owner","admin","member","readonly"]).withMessage('role غير صالح'),
  body('joinedAt').optional().isISO8601().withMessage('joinedAt يجب أن يكون تاريخ ISO صالح'),
  body('mutedUntil').optional().isISO8601().withMessage('mutedUntil يجب أن يكون تاريخ ISO صالح'),
  body('lastReadAt').optional().isISO8601().withMessage('lastReadAt يجب أن يكون تاريخ ISO صالح'),
  body('settings').optional().custom(isObjectId).withMessage('settings غير صالح'),
  body('isActive').optional().isBoolean().withMessage('isActive يجب أن يكون قيمة منطقية'),
  body('isArchived').optional().isBoolean().withMessage('isArchived يجب أن يكون قيمة منطقية'),
];

/* ═══ Channel: Update ═══ */
const updateChannel = [
  body('channelId').optional().trim().isString().withMessage('channelId يجب أن يكون نصاً'),
  body('name').optional().trim().isString().withMessage('name يجب أن يكون نصاً'),
  body('nameAr').optional().trim().isString().withMessage('nameAr يجب أن يكون نصاً'),
  body('type').optional().isIn(["direct","group","team","department","case","announcement","support"]).withMessage('type غير صالح'),
  body('domain').optional().trim().isString().withMessage('domain يجب أن يكون نصاً'),
  body('entityRef').optional().custom(isObjectId).withMessage('entityRef غير صالح'),
  body('userId').optional().custom(isObjectId).withMessage('userId غير صالح'),
  body('role').optional().isIn(["owner","admin","member","readonly"]).withMessage('role غير صالح'),
  body('joinedAt').optional().isISO8601().withMessage('joinedAt يجب أن يكون تاريخ ISO صالح'),
  body('mutedUntil').optional().isISO8601().withMessage('mutedUntil يجب أن يكون تاريخ ISO صالح'),
  body('lastReadAt').optional().isISO8601().withMessage('lastReadAt يجب أن يكون تاريخ ISO صالح'),
  body('settings').optional().custom(isObjectId).withMessage('settings غير صالح'),
  body('isActive').optional().isBoolean().withMessage('isActive يجب أن يكون قيمة منطقية'),
  body('isArchived').optional().isBoolean().withMessage('isArchived يجب أن يكون قيمة منطقية'),
];

/* ═══ Message: Create ═══ */
const createMessage = [
  body('messageId').trim().notEmpty().withMessage('messageId مطلوب'),
  body('channelId').trim().notEmpty().withMessage('channelId مطلوب'),
  body('sender').custom(isObjectId).withMessage('sender غير صالح'),
  body('content').optional().trim().isString().withMessage('content يجب أن يكون نصاً'),
  body('contentType').optional().isIn(["text","rich-text","system","file","image","link","clinical-note"]).withMessage('contentType غير صالح'),
  body('userId').optional().custom(isObjectId).withMessage('userId غير صالح'),
  body('type').optional().isIn(["user","role","channel","everyone"]).withMessage('type غير صالح'),
  body('fileId').optional().trim().isString().withMessage('fileId يجب أن يكون نصاً'),
  body('fileName').optional().trim().isString().withMessage('fileName يجب أن يكون نصاً'),
  body('fileType').optional().trim().isString().withMessage('fileType يجب أن يكون نصاً'),
  body('fileSize').optional().isFloat().withMessage('fileSize يجب أن يكون رقماً'),
  body('url').optional().trim().isString().withMessage('url يجب أن يكون نصاً'),
  body('threadId').optional().trim().isString().withMessage('threadId يجب أن يكون نصاً'),
  body('replyTo').optional().trim().isString().withMessage('replyTo يجب أن يكون نصاً'),
  body('emoji').optional().trim().isString().withMessage('emoji يجب أن يكون نصاً'),
  body('users').optional().isArray().withMessage('users يجب أن يكون مصفوفة'),
  body('userId').optional().custom(isObjectId).withMessage('userId غير صالح'),
  body('readAt').optional().isISO8601().withMessage('readAt يجب أن يكون تاريخ ISO صالح'),
  body('isPinned').optional().isBoolean().withMessage('isPinned يجب أن يكون قيمة منطقية'),
  body('isEdited').optional().isBoolean().withMessage('isEdited يجب أن يكون قيمة منطقية'),
  body('editedAt').optional().isISO8601().withMessage('editedAt يجب أن يكون تاريخ ISO صالح'),
  body('isDeleted').optional().isBoolean().withMessage('isDeleted يجب أن يكون قيمة منطقية'),
  body('deletedAt').optional().isISO8601().withMessage('deletedAt يجب أن يكون تاريخ ISO صالح'),
];

/* ═══ Message: Update ═══ */
const updateMessage = [
  body('messageId').optional().trim().isString().withMessage('messageId يجب أن يكون نصاً'),
  body('channelId').optional().trim().isString().withMessage('channelId يجب أن يكون نصاً'),
  body('sender').optional().custom(isObjectId).withMessage('sender غير صالح'),
  body('content').optional().trim().isString().withMessage('content يجب أن يكون نصاً'),
  body('contentType').optional().isIn(["text","rich-text","system","file","image","link","clinical-note"]).withMessage('contentType غير صالح'),
  body('userId').optional().custom(isObjectId).withMessage('userId غير صالح'),
  body('type').optional().isIn(["user","role","channel","everyone"]).withMessage('type غير صالح'),
  body('fileId').optional().trim().isString().withMessage('fileId يجب أن يكون نصاً'),
  body('fileName').optional().trim().isString().withMessage('fileName يجب أن يكون نصاً'),
  body('fileType').optional().trim().isString().withMessage('fileType يجب أن يكون نصاً'),
  body('fileSize').optional().isFloat().withMessage('fileSize يجب أن يكون رقماً'),
  body('url').optional().trim().isString().withMessage('url يجب أن يكون نصاً'),
  body('threadId').optional().trim().isString().withMessage('threadId يجب أن يكون نصاً'),
  body('replyTo').optional().trim().isString().withMessage('replyTo يجب أن يكون نصاً'),
  body('emoji').optional().trim().isString().withMessage('emoji يجب أن يكون نصاً'),
  body('users').optional().isArray().withMessage('users يجب أن يكون مصفوفة'),
  body('userId').optional().custom(isObjectId).withMessage('userId غير صالح'),
  body('readAt').optional().isISO8601().withMessage('readAt يجب أن يكون تاريخ ISO صالح'),
  body('isPinned').optional().isBoolean().withMessage('isPinned يجب أن يكون قيمة منطقية'),
  body('isEdited').optional().isBoolean().withMessage('isEdited يجب أن يكون قيمة منطقية'),
  body('editedAt').optional().isISO8601().withMessage('editedAt يجب أن يكون تاريخ ISO صالح'),
  body('isDeleted').optional().isBoolean().withMessage('isDeleted يجب أن يكون قيمة منطقية'),
  body('deletedAt').optional().isISO8601().withMessage('deletedAt يجب أن يكون تاريخ ISO صالح'),
];

/* ═══ Presence: Create ═══ */
const createPresence = [
  body('userId').custom(isObjectId).withMessage('userId غير صالح'),
  body('status').optional().isIn(["online","away","busy","dnd","offline"]).withMessage('status غير صالح'),
  body('statusMessage').optional().trim().isString().withMessage('statusMessage يجب أن يكون نصاً'),
  body('lastSeen').optional().isISO8601().withMessage('lastSeen يجب أن يكون تاريخ ISO صالح'),
  body('currentChannel').optional().trim().isString().withMessage('currentChannel يجب أن يكون نصاً'),
  body('device').optional().isIn(["web","mobile","desktop","tablet"]).withMessage('device غير صالح'),
  body('isTyping').optional().trim().isString().withMessage('isTyping يجب أن يكون نصاً'),
];

/* ═══ Presence: Update ═══ */
const updatePresence = [
  body('userId').optional().custom(isObjectId).withMessage('userId غير صالح'),
  body('status').optional().isIn(["online","away","busy","dnd","offline"]).withMessage('status غير صالح'),
  body('statusMessage').optional().trim().isString().withMessage('statusMessage يجب أن يكون نصاً'),
  body('lastSeen').optional().isISO8601().withMessage('lastSeen يجب أن يكون تاريخ ISO صالح'),
  body('currentChannel').optional().trim().isString().withMessage('currentChannel يجب أن يكون نصاً'),
  body('device').optional().isIn(["web","mobile","desktop","tablet"]).withMessage('device غير صالح'),
  body('isTyping').optional().trim().isString().withMessage('isTyping يجب أن يكون نصاً'),
];

module.exports = {
  createChannel,
  updateChannel,
  createMessage,
  updateMessage,
  createPresence,
  updatePresence,
};