'use strict';
/**
 * CommunityOutreach — Auto-Generated Validation Schemas
 * Generated by: scripts/gen-validation-from-model.js
 */

const { body, param } = require('express-validator');

const isObjectId = (value) => /^[a-f\d]{24}$/i.test(value);

/* ═══ OutreachProgram: Create ═══ */
const createOutreachProgram = [
  body('name').trim().notEmpty().withMessage('name مطلوب'),
  body('programType').isIn(["awareness_campaign","screening_event","workshop","support_group","educational_seminar","health_fair","community_walk","open_house","resource_fair","sports_day","art_exhibition","technology_expo"]).withMessage('programType غير صالح'),
  body('status').optional().isIn(["concept","planning","approved","budgeted","marketing","registration_open","in_progress","completed","post_evaluation","archived"]).withMessage('status غير صالح'),
  body('targetAudience').optional().isArray().withMessage('targetAudience يجب أن يكون مصفوفة'),
  body('channels').optional().isArray().withMessage('channels يجب أن يكون مصفوفة'),
  body('startDate').optional().isISO8601().withMessage('startDate يجب أن يكون تاريخ ISO صالح'),
  body('endDate').optional().isISO8601().withMessage('endDate يجب أن يكون تاريخ ISO صالح'),
  body('budget').optional().isFloat().withMessage('budget يجب أن يكون رقماً'),
  body('location').optional().trim().isString().withMessage('location يجب أن يكون نصاً'),
  body('description').optional().trim().isString().withMessage('description يجب أن يكون نصاً'),
  body('goals').optional().isArray().withMessage('goals يجب أن يكون مصفوفة'),
  body('coordinatorId').optional().custom(isObjectId).withMessage('coordinatorId غير صالح'),
  body('departmentId').optional().custom(isObjectId).withMessage('departmentId غير صالح'),
  body('createdBy').optional().custom(isObjectId).withMessage('createdBy غير صالح'),
];

/* ═══ OutreachProgram: Update ═══ */
const updateOutreachProgram = [
  body('name').optional().trim().isString().withMessage('name يجب أن يكون نصاً'),
  body('programType').optional().isIn(["awareness_campaign","screening_event","workshop","support_group","educational_seminar","health_fair","community_walk","open_house","resource_fair","sports_day","art_exhibition","technology_expo"]).withMessage('programType غير صالح'),
  body('status').optional().isIn(["concept","planning","approved","budgeted","marketing","registration_open","in_progress","completed","post_evaluation","archived"]).withMessage('status غير صالح'),
  body('targetAudience').optional().isArray().withMessage('targetAudience يجب أن يكون مصفوفة'),
  body('channels').optional().isArray().withMessage('channels يجب أن يكون مصفوفة'),
  body('startDate').optional().isISO8601().withMessage('startDate يجب أن يكون تاريخ ISO صالح'),
  body('endDate').optional().isISO8601().withMessage('endDate يجب أن يكون تاريخ ISO صالح'),
  body('budget').optional().isFloat().withMessage('budget يجب أن يكون رقماً'),
  body('location').optional().trim().isString().withMessage('location يجب أن يكون نصاً'),
  body('description').optional().trim().isString().withMessage('description يجب أن يكون نصاً'),
  body('goals').optional().isArray().withMessage('goals يجب أن يكون مصفوفة'),
  body('coordinatorId').optional().custom(isObjectId).withMessage('coordinatorId غير صالح'),
  body('departmentId').optional().custom(isObjectId).withMessage('departmentId غير صالح'),
  body('createdBy').optional().custom(isObjectId).withMessage('createdBy غير صالح'),
];

/* ═══ CommunityPartner: Create ═══ */
const createCommunityPartner = [
  body('organizationName').trim().notEmpty().withMessage('organizationName مطلوب'),
  body('partnerType').isIn(["hospital","university","ngo","government","corporate","school","community_center","religious_organization","media_outlet","international"]).withMessage('partnerType غير صالح'),
  body('contactPerson').optional().trim().isString().withMessage('contactPerson يجب أن يكون نصاً'),
  body('contactEmail').optional().trim().isString().withMessage('contactEmail يجب أن يكون نصاً'),
  body('contactPhone').optional().trim().isString().withMessage('contactPhone يجب أن يكون نصاً'),
  body('status').optional().isIn(["prospective","active","inactive","former"]).withMessage('status غير صالح'),
  body('agreementDate').optional().isISO8601().withMessage('agreementDate يجب أن يكون تاريخ ISO صالح'),
  body('agreementExpiry').optional().isISO8601().withMessage('agreementExpiry يجب أن يكون تاريخ ISO صالح'),
  body('contributions').optional().isArray().withMessage('contributions يجب أن يكون مصفوفة'),
  body('programIds').optional().isArray().withMessage('programIds يجب أن يكون مصفوفة'),
  body('notes').optional().trim().isString().withMessage('notes يجب أن يكون نصاً'),
  body('createdBy').optional().custom(isObjectId).withMessage('createdBy غير صالح'),
];

/* ═══ CommunityPartner: Update ═══ */
const updateCommunityPartner = [
  body('organizationName').optional().trim().isString().withMessage('organizationName يجب أن يكون نصاً'),
  body('partnerType').optional().isIn(["hospital","university","ngo","government","corporate","school","community_center","religious_organization","media_outlet","international"]).withMessage('partnerType غير صالح'),
  body('contactPerson').optional().trim().isString().withMessage('contactPerson يجب أن يكون نصاً'),
  body('contactEmail').optional().trim().isString().withMessage('contactEmail يجب أن يكون نصاً'),
  body('contactPhone').optional().trim().isString().withMessage('contactPhone يجب أن يكون نصاً'),
  body('status').optional().isIn(["prospective","active","inactive","former"]).withMessage('status غير صالح'),
  body('agreementDate').optional().isISO8601().withMessage('agreementDate يجب أن يكون تاريخ ISO صالح'),
  body('agreementExpiry').optional().isISO8601().withMessage('agreementExpiry يجب أن يكون تاريخ ISO صالح'),
  body('contributions').optional().isArray().withMessage('contributions يجب أن يكون مصفوفة'),
  body('programIds').optional().isArray().withMessage('programIds يجب أن يكون مصفوفة'),
  body('notes').optional().trim().isString().withMessage('notes يجب أن يكون نصاً'),
  body('createdBy').optional().custom(isObjectId).withMessage('createdBy غير صالح'),
];

/* ═══ OutreachEvent: Create ═══ */
const createOutreachEvent = [
  body('programId').custom(isObjectId).withMessage('programId غير صالح'),
  body('title').trim().notEmpty().withMessage('title مطلوب'),
  body('eventDate').isISO8601().withMessage('eventDate يجب أن يكون تاريخ ISO صالح'),
  body('location').optional().trim().isString().withMessage('location يجب أن يكون نصاً'),
  body('expectedAttendees').optional().isFloat().withMessage('expectedAttendees يجب أن يكون رقماً'),
  body('channels').optional().isArray().withMessage('channels يجب أن يكون مصفوفة'),
  body('staffAssigned').optional().isArray().withMessage('staffAssigned يجب أن يكون مصفوفة'),
  body('volunteersNeeded').optional().isFloat().withMessage('volunteersNeeded يجب أن يكون رقماً'),
  body('status').optional().isIn(["planned","confirmed","in_progress","completed","cancelled"]).withMessage('status غير صالح'),
  body('feedback').optional().trim().isString().withMessage('feedback يجب أن يكون نصاً'),
];

/* ═══ OutreachEvent: Update ═══ */
const updateOutreachEvent = [
  body('programId').optional().custom(isObjectId).withMessage('programId غير صالح'),
  body('title').optional().trim().isString().withMessage('title يجب أن يكون نصاً'),
  body('eventDate').optional().isISO8601().withMessage('eventDate يجب أن يكون تاريخ ISO صالح'),
  body('location').optional().trim().isString().withMessage('location يجب أن يكون نصاً'),
  body('expectedAttendees').optional().isFloat().withMessage('expectedAttendees يجب أن يكون رقماً'),
  body('channels').optional().isArray().withMessage('channels يجب أن يكون مصفوفة'),
  body('staffAssigned').optional().isArray().withMessage('staffAssigned يجب أن يكون مصفوفة'),
  body('volunteersNeeded').optional().isFloat().withMessage('volunteersNeeded يجب أن يكون رقماً'),
  body('status').optional().isIn(["planned","confirmed","in_progress","completed","cancelled"]).withMessage('status غير صالح'),
  body('feedback').optional().trim().isString().withMessage('feedback يجب أن يكون نصاً'),
];

/* ═══ ImpactReport: Create ═══ */
const createImpactReport = [
  body('programId').custom(isObjectId).withMessage('programId غير صالح'),
  body('reportDate').optional().isISO8601().withMessage('reportDate يجب أن يكون تاريخ ISO صالح'),
  body('metrics').optional().isArray().withMessage('metrics يجب أن يكون مصفوفة'),
  body('totalReach').optional().isFloat().withMessage('totalReach يجب أن يكون رقماً'),
  body('referralsGenerated').optional().isFloat().withMessage('referralsGenerated يجب أن يكون رقماً'),
  body('satisfactionAvg').optional().isFloat().withMessage('satisfactionAvg يجب أن يكون رقماً'),
  body('summary').optional().trim().isString().withMessage('summary يجب أن يكون نصاً'),
  body('recommendations').optional().isArray().withMessage('recommendations يجب أن يكون مصفوفة'),
  body('preparedBy').optional().custom(isObjectId).withMessage('preparedBy غير صالح'),
];

/* ═══ ImpactReport: Update ═══ */
const updateImpactReport = [
  body('programId').optional().custom(isObjectId).withMessage('programId غير صالح'),
  body('reportDate').optional().isISO8601().withMessage('reportDate يجب أن يكون تاريخ ISO صالح'),
  body('metrics').optional().isArray().withMessage('metrics يجب أن يكون مصفوفة'),
  body('totalReach').optional().isFloat().withMessage('totalReach يجب أن يكون رقماً'),
  body('referralsGenerated').optional().isFloat().withMessage('referralsGenerated يجب أن يكون رقماً'),
  body('satisfactionAvg').optional().isFloat().withMessage('satisfactionAvg يجب أن يكون رقماً'),
  body('summary').optional().trim().isString().withMessage('summary يجب أن يكون نصاً'),
  body('recommendations').optional().isArray().withMessage('recommendations يجب أن يكون مصفوفة'),
  body('preparedBy').optional().custom(isObjectId).withMessage('preparedBy غير صالح'),
];

module.exports = {
  createOutreachProgram,
  updateOutreachProgram,
  createCommunityPartner,
  updateCommunityPartner,
  createOutreachEvent,
  updateOutreachEvent,
  createImpactReport,
  updateImpactReport,
};