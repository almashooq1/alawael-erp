'use strict';
/**
 * CommunityProgram — Auto-Generated Validation Schemas
 * Generated by: scripts/gen-validation-from-model.js
 */

const { body, param } = require('express-validator');

const isObjectId = (value) => /^[a-f\d]{24}$/i.test(value);

/* ═══ Program: Create ═══ */
const createProgram = [
  body('code').trim().notEmpty().withMessage('code مطلوب'),
  body('name').trim().notEmpty().withMessage('name مطلوب'),
  body('nameAr').optional().trim().isString().withMessage('nameAr يجب أن يكون نصاً'),
  body('description').optional().trim().isString().withMessage('description يجب أن يكون نصاً'),
  body('type').isIn(["support_group","skills_workshop","awareness_campaign","family_program","peer_mentoring","advocacy_program","social_integration","vocational_training","recreational","wellness_program","educational","research_study"]).withMessage('type غير صالح'),
  body('status').optional().isIn(["planning","approved","open_enrollment","active","in_progress","completed","suspended","cancelled","archived","evaluation"]).withMessage('status غير صالح'),
  body('coordinatorId').optional().custom(isObjectId).withMessage('coordinatorId غير صالح'),
  body('startDate').optional().isISO8601().withMessage('startDate يجب أن يكون تاريخ ISO صالح'),
  body('endDate').optional().isISO8601().withMessage('endDate يجب أن يكون تاريخ ISO صالح'),
  body('maxParticipants').optional().isFloat().withMessage('maxParticipants يجب أن يكون رقماً'),
  body('currentEnrolled').optional().isFloat().withMessage('currentEnrolled يجب أن يكون رقماً'),
  body('location').optional().trim().isString().withMessage('location يجب أن يكون نصاً'),
  body('fundingSource').optional().isIn(["government_grant","corporate_sponsorship","individual_donation","foundation_grant","insurance","fee_for_service","endowment","crowdfunding","in_kind","volunteer_hours"]).withMessage('fundingSource غير صالح'),
  body('budget').optional().isFloat().withMessage('budget يجب أن يكون رقماً'),
  body('goals').optional().isArray().withMessage('goals يجب أن يكون مصفوفة'),
  body('eligibilityRequirements').optional().trim().isString().withMessage('eligibilityRequirements يجب أن يكون نصاً'),
  body('isActive').optional().isBoolean().withMessage('isActive يجب أن يكون قيمة منطقية'),
];

/* ═══ Program: Update ═══ */
const updateProgram = [
  body('code').optional().trim().isString().withMessage('code يجب أن يكون نصاً'),
  body('name').optional().trim().isString().withMessage('name يجب أن يكون نصاً'),
  body('nameAr').optional().trim().isString().withMessage('nameAr يجب أن يكون نصاً'),
  body('description').optional().trim().isString().withMessage('description يجب أن يكون نصاً'),
  body('type').optional().isIn(["support_group","skills_workshop","awareness_campaign","family_program","peer_mentoring","advocacy_program","social_integration","vocational_training","recreational","wellness_program","educational","research_study"]).withMessage('type غير صالح'),
  body('status').optional().isIn(["planning","approved","open_enrollment","active","in_progress","completed","suspended","cancelled","archived","evaluation"]).withMessage('status غير صالح'),
  body('coordinatorId').optional().custom(isObjectId).withMessage('coordinatorId غير صالح'),
  body('startDate').optional().isISO8601().withMessage('startDate يجب أن يكون تاريخ ISO صالح'),
  body('endDate').optional().isISO8601().withMessage('endDate يجب أن يكون تاريخ ISO صالح'),
  body('maxParticipants').optional().isFloat().withMessage('maxParticipants يجب أن يكون رقماً'),
  body('currentEnrolled').optional().isFloat().withMessage('currentEnrolled يجب أن يكون رقماً'),
  body('location').optional().trim().isString().withMessage('location يجب أن يكون نصاً'),
  body('fundingSource').optional().isIn(["government_grant","corporate_sponsorship","individual_donation","foundation_grant","insurance","fee_for_service","endowment","crowdfunding","in_kind","volunteer_hours"]).withMessage('fundingSource غير صالح'),
  body('budget').optional().isFloat().withMessage('budget يجب أن يكون رقماً'),
  body('goals').optional().isArray().withMessage('goals يجب أن يكون مصفوفة'),
  body('eligibilityRequirements').optional().trim().isString().withMessage('eligibilityRequirements يجب أن يكون نصاً'),
  body('isActive').optional().isBoolean().withMessage('isActive يجب أن يكون قيمة منطقية'),
];

/* ═══ ProgramEnrollment: Create ═══ */
const createProgramEnrollment = [
  body('enrollmentCode').trim().notEmpty().withMessage('enrollmentCode مطلوب'),
  body('programId').custom(isObjectId).withMessage('programId غير صالح'),
  body('beneficiaryId').optional().custom(isObjectId).withMessage('beneficiaryId غير صالح'),
  body('participantName').optional().trim().isString().withMessage('participantName يجب أن يكون نصاً'),
  body('status').optional().isIn(["applied","waitlisted","enrolled","active","on_hold","withdrew","completed","graduated","dropped","transferred"]).withMessage('status غير صالح'),
  body('enrolledAt').optional().isISO8601().withMessage('enrolledAt يجب أن يكون تاريخ ISO صالح'),
  body('attendanceRate').optional().isFloat({ min: 0, max: 100 }).withMessage('attendanceRate يجب أن يكون بين 0 و 100'),
  body('progressNotes').optional().isArray().withMessage('progressNotes يجب أن يكون مصفوفة'),
  body('finalAssessment').optional().trim().isString().withMessage('finalAssessment يجب أن يكون نصاً'),
  body('satisfactionScore').optional().isFloat({ min: 1, max: 5 }).withMessage('satisfactionScore يجب أن يكون بين 1 و 5'),
];

/* ═══ ProgramEnrollment: Update ═══ */
const updateProgramEnrollment = [
  body('enrollmentCode').optional().trim().isString().withMessage('enrollmentCode يجب أن يكون نصاً'),
  body('programId').optional().custom(isObjectId).withMessage('programId غير صالح'),
  body('beneficiaryId').optional().custom(isObjectId).withMessage('beneficiaryId غير صالح'),
  body('participantName').optional().trim().isString().withMessage('participantName يجب أن يكون نصاً'),
  body('status').optional().isIn(["applied","waitlisted","enrolled","active","on_hold","withdrew","completed","graduated","dropped","transferred"]).withMessage('status غير صالح'),
  body('enrolledAt').optional().isISO8601().withMessage('enrolledAt يجب أن يكون تاريخ ISO صالح'),
  body('attendanceRate').optional().isFloat({ min: 0, max: 100 }).withMessage('attendanceRate يجب أن يكون بين 0 و 100'),
  body('progressNotes').optional().isArray().withMessage('progressNotes يجب أن يكون مصفوفة'),
  body('finalAssessment').optional().trim().isString().withMessage('finalAssessment يجب أن يكون نصاً'),
  body('satisfactionScore').optional().isFloat({ min: 1, max: 5 }).withMessage('satisfactionScore يجب أن يكون بين 1 و 5'),
];

/* ═══ ProgramActivity: Create ═══ */
const createProgramActivity = [
  body('activityCode').trim().notEmpty().withMessage('activityCode مطلوب'),
  body('programId').custom(isObjectId).withMessage('programId غير صالح'),
  body('title').trim().notEmpty().withMessage('title مطلوب'),
  body('type').isIn(["workshop","seminar","field_trip","group_session","one_on_one","online_session","assessment","celebration","orientation","guest_speaker","skill_practice","evaluation"]).withMessage('type غير صالح'),
  body('scheduledDate').optional().isISO8601().withMessage('scheduledDate يجب أن يكون تاريخ ISO صالح'),
  body('startTime').optional().trim().isString().withMessage('startTime يجب أن يكون نصاً'),
  body('endTime').optional().trim().isString().withMessage('endTime يجب أن يكون نصاً'),
  body('location').optional().trim().isString().withMessage('location يجب أن يكون نصاً'),
  body('facilitator').optional().custom(isObjectId).withMessage('facilitator غير صالح'),
  body('maxAttendees').optional().isFloat().withMessage('maxAttendees يجب أن يكون رقماً'),
  body('status').optional().isIn(["planned","confirmed","in_progress","completed","cancelled"]).withMessage('status غير صالح'),
  body('materials').optional().isArray().withMessage('materials يجب أن يكون مصفوفة'),
  body('notes').optional().trim().isString().withMessage('notes يجب أن يكون نصاً'),
];

/* ═══ ProgramActivity: Update ═══ */
const updateProgramActivity = [
  body('activityCode').optional().trim().isString().withMessage('activityCode يجب أن يكون نصاً'),
  body('programId').optional().custom(isObjectId).withMessage('programId غير صالح'),
  body('title').optional().trim().isString().withMessage('title يجب أن يكون نصاً'),
  body('type').optional().isIn(["workshop","seminar","field_trip","group_session","one_on_one","online_session","assessment","celebration","orientation","guest_speaker","skill_practice","evaluation"]).withMessage('type غير صالح'),
  body('scheduledDate').optional().isISO8601().withMessage('scheduledDate يجب أن يكون تاريخ ISO صالح'),
  body('startTime').optional().trim().isString().withMessage('startTime يجب أن يكون نصاً'),
  body('endTime').optional().trim().isString().withMessage('endTime يجب أن يكون نصاً'),
  body('location').optional().trim().isString().withMessage('location يجب أن يكون نصاً'),
  body('facilitator').optional().custom(isObjectId).withMessage('facilitator غير صالح'),
  body('maxAttendees').optional().isFloat().withMessage('maxAttendees يجب أن يكون رقماً'),
  body('status').optional().isIn(["planned","confirmed","in_progress","completed","cancelled"]).withMessage('status غير صالح'),
  body('materials').optional().isArray().withMessage('materials يجب أن يكون مصفوفة'),
  body('notes').optional().trim().isString().withMessage('notes يجب أن يكون نصاً'),
];

/* ═══ ProgramOutcome: Create ═══ */
const createProgramOutcome = [
  body('outcomeCode').trim().notEmpty().withMessage('outcomeCode مطلوب'),
  body('programId').custom(isObjectId).withMessage('programId غير صالح'),
  body('enrollmentId').optional().custom(isObjectId).withMessage('enrollmentId غير صالح'),
  body('type').isIn(["skill_acquisition","behavior_change","social_integration","independence_gain","employment_outcome","education_achievement","health_improvement","quality_of_life","family_satisfaction","community_participation","self_advocacy","goal_attainment"]).withMessage('type غير صالح'),
  body('description').optional().trim().isString().withMessage('description يجب أن يكون نصاً'),
  body('baselineValue').optional().isFloat().withMessage('baselineValue يجب أن يكون رقماً'),
  body('targetValue').optional().isFloat().withMessage('targetValue يجب أن يكون رقماً'),
  body('actualValue').optional().isFloat().withMessage('actualValue يجب أن يكون رقماً'),
  body('measureDate').optional().isISO8601().withMessage('measureDate يجب أن يكون تاريخ ISO صالح'),
  body('measuredBy').optional().custom(isObjectId).withMessage('measuredBy غير صالح'),
  body('achievementPercent').optional().isFloat({ min: 0, max: 100 }).withMessage('achievementPercent يجب أن يكون بين 0 و 100'),
  body('notes').optional().trim().isString().withMessage('notes يجب أن يكون نصاً'),
];

/* ═══ ProgramOutcome: Update ═══ */
const updateProgramOutcome = [
  body('outcomeCode').optional().trim().isString().withMessage('outcomeCode يجب أن يكون نصاً'),
  body('programId').optional().custom(isObjectId).withMessage('programId غير صالح'),
  body('enrollmentId').optional().custom(isObjectId).withMessage('enrollmentId غير صالح'),
  body('type').optional().isIn(["skill_acquisition","behavior_change","social_integration","independence_gain","employment_outcome","education_achievement","health_improvement","quality_of_life","family_satisfaction","community_participation","self_advocacy","goal_attainment"]).withMessage('type غير صالح'),
  body('description').optional().trim().isString().withMessage('description يجب أن يكون نصاً'),
  body('baselineValue').optional().isFloat().withMessage('baselineValue يجب أن يكون رقماً'),
  body('targetValue').optional().isFloat().withMessage('targetValue يجب أن يكون رقماً'),
  body('actualValue').optional().isFloat().withMessage('actualValue يجب أن يكون رقماً'),
  body('measureDate').optional().isISO8601().withMessage('measureDate يجب أن يكون تاريخ ISO صالح'),
  body('measuredBy').optional().custom(isObjectId).withMessage('measuredBy غير صالح'),
  body('achievementPercent').optional().isFloat({ min: 0, max: 100 }).withMessage('achievementPercent يجب أن يكون بين 0 و 100'),
  body('notes').optional().trim().isString().withMessage('notes يجب أن يكون نصاً'),
];

module.exports = {
  createProgram,
  updateProgram,
  createProgramEnrollment,
  updateProgramEnrollment,
  createProgramActivity,
  updateProgramActivity,
  createProgramOutcome,
  updateProgramOutcome,
};