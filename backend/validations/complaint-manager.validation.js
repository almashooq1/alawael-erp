'use strict';
/**
 * ComplaintManager — Auto-Generated Validation Schemas
 * Generated by: scripts/gen-validation-from-model.js
 */

const { body, param } = require('express-validator');

const isObjectId = (value) => /^[a-f\d]{24}$/i.test(value);

/* ═══ Complaint: Create ═══ */
const createComplaint = [
  body('complaintId').trim().notEmpty().withMessage('complaintId مطلوب'),
  body('beneficiaryId').optional().custom(isObjectId).withMessage('beneficiaryId غير صالح'),
  body('episodeId').optional().custom(isObjectId).withMessage('episodeId غير صالح'),
  body('type').isIn(["service_quality","staff_behavior","wait_time","facility_condition","billing_dispute","privacy_breach","accessibility","communication","treatment_outcome","scheduling","equipment","policy"]).withMessage('type غير صالح'),
  body('status').optional().isIn(["received","acknowledged","investigating","in_resolution","resolved","closed","escalated","reopened","withdrawn","rejected"]).withMessage('status غير صالح'),
  body('priority').optional().isIn(["critical","high","medium","low","informational","urgent","safety_related","regulatory","routine","monitoring"]).withMessage('priority غير صالح'),
  body('subject').trim().notEmpty().withMessage('subject مطلوب'),
  body('description').trim().notEmpty().withMessage('description مطلوب'),
  body('category').optional().isIn(["clinical_care","administrative","environmental","interpersonal","financial","rights_violation","discrimination","safety","confidentiality","informed_consent","cultural_sensitivity","accessibility"]).withMessage('category غير صالح'),
  body('anonymous').optional().isBoolean().withMessage('anonymous يجب أن يكون قيمة منطقية'),
  body('submittedBy').optional().custom(isObjectId).withMessage('submittedBy غير صالح'),
  body('assignedTo').optional().custom(isObjectId).withMessage('assignedTo غير صالح'),
  body('attachments').optional().isArray().withMessage('attachments يجب أن يكون مصفوفة'),
  body('dueDate').optional().isISO8601().withMessage('dueDate يجب أن يكون تاريخ ISO صالح'),
];

/* ═══ Complaint: Update ═══ */
const updateComplaint = [
  body('complaintId').optional().trim().isString().withMessage('complaintId يجب أن يكون نصاً'),
  body('beneficiaryId').optional().custom(isObjectId).withMessage('beneficiaryId غير صالح'),
  body('episodeId').optional().custom(isObjectId).withMessage('episodeId غير صالح'),
  body('type').optional().isIn(["service_quality","staff_behavior","wait_time","facility_condition","billing_dispute","privacy_breach","accessibility","communication","treatment_outcome","scheduling","equipment","policy"]).withMessage('type غير صالح'),
  body('status').optional().isIn(["received","acknowledged","investigating","in_resolution","resolved","closed","escalated","reopened","withdrawn","rejected"]).withMessage('status غير صالح'),
  body('priority').optional().isIn(["critical","high","medium","low","informational","urgent","safety_related","regulatory","routine","monitoring"]).withMessage('priority غير صالح'),
  body('subject').optional().trim().isString().withMessage('subject يجب أن يكون نصاً'),
  body('description').optional().trim().isString().withMessage('description يجب أن يكون نصاً'),
  body('category').optional().isIn(["clinical_care","administrative","environmental","interpersonal","financial","rights_violation","discrimination","safety","confidentiality","informed_consent","cultural_sensitivity","accessibility"]).withMessage('category غير صالح'),
  body('anonymous').optional().isBoolean().withMessage('anonymous يجب أن يكون قيمة منطقية'),
  body('submittedBy').optional().custom(isObjectId).withMessage('submittedBy غير صالح'),
  body('assignedTo').optional().custom(isObjectId).withMessage('assignedTo غير صالح'),
  body('attachments').optional().isArray().withMessage('attachments يجب أن يكون مصفوفة'),
  body('dueDate').optional().isISO8601().withMessage('dueDate يجب أن يكون تاريخ ISO صالح'),
];

/* ═══ Resolution: Create ═══ */
const createResolution = [
  body('resolutionId').trim().notEmpty().withMessage('resolutionId مطلوب'),
  body('complaintId').custom(isObjectId).withMessage('complaintId غير صالح'),
  body('type').isIn(["apology","corrective_action","policy_change","staff_training","refund","service_recovery","mediation","investigation_finding","no_action_required","referral","compensation","process_improvement"]).withMessage('type غير صالح'),
  body('description').trim().notEmpty().withMessage('description مطلوب'),
  body('actions').optional().isArray().withMessage('actions يجب أن يكون مصفوفة'),
  body('resolvedBy').optional().custom(isObjectId).withMessage('resolvedBy غير صالح'),
  body('resolvedAt').optional().isISO8601().withMessage('resolvedAt يجب أن يكون تاريخ ISO صالح'),
  body('satisfactionScore').optional().isFloat({ min: 1, max: 5 }).withMessage('satisfactionScore يجب أن يكون بين 1 و 5'),
  body('followUpRequired').optional().isBoolean().withMessage('followUpRequired يجب أن يكون قيمة منطقية'),
  body('followUpDate').optional().isISO8601().withMessage('followUpDate يجب أن يكون تاريخ ISO صالح'),
];

/* ═══ Resolution: Update ═══ */
const updateResolution = [
  body('resolutionId').optional().trim().isString().withMessage('resolutionId يجب أن يكون نصاً'),
  body('complaintId').optional().custom(isObjectId).withMessage('complaintId غير صالح'),
  body('type').optional().isIn(["apology","corrective_action","policy_change","staff_training","refund","service_recovery","mediation","investigation_finding","no_action_required","referral","compensation","process_improvement"]).withMessage('type غير صالح'),
  body('description').optional().trim().isString().withMessage('description يجب أن يكون نصاً'),
  body('actions').optional().isArray().withMessage('actions يجب أن يكون مصفوفة'),
  body('resolvedBy').optional().custom(isObjectId).withMessage('resolvedBy غير صالح'),
  body('resolvedAt').optional().isISO8601().withMessage('resolvedAt يجب أن يكون تاريخ ISO صالح'),
  body('satisfactionScore').optional().isFloat({ min: 1, max: 5 }).withMessage('satisfactionScore يجب أن يكون بين 1 و 5'),
  body('followUpRequired').optional().isBoolean().withMessage('followUpRequired يجب أن يكون قيمة منطقية'),
  body('followUpDate').optional().isISO8601().withMessage('followUpDate يجب أن يكون تاريخ ISO صالح'),
];

/* ═══ Escalation: Create ═══ */
const createEscalation = [
  body('escalationId').trim().notEmpty().withMessage('escalationId مطلوب'),
  body('complaintId').custom(isObjectId).withMessage('complaintId غير صالح'),
  body('level').isIn(["level_1_frontline","level_2_supervisor","level_3_manager","level_4_director","level_5_executive","external_ombudsman","regulatory_body","legal","board_level","ministerial"]).withMessage('level غير صالح'),
  body('reason').trim().notEmpty().withMessage('reason مطلوب'),
  body('escalatedBy').optional().custom(isObjectId).withMessage('escalatedBy غير صالح'),
  body('escalatedTo').optional().custom(isObjectId).withMessage('escalatedTo غير صالح'),
  body('escalatedAt').optional().isISO8601().withMessage('escalatedAt يجب أن يكون تاريخ ISO صالح'),
  body('resolvedAt').optional().isISO8601().withMessage('resolvedAt يجب أن يكون تاريخ ISO صالح'),
  body('outcome').optional().trim().isString().withMessage('outcome يجب أن يكون نصاً'),
];

/* ═══ Escalation: Update ═══ */
const updateEscalation = [
  body('escalationId').optional().trim().isString().withMessage('escalationId يجب أن يكون نصاً'),
  body('complaintId').optional().custom(isObjectId).withMessage('complaintId غير صالح'),
  body('level').optional().isIn(["level_1_frontline","level_2_supervisor","level_3_manager","level_4_director","level_5_executive","external_ombudsman","regulatory_body","legal","board_level","ministerial"]).withMessage('level غير صالح'),
  body('reason').optional().trim().isString().withMessage('reason يجب أن يكون نصاً'),
  body('escalatedBy').optional().custom(isObjectId).withMessage('escalatedBy غير صالح'),
  body('escalatedTo').optional().custom(isObjectId).withMessage('escalatedTo غير صالح'),
  body('escalatedAt').optional().isISO8601().withMessage('escalatedAt يجب أن يكون تاريخ ISO صالح'),
  body('resolvedAt').optional().isISO8601().withMessage('resolvedAt يجب أن يكون تاريخ ISO صالح'),
  body('outcome').optional().trim().isString().withMessage('outcome يجب أن يكون نصاً'),
];

/* ═══ ComplaintAnalytics: Create ═══ */
const createComplaintAnalytics = [
  body('analyticsId').trim().notEmpty().withMessage('analyticsId مطلوب'),
  body('period').trim().notEmpty().withMessage('period مطلوب'),
  body('periodStart').isISO8601().withMessage('periodStart يجب أن يكون تاريخ ISO صالح'),
  body('periodEnd').isISO8601().withMessage('periodEnd يجب أن يكون تاريخ ISO صالح'),
  body('totalComplaints').optional().isFloat().withMessage('totalComplaints يجب أن يكون رقماً'),
  body('resolvedCount').optional().isFloat().withMessage('resolvedCount يجب أن يكون رقماً'),
  body('averageResolutionDays').optional().isFloat().withMessage('averageResolutionDays يجب أن يكون رقماً'),
  body('escalationRate').optional().isFloat().withMessage('escalationRate يجب أن يكون رقماً'),
  body('satisfactionAfterResolution').optional().isFloat().withMessage('satisfactionAfterResolution يجب أن يكون رقماً'),
  body('topCategories').optional().isArray().withMessage('topCategories يجب أن يكون مصفوفة'),
];

/* ═══ ComplaintAnalytics: Update ═══ */
const updateComplaintAnalytics = [
  body('analyticsId').optional().trim().isString().withMessage('analyticsId يجب أن يكون نصاً'),
  body('period').optional().trim().isString().withMessage('period يجب أن يكون نصاً'),
  body('periodStart').optional().isISO8601().withMessage('periodStart يجب أن يكون تاريخ ISO صالح'),
  body('periodEnd').optional().isISO8601().withMessage('periodEnd يجب أن يكون تاريخ ISO صالح'),
  body('totalComplaints').optional().isFloat().withMessage('totalComplaints يجب أن يكون رقماً'),
  body('resolvedCount').optional().isFloat().withMessage('resolvedCount يجب أن يكون رقماً'),
  body('averageResolutionDays').optional().isFloat().withMessage('averageResolutionDays يجب أن يكون رقماً'),
  body('escalationRate').optional().isFloat().withMessage('escalationRate يجب أن يكون رقماً'),
  body('satisfactionAfterResolution').optional().isFloat().withMessage('satisfactionAfterResolution يجب أن يكون رقماً'),
  body('topCategories').optional().isArray().withMessage('topCategories يجب أن يكون مصفوفة'),
];

module.exports = {
  createComplaint,
  updateComplaint,
  createResolution,
  updateResolution,
  createEscalation,
  updateEscalation,
  createComplaintAnalytics,
  updateComplaintAnalytics,
};