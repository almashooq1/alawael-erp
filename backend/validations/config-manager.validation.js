'use strict';
/**
 * ConfigManager — Auto-Generated Validation Schemas
 * Generated by: scripts/gen-validation-from-model.js
 */

const { body, param } = require('express-validator');

const isObjectId = (value) => /^[a-f\d]{24}$/i.test(value);

/* ═══ Config: Create ═══ */
const createConfig = [
  body('key').trim().notEmpty().withMessage('key مطلوب'),
  body('scope').optional().isIn(["system","branch","domain","user"]).withMessage('scope غير صالح'),
  body('scopeId').optional().trim().isString().withMessage('scopeId يجب أن يكون نصاً'),
  body('environment').optional().isIn(["all","development","staging","production"]).withMessage('environment غير صالح'),
  body('value').trim().notEmpty().withMessage('value مطلوب'),
  body('valueType').optional().isIn(["string","number","boolean","json","encrypted"]).withMessage('valueType غير صالح'),
  body('description').optional().trim().isString().withMessage('description يجب أن يكون نصاً'),
  body('category').optional().isIn(["general","clinical","scheduling","notifications","integrations","security","ui","reporting","compliance"]).withMessage('category غير صالح'),
  body('isSecret').optional().isBoolean().withMessage('isSecret يجب أن يكون قيمة منطقية'),
  body('validation').optional().trim().isString().withMessage('validation يجب أن يكون نصاً'),
  body('lastModifiedBy').optional().custom(isObjectId).withMessage('lastModifiedBy غير صالح'),
  body('isDeleted').optional().isBoolean().withMessage('isDeleted يجب أن يكون قيمة منطقية'),
];

/* ═══ Config: Update ═══ */
const updateConfig = [
  body('key').optional().trim().isString().withMessage('key يجب أن يكون نصاً'),
  body('scope').optional().isIn(["system","branch","domain","user"]).withMessage('scope غير صالح'),
  body('scopeId').optional().trim().isString().withMessage('scopeId يجب أن يكون نصاً'),
  body('environment').optional().isIn(["all","development","staging","production"]).withMessage('environment غير صالح'),
  body('value').optional().trim().isString().withMessage('value يجب أن يكون نصاً'),
  body('valueType').optional().isIn(["string","number","boolean","json","encrypted"]).withMessage('valueType غير صالح'),
  body('description').optional().trim().isString().withMessage('description يجب أن يكون نصاً'),
  body('category').optional().isIn(["general","clinical","scheduling","notifications","integrations","security","ui","reporting","compliance"]).withMessage('category غير صالح'),
  body('isSecret').optional().isBoolean().withMessage('isSecret يجب أن يكون قيمة منطقية'),
  body('validation').optional().trim().isString().withMessage('validation يجب أن يكون نصاً'),
  body('lastModifiedBy').optional().custom(isObjectId).withMessage('lastModifiedBy غير صالح'),
  body('isDeleted').optional().isBoolean().withMessage('isDeleted يجب أن يكون قيمة منطقية'),
];

/* ═══ ConfigVersion: Create ═══ */
const createConfigVersion = [
  body('configId').custom(isObjectId).withMessage('configId غير صالح'),
  body('key').trim().notEmpty().withMessage('key مطلوب'),
  body('changedBy').optional().custom(isObjectId).withMessage('changedBy غير صالح'),
  body('reason').optional().trim().isString().withMessage('reason يجب أن يكون نصاً'),
  body('isDeleted').optional().isBoolean().withMessage('isDeleted يجب أن يكون قيمة منطقية'),
];

/* ═══ ConfigVersion: Update ═══ */
const updateConfigVersion = [
  body('configId').optional().custom(isObjectId).withMessage('configId غير صالح'),
  body('key').optional().trim().isString().withMessage('key يجب أن يكون نصاً'),
  body('changedBy').optional().custom(isObjectId).withMessage('changedBy غير صالح'),
  body('reason').optional().trim().isString().withMessage('reason يجب أن يكون نصاً'),
  body('isDeleted').optional().isBoolean().withMessage('isDeleted يجب أن يكون قيمة منطقية'),
];

module.exports = {
  createConfig,
  updateConfig,
  createConfigVersion,
  updateConfigVersion,
};