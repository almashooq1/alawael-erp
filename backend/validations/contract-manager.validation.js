'use strict';
/**
 * ContractManager — Auto-Generated Validation Schemas
 * Generated by: scripts/gen-validation-from-model.js
 */

const { body, param } = require('express-validator');

const isObjectId = (value) => /^[a-f\d]{24}$/i.test(value);

/* ═══ Contract: Create ═══ */
const createContract = [
  body('contractCode').trim().notEmpty().withMessage('contractCode مطلوب'),
  body('title').trim().notEmpty().withMessage('title مطلوب'),
  body('titleAr').optional().trim().isString().withMessage('titleAr يجب أن يكون نصاً'),
  body('type').isIn(["service_agreement","employment","vendor","lease","licensing","partnership","nda","mou","consulting","maintenance","insurance","procurement"]).withMessage('type غير صالح'),
  body('status').optional().isIn(["draft","pending_review","under_negotiation","approved","active","suspended","expired","terminated","renewed","archived"]).withMessage('status غير صالح'),
  body('partyA').trim().notEmpty().withMessage('partyA مطلوب'),
  body('partyB').trim().notEmpty().withMessage('partyB مطلوب'),
  body('value').optional().isFloat().withMessage('value يجب أن يكون رقماً'),
  body('currency').optional().trim().isString().withMessage('currency يجب أن يكون نصاً'),
  body('startDate').optional().isISO8601().withMessage('startDate يجب أن يكون تاريخ ISO صالح'),
  body('endDate').optional().isISO8601().withMessage('endDate يجب أن يكون تاريخ ISO صالح'),
  body('signedDate').optional().isISO8601().withMessage('signedDate يجب أن يكون تاريخ ISO صالح'),
  body('renewalDate').optional().isISO8601().withMessage('renewalDate يجب أن يكون تاريخ ISO صالح'),
  body('autoRenew').optional().isBoolean().withMessage('autoRenew يجب أن يكون قيمة منطقية'),
  body('templateId').optional().custom(isObjectId).withMessage('templateId غير صالح'),
  body('managerId').optional().custom(isObjectId).withMessage('managerId غير صالح'),
  body('tags').optional().isArray().withMessage('tags يجب أن يكون مصفوفة'),
  body('description').optional().trim().isString().withMessage('description يجب أن يكون نصاً'),
];

/* ═══ Contract: Update ═══ */
const updateContract = [
  body('contractCode').optional().trim().isString().withMessage('contractCode يجب أن يكون نصاً'),
  body('title').optional().trim().isString().withMessage('title يجب أن يكون نصاً'),
  body('titleAr').optional().trim().isString().withMessage('titleAr يجب أن يكون نصاً'),
  body('type').optional().isIn(["service_agreement","employment","vendor","lease","licensing","partnership","nda","mou","consulting","maintenance","insurance","procurement"]).withMessage('type غير صالح'),
  body('status').optional().isIn(["draft","pending_review","under_negotiation","approved","active","suspended","expired","terminated","renewed","archived"]).withMessage('status غير صالح'),
  body('partyA').optional().trim().isString().withMessage('partyA يجب أن يكون نصاً'),
  body('partyB').optional().trim().isString().withMessage('partyB يجب أن يكون نصاً'),
  body('value').optional().isFloat().withMessage('value يجب أن يكون رقماً'),
  body('currency').optional().trim().isString().withMessage('currency يجب أن يكون نصاً'),
  body('startDate').optional().isISO8601().withMessage('startDate يجب أن يكون تاريخ ISO صالح'),
  body('endDate').optional().isISO8601().withMessage('endDate يجب أن يكون تاريخ ISO صالح'),
  body('signedDate').optional().isISO8601().withMessage('signedDate يجب أن يكون تاريخ ISO صالح'),
  body('renewalDate').optional().isISO8601().withMessage('renewalDate يجب أن يكون تاريخ ISO صالح'),
  body('autoRenew').optional().isBoolean().withMessage('autoRenew يجب أن يكون قيمة منطقية'),
  body('templateId').optional().custom(isObjectId).withMessage('templateId غير صالح'),
  body('managerId').optional().custom(isObjectId).withMessage('managerId غير صالح'),
  body('tags').optional().isArray().withMessage('tags يجب أن يكون مصفوفة'),
  body('description').optional().trim().isString().withMessage('description يجب أن يكون نصاً'),
];

/* ═══ ContractTemplate: Create ═══ */
const createContractTemplate = [
  body('code').trim().notEmpty().withMessage('code مطلوب'),
  body('name').trim().notEmpty().withMessage('name مطلوب'),
  body('nameAr').optional().trim().isString().withMessage('nameAr يجب أن يكون نصاً'),
  body('category').isIn(["service_level","employment","vendor_supply","lease_rental","partnership","research","consulting","maintenance","insurance","procurement","confidentiality","regulatory"]).withMessage('category غير صالح'),
  body('content').optional().trim().isString().withMessage('content يجب أن يكون نصاً'),
  body('clauses').optional().isArray().withMessage('clauses يجب أن يكون مصفوفة'),
  body('isActive').optional().isBoolean().withMessage('isActive يجب أن يكون قيمة منطقية'),
];

/* ═══ ContractTemplate: Update ═══ */
const updateContractTemplate = [
  body('code').optional().trim().isString().withMessage('code يجب أن يكون نصاً'),
  body('name').optional().trim().isString().withMessage('name يجب أن يكون نصاً'),
  body('nameAr').optional().trim().isString().withMessage('nameAr يجب أن يكون نصاً'),
  body('category').optional().isIn(["service_level","employment","vendor_supply","lease_rental","partnership","research","consulting","maintenance","insurance","procurement","confidentiality","regulatory"]).withMessage('category غير صالح'),
  body('content').optional().trim().isString().withMessage('content يجب أن يكون نصاً'),
  body('clauses').optional().isArray().withMessage('clauses يجب أن يكون مصفوفة'),
  body('isActive').optional().isBoolean().withMessage('isActive يجب أن يكون قيمة منطقية'),
];

/* ═══ ContractAmendment: Create ═══ */
const createContractAmendment = [
  body('amendmentCode').trim().notEmpty().withMessage('amendmentCode مطلوب'),
  body('contractId').custom(isObjectId).withMessage('contractId غير صالح'),
  body('type').isIn(["addendum","modification","extension","renewal","scope_change","price_adjustment","term_change","party_change","schedule_change","compliance_update"]).withMessage('type غير صالح'),
  body('description').optional().trim().isString().withMessage('description يجب أن يكون نصاً'),
  body('effectiveDate').optional().isISO8601().withMessage('effectiveDate يجب أن يكون تاريخ ISO صالح'),
  body('approvedBy').optional().custom(isObjectId).withMessage('approvedBy غير صالح'),
  body('status').optional().isIn(["draft","pending","approved","rejected","applied"]).withMessage('status غير صالح'),
];

/* ═══ ContractAmendment: Update ═══ */
const updateContractAmendment = [
  body('amendmentCode').optional().trim().isString().withMessage('amendmentCode يجب أن يكون نصاً'),
  body('contractId').optional().custom(isObjectId).withMessage('contractId غير صالح'),
  body('type').optional().isIn(["addendum","modification","extension","renewal","scope_change","price_adjustment","term_change","party_change","schedule_change","compliance_update"]).withMessage('type غير صالح'),
  body('description').optional().trim().isString().withMessage('description يجب أن يكون نصاً'),
  body('effectiveDate').optional().isISO8601().withMessage('effectiveDate يجب أن يكون تاريخ ISO صالح'),
  body('approvedBy').optional().custom(isObjectId).withMessage('approvedBy غير صالح'),
  body('status').optional().isIn(["draft","pending","approved","rejected","applied"]).withMessage('status غير صالح'),
];

/* ═══ ContractObligation: Create ═══ */
const createContractObligation = [
  body('obligationCode').trim().notEmpty().withMessage('obligationCode مطلوب'),
  body('contractId').custom(isObjectId).withMessage('contractId غير صالح'),
  body('type').isIn(["payment","delivery","reporting","compliance","performance","confidentiality","indemnification","insurance","audit","renewal_notice","termination_notice","regulatory"]).withMessage('type غير صالح'),
  body('status').optional().isIn(["pending","in_progress","fulfilled","overdue","waived","breached","escalated","cancelled","partially_fulfilled","under_review"]).withMessage('status غير صالح'),
  body('description').optional().trim().isString().withMessage('description يجب أن يكون نصاً'),
  body('dueDate').optional().isISO8601().withMessage('dueDate يجب أن يكون تاريخ ISO صالح'),
  body('assigneeId').optional().custom(isObjectId).withMessage('assigneeId غير صالح'),
  body('reminderSent').optional().isBoolean().withMessage('reminderSent يجب أن يكون قيمة منطقية'),
  body('escalated').optional().isBoolean().withMessage('escalated يجب أن يكون قيمة منطقية'),
];

/* ═══ ContractObligation: Update ═══ */
const updateContractObligation = [
  body('obligationCode').optional().trim().isString().withMessage('obligationCode يجب أن يكون نصاً'),
  body('contractId').optional().custom(isObjectId).withMessage('contractId غير صالح'),
  body('type').optional().isIn(["payment","delivery","reporting","compliance","performance","confidentiality","indemnification","insurance","audit","renewal_notice","termination_notice","regulatory"]).withMessage('type غير صالح'),
  body('status').optional().isIn(["pending","in_progress","fulfilled","overdue","waived","breached","escalated","cancelled","partially_fulfilled","under_review"]).withMessage('status غير صالح'),
  body('description').optional().trim().isString().withMessage('description يجب أن يكون نصاً'),
  body('dueDate').optional().isISO8601().withMessage('dueDate يجب أن يكون تاريخ ISO صالح'),
  body('assigneeId').optional().custom(isObjectId).withMessage('assigneeId غير صالح'),
  body('reminderSent').optional().isBoolean().withMessage('reminderSent يجب أن يكون قيمة منطقية'),
  body('escalated').optional().isBoolean().withMessage('escalated يجب أن يكون قيمة منطقية'),
];

module.exports = {
  createContract,
  updateContract,
  createContractTemplate,
  updateContractTemplate,
  createContractAmendment,
  updateContractAmendment,
  createContractObligation,
  updateContractObligation,
};