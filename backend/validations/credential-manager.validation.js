'use strict';
/**
 * CredentialManager — Auto-Generated Validation Schemas
 * Generated by: scripts/gen-validation-from-model.js
 */

const { body, param } = require('express-validator');

const isObjectId = (value) => /^[a-f\d]{24}$/i.test(value);

/* ═══ Credential: Create ═══ */
const createCredential = [
  body('staffId').custom(isObjectId).withMessage('staffId غير صالح'),
  body('type').isIn(["license","certification","accreditation","registration","diploma","degree","fellowship","board_certification","ceu_course","specialization","endorsement","permit"]).withMessage('type غير صالح'),
  body('status').optional().isIn(["active","pending","expired","revoked","suspended","renewal_due","under_review","provisionally_approved","lapsed","denied"]).withMessage('status غير صالح'),
  body('name').trim().notEmpty().withMessage('name مطلوب'),
  body('issuingBody').optional().isIn(["ministry_of_health","scfhs","medical_council","therapy_board","nursing_council","psychology_board","social_work_board","education_authority","international_body","hospital_credentialing","professional_association","accreditation_agency"]).withMessage('issuingBody غير صالح'),
  body('credentialNumber').optional().trim().isString().withMessage('credentialNumber يجب أن يكون نصاً'),
  body('issueDate').optional().isISO8601().withMessage('issueDate يجب أن يكون تاريخ ISO صالح'),
  body('expiryDate').optional().isISO8601().withMessage('expiryDate يجب أن يكون تاريخ ISO صالح'),
  body('renewalFrequency').optional().isIn(["annual","biennial","triennial","quadrennial","quinquennial","one_time","continuous","upon_request","competency_based","variable"]).withMessage('renewalFrequency غير صالح'),
  body('verificationMethod').optional().isIn(["primary_source","database_check","document_review","online_verification","phone_verification","notarized_copy","digital_badge","blockchain_verified","peer_attestation","self_reported"]).withMessage('verificationMethod غير صالح'),
  body('verifiedBy').optional().custom(isObjectId).withMessage('verifiedBy غير صالح'),
  body('documentUrl').optional().trim().isString().withMessage('documentUrl يجب أن يكون نصاً'),
  body('notes').optional().trim().isString().withMessage('notes يجب أن يكون نصاً'),
  body('createdBy').optional().custom(isObjectId).withMessage('createdBy غير صالح'),
];

/* ═══ Credential: Update ═══ */
const updateCredential = [
  body('staffId').optional().custom(isObjectId).withMessage('staffId غير صالح'),
  body('type').optional().isIn(["license","certification","accreditation","registration","diploma","degree","fellowship","board_certification","ceu_course","specialization","endorsement","permit"]).withMessage('type غير صالح'),
  body('status').optional().isIn(["active","pending","expired","revoked","suspended","renewal_due","under_review","provisionally_approved","lapsed","denied"]).withMessage('status غير صالح'),
  body('name').optional().trim().isString().withMessage('name يجب أن يكون نصاً'),
  body('issuingBody').optional().isIn(["ministry_of_health","scfhs","medical_council","therapy_board","nursing_council","psychology_board","social_work_board","education_authority","international_body","hospital_credentialing","professional_association","accreditation_agency"]).withMessage('issuingBody غير صالح'),
  body('credentialNumber').optional().trim().isString().withMessage('credentialNumber يجب أن يكون نصاً'),
  body('issueDate').optional().isISO8601().withMessage('issueDate يجب أن يكون تاريخ ISO صالح'),
  body('expiryDate').optional().isISO8601().withMessage('expiryDate يجب أن يكون تاريخ ISO صالح'),
  body('renewalFrequency').optional().isIn(["annual","biennial","triennial","quadrennial","quinquennial","one_time","continuous","upon_request","competency_based","variable"]).withMessage('renewalFrequency غير صالح'),
  body('verificationMethod').optional().isIn(["primary_source","database_check","document_review","online_verification","phone_verification","notarized_copy","digital_badge","blockchain_verified","peer_attestation","self_reported"]).withMessage('verificationMethod غير صالح'),
  body('verifiedBy').optional().custom(isObjectId).withMessage('verifiedBy غير صالح'),
  body('documentUrl').optional().trim().isString().withMessage('documentUrl يجب أن يكون نصاً'),
  body('notes').optional().trim().isString().withMessage('notes يجب أن يكون نصاً'),
  body('createdBy').optional().custom(isObjectId).withMessage('createdBy غير صالح'),
];

/* ═══ CEURecord: Create ═══ */
const createCEURecord = [
  body('staffId').custom(isObjectId).withMessage('staffId غير صالح'),
  body('credentialId').optional().custom(isObjectId).withMessage('credentialId غير صالح'),
  body('category').isIn(["clinical_practice","ethics","safety","leadership","research","technology","cultural_competence","supervision","documentation","quality"]).withMessage('category غير صالح'),
  body('title').trim().notEmpty().withMessage('title مطلوب'),
  body('provider').optional().trim().isString().withMessage('provider يجب أن يكون نصاً'),
  body('hoursEarned').isFloat({ min: 0 }).withMessage('hoursEarned يجب أن يكون ≥ 0'),
  body('completionDate').isISO8601().withMessage('completionDate يجب أن يكون تاريخ ISO صالح'),
  body('certificateUrl').optional().trim().isString().withMessage('certificateUrl يجب أن يكون نصاً'),
  body('verified').optional().isBoolean().withMessage('verified يجب أن يكون قيمة منطقية'),
  body('verifiedBy').optional().custom(isObjectId).withMessage('verifiedBy غير صالح'),
  body('createdBy').optional().custom(isObjectId).withMessage('createdBy غير صالح'),
];

/* ═══ CEURecord: Update ═══ */
const updateCEURecord = [
  body('staffId').optional().custom(isObjectId).withMessage('staffId غير صالح'),
  body('credentialId').optional().custom(isObjectId).withMessage('credentialId غير صالح'),
  body('category').optional().isIn(["clinical_practice","ethics","safety","leadership","research","technology","cultural_competence","supervision","documentation","quality"]).withMessage('category غير صالح'),
  body('title').optional().trim().isString().withMessage('title يجب أن يكون نصاً'),
  body('provider').optional().trim().isString().withMessage('provider يجب أن يكون نصاً'),
  body('hoursEarned').optional().isFloat({ min: 0 }).withMessage('hoursEarned يجب أن يكون ≥ 0'),
  body('completionDate').optional().isISO8601().withMessage('completionDate يجب أن يكون تاريخ ISO صالح'),
  body('certificateUrl').optional().trim().isString().withMessage('certificateUrl يجب أن يكون نصاً'),
  body('verified').optional().isBoolean().withMessage('verified يجب أن يكون قيمة منطقية'),
  body('verifiedBy').optional().custom(isObjectId).withMessage('verifiedBy غير صالح'),
  body('createdBy').optional().custom(isObjectId).withMessage('createdBy غير صالح'),
];

/* ═══ VerificationLog: Create ═══ */
const createVerificationLog = [
  body('credentialId').custom(isObjectId).withMessage('credentialId غير صالح'),
  body('method').isIn(["primary_source","database_check","document_review","online_verification","phone_verification","notarized_copy","digital_badge","blockchain_verified","peer_attestation","self_reported"]).withMessage('method غير صالح'),
  body('result').optional().isIn(["verified","failed","inconclusive","pending"]).withMessage('result غير صالح'),
  body('verifiedBy').optional().custom(isObjectId).withMessage('verifiedBy غير صالح'),
  body('notes').optional().trim().isString().withMessage('notes يجب أن يكون نصاً'),
  body('evidenceUrl').optional().trim().isString().withMessage('evidenceUrl يجب أن يكون نصاً'),
];

/* ═══ VerificationLog: Update ═══ */
const updateVerificationLog = [
  body('credentialId').optional().custom(isObjectId).withMessage('credentialId غير صالح'),
  body('method').optional().isIn(["primary_source","database_check","document_review","online_verification","phone_verification","notarized_copy","digital_badge","blockchain_verified","peer_attestation","self_reported"]).withMessage('method غير صالح'),
  body('result').optional().isIn(["verified","failed","inconclusive","pending"]).withMessage('result غير صالح'),
  body('verifiedBy').optional().custom(isObjectId).withMessage('verifiedBy غير صالح'),
  body('notes').optional().trim().isString().withMessage('notes يجب أن يكون نصاً'),
  body('evidenceUrl').optional().trim().isString().withMessage('evidenceUrl يجب أن يكون نصاً'),
];

/* ═══ ComplianceRequirement: Create ═══ */
const createComplianceRequirement = [
  body('department').optional().trim().isString().withMessage('department يجب أن يكون نصاً'),
  body('role').optional().trim().isString().withMessage('role يجب أن يكون نصاً'),
  body('credentialType').isIn(["license","certification","accreditation","registration","diploma","degree","fellowship","board_certification","ceu_course","specialization","endorsement","permit"]).withMessage('credentialType غير صالح'),
  body('credentialName').trim().notEmpty().withMessage('credentialName مطلوب'),
  body('isRequired').optional().isBoolean().withMessage('isRequired يجب أن يكون قيمة منطقية'),
  body('ceuHoursRequired').optional().isFloat().withMessage('ceuHoursRequired يجب أن يكون رقماً'),
  body('renewalPeriodMonths').optional().isFloat().withMessage('renewalPeriodMonths يجب أن يكون رقماً'),
  body('description').optional().trim().isString().withMessage('description يجب أن يكون نصاً'),
  body('effectiveDate').optional().isISO8601().withMessage('effectiveDate يجب أن يكون تاريخ ISO صالح'),
  body('createdBy').optional().custom(isObjectId).withMessage('createdBy غير صالح'),
];

/* ═══ ComplianceRequirement: Update ═══ */
const updateComplianceRequirement = [
  body('department').optional().trim().isString().withMessage('department يجب أن يكون نصاً'),
  body('role').optional().trim().isString().withMessage('role يجب أن يكون نصاً'),
  body('credentialType').optional().isIn(["license","certification","accreditation","registration","diploma","degree","fellowship","board_certification","ceu_course","specialization","endorsement","permit"]).withMessage('credentialType غير صالح'),
  body('credentialName').optional().trim().isString().withMessage('credentialName يجب أن يكون نصاً'),
  body('isRequired').optional().isBoolean().withMessage('isRequired يجب أن يكون قيمة منطقية'),
  body('ceuHoursRequired').optional().isFloat().withMessage('ceuHoursRequired يجب أن يكون رقماً'),
  body('renewalPeriodMonths').optional().isFloat().withMessage('renewalPeriodMonths يجب أن يكون رقماً'),
  body('description').optional().trim().isString().withMessage('description يجب أن يكون نصاً'),
  body('effectiveDate').optional().isISO8601().withMessage('effectiveDate يجب أن يكون تاريخ ISO صالح'),
  body('createdBy').optional().custom(isObjectId).withMessage('createdBy غير صالح'),
];

module.exports = {
  createCredential,
  updateCredential,
  createCEURecord,
  updateCEURecord,
  createVerificationLog,
  updateVerificationLog,
  createComplianceRequirement,
  updateComplianceRequirement,
};