'use strict';
/**
 * DevPortal — Auto-Generated Validation Schemas
 * Generated by: scripts/gen-validation-from-model.js
 */

const { body, param } = require('express-validator');

const isObjectId = (value) => /^[a-f\d]{24}$/i.test(value);

/* ═══ Changelog: Create ═══ */
const createChangelog = [
  body('date').optional().isISO8601().withMessage('date يجب أن يكون تاريخ ISO صالح'),
  body('title').trim().notEmpty().withMessage('title مطلوب'),
  body('titleAr').optional().trim().isString().withMessage('titleAr يجب أن يكون نصاً'),
  body('description').optional().trim().isString().withMessage('description يجب أن يكون نصاً'),
  body('descriptionAr').optional().trim().isString().withMessage('descriptionAr يجب أن يكون نصاً'),
  body('category').optional().isIn(["feature","bugfix","improvement","breaking","deprecation","security"]).withMessage('category غير صالح'),
  body('domain').optional().trim().isString().withMessage('domain يجب أن يكون نصاً'),
  body('breaking').optional().isBoolean().withMessage('breaking يجب أن يكون قيمة منطقية'),
  body('author').optional().trim().isString().withMessage('author يجب أن يكون نصاً'),
  body('isDeleted').optional().isBoolean().withMessage('isDeleted يجب أن يكون قيمة منطقية'),
];

/* ═══ Changelog: Update ═══ */
const updateChangelog = [
  body('date').optional().isISO8601().withMessage('date يجب أن يكون تاريخ ISO صالح'),
  body('title').optional().trim().isString().withMessage('title يجب أن يكون نصاً'),
  body('titleAr').optional().trim().isString().withMessage('titleAr يجب أن يكون نصاً'),
  body('description').optional().trim().isString().withMessage('description يجب أن يكون نصاً'),
  body('descriptionAr').optional().trim().isString().withMessage('descriptionAr يجب أن يكون نصاً'),
  body('category').optional().isIn(["feature","bugfix","improvement","breaking","deprecation","security"]).withMessage('category غير صالح'),
  body('domain').optional().trim().isString().withMessage('domain يجب أن يكون نصاً'),
  body('breaking').optional().isBoolean().withMessage('breaking يجب أن يكون قيمة منطقية'),
  body('author').optional().trim().isString().withMessage('author يجب أن يكون نصاً'),
  body('isDeleted').optional().isBoolean().withMessage('isDeleted يجب أن يكون قيمة منطقية'),
];

module.exports = {
  createChangelog,
  updateChangelog,
};