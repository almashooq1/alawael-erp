'use strict';
/**
 * DigitalSignature — Auto-Generated Validation Schemas
 * Generated by: scripts/gen-validation-from-model.js
 */

const { body, param } = require('express-validator');

const isObjectId = (value) => /^[a-f\d]{24}$/i.test(value);

/* ═══ SignatureRequest: Create ═══ */
const createSignatureRequest = [
  body('requestCode').trim().notEmpty().withMessage('requestCode مطلوب'),
  body('title').trim().notEmpty().withMessage('title مطلوب'),
  body('description').optional().trim().isString().withMessage('description يجب أن يكون نصاً'),
  body('signatureType').isIn(["simple_electronic","advanced_electronic","qualified_electronic","digital_certificate","biometric","otp_verified","handwritten_capture","click_to_sign","email_verified","sms_verified","multi_factor","witness_required"]).withMessage('signatureType غير صالح'),
  body('status').optional().isIn(["draft","pending","sent","viewed","signed","declined","expired","cancelled","voided","completed"]).withMessage('status غير صالح'),
  body('documentId').optional().custom(isObjectId).withMessage('documentId غير صالح'),
  body('documentType').optional().trim().isString().withMessage('documentType يجب أن يكون نصاً'),
  body('templateId').optional().custom(isObjectId).withMessage('templateId غير صالح'),
  body('requestedBy').optional().custom(isObjectId).withMessage('requestedBy غير صالح'),
  body('userId').optional().custom(isObjectId).withMessage('userId غير صالح'),
  body('role').optional().isIn(["primary_signer","co_signer","witness","approver","reviewer","notary","guardian","legal_representative","clinician","administrator"]).withMessage('role غير صالح'),
  body('email').optional().trim().isString().withMessage('email يجب أن يكون نصاً'),
  body('name').optional().trim().isString().withMessage('name يجب أن يكون نصاً'),
  body('order').optional().isFloat().withMessage('order يجب أن يكون رقماً'),
  body('status').optional().isIn(["draft","pending","sent","viewed","signed","declined","expired","cancelled","voided","completed"]).withMessage('status غير صالح'),
  body('signedAt').optional().isISO8601().withMessage('signedAt يجب أن يكون تاريخ ISO صالح'),
  body('declinedAt').optional().isISO8601().withMessage('declinedAt يجب أن يكون تاريخ ISO صالح'),
  body('declineReason').optional().trim().isString().withMessage('declineReason يجب أن يكون نصاً'),
  body('signatureData').optional().trim().isString().withMessage('signatureData يجب أن يكون نصاً'),
  body('ipAddress').optional().trim().isString().withMessage('ipAddress يجب أن يكون نصاً'),
  body('userAgent').optional().trim().isString().withMessage('userAgent يجب أن يكون نصاً'),
  body('expiresAt').optional().isISO8601().withMessage('expiresAt يجب أن يكون تاريخ ISO صالح'),
  body('reminderCount').optional().isFloat().withMessage('reminderCount يجب أن يكون رقماً'),
  body('lastReminderAt').optional().isISO8601().withMessage('lastReminderAt يجب أن يكون تاريخ ISO صالح'),
];

/* ═══ SignatureRequest: Update ═══ */
const updateSignatureRequest = [
  body('requestCode').optional().trim().isString().withMessage('requestCode يجب أن يكون نصاً'),
  body('title').optional().trim().isString().withMessage('title يجب أن يكون نصاً'),
  body('description').optional().trim().isString().withMessage('description يجب أن يكون نصاً'),
  body('signatureType').optional().isIn(["simple_electronic","advanced_electronic","qualified_electronic","digital_certificate","biometric","otp_verified","handwritten_capture","click_to_sign","email_verified","sms_verified","multi_factor","witness_required"]).withMessage('signatureType غير صالح'),
  body('status').optional().isIn(["draft","pending","sent","viewed","signed","declined","expired","cancelled","voided","completed"]).withMessage('status غير صالح'),
  body('documentId').optional().custom(isObjectId).withMessage('documentId غير صالح'),
  body('documentType').optional().trim().isString().withMessage('documentType يجب أن يكون نصاً'),
  body('templateId').optional().custom(isObjectId).withMessage('templateId غير صالح'),
  body('requestedBy').optional().custom(isObjectId).withMessage('requestedBy غير صالح'),
  body('userId').optional().custom(isObjectId).withMessage('userId غير صالح'),
  body('role').optional().isIn(["primary_signer","co_signer","witness","approver","reviewer","notary","guardian","legal_representative","clinician","administrator"]).withMessage('role غير صالح'),
  body('email').optional().trim().isString().withMessage('email يجب أن يكون نصاً'),
  body('name').optional().trim().isString().withMessage('name يجب أن يكون نصاً'),
  body('order').optional().isFloat().withMessage('order يجب أن يكون رقماً'),
  body('status').optional().isIn(["draft","pending","sent","viewed","signed","declined","expired","cancelled","voided","completed"]).withMessage('status غير صالح'),
  body('signedAt').optional().isISO8601().withMessage('signedAt يجب أن يكون تاريخ ISO صالح'),
  body('declinedAt').optional().isISO8601().withMessage('declinedAt يجب أن يكون تاريخ ISO صالح'),
  body('declineReason').optional().trim().isString().withMessage('declineReason يجب أن يكون نصاً'),
  body('signatureData').optional().trim().isString().withMessage('signatureData يجب أن يكون نصاً'),
  body('ipAddress').optional().trim().isString().withMessage('ipAddress يجب أن يكون نصاً'),
  body('userAgent').optional().trim().isString().withMessage('userAgent يجب أن يكون نصاً'),
  body('expiresAt').optional().isISO8601().withMessage('expiresAt يجب أن يكون تاريخ ISO صالح'),
  body('reminderCount').optional().isFloat().withMessage('reminderCount يجب أن يكون رقماً'),
  body('lastReminderAt').optional().isISO8601().withMessage('lastReminderAt يجب أن يكون تاريخ ISO صالح'),
];

/* ═══ SignatureTemplate: Create ═══ */
const createSignatureTemplate = [
  body('code').trim().notEmpty().withMessage('code مطلوب'),
  body('name').trim().notEmpty().withMessage('name مطلوب'),
  body('nameAr').optional().trim().isString().withMessage('nameAr يجب أن يكون نصاً'),
  body('description').optional().trim().isString().withMessage('description يجب أن يكون نصاً'),
  body('signatureType').isIn(["simple_electronic","advanced_electronic","qualified_electronic","digital_certificate","biometric","otp_verified","handwritten_capture","click_to_sign","email_verified","sms_verified","multi_factor","witness_required"]).withMessage('signatureType غير صالح'),
  body('requiredSigners').optional().isFloat().withMessage('requiredSigners يجب أن يكون رقماً'),
  body('expirationDays').optional().isFloat().withMessage('expirationDays يجب أن يكون رقماً'),
  body('reminderDays').optional().isArray().withMessage('reminderDays يجب أن يكون مصفوفة'),
  body('isActive').optional().isBoolean().withMessage('isActive يجب أن يكون قيمة منطقية'),
];

/* ═══ SignatureTemplate: Update ═══ */
const updateSignatureTemplate = [
  body('code').optional().trim().isString().withMessage('code يجب أن يكون نصاً'),
  body('name').optional().trim().isString().withMessage('name يجب أن يكون نصاً'),
  body('nameAr').optional().trim().isString().withMessage('nameAr يجب أن يكون نصاً'),
  body('description').optional().trim().isString().withMessage('description يجب أن يكون نصاً'),
  body('signatureType').optional().isIn(["simple_electronic","advanced_electronic","qualified_electronic","digital_certificate","biometric","otp_verified","handwritten_capture","click_to_sign","email_verified","sms_verified","multi_factor","witness_required"]).withMessage('signatureType غير صالح'),
  body('requiredSigners').optional().isFloat().withMessage('requiredSigners يجب أن يكون رقماً'),
  body('expirationDays').optional().isFloat().withMessage('expirationDays يجب أن يكون رقماً'),
  body('reminderDays').optional().isArray().withMessage('reminderDays يجب أن يكون مصفوفة'),
  body('isActive').optional().isBoolean().withMessage('isActive يجب أن يكون قيمة منطقية'),
];

/* ═══ Certificate: Create ═══ */
const createCertificate = [
  body('certCode').trim().notEmpty().withMessage('certCode مطلوب'),
  body('userId').custom(isObjectId).withMessage('userId غير صالح'),
  body('issuer').trim().notEmpty().withMessage('issuer مطلوب'),
  body('subject').optional().trim().isString().withMessage('subject يجب أن يكون نصاً'),
  body('serialNumber').optional().trim().isString().withMessage('serialNumber يجب أن يكون نصاً'),
  body('algorithm').optional().isIn(["RSA-SHA256","RSA-SHA512","ECDSA-P256","ECDSA-P384","Ed25519","HMAC-SHA256"]).withMessage('algorithm غير صالح'),
  body('publicKey').optional().trim().isString().withMessage('publicKey يجب أن يكون نصاً'),
  body('fingerprint').optional().trim().isString().withMessage('fingerprint يجب أن يكون نصاً'),
  body('status').optional().isIn(["active","expired","revoked","suspended","pending_activation","pending_renewal","archived","compromised"]).withMessage('status غير صالح'),
  body('issuedAt').optional().isISO8601().withMessage('issuedAt يجب أن يكون تاريخ ISO صالح'),
  body('expiresAt').optional().isISO8601().withMessage('expiresAt يجب أن يكون تاريخ ISO صالح'),
  body('revokedAt').optional().isISO8601().withMessage('revokedAt يجب أن يكون تاريخ ISO صالح'),
  body('revokeReason').optional().trim().isString().withMessage('revokeReason يجب أن يكون نصاً'),
];

/* ═══ Certificate: Update ═══ */
const updateCertificate = [
  body('certCode').optional().trim().isString().withMessage('certCode يجب أن يكون نصاً'),
  body('userId').optional().custom(isObjectId).withMessage('userId غير صالح'),
  body('issuer').optional().trim().isString().withMessage('issuer يجب أن يكون نصاً'),
  body('subject').optional().trim().isString().withMessage('subject يجب أن يكون نصاً'),
  body('serialNumber').optional().trim().isString().withMessage('serialNumber يجب أن يكون نصاً'),
  body('algorithm').optional().isIn(["RSA-SHA256","RSA-SHA512","ECDSA-P256","ECDSA-P384","Ed25519","HMAC-SHA256"]).withMessage('algorithm غير صالح'),
  body('publicKey').optional().trim().isString().withMessage('publicKey يجب أن يكون نصاً'),
  body('fingerprint').optional().trim().isString().withMessage('fingerprint يجب أن يكون نصاً'),
  body('status').optional().isIn(["active","expired","revoked","suspended","pending_activation","pending_renewal","archived","compromised"]).withMessage('status غير صالح'),
  body('issuedAt').optional().isISO8601().withMessage('issuedAt يجب أن يكون تاريخ ISO صالح'),
  body('expiresAt').optional().isISO8601().withMessage('expiresAt يجب أن يكون تاريخ ISO صالح'),
  body('revokedAt').optional().isISO8601().withMessage('revokedAt يجب أن يكون تاريخ ISO صالح'),
  body('revokeReason').optional().trim().isString().withMessage('revokeReason يجب أن يكون نصاً'),
];

/* ═══ SignatureAudit: Create ═══ */
const createSignatureAudit = [
  body('requestId').custom(isObjectId).withMessage('requestId غير صالح'),
  body('action').trim().notEmpty().withMessage('action مطلوب'),
  body('userId').optional().custom(isObjectId).withMessage('userId غير صالح'),
  body('ipAddress').optional().trim().isString().withMessage('ipAddress يجب أن يكون نصاً'),
  body('userAgent').optional().trim().isString().withMessage('userAgent يجب أن يكون نصاً'),
  body('details').optional().trim().isString().withMessage('details يجب أن يكون نصاً'),
  body('verificationMethod').optional().isIn(["certificate_chain","timestamp_authority","ocsp_check","crl_check","biometric_match","otp_validation","email_confirmation","knowledge_based"]).withMessage('verificationMethod غير صالح'),
  body('isValid').optional().isBoolean().withMessage('isValid يجب أن يكون قيمة منطقية'),
];

/* ═══ SignatureAudit: Update ═══ */
const updateSignatureAudit = [
  body('requestId').optional().custom(isObjectId).withMessage('requestId غير صالح'),
  body('action').optional().trim().isString().withMessage('action يجب أن يكون نصاً'),
  body('userId').optional().custom(isObjectId).withMessage('userId غير صالح'),
  body('ipAddress').optional().trim().isString().withMessage('ipAddress يجب أن يكون نصاً'),
  body('userAgent').optional().trim().isString().withMessage('userAgent يجب أن يكون نصاً'),
  body('details').optional().trim().isString().withMessage('details يجب أن يكون نصاً'),
  body('verificationMethod').optional().isIn(["certificate_chain","timestamp_authority","ocsp_check","crl_check","biometric_match","otp_validation","email_confirmation","knowledge_based"]).withMessage('verificationMethod غير صالح'),
  body('isValid').optional().isBoolean().withMessage('isValid يجب أن يكون قيمة منطقية'),
];

module.exports = {
  createSignatureRequest,
  updateSignatureRequest,
  createSignatureTemplate,
  updateSignatureTemplate,
  createCertificate,
  updateCertificate,
  createSignatureAudit,
  updateSignatureAudit,
};