'use strict';
/**
 * DocumentCollaboration — Auto-Generated Validation Schemas
 * Generated by: scripts/gen-validation-from-model.js
 */

const { body, param } = require('express-validator');

const isObjectId = (value) => /^[a-f\d]{24}$/i.test(value);

/* ═══ CollabDocument: Create ═══ */
const createCollabDocument = [
  body('documentId').trim().notEmpty().withMessage('documentId مطلوب'),
  body('title').trim().notEmpty().withMessage('title مطلوب'),
  body('titleAr').optional().trim().isString().withMessage('titleAr يجب أن يكون نصاً'),
  body('type').optional().isIn(["clinical-note","care-plan-draft","assessment-report","progress-note","discharge-summary","policy","protocol","guideline","form-template","research-paper","meeting-minutes","general"]).withMessage('type غير صالح'),
  body('domain').optional().trim().isString().withMessage('domain يجب أن يكون نصاً'),
  body('content').optional().trim().isString().withMessage('content يجب أن يكون نصاً'),
  body('format').optional().isIn(["markdown","rich-text","plain","html"]).withMessage('format غير صالح'),
  body('entityRef').optional().custom(isObjectId).withMessage('entityRef غير صالح'),
  body('owner').custom(isObjectId).withMessage('owner غير صالح'),
  body('userId').optional().custom(isObjectId).withMessage('userId غير صالح'),
  body('permission').optional().isIn(["view","comment","edit","admin"]).withMessage('permission غير صالح'),
  body('addedAt').optional().isISO8601().withMessage('addedAt يجب أن يكون تاريخ ISO صالح'),
  body('lastAccessedAt').optional().isISO8601().withMessage('lastAccessedAt يجب أن يكون تاريخ ISO صالح'),
  body('reviewStatus').optional().isIn(["draft","in-review","approved","rejected","archived"]).withMessage('reviewStatus غير صالح'),
  body('userId').optional().custom(isObjectId).withMessage('userId غير صالح'),
  body('status').optional().isIn(["pending","approved","changes-requested","rejected"]).withMessage('status غير صالح'),
  body('feedback').optional().trim().isString().withMessage('feedback يجب أن يكون نصاً'),
  body('content').optional().trim().isString().withMessage('content يجب أن يكون نصاً'),
  body('editedBy').optional().custom(isObjectId).withMessage('editedBy غير صالح'),
  body('editedAt').optional().isISO8601().withMessage('editedAt يجب أن يكون تاريخ ISO صالح'),
  body('changeDescription').optional().trim().isString().withMessage('changeDescription يجب أن يكون نصاً'),
  body('isLocked').optional().isBoolean().withMessage('isLocked يجب أن يكون قيمة منطقية'),
  body('lockedBy').optional().custom(isObjectId).withMessage('lockedBy غير صالح'),
  body('lockedAt').optional().isISO8601().withMessage('lockedAt يجب أن يكون تاريخ ISO صالح'),
  body('isActive').optional().isBoolean().withMessage('isActive يجب أن يكون قيمة منطقية'),
];

/* ═══ CollabDocument: Update ═══ */
const updateCollabDocument = [
  body('documentId').optional().trim().isString().withMessage('documentId يجب أن يكون نصاً'),
  body('title').optional().trim().isString().withMessage('title يجب أن يكون نصاً'),
  body('titleAr').optional().trim().isString().withMessage('titleAr يجب أن يكون نصاً'),
  body('type').optional().isIn(["clinical-note","care-plan-draft","assessment-report","progress-note","discharge-summary","policy","protocol","guideline","form-template","research-paper","meeting-minutes","general"]).withMessage('type غير صالح'),
  body('domain').optional().trim().isString().withMessage('domain يجب أن يكون نصاً'),
  body('content').optional().trim().isString().withMessage('content يجب أن يكون نصاً'),
  body('format').optional().isIn(["markdown","rich-text","plain","html"]).withMessage('format غير صالح'),
  body('entityRef').optional().custom(isObjectId).withMessage('entityRef غير صالح'),
  body('owner').optional().custom(isObjectId).withMessage('owner غير صالح'),
  body('userId').optional().custom(isObjectId).withMessage('userId غير صالح'),
  body('permission').optional().isIn(["view","comment","edit","admin"]).withMessage('permission غير صالح'),
  body('addedAt').optional().isISO8601().withMessage('addedAt يجب أن يكون تاريخ ISO صالح'),
  body('lastAccessedAt').optional().isISO8601().withMessage('lastAccessedAt يجب أن يكون تاريخ ISO صالح'),
  body('reviewStatus').optional().isIn(["draft","in-review","approved","rejected","archived"]).withMessage('reviewStatus غير صالح'),
  body('userId').optional().custom(isObjectId).withMessage('userId غير صالح'),
  body('status').optional().isIn(["pending","approved","changes-requested","rejected"]).withMessage('status غير صالح'),
  body('feedback').optional().trim().isString().withMessage('feedback يجب أن يكون نصاً'),
  body('content').optional().trim().isString().withMessage('content يجب أن يكون نصاً'),
  body('editedBy').optional().custom(isObjectId).withMessage('editedBy غير صالح'),
  body('editedAt').optional().isISO8601().withMessage('editedAt يجب أن يكون تاريخ ISO صالح'),
  body('changeDescription').optional().trim().isString().withMessage('changeDescription يجب أن يكون نصاً'),
  body('isLocked').optional().isBoolean().withMessage('isLocked يجب أن يكون قيمة منطقية'),
  body('lockedBy').optional().custom(isObjectId).withMessage('lockedBy غير صالح'),
  body('lockedAt').optional().isISO8601().withMessage('lockedAt يجب أن يكون تاريخ ISO صالح'),
  body('isActive').optional().isBoolean().withMessage('isActive يجب أن يكون قيمة منطقية'),
];

/* ═══ Comment: Create ═══ */
const createComment = [
  body('commentId').trim().notEmpty().withMessage('commentId مطلوب'),
  body('documentId').trim().notEmpty().withMessage('documentId مطلوب'),
  body('author').custom(isObjectId).withMessage('author غير صالح'),
  body('content').trim().notEmpty().withMessage('content مطلوب'),
  body('type').optional().isIn(["comment","suggestion","question","approval","concern","annotation"]).withMessage('type غير صالح'),
  body('anchor').optional().trim().isString().withMessage('anchor يجب أن يكون نصاً'),
  body('parentId').optional().trim().isString().withMessage('parentId يجب أن يكون نصاً'),
  body('replyId').optional().trim().isString().withMessage('replyId يجب أن يكون نصاً'),
  body('author').optional().custom(isObjectId).withMessage('author غير صالح'),
  body('content').optional().trim().isString().withMessage('content يجب أن يكون نصاً'),
  body('status').optional().isIn(["open","resolved","accepted","rejected","deferred"]).withMessage('status غير صالح'),
  body('resolvedBy').optional().custom(isObjectId).withMessage('resolvedBy غير صالح'),
  body('resolvedAt').optional().isISO8601().withMessage('resolvedAt يجب أن يكون تاريخ ISO صالح'),
  body('priority').optional().isIn(["low","medium","high","critical"]).withMessage('priority غير صالح'),
  body('isActive').optional().isBoolean().withMessage('isActive يجب أن يكون قيمة منطقية'),
];

/* ═══ Comment: Update ═══ */
const updateComment = [
  body('commentId').optional().trim().isString().withMessage('commentId يجب أن يكون نصاً'),
  body('documentId').optional().trim().isString().withMessage('documentId يجب أن يكون نصاً'),
  body('author').optional().custom(isObjectId).withMessage('author غير صالح'),
  body('content').optional().trim().isString().withMessage('content يجب أن يكون نصاً'),
  body('type').optional().isIn(["comment","suggestion","question","approval","concern","annotation"]).withMessage('type غير صالح'),
  body('anchor').optional().trim().isString().withMessage('anchor يجب أن يكون نصاً'),
  body('parentId').optional().trim().isString().withMessage('parentId يجب أن يكون نصاً'),
  body('replyId').optional().trim().isString().withMessage('replyId يجب أن يكون نصاً'),
  body('author').optional().custom(isObjectId).withMessage('author غير صالح'),
  body('content').optional().trim().isString().withMessage('content يجب أن يكون نصاً'),
  body('status').optional().isIn(["open","resolved","accepted","rejected","deferred"]).withMessage('status غير صالح'),
  body('resolvedBy').optional().custom(isObjectId).withMessage('resolvedBy غير صالح'),
  body('resolvedAt').optional().isISO8601().withMessage('resolvedAt يجب أن يكون تاريخ ISO صالح'),
  body('priority').optional().isIn(["low","medium","high","critical"]).withMessage('priority غير صالح'),
  body('isActive').optional().isBoolean().withMessage('isActive يجب أن يكون قيمة منطقية'),
];

module.exports = {
  createCollabDocument,
  updateCollabDocument,
  createComment,
  updateComment,
};