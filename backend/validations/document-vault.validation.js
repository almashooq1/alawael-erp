'use strict';
/**
 * DocumentVault — Auto-Generated Validation Schemas
 * Generated by: scripts/gen-validation-from-model.js
 */

const { body, param } = require('express-validator');

const isObjectId = (value) => /^[a-f\d]{24}$/i.test(value);

/* ═══ VaultDocument: Create ═══ */
const createVaultDocument = [
  body('documentCode').trim().notEmpty().withMessage('documentCode مطلوب'),
  body('title').trim().notEmpty().withMessage('title مطلوب'),
  body('titleAr').optional().trim().isString().withMessage('titleAr يجب أن يكون نصاً'),
  body('description').optional().trim().isString().withMessage('description يجب أن يكون نصاً'),
  body('type').isIn(["clinical_report","assessment_form","care_plan","progress_note","consent_form","referral_letter","prescription","lab_result","imaging_report","discharge_summary","policy_document","template"]).withMessage('type غير صالح'),
  body('status').optional().isIn(["draft","pending_review","approved","published","archived","expired","revoked","superseded","locked","under_revision"]).withMessage('status غير صالح'),
  body('classification').optional().isIn(["public","internal","confidential","restricted","top_secret","patient_sensitive","legal_privileged","research_only"]).withMessage('classification غير صالح'),
  body('mimeType').optional().trim().isString().withMessage('mimeType يجب أن يكون نصاً'),
  body('fileSize').optional().isFloat().withMessage('fileSize يجب أن يكون رقماً'),
  body('filePath').optional().trim().isString().withMessage('filePath يجب أن يكون نصاً'),
  body('storageType').optional().isIn(["local","cloud_s3","cloud_azure","cloud_gcs","ftp","network_share","database_blob","cdn","encrypted_vault","cold_storage","tape_archive","hybrid"]).withMessage('storageType غير صالح'),
  body('checksum').optional().trim().isString().withMessage('checksum يجب أن يكون نصاً'),
  body('folderId').optional().custom(isObjectId).withMessage('folderId غير صالح'),
  body('tags').optional().isArray().withMessage('tags يجب أن يكون مصفوفة'),
  body('filePath').optional().trim().isString().withMessage('filePath يجب أن يكون نصاً'),
  body('fileSize').optional().isFloat().withMessage('fileSize يجب أن يكون رقماً'),
  body('checksum').optional().trim().isString().withMessage('checksum يجب أن يكون نصاً'),
  body('uploadedBy').optional().custom(isObjectId).withMessage('uploadedBy غير صالح'),
  body('uploadedAt').optional().isISO8601().withMessage('uploadedAt يجب أن يكون تاريخ ISO صالح'),
  body('changeNote').optional().trim().isString().withMessage('changeNote يجب أن يكون نصاً'),
  body('uploadedBy').optional().custom(isObjectId).withMessage('uploadedBy غير صالح'),
  body('ownerId').optional().custom(isObjectId).withMessage('ownerId غير صالح'),
  body('ownerType').optional().isIn(["user","department","beneficiary","system"]).withMessage('ownerType غير صالح'),
  body('relatedEntityId').optional().custom(isObjectId).withMessage('relatedEntityId غير صالح'),
  body('relatedEntityType').optional().trim().isString().withMessage('relatedEntityType يجب أن يكون نصاً'),
  body('retentionDate').optional().isISO8601().withMessage('retentionDate يجب أن يكون تاريخ ISO صالح'),
  body('expiresAt').optional().isISO8601().withMessage('expiresAt يجب أن يكون تاريخ ISO صالح'),
  body('isEncrypted').optional().isBoolean().withMessage('isEncrypted يجب أن يكون قيمة منطقية'),
  body('downloadCount').optional().isFloat().withMessage('downloadCount يجب أن يكون رقماً'),
];

/* ═══ VaultDocument: Update ═══ */
const updateVaultDocument = [
  body('documentCode').optional().trim().isString().withMessage('documentCode يجب أن يكون نصاً'),
  body('title').optional().trim().isString().withMessage('title يجب أن يكون نصاً'),
  body('titleAr').optional().trim().isString().withMessage('titleAr يجب أن يكون نصاً'),
  body('description').optional().trim().isString().withMessage('description يجب أن يكون نصاً'),
  body('type').optional().isIn(["clinical_report","assessment_form","care_plan","progress_note","consent_form","referral_letter","prescription","lab_result","imaging_report","discharge_summary","policy_document","template"]).withMessage('type غير صالح'),
  body('status').optional().isIn(["draft","pending_review","approved","published","archived","expired","revoked","superseded","locked","under_revision"]).withMessage('status غير صالح'),
  body('classification').optional().isIn(["public","internal","confidential","restricted","top_secret","patient_sensitive","legal_privileged","research_only"]).withMessage('classification غير صالح'),
  body('mimeType').optional().trim().isString().withMessage('mimeType يجب أن يكون نصاً'),
  body('fileSize').optional().isFloat().withMessage('fileSize يجب أن يكون رقماً'),
  body('filePath').optional().trim().isString().withMessage('filePath يجب أن يكون نصاً'),
  body('storageType').optional().isIn(["local","cloud_s3","cloud_azure","cloud_gcs","ftp","network_share","database_blob","cdn","encrypted_vault","cold_storage","tape_archive","hybrid"]).withMessage('storageType غير صالح'),
  body('checksum').optional().trim().isString().withMessage('checksum يجب أن يكون نصاً'),
  body('folderId').optional().custom(isObjectId).withMessage('folderId غير صالح'),
  body('tags').optional().isArray().withMessage('tags يجب أن يكون مصفوفة'),
  body('filePath').optional().trim().isString().withMessage('filePath يجب أن يكون نصاً'),
  body('fileSize').optional().isFloat().withMessage('fileSize يجب أن يكون رقماً'),
  body('checksum').optional().trim().isString().withMessage('checksum يجب أن يكون نصاً'),
  body('uploadedBy').optional().custom(isObjectId).withMessage('uploadedBy غير صالح'),
  body('uploadedAt').optional().isISO8601().withMessage('uploadedAt يجب أن يكون تاريخ ISO صالح'),
  body('changeNote').optional().trim().isString().withMessage('changeNote يجب أن يكون نصاً'),
  body('uploadedBy').optional().custom(isObjectId).withMessage('uploadedBy غير صالح'),
  body('ownerId').optional().custom(isObjectId).withMessage('ownerId غير صالح'),
  body('ownerType').optional().isIn(["user","department","beneficiary","system"]).withMessage('ownerType غير صالح'),
  body('relatedEntityId').optional().custom(isObjectId).withMessage('relatedEntityId غير صالح'),
  body('relatedEntityType').optional().trim().isString().withMessage('relatedEntityType يجب أن يكون نصاً'),
  body('retentionDate').optional().isISO8601().withMessage('retentionDate يجب أن يكون تاريخ ISO صالح'),
  body('expiresAt').optional().isISO8601().withMessage('expiresAt يجب أن يكون تاريخ ISO صالح'),
  body('isEncrypted').optional().isBoolean().withMessage('isEncrypted يجب أن يكون قيمة منطقية'),
  body('downloadCount').optional().isFloat().withMessage('downloadCount يجب أن يكون رقماً'),
];

/* ═══ Folder: Create ═══ */
const createFolder = [
  body('folderCode').trim().notEmpty().withMessage('folderCode مطلوب'),
  body('name').trim().notEmpty().withMessage('name مطلوب'),
  body('nameAr').optional().trim().isString().withMessage('nameAr يجب أن يكون نصاً'),
  body('parentId').optional().custom(isObjectId).withMessage('parentId غير صالح'),
  body('path').optional().trim().isString().withMessage('path يجب أن يكون نصاً'),
  body('depth').optional().isFloat().withMessage('depth يجب أن يكون رقماً'),
  body('ownerId').optional().custom(isObjectId).withMessage('ownerId غير صالح'),
  body('isSystem').optional().isBoolean().withMessage('isSystem يجب أن يكون قيمة منطقية'),
];

/* ═══ Folder: Update ═══ */
const updateFolder = [
  body('folderCode').optional().trim().isString().withMessage('folderCode يجب أن يكون نصاً'),
  body('name').optional().trim().isString().withMessage('name يجب أن يكون نصاً'),
  body('nameAr').optional().trim().isString().withMessage('nameAr يجب أن يكون نصاً'),
  body('parentId').optional().custom(isObjectId).withMessage('parentId غير صالح'),
  body('path').optional().trim().isString().withMessage('path يجب أن يكون نصاً'),
  body('depth').optional().isFloat().withMessage('depth يجب أن يكون رقماً'),
  body('ownerId').optional().custom(isObjectId).withMessage('ownerId غير صالح'),
  body('isSystem').optional().isBoolean().withMessage('isSystem يجب أن يكون قيمة منطقية'),
];

/* ═══ DocumentTag: Create ═══ */
const createDocumentTag = [
  body('code').trim().notEmpty().withMessage('code مطلوب'),
  body('name').trim().notEmpty().withMessage('name مطلوب'),
  body('nameAr').optional().trim().isString().withMessage('nameAr يجب أن يكون نصاً'),
  body('color').optional().trim().isString().withMessage('color يجب أن يكون نصاً'),
  body('isActive').optional().isBoolean().withMessage('isActive يجب أن يكون قيمة منطقية'),
];

/* ═══ DocumentTag: Update ═══ */
const updateDocumentTag = [
  body('code').optional().trim().isString().withMessage('code يجب أن يكون نصاً'),
  body('name').optional().trim().isString().withMessage('name يجب أن يكون نصاً'),
  body('nameAr').optional().trim().isString().withMessage('nameAr يجب أن يكون نصاً'),
  body('color').optional().trim().isString().withMessage('color يجب أن يكون نصاً'),
  body('isActive').optional().isBoolean().withMessage('isActive يجب أن يكون قيمة منطقية'),
];

/* ═══ DocumentAccess: Create ═══ */
const createDocumentAccess = [
  body('documentId').custom(isObjectId).withMessage('documentId غير صالح'),
  body('userId').custom(isObjectId).withMessage('userId غير صالح'),
  body('level').isIn(["owner","editor","reviewer","viewer","commenter","downloader"]).withMessage('level غير صالح'),
  body('grantedBy').optional().custom(isObjectId).withMessage('grantedBy غير صالح'),
  body('expiresAt').optional().isISO8601().withMessage('expiresAt يجب أن يكون تاريخ ISO صالح'),
  body('isActive').optional().isBoolean().withMessage('isActive يجب أن يكون قيمة منطقية'),
];

/* ═══ DocumentAccess: Update ═══ */
const updateDocumentAccess = [
  body('documentId').optional().custom(isObjectId).withMessage('documentId غير صالح'),
  body('userId').optional().custom(isObjectId).withMessage('userId غير صالح'),
  body('level').optional().isIn(["owner","editor","reviewer","viewer","commenter","downloader"]).withMessage('level غير صالح'),
  body('grantedBy').optional().custom(isObjectId).withMessage('grantedBy غير صالح'),
  body('expiresAt').optional().isISO8601().withMessage('expiresAt يجب أن يكون تاريخ ISO صالح'),
  body('isActive').optional().isBoolean().withMessage('isActive يجب أن يكون قيمة منطقية'),
];

module.exports = {
  createVaultDocument,
  updateVaultDocument,
  createFolder,
  updateFolder,
  createDocumentTag,
  updateDocumentTag,
  createDocumentAccess,
  updateDocumentAccess,
};