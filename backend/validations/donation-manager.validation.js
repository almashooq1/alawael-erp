'use strict';
/**
 * DonationManager — Auto-Generated Validation Schemas
 * Generated by: scripts/gen-validation-from-model.js
 */

const { body, param } = require('express-validator');

const isObjectId = (value) => /^[a-f\d]{24}$/i.test(value);

/* ═══ Donation: Create ═══ */
const createDonation = [
  body('donationCode').trim().notEmpty().withMessage('donationCode مطلوب'),
  body('donorId').optional().custom(isObjectId).withMessage('donorId غير صالح'),
  body('fundraiserId').optional().custom(isObjectId).withMessage('fundraiserId غير صالح'),
  body('type').isIn(["one_time","recurring","pledge","in_kind","memorial","honorary","matching","corporate","grant","endowment","estate","crowdfunding"]).withMessage('type غير صالح'),
  body('status').optional().isIn(["pending","received","processed","acknowledged","declined","refunded","pledged","partially_paid","cancelled","cleared"]).withMessage('status غير صالح'),
  body('amount').isFloat().withMessage('amount يجب أن يكون رقماً'),
  body('currency').optional().trim().isString().withMessage('currency يجب أن يكون نصاً'),
  body('paymentMethod').optional().isIn(["cash","check","credit_card","debit_card","bank_transfer","wire_transfer","paypal","apple_pay","google_pay","cryptocurrency","stock_transfer","zakat"]).withMessage('paymentMethod غير صالح'),
  body('transactionId').optional().trim().isString().withMessage('transactionId يجب أن يكون نصاً'),
  body('donatedAt').optional().isISO8601().withMessage('donatedAt يجب أن يكون تاريخ ISO صالح'),
  body('isAnonymous').optional().isBoolean().withMessage('isAnonymous يجب أن يكون قيمة منطقية'),
  body('dedication').optional().trim().isString().withMessage('dedication يجب أن يكون نصاً'),
  body('notes').optional().trim().isString().withMessage('notes يجب أن يكون نصاً'),
  body('taxDeductible').optional().isBoolean().withMessage('taxDeductible يجب أن يكون قيمة منطقية'),
  body('receiptIssued').optional().isBoolean().withMessage('receiptIssued يجب أن يكون قيمة منطقية'),
];

/* ═══ Donation: Update ═══ */
const updateDonation = [
  body('donationCode').optional().trim().isString().withMessage('donationCode يجب أن يكون نصاً'),
  body('donorId').optional().custom(isObjectId).withMessage('donorId غير صالح'),
  body('fundraiserId').optional().custom(isObjectId).withMessage('fundraiserId غير صالح'),
  body('type').optional().isIn(["one_time","recurring","pledge","in_kind","memorial","honorary","matching","corporate","grant","endowment","estate","crowdfunding"]).withMessage('type غير صالح'),
  body('status').optional().isIn(["pending","received","processed","acknowledged","declined","refunded","pledged","partially_paid","cancelled","cleared"]).withMessage('status غير صالح'),
  body('amount').optional().isFloat().withMessage('amount يجب أن يكون رقماً'),
  body('currency').optional().trim().isString().withMessage('currency يجب أن يكون نصاً'),
  body('paymentMethod').optional().isIn(["cash","check","credit_card","debit_card","bank_transfer","wire_transfer","paypal","apple_pay","google_pay","cryptocurrency","stock_transfer","zakat"]).withMessage('paymentMethod غير صالح'),
  body('transactionId').optional().trim().isString().withMessage('transactionId يجب أن يكون نصاً'),
  body('donatedAt').optional().isISO8601().withMessage('donatedAt يجب أن يكون تاريخ ISO صالح'),
  body('isAnonymous').optional().isBoolean().withMessage('isAnonymous يجب أن يكون قيمة منطقية'),
  body('dedication').optional().trim().isString().withMessage('dedication يجب أن يكون نصاً'),
  body('notes').optional().trim().isString().withMessage('notes يجب أن يكون نصاً'),
  body('taxDeductible').optional().isBoolean().withMessage('taxDeductible يجب أن يكون قيمة منطقية'),
  body('receiptIssued').optional().isBoolean().withMessage('receiptIssued يجب أن يكون قيمة منطقية'),
];

/* ═══ Donor: Create ═══ */
const createDonor = [
  body('donorCode').trim().notEmpty().withMessage('donorCode مطلوب'),
  body('name').trim().notEmpty().withMessage('name مطلوب'),
  body('nameAr').optional().trim().isString().withMessage('nameAr يجب أن يكون نصاً'),
  body('category').isIn(["individual","corporate","foundation","government","religious_organization","ngo","school","alumni","anonymous","estate","family_trust","employee_group"]).withMessage('category غير صالح'),
  body('email').optional().trim().isString().withMessage('email يجب أن يكون نصاً'),
  body('phone').optional().trim().isString().withMessage('phone يجب أن يكون نصاً'),
  body('organization').optional().trim().isString().withMessage('organization يجب أن يكون نصاً'),
  body('address').optional().trim().isString().withMessage('address يجب أن يكون نصاً'),
  body('totalDonated').optional().isFloat().withMessage('totalDonated يجب أن يكون رقماً'),
  body('donationCount').optional().isFloat().withMessage('donationCount يجب أن يكون رقماً'),
  body('firstDonationDate').optional().isISO8601().withMessage('firstDonationDate يجب أن يكون تاريخ ISO صالح'),
  body('lastDonationDate').optional().isISO8601().withMessage('lastDonationDate يجب أن يكون تاريخ ISO صالح'),
  body('preferredChannel').optional().trim().isString().withMessage('preferredChannel يجب أن يكون نصاً'),
  body('communicationPreference').optional().isIn(["email","phone","mail","sms","none"]).withMessage('communicationPreference غير صالح'),
  body('isActive').optional().isBoolean().withMessage('isActive يجب أن يكون قيمة منطقية'),
];

/* ═══ Donor: Update ═══ */
const updateDonor = [
  body('donorCode').optional().trim().isString().withMessage('donorCode يجب أن يكون نصاً'),
  body('name').optional().trim().isString().withMessage('name يجب أن يكون نصاً'),
  body('nameAr').optional().trim().isString().withMessage('nameAr يجب أن يكون نصاً'),
  body('category').optional().isIn(["individual","corporate","foundation","government","religious_organization","ngo","school","alumni","anonymous","estate","family_trust","employee_group"]).withMessage('category غير صالح'),
  body('email').optional().trim().isString().withMessage('email يجب أن يكون نصاً'),
  body('phone').optional().trim().isString().withMessage('phone يجب أن يكون نصاً'),
  body('organization').optional().trim().isString().withMessage('organization يجب أن يكون نصاً'),
  body('address').optional().trim().isString().withMessage('address يجب أن يكون نصاً'),
  body('totalDonated').optional().isFloat().withMessage('totalDonated يجب أن يكون رقماً'),
  body('donationCount').optional().isFloat().withMessage('donationCount يجب أن يكون رقماً'),
  body('firstDonationDate').optional().isISO8601().withMessage('firstDonationDate يجب أن يكون تاريخ ISO صالح'),
  body('lastDonationDate').optional().isISO8601().withMessage('lastDonationDate يجب أن يكون تاريخ ISO صالح'),
  body('preferredChannel').optional().trim().isString().withMessage('preferredChannel يجب أن يكون نصاً'),
  body('communicationPreference').optional().isIn(["email","phone","mail","sms","none"]).withMessage('communicationPreference غير صالح'),
  body('isActive').optional().isBoolean().withMessage('isActive يجب أن يكون قيمة منطقية'),
];

/* ═══ Fundraiser: Create ═══ */
const createFundraiser = [
  body('code').trim().notEmpty().withMessage('code مطلوب'),
  body('name').trim().notEmpty().withMessage('name مطلوب'),
  body('nameAr').optional().trim().isString().withMessage('nameAr يجب أن يكون نصاً'),
  body('type').isIn(["annual_appeal","capital_campaign","event_fundraiser","crowdfunding","matching_drive","sponsorship","auction","walkathon","gala_dinner","peer_to_peer"]).withMessage('type غير صالح'),
  body('status').optional().isIn(["planning","approved","active","paused","completed","exceeded_goal","below_target","cancelled","archived","evaluation"]).withMessage('status غير صالح'),
  body('goalAmount').optional().isFloat().withMessage('goalAmount يجب أن يكون رقماً'),
  body('raisedAmount').optional().isFloat().withMessage('raisedAmount يجب أن يكون رقماً'),
  body('donorCount').optional().isFloat().withMessage('donorCount يجب أن يكون رقماً'),
  body('startDate').optional().isISO8601().withMessage('startDate يجب أن يكون تاريخ ISO صالح'),
  body('endDate').optional().isISO8601().withMessage('endDate يجب أن يكون تاريخ ISO صالح'),
  body('description').optional().trim().isString().withMessage('description يجب أن يكون نصاً'),
  body('managerId').optional().custom(isObjectId).withMessage('managerId غير صالح'),
  body('isActive').optional().isBoolean().withMessage('isActive يجب أن يكون قيمة منطقية'),
];

/* ═══ Fundraiser: Update ═══ */
const updateFundraiser = [
  body('code').optional().trim().isString().withMessage('code يجب أن يكون نصاً'),
  body('name').optional().trim().isString().withMessage('name يجب أن يكون نصاً'),
  body('nameAr').optional().trim().isString().withMessage('nameAr يجب أن يكون نصاً'),
  body('type').optional().isIn(["annual_appeal","capital_campaign","event_fundraiser","crowdfunding","matching_drive","sponsorship","auction","walkathon","gala_dinner","peer_to_peer"]).withMessage('type غير صالح'),
  body('status').optional().isIn(["planning","approved","active","paused","completed","exceeded_goal","below_target","cancelled","archived","evaluation"]).withMessage('status غير صالح'),
  body('goalAmount').optional().isFloat().withMessage('goalAmount يجب أن يكون رقماً'),
  body('raisedAmount').optional().isFloat().withMessage('raisedAmount يجب أن يكون رقماً'),
  body('donorCount').optional().isFloat().withMessage('donorCount يجب أن يكون رقماً'),
  body('startDate').optional().isISO8601().withMessage('startDate يجب أن يكون تاريخ ISO صالح'),
  body('endDate').optional().isISO8601().withMessage('endDate يجب أن يكون تاريخ ISO صالح'),
  body('description').optional().trim().isString().withMessage('description يجب أن يكون نصاً'),
  body('managerId').optional().custom(isObjectId).withMessage('managerId غير صالح'),
  body('isActive').optional().isBoolean().withMessage('isActive يجب أن يكون قيمة منطقية'),
];

/* ═══ DonationReceipt: Create ═══ */
const createDonationReceipt = [
  body('receiptCode').trim().notEmpty().withMessage('receiptCode مطلوب'),
  body('donationId').custom(isObjectId).withMessage('donationId غير صالح'),
  body('donorId').optional().custom(isObjectId).withMessage('donorId غير صالح'),
  body('amount').isFloat().withMessage('amount يجب أن يكون رقماً'),
  body('currency').optional().trim().isString().withMessage('currency يجب أن يكون نصاً'),
  body('issuedAt').optional().isISO8601().withMessage('issuedAt يجب أن يكون تاريخ ISO صالح'),
  body('taxYear').optional().isFloat().withMessage('taxYear يجب أن يكون رقماً'),
  body('receiptUrl').optional().trim().isString().withMessage('receiptUrl يجب أن يكون نصاً'),
  body('emailSent').optional().isBoolean().withMessage('emailSent يجب أن يكون قيمة منطقية'),
  body('emailSentAt').optional().isISO8601().withMessage('emailSentAt يجب أن يكون تاريخ ISO صالح'),
  body('notes').optional().trim().isString().withMessage('notes يجب أن يكون نصاً'),
];

/* ═══ DonationReceipt: Update ═══ */
const updateDonationReceipt = [
  body('receiptCode').optional().trim().isString().withMessage('receiptCode يجب أن يكون نصاً'),
  body('donationId').optional().custom(isObjectId).withMessage('donationId غير صالح'),
  body('donorId').optional().custom(isObjectId).withMessage('donorId غير صالح'),
  body('amount').optional().isFloat().withMessage('amount يجب أن يكون رقماً'),
  body('currency').optional().trim().isString().withMessage('currency يجب أن يكون نصاً'),
  body('issuedAt').optional().isISO8601().withMessage('issuedAt يجب أن يكون تاريخ ISO صالح'),
  body('taxYear').optional().isFloat().withMessage('taxYear يجب أن يكون رقماً'),
  body('receiptUrl').optional().trim().isString().withMessage('receiptUrl يجب أن يكون نصاً'),
  body('emailSent').optional().isBoolean().withMessage('emailSent يجب أن يكون قيمة منطقية'),
  body('emailSentAt').optional().isISO8601().withMessage('emailSentAt يجب أن يكون تاريخ ISO صالح'),
  body('notes').optional().trim().isString().withMessage('notes يجب أن يكون نصاً'),
];

module.exports = {
  createDonation,
  updateDonation,
  createDonor,
  updateDonor,
  createFundraiser,
  updateFundraiser,
  createDonationReceipt,
  updateDonationReceipt,
};