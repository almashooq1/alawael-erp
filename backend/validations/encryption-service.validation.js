'use strict';
/**
 * EncryptionService — Auto-Generated Validation Schemas
 * Generated by: scripts/gen-validation-from-model.js
 */

const { body, param } = require('express-validator');

const isObjectId = (value) => /^[a-f\d]{24}$/i.test(value);

/* ═══ EncryptionKey: Create ═══ */
const createEncryptionKey = [
  body('keyId').trim().notEmpty().withMessage('keyId مطلوب'),
  body('algorithm').optional().trim().isString().withMessage('algorithm يجب أن يكون نصاً'),
  body('encryptedKey').trim().notEmpty().withMessage('encryptedKey مطلوب'),
  body('status').optional().isIn(["active","rotated","revoked","expired"]).withMessage('status غير صالح'),
  body('purpose').optional().isIn(["field-encryption","token-encryption","backup-encryption","transport"]).withMessage('purpose غير صالح'),
  body('createdBy').optional().custom(isObjectId).withMessage('createdBy غير صالح'),
  body('rotatedAt').optional().isISO8601().withMessage('rotatedAt يجب أن يكون تاريخ ISO صالح'),
  body('expiresAt').optional().isISO8601().withMessage('expiresAt يجب أن يكون تاريخ ISO صالح'),
  body('isDeleted').optional().isBoolean().withMessage('isDeleted يجب أن يكون قيمة منطقية'),
];

/* ═══ EncryptionKey: Update ═══ */
const updateEncryptionKey = [
  body('keyId').optional().trim().isString().withMessage('keyId يجب أن يكون نصاً'),
  body('algorithm').optional().trim().isString().withMessage('algorithm يجب أن يكون نصاً'),
  body('encryptedKey').optional().trim().isString().withMessage('encryptedKey يجب أن يكون نصاً'),
  body('status').optional().isIn(["active","rotated","revoked","expired"]).withMessage('status غير صالح'),
  body('purpose').optional().isIn(["field-encryption","token-encryption","backup-encryption","transport"]).withMessage('purpose غير صالح'),
  body('createdBy').optional().custom(isObjectId).withMessage('createdBy غير صالح'),
  body('rotatedAt').optional().isISO8601().withMessage('rotatedAt يجب أن يكون تاريخ ISO صالح'),
  body('expiresAt').optional().isISO8601().withMessage('expiresAt يجب أن يكون تاريخ ISO صالح'),
  body('isDeleted').optional().isBoolean().withMessage('isDeleted يجب أن يكون قيمة منطقية'),
];

module.exports = {
  createEncryptionKey,
  updateEncryptionKey,
};