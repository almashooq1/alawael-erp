'use strict';
/**
 * FeatureFlags — Auto-Generated Validation Schemas
 * Generated by: scripts/gen-validation-from-model.js
 */

const { body, param } = require('express-validator');

const isObjectId = (value) => /^[a-f\d]{24}$/i.test(value);

/* ═══ FeatureFlag: Create ═══ */
const createFeatureFlag = [
  body('key').trim().notEmpty().withMessage('key مطلوب'),
  body('name').trim().notEmpty().withMessage('name مطلوب'),
  body('description').optional().trim().isString().withMessage('description يجب أن يكون نصاً'),
  body('category').optional().isIn(["domain","feature","experiment","operational","ui","integration"]).withMessage('category غير صالح'),
  body('enabled').optional().isBoolean().withMessage('enabled يجب أن يكون قيمة منطقية'),
  body('percentage').optional().isFloat({ min: 0, max: 100 }).withMessage('percentage يجب أن يكون بين 0 و 100'),
  body('targeting').optional().custom(isObjectId).withMessage('targeting غير صالح'),
  body('key').trim().notEmpty().withMessage('key مطلوب'),
  body('name').optional().trim().isString().withMessage('name يجب أن يكون نصاً'),
  body('weight').optional().isFloat().withMessage('weight يجب أن يكون رقماً'),
  body('startDate').optional().isISO8601().withMessage('startDate يجب أن يكون تاريخ ISO صالح'),
  body('endDate').optional().isISO8601().withMessage('endDate يجب أن يكون تاريخ ISO صالح'),
  body('owner').optional().trim().isString().withMessage('owner يجب أن يكون نصاً'),
  body('isDeleted').optional().isBoolean().withMessage('isDeleted يجب أن يكون قيمة منطقية'),
];

/* ═══ FeatureFlag: Update ═══ */
const updateFeatureFlag = [
  body('key').optional().trim().isString().withMessage('key يجب أن يكون نصاً'),
  body('name').optional().trim().isString().withMessage('name يجب أن يكون نصاً'),
  body('description').optional().trim().isString().withMessage('description يجب أن يكون نصاً'),
  body('category').optional().isIn(["domain","feature","experiment","operational","ui","integration"]).withMessage('category غير صالح'),
  body('enabled').optional().isBoolean().withMessage('enabled يجب أن يكون قيمة منطقية'),
  body('percentage').optional().isFloat({ min: 0, max: 100 }).withMessage('percentage يجب أن يكون بين 0 و 100'),
  body('targeting').optional().custom(isObjectId).withMessage('targeting غير صالح'),
  body('key').optional().trim().isString().withMessage('key يجب أن يكون نصاً'),
  body('name').optional().trim().isString().withMessage('name يجب أن يكون نصاً'),
  body('weight').optional().isFloat().withMessage('weight يجب أن يكون رقماً'),
  body('startDate').optional().isISO8601().withMessage('startDate يجب أن يكون تاريخ ISO صالح'),
  body('endDate').optional().isISO8601().withMessage('endDate يجب أن يكون تاريخ ISO صالح'),
  body('owner').optional().trim().isString().withMessage('owner يجب أن يكون نصاً'),
  body('isDeleted').optional().isBoolean().withMessage('isDeleted يجب أن يكون قيمة منطقية'),
];

/* ═══ FlagAudit: Create ═══ */
const createFlagAudit = [
  body('flagKey').trim().notEmpty().withMessage('flagKey مطلوب'),
  body('action').isIn(["created","updated","enabled","disabled","deleted","rollout-changed","targeting-changed"]).withMessage('action غير صالح'),
  body('changedBy').optional().custom(isObjectId).withMessage('changedBy غير صالح'),
  body('reason').optional().trim().isString().withMessage('reason يجب أن يكون نصاً'),
  body('isDeleted').optional().isBoolean().withMessage('isDeleted يجب أن يكون قيمة منطقية'),
];

/* ═══ FlagAudit: Update ═══ */
const updateFlagAudit = [
  body('flagKey').optional().trim().isString().withMessage('flagKey يجب أن يكون نصاً'),
  body('action').optional().isIn(["created","updated","enabled","disabled","deleted","rollout-changed","targeting-changed"]).withMessage('action غير صالح'),
  body('changedBy').optional().custom(isObjectId).withMessage('changedBy غير صالح'),
  body('reason').optional().trim().isString().withMessage('reason يجب أن يكون نصاً'),
  body('isDeleted').optional().isBoolean().withMessage('isDeleted يجب أن يكون قيمة منطقية'),
];

module.exports = {
  createFeatureFlag,
  updateFeatureFlag,
  createFlagAudit,
  updateFlagAudit,
};