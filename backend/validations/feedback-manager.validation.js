'use strict';
/**
 * FeedbackManager — Auto-Generated Validation Schemas
 * Generated by: scripts/gen-validation-from-model.js
 */

const { body, param } = require('express-validator');

const isObjectId = (value) => /^[a-f\d]{24}$/i.test(value);

/* ═══ Feedback: Create ═══ */
const createFeedback = [
  body('feedbackId').trim().notEmpty().withMessage('feedbackId مطلوب'),
  body('beneficiaryId').optional().custom(isObjectId).withMessage('beneficiaryId غير صالح'),
  body('episodeId').optional().custom(isObjectId).withMessage('episodeId غير صالح'),
  body('type').isIn(["service_rating","therapist_rating","facility_rating","program_evaluation","session_feedback","discharge_feedback","complaint","suggestion","compliment","accessibility_feedback","communication_feedback","general"]).withMessage('type غير صالح'),
  body('status').optional().isIn(["submitted","under_review","acknowledged","in_progress","resolved","closed","escalated","archived","rejected","pending"]).withMessage('status غير صالح'),
  body('subject').trim().notEmpty().withMessage('subject مطلوب'),
  body('description').optional().trim().isString().withMessage('description يجب أن يكون نصاً'),
  body('rating').optional().isFloat({ min: 1, max: 5 }).withMessage('rating يجب أن يكون بين 1 و 5'),
  body('category').optional().isIn(["overall","therapist","facility","communication","timeliness","cleanliness","professionalism","empathy","outcome","accessibility"]).withMessage('category غير صالح'),
  body('anonymous').optional().isBoolean().withMessage('anonymous يجب أن يكون قيمة منطقية'),
  body('attachments').optional().isArray().withMessage('attachments يجب أن يكون مصفوفة'),
  body('response').optional().trim().isString().withMessage('response يجب أن يكون نصاً'),
  body('respondedBy').optional().custom(isObjectId).withMessage('respondedBy غير صالح'),
  body('respondedAt').optional().isISO8601().withMessage('respondedAt يجب أن يكون تاريخ ISO صالح'),
];

/* ═══ Feedback: Update ═══ */
const updateFeedback = [
  body('feedbackId').optional().trim().isString().withMessage('feedbackId يجب أن يكون نصاً'),
  body('beneficiaryId').optional().custom(isObjectId).withMessage('beneficiaryId غير صالح'),
  body('episodeId').optional().custom(isObjectId).withMessage('episodeId غير صالح'),
  body('type').optional().isIn(["service_rating","therapist_rating","facility_rating","program_evaluation","session_feedback","discharge_feedback","complaint","suggestion","compliment","accessibility_feedback","communication_feedback","general"]).withMessage('type غير صالح'),
  body('status').optional().isIn(["submitted","under_review","acknowledged","in_progress","resolved","closed","escalated","archived","rejected","pending"]).withMessage('status غير صالح'),
  body('subject').optional().trim().isString().withMessage('subject يجب أن يكون نصاً'),
  body('description').optional().trim().isString().withMessage('description يجب أن يكون نصاً'),
  body('rating').optional().isFloat({ min: 1, max: 5 }).withMessage('rating يجب أن يكون بين 1 و 5'),
  body('category').optional().isIn(["overall","therapist","facility","communication","timeliness","cleanliness","professionalism","empathy","outcome","accessibility"]).withMessage('category غير صالح'),
  body('anonymous').optional().isBoolean().withMessage('anonymous يجب أن يكون قيمة منطقية'),
  body('attachments').optional().isArray().withMessage('attachments يجب أن يكون مصفوفة'),
  body('response').optional().trim().isString().withMessage('response يجب أن يكون نصاً'),
  body('respondedBy').optional().custom(isObjectId).withMessage('respondedBy غير صالح'),
  body('respondedAt').optional().isISO8601().withMessage('respondedAt يجب أن يكون تاريخ ISO صالح'),
];

/* ═══ Survey: Create ═══ */
const createSurvey = [
  body('surveyId').trim().notEmpty().withMessage('surveyId مطلوب'),
  body('title').trim().notEmpty().withMessage('title مطلوب'),
  body('description').optional().trim().isString().withMessage('description يجب أن يكون نصاً'),
  body('type').isIn(["satisfaction","nps","csat","ces","clinical_outcome","experience","accessibility","communication","discharge","follow_up","annual","custom"]).withMessage('type غير صالح'),
  body('status').optional().isIn(["draft","active","paused","completed","archived","scheduled","expired","cancelled","in_review","published"]).withMessage('status غير صالح'),
  body('questionId').optional().trim().isString().withMessage('questionId يجب أن يكون نصاً'),
  body('text').optional().trim().isString().withMessage('text يجب أن يكون نصاً'),
  body('type').optional().isIn(["likert_scale","multiple_choice","open_text","yes_no","rating_stars","nps_scale","ranking","matrix","slider","date_picker","file_upload","dropdown"]).withMessage('type غير صالح'),
  body('required').optional().isBoolean().withMessage('required يجب أن يكون قيمة منطقية'),
  body('order').optional().isFloat().withMessage('order يجب أن يكون رقماً'),
  body('targetAudience').optional().trim().isString().withMessage('targetAudience يجب أن يكون نصاً'),
  body('startDate').optional().isISO8601().withMessage('startDate يجب أن يكون تاريخ ISO صالح'),
  body('endDate').optional().isISO8601().withMessage('endDate يجب أن يكون تاريخ ISO صالح'),
  body('createdBy').optional().custom(isObjectId).withMessage('createdBy غير صالح'),
];

/* ═══ Survey: Update ═══ */
const updateSurvey = [
  body('surveyId').optional().trim().isString().withMessage('surveyId يجب أن يكون نصاً'),
  body('title').optional().trim().isString().withMessage('title يجب أن يكون نصاً'),
  body('description').optional().trim().isString().withMessage('description يجب أن يكون نصاً'),
  body('type').optional().isIn(["satisfaction","nps","csat","ces","clinical_outcome","experience","accessibility","communication","discharge","follow_up","annual","custom"]).withMessage('type غير صالح'),
  body('status').optional().isIn(["draft","active","paused","completed","archived","scheduled","expired","cancelled","in_review","published"]).withMessage('status غير صالح'),
  body('questionId').optional().trim().isString().withMessage('questionId يجب أن يكون نصاً'),
  body('text').optional().trim().isString().withMessage('text يجب أن يكون نصاً'),
  body('type').optional().isIn(["likert_scale","multiple_choice","open_text","yes_no","rating_stars","nps_scale","ranking","matrix","slider","date_picker","file_upload","dropdown"]).withMessage('type غير صالح'),
  body('required').optional().isBoolean().withMessage('required يجب أن يكون قيمة منطقية'),
  body('order').optional().isFloat().withMessage('order يجب أن يكون رقماً'),
  body('targetAudience').optional().trim().isString().withMessage('targetAudience يجب أن يكون نصاً'),
  body('startDate').optional().isISO8601().withMessage('startDate يجب أن يكون تاريخ ISO صالح'),
  body('endDate').optional().isISO8601().withMessage('endDate يجب أن يكون تاريخ ISO صالح'),
  body('createdBy').optional().custom(isObjectId).withMessage('createdBy غير صالح'),
];

/* ═══ SurveyResponse: Create ═══ */
const createSurveyResponse = [
  body('responseId').trim().notEmpty().withMessage('responseId مطلوب'),
  body('surveyId').optional().custom(isObjectId).withMessage('surveyId غير صالح'),
  body('beneficiaryId').optional().custom(isObjectId).withMessage('beneficiaryId غير صالح'),
  body('questionId').optional().trim().isString().withMessage('questionId يجب أن يكون نصاً'),
  body('comment').optional().trim().isString().withMessage('comment يجب أن يكون نصاً'),
  body('timeTaken').optional().isFloat().withMessage('timeTaken يجب أن يكون رقماً'),
  body('anonymous').optional().isBoolean().withMessage('anonymous يجب أن يكون قيمة منطقية'),
];

/* ═══ SurveyResponse: Update ═══ */
const updateSurveyResponse = [
  body('responseId').optional().trim().isString().withMessage('responseId يجب أن يكون نصاً'),
  body('surveyId').optional().custom(isObjectId).withMessage('surveyId غير صالح'),
  body('beneficiaryId').optional().custom(isObjectId).withMessage('beneficiaryId غير صالح'),
  body('questionId').optional().trim().isString().withMessage('questionId يجب أن يكون نصاً'),
  body('comment').optional().trim().isString().withMessage('comment يجب أن يكون نصاً'),
  body('timeTaken').optional().isFloat().withMessage('timeTaken يجب أن يكون رقماً'),
  body('anonymous').optional().isBoolean().withMessage('anonymous يجب أن يكون قيمة منطقية'),
];

/* ═══ FeedbackAnalytics: Create ═══ */
const createFeedbackAnalytics = [
  body('analyticsId').trim().notEmpty().withMessage('analyticsId مطلوب'),
  body('period').trim().notEmpty().withMessage('period مطلوب'),
  body('periodStart').isISO8601().withMessage('periodStart يجب أن يكون تاريخ ISO صالح'),
  body('periodEnd').isISO8601().withMessage('periodEnd يجب أن يكون تاريخ ISO صالح'),
  body('totalFeedbacks').optional().isFloat().withMessage('totalFeedbacks يجب أن يكون رقماً'),
  body('averageRating').optional().isFloat().withMessage('averageRating يجب أن يكون رقماً'),
  body('ratingDistribution').optional().isFloat().withMessage('ratingDistribution يجب أن يكون رقماً'),
  body('sentimentBreakdown').optional().isFloat().withMessage('sentimentBreakdown يجب أن يكون رقماً'),
  body('topIssues').optional().isArray().withMessage('topIssues يجب أن يكون مصفوفة'),
];

/* ═══ FeedbackAnalytics: Update ═══ */
const updateFeedbackAnalytics = [
  body('analyticsId').optional().trim().isString().withMessage('analyticsId يجب أن يكون نصاً'),
  body('period').optional().trim().isString().withMessage('period يجب أن يكون نصاً'),
  body('periodStart').optional().isISO8601().withMessage('periodStart يجب أن يكون تاريخ ISO صالح'),
  body('periodEnd').optional().isISO8601().withMessage('periodEnd يجب أن يكون تاريخ ISO صالح'),
  body('totalFeedbacks').optional().isFloat().withMessage('totalFeedbacks يجب أن يكون رقماً'),
  body('averageRating').optional().isFloat().withMessage('averageRating يجب أن يكون رقماً'),
  body('ratingDistribution').optional().isFloat().withMessage('ratingDistribution يجب أن يكون رقماً'),
  body('sentimentBreakdown').optional().isFloat().withMessage('sentimentBreakdown يجب أن يكون رقماً'),
  body('topIssues').optional().isArray().withMessage('topIssues يجب أن يكون مصفوفة'),
];

module.exports = {
  createFeedback,
  updateFeedback,
  createSurvey,
  updateSurvey,
  createSurveyResponse,
  updateSurveyResponse,
  createFeedbackAnalytics,
  updateFeedbackAnalytics,
};