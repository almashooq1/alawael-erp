'use strict';
/**
 * FleetTracker — Auto-Generated Validation Schemas
 * Generated by: scripts/gen-validation-from-model.js
 */

const { body, param } = require('express-validator');

const isObjectId = (value) => /^[a-f\d]{24}$/i.test(value);

/* ═══ FuelLog: Create ═══ */
const createFuelLog = [
  body('logCode').trim().notEmpty().withMessage('logCode مطلوب'),
  body('vehicleId').custom(isObjectId).withMessage('vehicleId غير صالح'),
  body('driverId').optional().custom(isObjectId).withMessage('driverId غير صالح'),
  body('fuelType').isIn(["gasoline_91","gasoline_95","diesel","premium_diesel","electric_charge","hybrid_fuel","cng","lpg","hydrogen","biodiesel","ethanol","flex_fuel"]).withMessage('fuelType غير صالح'),
  body('liters').isFloat().withMessage('liters يجب أن يكون رقماً'),
  body('costPerLiter').optional().isFloat().withMessage('costPerLiter يجب أن يكون رقماً'),
  body('totalCost').optional().isFloat().withMessage('totalCost يجب أن يكون رقماً'),
  body('odometerReading').optional().isFloat().withMessage('odometerReading يجب أن يكون رقماً'),
  body('station').optional().trim().isString().withMessage('station يجب أن يكون نصاً'),
  body('filledAt').optional().isISO8601().withMessage('filledAt يجب أن يكون تاريخ ISO صالح'),
  body('fullTank').optional().isBoolean().withMessage('fullTank يجب أن يكون قيمة منطقية'),
  body('receiptUrl').optional().trim().isString().withMessage('receiptUrl يجب أن يكون نصاً'),
];

/* ═══ FuelLog: Update ═══ */
const updateFuelLog = [
  body('logCode').optional().trim().isString().withMessage('logCode يجب أن يكون نصاً'),
  body('vehicleId').optional().custom(isObjectId).withMessage('vehicleId غير صالح'),
  body('driverId').optional().custom(isObjectId).withMessage('driverId غير صالح'),
  body('fuelType').optional().isIn(["gasoline_91","gasoline_95","diesel","premium_diesel","electric_charge","hybrid_fuel","cng","lpg","hydrogen","biodiesel","ethanol","flex_fuel"]).withMessage('fuelType غير صالح'),
  body('liters').optional().isFloat().withMessage('liters يجب أن يكون رقماً'),
  body('costPerLiter').optional().isFloat().withMessage('costPerLiter يجب أن يكون رقماً'),
  body('totalCost').optional().isFloat().withMessage('totalCost يجب أن يكون رقماً'),
  body('odometerReading').optional().isFloat().withMessage('odometerReading يجب أن يكون رقماً'),
  body('station').optional().trim().isString().withMessage('station يجب أن يكون نصاً'),
  body('filledAt').optional().isISO8601().withMessage('filledAt يجب أن يكون تاريخ ISO صالح'),
  body('fullTank').optional().isBoolean().withMessage('fullTank يجب أن يكون قيمة منطقية'),
  body('receiptUrl').optional().trim().isString().withMessage('receiptUrl يجب أن يكون نصاً'),
];

/* ═══ GPSTracking: Create ═══ */
const createGPSTracking = [
  body('vehicleId').custom(isObjectId).withMessage('vehicleId غير صالح'),
  body('driverId').optional().custom(isObjectId).withMessage('driverId غير صالح'),
  body('latitude').isFloat().withMessage('latitude يجب أن يكون رقماً'),
  body('longitude').isFloat().withMessage('longitude يجب أن يكون رقماً'),
  body('speed').optional().isFloat().withMessage('speed يجب أن يكون رقماً'),
  body('heading').optional().isFloat().withMessage('heading يجب أن يكون رقماً'),
  body('altitude').optional().isFloat().withMessage('altitude يجب أن يكون رقماً'),
  body('accuracy').optional().isFloat().withMessage('accuracy يجب أن يكون رقماً'),
  body('event').optional().isIn(["ignition_on","ignition_off","moving","stopped","speeding","hard_brake","hard_acceleration","sharp_turn","geofence_enter","geofence_exit","idle","tow_alert"]).withMessage('event غير صالح'),
  body('address').optional().trim().isString().withMessage('address يجب أن يكون نصاً'),
  body('timestamp').optional().isISO8601().withMessage('timestamp يجب أن يكون تاريخ ISO صالح'),
];

/* ═══ GPSTracking: Update ═══ */
const updateGPSTracking = [
  body('vehicleId').optional().custom(isObjectId).withMessage('vehicleId غير صالح'),
  body('driverId').optional().custom(isObjectId).withMessage('driverId غير صالح'),
  body('latitude').optional().isFloat().withMessage('latitude يجب أن يكون رقماً'),
  body('longitude').optional().isFloat().withMessage('longitude يجب أن يكون رقماً'),
  body('speed').optional().isFloat().withMessage('speed يجب أن يكون رقماً'),
  body('heading').optional().isFloat().withMessage('heading يجب أن يكون رقماً'),
  body('altitude').optional().isFloat().withMessage('altitude يجب أن يكون رقماً'),
  body('accuracy').optional().isFloat().withMessage('accuracy يجب أن يكون رقماً'),
  body('event').optional().isIn(["ignition_on","ignition_off","moving","stopped","speeding","hard_brake","hard_acceleration","sharp_turn","geofence_enter","geofence_exit","idle","tow_alert"]).withMessage('event غير صالح'),
  body('address').optional().trim().isString().withMessage('address يجب أن يكون نصاً'),
  body('timestamp').optional().isISO8601().withMessage('timestamp يجب أن يكون تاريخ ISO صالح'),
];

/* ═══ VehicleMaintenance: Create ═══ */
const createVehicleMaintenance = [
  body('maintenanceCode').trim().notEmpty().withMessage('maintenanceCode مطلوب'),
  body('vehicleId').custom(isObjectId).withMessage('vehicleId غير صالح'),
  body('category').isIn(["oil_change","tire_rotation","brake_service","engine_repair","transmission","electrical","ac_heating","body_work","suspension","exhaust","battery","general_inspection"]).withMessage('category غير صالح'),
  body('status').optional().isIn(["scheduled","in_progress","completed","deferred","cancelled","awaiting_parts","under_review","approved","warranty_claim","outsourced"]).withMessage('status غير صالح'),
  body('description').optional().trim().isString().withMessage('description يجب أن يكون نصاً'),
  body('scheduledDate').optional().isISO8601().withMessage('scheduledDate يجب أن يكون تاريخ ISO صالح'),
  body('startedAt').optional().isISO8601().withMessage('startedAt يجب أن يكون تاريخ ISO صالح'),
  body('odometerAtService').optional().isFloat().withMessage('odometerAtService يجب أن يكون رقماً'),
  body('cost').optional().isFloat().withMessage('cost يجب أن يكون رقماً'),
  body('vendor').optional().trim().isString().withMessage('vendor يجب أن يكون نصاً'),
  body('parts').optional().isArray().withMessage('parts يجب أن يكون مصفوفة'),
  body('laborHours').optional().isFloat().withMessage('laborHours يجب أن يكون رقماً'),
  body('notes').optional().trim().isString().withMessage('notes يجب أن يكون نصاً'),
  body('nextServiceDate').optional().isISO8601().withMessage('nextServiceDate يجب أن يكون تاريخ ISO صالح'),
  body('nextServiceKm').optional().isFloat().withMessage('nextServiceKm يجب أن يكون رقماً'),
  body('performedBy').optional().custom(isObjectId).withMessage('performedBy غير صالح'),
  body('approvedBy').optional().custom(isObjectId).withMessage('approvedBy غير صالح'),
];

/* ═══ VehicleMaintenance: Update ═══ */
const updateVehicleMaintenance = [
  body('maintenanceCode').optional().trim().isString().withMessage('maintenanceCode يجب أن يكون نصاً'),
  body('vehicleId').optional().custom(isObjectId).withMessage('vehicleId غير صالح'),
  body('category').optional().isIn(["oil_change","tire_rotation","brake_service","engine_repair","transmission","electrical","ac_heating","body_work","suspension","exhaust","battery","general_inspection"]).withMessage('category غير صالح'),
  body('status').optional().isIn(["scheduled","in_progress","completed","deferred","cancelled","awaiting_parts","under_review","approved","warranty_claim","outsourced"]).withMessage('status غير صالح'),
  body('description').optional().trim().isString().withMessage('description يجب أن يكون نصاً'),
  body('scheduledDate').optional().isISO8601().withMessage('scheduledDate يجب أن يكون تاريخ ISO صالح'),
  body('startedAt').optional().isISO8601().withMessage('startedAt يجب أن يكون تاريخ ISO صالح'),
  body('odometerAtService').optional().isFloat().withMessage('odometerAtService يجب أن يكون رقماً'),
  body('cost').optional().isFloat().withMessage('cost يجب أن يكون رقماً'),
  body('vendor').optional().trim().isString().withMessage('vendor يجب أن يكون نصاً'),
  body('parts').optional().isArray().withMessage('parts يجب أن يكون مصفوفة'),
  body('laborHours').optional().isFloat().withMessage('laborHours يجب أن يكون رقماً'),
  body('notes').optional().trim().isString().withMessage('notes يجب أن يكون نصاً'),
  body('nextServiceDate').optional().isISO8601().withMessage('nextServiceDate يجب أن يكون تاريخ ISO صالح'),
  body('nextServiceKm').optional().isFloat().withMessage('nextServiceKm يجب أن يكون رقماً'),
  body('performedBy').optional().custom(isObjectId).withMessage('performedBy غير صالح'),
  body('approvedBy').optional().custom(isObjectId).withMessage('approvedBy غير صالح'),
];

/* ═══ VehicleInspection: Create ═══ */
const createVehicleInspection = [
  body('inspectionCode').trim().notEmpty().withMessage('inspectionCode مطلوب'),
  body('vehicleId').custom(isObjectId).withMessage('vehicleId غير صالح'),
  body('driverId').optional().custom(isObjectId).withMessage('driverId غير صالح'),
  body('type').isIn(["pre_trip","post_trip","daily","weekly","monthly","annual","post_incident","random","regulatory","safety_recall"]).withMessage('type غير صالح'),
  body('status').optional().isIn(["pending","in_progress","passed","failed","conditional_pass","needs_repair","completed","cancelled","overdue","scheduled"]).withMessage('status غير صالح'),
  body('scheduledDate').optional().isISO8601().withMessage('scheduledDate يجب أن يكون تاريخ ISO صالح'),
  body('item').optional().trim().isString().withMessage('item يجب أن يكون نصاً'),
  body('status').optional().isIn(["pass","fail","na","needs_attention"]).withMessage('status غير صالح'),
  body('notes').optional().trim().isString().withMessage('notes يجب أن يكون نصاً'),
  body('overallResult').optional().isIn(["pass","fail","conditional"]).withMessage('overallResult غير صالح'),
  body('odometerReading').optional().isFloat().withMessage('odometerReading يجب أن يكون رقماً'),
  body('findings').optional().isArray().withMessage('findings يجب أن يكون مصفوفة'),
  body('photos').optional().isArray().withMessage('photos يجب أن يكون مصفوفة'),
  body('inspectedBy').optional().custom(isObjectId).withMessage('inspectedBy غير صالح'),
];

/* ═══ VehicleInspection: Update ═══ */
const updateVehicleInspection = [
  body('inspectionCode').optional().trim().isString().withMessage('inspectionCode يجب أن يكون نصاً'),
  body('vehicleId').optional().custom(isObjectId).withMessage('vehicleId غير صالح'),
  body('driverId').optional().custom(isObjectId).withMessage('driverId غير صالح'),
  body('type').optional().isIn(["pre_trip","post_trip","daily","weekly","monthly","annual","post_incident","random","regulatory","safety_recall"]).withMessage('type غير صالح'),
  body('status').optional().isIn(["pending","in_progress","passed","failed","conditional_pass","needs_repair","completed","cancelled","overdue","scheduled"]).withMessage('status غير صالح'),
  body('scheduledDate').optional().isISO8601().withMessage('scheduledDate يجب أن يكون تاريخ ISO صالح'),
  body('item').optional().trim().isString().withMessage('item يجب أن يكون نصاً'),
  body('status').optional().isIn(["pass","fail","na","needs_attention"]).withMessage('status غير صالح'),
  body('notes').optional().trim().isString().withMessage('notes يجب أن يكون نصاً'),
  body('overallResult').optional().isIn(["pass","fail","conditional"]).withMessage('overallResult غير صالح'),
  body('odometerReading').optional().isFloat().withMessage('odometerReading يجب أن يكون رقماً'),
  body('findings').optional().isArray().withMessage('findings يجب أن يكون مصفوفة'),
  body('photos').optional().isArray().withMessage('photos يجب أن يكون مصفوفة'),
  body('inspectedBy').optional().custom(isObjectId).withMessage('inspectedBy غير صالح'),
];

module.exports = {
  createFuelLog,
  updateFuelLog,
  createGPSTracking,
  updateGPSTracking,
  createVehicleMaintenance,
  updateVehicleMaintenance,
  createVehicleInspection,
  updateVehicleInspection,
};