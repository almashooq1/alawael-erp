'use strict';
/**
 * InteroperabilityGateway — Auto-Generated Validation Schemas
 * Generated by: scripts/gen-validation-from-model.js
 */

const { body, param } = require('express-validator');

const isObjectId = (value) => /^[a-f\d]{24}$/i.test(value);

/* ═══ IntegrationLog: Create ═══ */
const createIntegrationLog = [
  body('direction').isIn(["inbound","outbound"]).withMessage('direction غير صالح'),
  body('protocol').isIn(["fhir-r4","hl7-v2","custom-api","bulk-ndjson"]).withMessage('protocol غير صالح'),
  body('resourceType').optional().trim().isString().withMessage('resourceType يجب أن يكون نصاً'),
  body('operation').isIn(["read","search","create","update","export","import","capability"]).withMessage('operation غير صالح'),
  body('status').optional().isIn(["success","partial","failed","pending"]).withMessage('status غير صالح'),
  body('endpoint').optional().trim().isString().withMessage('endpoint يجب أن يكون نصاً'),
  body('requestSize').optional().isFloat().withMessage('requestSize يجب أن يكون رقماً'),
  body('responseSize').optional().isFloat().withMessage('responseSize يجب أن يكون رقماً'),
  body('recordCount').optional().isFloat().withMessage('recordCount يجب أن يكون رقماً'),
  body('durationMs').optional().isFloat().withMessage('durationMs يجب أن يكون رقماً'),
  body('error').optional().trim().isString().withMessage('error يجب أن يكون نصاً'),
  body('isDeleted').optional().isBoolean().withMessage('isDeleted يجب أن يكون قيمة منطقية'),
];

/* ═══ IntegrationLog: Update ═══ */
const updateIntegrationLog = [
  body('direction').optional().isIn(["inbound","outbound"]).withMessage('direction غير صالح'),
  body('protocol').optional().isIn(["fhir-r4","hl7-v2","custom-api","bulk-ndjson"]).withMessage('protocol غير صالح'),
  body('resourceType').optional().trim().isString().withMessage('resourceType يجب أن يكون نصاً'),
  body('operation').optional().isIn(["read","search","create","update","export","import","capability"]).withMessage('operation غير صالح'),
  body('status').optional().isIn(["success","partial","failed","pending"]).withMessage('status غير صالح'),
  body('endpoint').optional().trim().isString().withMessage('endpoint يجب أن يكون نصاً'),
  body('requestSize').optional().isFloat().withMessage('requestSize يجب أن يكون رقماً'),
  body('responseSize').optional().isFloat().withMessage('responseSize يجب أن يكون رقماً'),
  body('recordCount').optional().isFloat().withMessage('recordCount يجب أن يكون رقماً'),
  body('durationMs').optional().isFloat().withMessage('durationMs يجب أن يكون رقماً'),
  body('error').optional().trim().isString().withMessage('error يجب أن يكون نصاً'),
  body('isDeleted').optional().isBoolean().withMessage('isDeleted يجب أن يكون قيمة منطقية'),
];

module.exports = {
  createIntegrationLog,
  updateIntegrationLog,
};