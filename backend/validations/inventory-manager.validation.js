'use strict';
/**
 * InventoryManager — Auto-Generated Validation Schemas
 * Generated by: scripts/gen-validation-from-model.js
 */

const { body, param } = require('express-validator');

const isObjectId = (value) => /^[a-f\d]{24}$/i.test(value);

/* ═══ InventoryItem: Create ═══ */
const createInventoryItem = [
  body('sku').trim().notEmpty().withMessage('sku مطلوب'),
  body('name').trim().notEmpty().withMessage('name مطلوب'),
  body('nameAr').optional().trim().isString().withMessage('nameAr يجب أن يكون نصاً'),
  body('description').optional().trim().isString().withMessage('description يجب أن يكون نصاً'),
  body('category').isIn(["medical_supplies","therapeutic_equipment","assistive_devices","consumables","pharmaceuticals","orthotics_prosthetics","diagnostic_tools","safety_equipment","office_supplies","cleaning_supplies","linen_textiles","it_equipment","furniture","maintenance_parts","lab_supplies"]).withMessage('category غير صالح'),
  body('status').optional().isIn(["active","inactive","discontinued","pending_approval","recalled","out_of_stock","on_order","quarantined","expired","damaged"]).withMessage('status غير صالح'),
  body('uom').optional().isIn(["each","box","pack","case","roll","bottle","tube","pair","set","kg","litre","metre"]).withMessage('uom غير صالح'),
  body('barcode').optional().trim().isString().withMessage('barcode يجب أن يكون نصاً'),
  body('manufacturer').optional().trim().isString().withMessage('manufacturer يجب أن يكون نصاً'),
  body('model').optional().trim().isString().withMessage('model يجب أن يكون نصاً'),
  body('storageCondition').optional().isIn(["room_temperature","refrigerated","frozen","cool_dry","humidity_controlled","light_sensitive","hazardous","sterile","flammable","controlled_substance"]).withMessage('storageCondition غير صالح'),
  body('unitCost').optional().isFloat().withMessage('unitCost يجب أن يكون رقماً'),
  body('sellingPrice').optional().isFloat().withMessage('sellingPrice يجب أن يكون رقماً'),
  body('valuationMethod').optional().isIn(["fifo","lifo","weighted_average","specific_identification","standard_cost","replacement_cost"]).withMessage('valuationMethod غير صالح'),
  body('isSerialTracked').optional().isBoolean().withMessage('isSerialTracked يجب أن يكون قيمة منطقية'),
  body('isLotTracked').optional().isBoolean().withMessage('isLotTracked يجب أن يكون قيمة منطقية'),
  body('isPerishable').optional().isBoolean().withMessage('isPerishable يجب أن يكون قيمة منطقية'),
  body('shelfLifeDays').optional().isFloat().withMessage('shelfLifeDays يجب أن يكون رقماً'),
  body('supplierId').optional().custom(isObjectId).withMessage('supplierId غير صالح'),
  body('tags').optional().isArray().withMessage('tags يجب أن يكون مصفوفة'),
  body('images').optional().isArray().withMessage('images يجب أن يكون مصفوفة'),
];

/* ═══ InventoryItem: Update ═══ */
const updateInventoryItem = [
  body('sku').optional().trim().isString().withMessage('sku يجب أن يكون نصاً'),
  body('name').optional().trim().isString().withMessage('name يجب أن يكون نصاً'),
  body('nameAr').optional().trim().isString().withMessage('nameAr يجب أن يكون نصاً'),
  body('description').optional().trim().isString().withMessage('description يجب أن يكون نصاً'),
  body('category').optional().isIn(["medical_supplies","therapeutic_equipment","assistive_devices","consumables","pharmaceuticals","orthotics_prosthetics","diagnostic_tools","safety_equipment","office_supplies","cleaning_supplies","linen_textiles","it_equipment","furniture","maintenance_parts","lab_supplies"]).withMessage('category غير صالح'),
  body('status').optional().isIn(["active","inactive","discontinued","pending_approval","recalled","out_of_stock","on_order","quarantined","expired","damaged"]).withMessage('status غير صالح'),
  body('uom').optional().isIn(["each","box","pack","case","roll","bottle","tube","pair","set","kg","litre","metre"]).withMessage('uom غير صالح'),
  body('barcode').optional().trim().isString().withMessage('barcode يجب أن يكون نصاً'),
  body('manufacturer').optional().trim().isString().withMessage('manufacturer يجب أن يكون نصاً'),
  body('model').optional().trim().isString().withMessage('model يجب أن يكون نصاً'),
  body('storageCondition').optional().isIn(["room_temperature","refrigerated","frozen","cool_dry","humidity_controlled","light_sensitive","hazardous","sterile","flammable","controlled_substance"]).withMessage('storageCondition غير صالح'),
  body('unitCost').optional().isFloat().withMessage('unitCost يجب أن يكون رقماً'),
  body('sellingPrice').optional().isFloat().withMessage('sellingPrice يجب أن يكون رقماً'),
  body('valuationMethod').optional().isIn(["fifo","lifo","weighted_average","specific_identification","standard_cost","replacement_cost"]).withMessage('valuationMethod غير صالح'),
  body('isSerialTracked').optional().isBoolean().withMessage('isSerialTracked يجب أن يكون قيمة منطقية'),
  body('isLotTracked').optional().isBoolean().withMessage('isLotTracked يجب أن يكون قيمة منطقية'),
  body('isPerishable').optional().isBoolean().withMessage('isPerishable يجب أن يكون قيمة منطقية'),
  body('shelfLifeDays').optional().isFloat().withMessage('shelfLifeDays يجب أن يكون رقماً'),
  body('supplierId').optional().custom(isObjectId).withMessage('supplierId غير صالح'),
  body('tags').optional().isArray().withMessage('tags يجب أن يكون مصفوفة'),
  body('images').optional().isArray().withMessage('images يجب أن يكون مصفوفة'),
];

/* ═══ StockLevel: Create ═══ */
const createStockLevel = [
  body('itemId').custom(isObjectId).withMessage('itemId غير صالح'),
  body('locationId').custom(isObjectId).withMessage('locationId غير صالح'),
  body('quantityOnHand').optional().isFloat().withMessage('quantityOnHand يجب أن يكون رقماً'),
  body('quantityReserved').optional().isFloat().withMessage('quantityReserved يجب أن يكون رقماً'),
  body('quantityOnOrder').optional().isFloat().withMessage('quantityOnOrder يجب أن يكون رقماً'),
  body('quantityAvailable').optional().isFloat().withMessage('quantityAvailable يجب أن يكون رقماً'),
  body('lotNumber').optional().trim().isString().withMessage('lotNumber يجب أن يكون نصاً'),
  body('serialNumber').optional().trim().isString().withMessage('serialNumber يجب أن يكون نصاً'),
  body('expiryDate').optional().isISO8601().withMessage('expiryDate يجب أن يكون تاريخ ISO صالح'),
  body('lastCountDate').optional().isISO8601().withMessage('lastCountDate يجب أن يكون تاريخ ISO صالح'),
  body('lastCountQty').optional().isFloat().withMessage('lastCountQty يجب أن يكون رقماً'),
  body('binLocation').optional().trim().isString().withMessage('binLocation يجب أن يكون نصاً'),
];

/* ═══ StockLevel: Update ═══ */
const updateStockLevel = [
  body('itemId').optional().custom(isObjectId).withMessage('itemId غير صالح'),
  body('locationId').optional().custom(isObjectId).withMessage('locationId غير صالح'),
  body('quantityOnHand').optional().isFloat().withMessage('quantityOnHand يجب أن يكون رقماً'),
  body('quantityReserved').optional().isFloat().withMessage('quantityReserved يجب أن يكون رقماً'),
  body('quantityOnOrder').optional().isFloat().withMessage('quantityOnOrder يجب أن يكون رقماً'),
  body('quantityAvailable').optional().isFloat().withMessage('quantityAvailable يجب أن يكون رقماً'),
  body('lotNumber').optional().trim().isString().withMessage('lotNumber يجب أن يكون نصاً'),
  body('serialNumber').optional().trim().isString().withMessage('serialNumber يجب أن يكون نصاً'),
  body('expiryDate').optional().isISO8601().withMessage('expiryDate يجب أن يكون تاريخ ISO صالح'),
  body('lastCountDate').optional().isISO8601().withMessage('lastCountDate يجب أن يكون تاريخ ISO صالح'),
  body('lastCountQty').optional().isFloat().withMessage('lastCountQty يجب أن يكون رقماً'),
  body('binLocation').optional().trim().isString().withMessage('binLocation يجب أن يكون نصاً'),
];

/* ═══ StockTransaction: Create ═══ */
const createStockTransaction = [
  body('transactionNumber').trim().notEmpty().withMessage('transactionNumber مطلوب'),
  body('itemId').custom(isObjectId).withMessage('itemId غير صالح'),
  body('locationId').custom(isObjectId).withMessage('locationId غير صالح'),
  body('type').isIn(["receipt","issue","adjustment_increase","adjustment_decrease","transfer_in","transfer_out","return_to_supplier","return_from_patient","write_off","disposal","cycle_count","initial_stock"]).withMessage('type غير صالح'),
  body('quantity').isFloat().withMessage('quantity يجب أن يكون رقماً'),
  body('unitCost').optional().isFloat().withMessage('unitCost يجب أن يكون رقماً'),
  body('totalCost').optional().isFloat().withMessage('totalCost يجب أن يكون رقماً'),
  body('referenceType').optional().trim().isString().withMessage('referenceType يجب أن يكون نصاً'),
  body('referenceId').optional().custom(isObjectId).withMessage('referenceId غير صالح'),
  body('lotNumber').optional().trim().isString().withMessage('lotNumber يجب أن يكون نصاً'),
  body('serialNumber').optional().trim().isString().withMessage('serialNumber يجب أن يكون نصاً'),
  body('expiryDate').optional().isISO8601().withMessage('expiryDate يجب أن يكون تاريخ ISO صالح'),
  body('reason').optional().trim().isString().withMessage('reason يجب أن يكون نصاً'),
  body('notes').optional().trim().isString().withMessage('notes يجب أن يكون نصاً'),
  body('performedBy').custom(isObjectId).withMessage('performedBy غير صالح'),
];

/* ═══ StockTransaction: Update ═══ */
const updateStockTransaction = [
  body('transactionNumber').optional().trim().isString().withMessage('transactionNumber يجب أن يكون نصاً'),
  body('itemId').optional().custom(isObjectId).withMessage('itemId غير صالح'),
  body('locationId').optional().custom(isObjectId).withMessage('locationId غير صالح'),
  body('type').optional().isIn(["receipt","issue","adjustment_increase","adjustment_decrease","transfer_in","transfer_out","return_to_supplier","return_from_patient","write_off","disposal","cycle_count","initial_stock"]).withMessage('type غير صالح'),
  body('quantity').optional().isFloat().withMessage('quantity يجب أن يكون رقماً'),
  body('unitCost').optional().isFloat().withMessage('unitCost يجب أن يكون رقماً'),
  body('totalCost').optional().isFloat().withMessage('totalCost يجب أن يكون رقماً'),
  body('referenceType').optional().trim().isString().withMessage('referenceType يجب أن يكون نصاً'),
  body('referenceId').optional().custom(isObjectId).withMessage('referenceId غير صالح'),
  body('lotNumber').optional().trim().isString().withMessage('lotNumber يجب أن يكون نصاً'),
  body('serialNumber').optional().trim().isString().withMessage('serialNumber يجب أن يكون نصاً'),
  body('expiryDate').optional().isISO8601().withMessage('expiryDate يجب أن يكون تاريخ ISO صالح'),
  body('reason').optional().trim().isString().withMessage('reason يجب أن يكون نصاً'),
  body('notes').optional().trim().isString().withMessage('notes يجب أن يكون نصاً'),
  body('performedBy').optional().custom(isObjectId).withMessage('performedBy غير صالح'),
];

/* ═══ ReorderRule: Create ═══ */
const createReorderRule = [
  body('itemId').custom(isObjectId).withMessage('itemId غير صالح'),
  body('locationId').optional().custom(isObjectId).withMessage('locationId غير صالح'),
  body('reorderPoint').isFloat().withMessage('reorderPoint يجب أن يكون رقماً'),
  body('reorderQuantity').isFloat().withMessage('reorderQuantity يجب أن يكون رقماً'),
  body('maxStockLevel').optional().isFloat().withMessage('maxStockLevel يجب أن يكون رقماً'),
  body('minStockLevel').optional().isFloat().withMessage('minStockLevel يجب أن يكون رقماً'),
  body('leadTimeDays').optional().isFloat().withMessage('leadTimeDays يجب أن يكون رقماً'),
  body('isAutoReorder').optional().isBoolean().withMessage('isAutoReorder يجب أن يكون قيمة منطقية'),
  body('preferredSupplierId').optional().custom(isObjectId).withMessage('preferredSupplierId غير صالح'),
  body('isActive').optional().isBoolean().withMessage('isActive يجب أن يكون قيمة منطقية'),
  body('lastTriggered').optional().isISO8601().withMessage('lastTriggered يجب أن يكون تاريخ ISO صالح'),
];

/* ═══ ReorderRule: Update ═══ */
const updateReorderRule = [
  body('itemId').optional().custom(isObjectId).withMessage('itemId غير صالح'),
  body('locationId').optional().custom(isObjectId).withMessage('locationId غير صالح'),
  body('reorderPoint').optional().isFloat().withMessage('reorderPoint يجب أن يكون رقماً'),
  body('reorderQuantity').optional().isFloat().withMessage('reorderQuantity يجب أن يكون رقماً'),
  body('maxStockLevel').optional().isFloat().withMessage('maxStockLevel يجب أن يكون رقماً'),
  body('minStockLevel').optional().isFloat().withMessage('minStockLevel يجب أن يكون رقماً'),
  body('leadTimeDays').optional().isFloat().withMessage('leadTimeDays يجب أن يكون رقماً'),
  body('isAutoReorder').optional().isBoolean().withMessage('isAutoReorder يجب أن يكون قيمة منطقية'),
  body('preferredSupplierId').optional().custom(isObjectId).withMessage('preferredSupplierId غير صالح'),
  body('isActive').optional().isBoolean().withMessage('isActive يجب أن يكون قيمة منطقية'),
  body('lastTriggered').optional().isISO8601().withMessage('lastTriggered يجب أن يكون تاريخ ISO صالح'),
];

module.exports = {
  createInventoryItem,
  updateInventoryItem,
  createStockLevel,
  updateStockLevel,
  createStockTransaction,
  updateStockTransaction,
  createReorderRule,
  updateReorderRule,
};