'use strict';
/**
 * LearningManagement — Auto-Generated Validation Schemas
 * Generated by: scripts/gen-validation-from-model.js
 */

const { body, param } = require('express-validator');

const isObjectId = (value) => /^[a-f\d]{24}$/i.test(value);

/* ═══ Course: Create ═══ */
const createCourse = [
  body('code').trim().notEmpty().withMessage('code مطلوب'),
  body('title').trim().notEmpty().withMessage('title مطلوب'),
  body('titleAr').optional().trim().isString().withMessage('titleAr يجب أن يكون نصاً'),
  body('description').optional().trim().isString().withMessage('description يجب أن يكون نصاً'),
  body('descriptionAr').optional().trim().isString().withMessage('descriptionAr يجب أن يكون نصاً'),
  body('category').isIn(["physical_therapy","occupational_therapy","speech_therapy","psychology","nursing","medical","administrative","safety_compliance","technology","leadership","patient_communication","cultural_competency","research_methods","ethics","disability_awareness","assistive_technology"]).withMessage('category غير صالح'),
  body('level').optional().isIn(["beginner","intermediate","advanced","expert","all_levels"]).withMessage('level غير صالح'),
  body('status').optional().isIn(["draft","under_review","published","archived","suspended","updated","retired"]).withMessage('status غير صالح'),
  body('deliveryMode').optional().isIn(["self_paced","instructor_led","blended","live_virtual","on_demand","cohort_based","mentored","simulation"]).withMessage('deliveryMode غير صالح'),
  body('durationHours').optional().isFloat().withMessage('durationHours يجب أن يكون رقماً'),
  body('prerequisites').optional().isArray().withMessage('prerequisites يجب أن يكون مصفوفة'),
  body('tags').optional().isArray().withMessage('tags يجب أن يكون مصفوفة'),
  body('thumbnail').optional().trim().isString().withMessage('thumbnail يجب أن يكون نصاً'),
  body('instructor').optional().custom(isObjectId).withMessage('instructor غير صالح'),
  body('maxEnrollments').optional().isFloat().withMessage('maxEnrollments يجب أن يكون رقماً'),
  body('enrollmentCount').optional().isFloat().withMessage('enrollmentCount يجب أن يكون رقماً'),
  body('rating').optional().isFloat({ min: 0, max: 5 }).withMessage('rating يجب أن يكون بين 0 و 5'),
  body('ratingCount').optional().isFloat().withMessage('ratingCount يجب أن يكون رقماً'),
  body('certificateType').optional().isIn(["completion","competency","specialization","accredited","micro_credential","professional"]).withMessage('certificateType غير صالح'),
  body('ceuCredits').optional().isFloat().withMessage('ceuCredits يجب أن يكون رقماً'),
  body('createdBy').optional().custom(isObjectId).withMessage('createdBy غير صالح'),
];

/* ═══ Course: Update ═══ */
const updateCourse = [
  body('code').optional().trim().isString().withMessage('code يجب أن يكون نصاً'),
  body('title').optional().trim().isString().withMessage('title يجب أن يكون نصاً'),
  body('titleAr').optional().trim().isString().withMessage('titleAr يجب أن يكون نصاً'),
  body('description').optional().trim().isString().withMessage('description يجب أن يكون نصاً'),
  body('descriptionAr').optional().trim().isString().withMessage('descriptionAr يجب أن يكون نصاً'),
  body('category').optional().isIn(["physical_therapy","occupational_therapy","speech_therapy","psychology","nursing","medical","administrative","safety_compliance","technology","leadership","patient_communication","cultural_competency","research_methods","ethics","disability_awareness","assistive_technology"]).withMessage('category غير صالح'),
  body('level').optional().isIn(["beginner","intermediate","advanced","expert","all_levels"]).withMessage('level غير صالح'),
  body('status').optional().isIn(["draft","under_review","published","archived","suspended","updated","retired"]).withMessage('status غير صالح'),
  body('deliveryMode').optional().isIn(["self_paced","instructor_led","blended","live_virtual","on_demand","cohort_based","mentored","simulation"]).withMessage('deliveryMode غير صالح'),
  body('durationHours').optional().isFloat().withMessage('durationHours يجب أن يكون رقماً'),
  body('prerequisites').optional().isArray().withMessage('prerequisites يجب أن يكون مصفوفة'),
  body('tags').optional().isArray().withMessage('tags يجب أن يكون مصفوفة'),
  body('thumbnail').optional().trim().isString().withMessage('thumbnail يجب أن يكون نصاً'),
  body('instructor').optional().custom(isObjectId).withMessage('instructor غير صالح'),
  body('maxEnrollments').optional().isFloat().withMessage('maxEnrollments يجب أن يكون رقماً'),
  body('enrollmentCount').optional().isFloat().withMessage('enrollmentCount يجب أن يكون رقماً'),
  body('rating').optional().isFloat({ min: 0, max: 5 }).withMessage('rating يجب أن يكون بين 0 و 5'),
  body('ratingCount').optional().isFloat().withMessage('ratingCount يجب أن يكون رقماً'),
  body('certificateType').optional().isIn(["completion","competency","specialization","accredited","micro_credential","professional"]).withMessage('certificateType غير صالح'),
  body('ceuCredits').optional().isFloat().withMessage('ceuCredits يجب أن يكون رقماً'),
  body('createdBy').optional().custom(isObjectId).withMessage('createdBy غير صالح'),
];

/* ═══ LearningPath: Create ═══ */
const createLearningPath = [
  body('code').trim().notEmpty().withMessage('code مطلوب'),
  body('title').trim().notEmpty().withMessage('title مطلوب'),
  body('titleAr').optional().trim().isString().withMessage('titleAr يجب أن يكون نصاً'),
  body('description').optional().trim().isString().withMessage('description يجب أن يكون نصاً'),
  body('category').optional().isIn(["physical_therapy","occupational_therapy","speech_therapy","psychology","nursing","medical","administrative","safety_compliance","technology","leadership","patient_communication","cultural_competency","research_methods","ethics","disability_awareness","assistive_technology"]).withMessage('category غير صالح'),
  body('level').optional().isIn(["beginner","intermediate","advanced","expert","all_levels"]).withMessage('level غير صالح'),
  body('status').optional().isIn(["draft","active","archived","retired"]).withMessage('status غير صالح'),
  body('courseId').custom(isObjectId).withMessage('courseId غير صالح'),
  body('order').isFloat().withMessage('order يجب أن يكون رقماً'),
  body('isRequired').optional().isBoolean().withMessage('isRequired يجب أن يكون قيمة منطقية'),
  body('totalDurationHours').optional().isFloat().withMessage('totalDurationHours يجب أن يكون رقماً'),
  body('totalCourses').optional().isFloat().withMessage('totalCourses يجب أن يكون رقماً'),
  body('targetRoles').optional().isArray().withMessage('targetRoles يجب أن يكون مصفوفة'),
  body('certificateType').optional().isIn(["completion","competency","specialization","accredited","micro_credential","professional"]).withMessage('certificateType غير صالح'),
  body('ceuCredits').optional().isFloat().withMessage('ceuCredits يجب أن يكون رقماً'),
  body('createdBy').optional().custom(isObjectId).withMessage('createdBy غير صالح'),
];

/* ═══ LearningPath: Update ═══ */
const updateLearningPath = [
  body('code').optional().trim().isString().withMessage('code يجب أن يكون نصاً'),
  body('title').optional().trim().isString().withMessage('title يجب أن يكون نصاً'),
  body('titleAr').optional().trim().isString().withMessage('titleAr يجب أن يكون نصاً'),
  body('description').optional().trim().isString().withMessage('description يجب أن يكون نصاً'),
  body('category').optional().isIn(["physical_therapy","occupational_therapy","speech_therapy","psychology","nursing","medical","administrative","safety_compliance","technology","leadership","patient_communication","cultural_competency","research_methods","ethics","disability_awareness","assistive_technology"]).withMessage('category غير صالح'),
  body('level').optional().isIn(["beginner","intermediate","advanced","expert","all_levels"]).withMessage('level غير صالح'),
  body('status').optional().isIn(["draft","active","archived","retired"]).withMessage('status غير صالح'),
  body('courseId').optional().custom(isObjectId).withMessage('courseId غير صالح'),
  body('order').optional().isFloat().withMessage('order يجب أن يكون رقماً'),
  body('isRequired').optional().isBoolean().withMessage('isRequired يجب أن يكون قيمة منطقية'),
  body('totalDurationHours').optional().isFloat().withMessage('totalDurationHours يجب أن يكون رقماً'),
  body('totalCourses').optional().isFloat().withMessage('totalCourses يجب أن يكون رقماً'),
  body('targetRoles').optional().isArray().withMessage('targetRoles يجب أن يكون مصفوفة'),
  body('certificateType').optional().isIn(["completion","competency","specialization","accredited","micro_credential","professional"]).withMessage('certificateType غير صالح'),
  body('ceuCredits').optional().isFloat().withMessage('ceuCredits يجب أن يكون رقماً'),
  body('createdBy').optional().custom(isObjectId).withMessage('createdBy غير صالح'),
];

/* ═══ Enrollment: Create ═══ */
const createEnrollment = [
  body('userId').custom(isObjectId).withMessage('userId غير صالح'),
  body('courseId').custom(isObjectId).withMessage('courseId غير صالح'),
  body('learningPathId').optional().custom(isObjectId).withMessage('learningPathId غير صالح'),
  body('status').optional().isIn(["enrolled","in_progress","completed","failed","withdrawn","expired","suspended","waitlisted"]).withMessage('status غير صالح'),
  body('progress').optional().isFloat({ min: 0, max: 100 }).withMessage('progress يجب أن يكون بين 0 و 100'),
  body('startedAt').optional().isISO8601().withMessage('startedAt يجب أن يكون تاريخ ISO صالح'),
  body('expiresAt').optional().isISO8601().withMessage('expiresAt يجب أن يكون تاريخ ISO صالح'),
  body('finalScore').optional().isFloat().withMessage('finalScore يجب أن يكون رقماً'),
  body('certificateIssued').optional().isBoolean().withMessage('certificateIssued يجب أن يكون قيمة منطقية'),
  body('certificateUrl').optional().trim().isString().withMessage('certificateUrl يجب أن يكون نصاً'),
  body('certificateIssuedAt').optional().isISO8601().withMessage('certificateIssuedAt يجب أن يكون تاريخ ISO صالح'),
  body('totalTimeSpent').optional().isFloat().withMessage('totalTimeSpent يجب أن يكون رقماً'),
  body('lastAccessedAt').optional().isISO8601().withMessage('lastAccessedAt يجب أن يكون تاريخ ISO صالح'),
  body('assignedBy').optional().custom(isObjectId).withMessage('assignedBy غير صالح'),
  body('notes').optional().trim().isString().withMessage('notes يجب أن يكون نصاً'),
];

/* ═══ Enrollment: Update ═══ */
const updateEnrollment = [
  body('userId').optional().custom(isObjectId).withMessage('userId غير صالح'),
  body('courseId').optional().custom(isObjectId).withMessage('courseId غير صالح'),
  body('learningPathId').optional().custom(isObjectId).withMessage('learningPathId غير صالح'),
  body('status').optional().isIn(["enrolled","in_progress","completed","failed","withdrawn","expired","suspended","waitlisted"]).withMessage('status غير صالح'),
  body('progress').optional().isFloat({ min: 0, max: 100 }).withMessage('progress يجب أن يكون بين 0 و 100'),
  body('startedAt').optional().isISO8601().withMessage('startedAt يجب أن يكون تاريخ ISO صالح'),
  body('expiresAt').optional().isISO8601().withMessage('expiresAt يجب أن يكون تاريخ ISO صالح'),
  body('finalScore').optional().isFloat().withMessage('finalScore يجب أن يكون رقماً'),
  body('certificateIssued').optional().isBoolean().withMessage('certificateIssued يجب أن يكون قيمة منطقية'),
  body('certificateUrl').optional().trim().isString().withMessage('certificateUrl يجب أن يكون نصاً'),
  body('certificateIssuedAt').optional().isISO8601().withMessage('certificateIssuedAt يجب أن يكون تاريخ ISO صالح'),
  body('totalTimeSpent').optional().isFloat().withMessage('totalTimeSpent يجب أن يكون رقماً'),
  body('lastAccessedAt').optional().isISO8601().withMessage('lastAccessedAt يجب أن يكون تاريخ ISO صالح'),
  body('assignedBy').optional().custom(isObjectId).withMessage('assignedBy غير صالح'),
  body('notes').optional().trim().isString().withMessage('notes يجب أن يكون نصاً'),
];

/* ═══ Quiz: Create ═══ */
const createQuiz = [
  body('title').trim().notEmpty().withMessage('title مطلوب'),
  body('titleAr').optional().trim().isString().withMessage('titleAr يجب أن يكون نصاً'),
  body('courseId').optional().custom(isObjectId).withMessage('courseId غير صالح'),
  body('moduleIndex').optional().isFloat().withMessage('moduleIndex يجب أن يكون رقماً'),
  body('totalPoints').optional().isFloat().withMessage('totalPoints يجب أن يكون رقماً'),
  body('passingScore').optional().isFloat().withMessage('passingScore يجب أن يكون رقماً'),
  body('timeLimit').optional().isFloat().withMessage('timeLimit يجب أن يكون رقماً'),
  body('maxAttempts').optional().isFloat().withMessage('maxAttempts يجب أن يكون رقماً'),
  body('shuffleQuestions').optional().isBoolean().withMessage('shuffleQuestions يجب أن يكون قيمة منطقية'),
  body('shuffleOptions').optional().isBoolean().withMessage('shuffleOptions يجب أن يكون قيمة منطقية'),
  body('showResults').optional().isBoolean().withMessage('showResults يجب أن يكون قيمة منطقية'),
  body('isActive').optional().isBoolean().withMessage('isActive يجب أن يكون قيمة منطقية'),
  body('createdBy').optional().custom(isObjectId).withMessage('createdBy غير صالح'),
];

/* ═══ Quiz: Update ═══ */
const updateQuiz = [
  body('title').optional().trim().isString().withMessage('title يجب أن يكون نصاً'),
  body('titleAr').optional().trim().isString().withMessage('titleAr يجب أن يكون نصاً'),
  body('courseId').optional().custom(isObjectId).withMessage('courseId غير صالح'),
  body('moduleIndex').optional().isFloat().withMessage('moduleIndex يجب أن يكون رقماً'),
  body('totalPoints').optional().isFloat().withMessage('totalPoints يجب أن يكون رقماً'),
  body('passingScore').optional().isFloat().withMessage('passingScore يجب أن يكون رقماً'),
  body('timeLimit').optional().isFloat().withMessage('timeLimit يجب أن يكون رقماً'),
  body('maxAttempts').optional().isFloat().withMessage('maxAttempts يجب أن يكون رقماً'),
  body('shuffleQuestions').optional().isBoolean().withMessage('shuffleQuestions يجب أن يكون قيمة منطقية'),
  body('shuffleOptions').optional().isBoolean().withMessage('shuffleOptions يجب أن يكون قيمة منطقية'),
  body('showResults').optional().isBoolean().withMessage('showResults يجب أن يكون قيمة منطقية'),
  body('isActive').optional().isBoolean().withMessage('isActive يجب أن يكون قيمة منطقية'),
  body('createdBy').optional().custom(isObjectId).withMessage('createdBy غير صالح'),
];

module.exports = {
  createCourse,
  updateCourse,
  createLearningPath,
  updateLearningPath,
  createEnrollment,
  updateEnrollment,
  createQuiz,
  updateQuiz,
};