'use strict';
/**
 * LocalizationEngine — Auto-Generated Validation Schemas
 * Generated by: scripts/gen-validation-from-model.js
 */

const { body, param } = require('express-validator');

const isObjectId = (value) => /^[a-f\d]{24}$/i.test(value);

/* ═══ Translation: Create ═══ */
const createTranslation = [
  body('key').trim().notEmpty().withMessage('key مطلوب'),
  body('namespace').optional().trim().isString().withMessage('namespace يجب أن يكون نصاً'),
  body('translations').optional().trim().isString().withMessage('translations يجب أن يكون نصاً'),
  body('context').optional().trim().isString().withMessage('context يجب أن يكون نصاً'),
  body('category').optional().isIn(["ui","clinical","domain","notification","report","error","system"]).withMessage('category غير صالح'),
  body('isCustom').optional().isBoolean().withMessage('isCustom يجب أن يكون قيمة منطقية'),
  body('isDeleted').optional().isBoolean().withMessage('isDeleted يجب أن يكون قيمة منطقية'),
];

/* ═══ Translation: Update ═══ */
const updateTranslation = [
  body('key').optional().trim().isString().withMessage('key يجب أن يكون نصاً'),
  body('namespace').optional().trim().isString().withMessage('namespace يجب أن يكون نصاً'),
  body('translations').optional().trim().isString().withMessage('translations يجب أن يكون نصاً'),
  body('context').optional().trim().isString().withMessage('context يجب أن يكون نصاً'),
  body('category').optional().isIn(["ui","clinical","domain","notification","report","error","system"]).withMessage('category غير صالح'),
  body('isCustom').optional().isBoolean().withMessage('isCustom يجب أن يكون قيمة منطقية'),
  body('isDeleted').optional().isBoolean().withMessage('isDeleted يجب أن يكون قيمة منطقية'),
];

module.exports = {
  createTranslation,
  updateTranslation,
};