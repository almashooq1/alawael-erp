'use strict';
/**
 * MessageCenter — Auto-Generated Validation Schemas
 * Generated by: scripts/gen-validation-from-model.js
 */

const { body, param } = require('express-validator');

const isObjectId = (value) => /^[a-f\d]{24}$/i.test(value);

/* ═══ Conversation: Create ═══ */
const createConversation = [
  body('conversationCode').trim().notEmpty().withMessage('conversationCode مطلوب'),
  body('type').isIn(["direct","group","clinical_team","family_communication","case_discussion","broadcast","support_ticket","referral","consultation","emergency","department_channel","announcement"]).withMessage('type غير صالح'),
  body('status').optional().isIn(["active","archived","closed","muted","blocked","pending","locked","flagged","resolved","deleted"]).withMessage('status غير صالح'),
  body('title').optional().trim().isString().withMessage('title يجب أن يكون نصاً'),
  body('role').optional().trim().isString().withMessage('role يجب أن يكون نصاً'),
  body('joinedAt').optional().isISO8601().withMessage('joinedAt يجب أن يكون تاريخ ISO صالح'),
  body('lastReadAt').optional().isISO8601().withMessage('lastReadAt يجب أن يكون تاريخ ISO صالح'),
  body('createdBy').custom(isObjectId).withMessage('createdBy غير صالح'),
  body('lastMessageAt').optional().isISO8601().withMessage('lastMessageAt يجب أن يكون تاريخ ISO صالح'),
  body('lastMessagePreview').optional().trim().isString().withMessage('lastMessagePreview يجب أن يكون نصاً'),
  body('messageCount').optional().isFloat().withMessage('messageCount يجب أن يكون رقماً'),
  body('isEncrypted').optional().isBoolean().withMessage('isEncrypted يجب أن يكون قيمة منطقية'),
  body('tags').optional().isArray().withMessage('tags يجب أن يكون مصفوفة'),
  body('relatedEntityId').optional().custom(isObjectId).withMessage('relatedEntityId غير صالح'),
  body('relatedEntityType').optional().trim().isString().withMessage('relatedEntityType يجب أن يكون نصاً'),
];

/* ═══ Conversation: Update ═══ */
const updateConversation = [
  body('conversationCode').optional().trim().isString().withMessage('conversationCode يجب أن يكون نصاً'),
  body('type').optional().isIn(["direct","group","clinical_team","family_communication","case_discussion","broadcast","support_ticket","referral","consultation","emergency","department_channel","announcement"]).withMessage('type غير صالح'),
  body('status').optional().isIn(["active","archived","closed","muted","blocked","pending","locked","flagged","resolved","deleted"]).withMessage('status غير صالح'),
  body('title').optional().trim().isString().withMessage('title يجب أن يكون نصاً'),
  body('role').optional().trim().isString().withMessage('role يجب أن يكون نصاً'),
  body('joinedAt').optional().isISO8601().withMessage('joinedAt يجب أن يكون تاريخ ISO صالح'),
  body('lastReadAt').optional().isISO8601().withMessage('lastReadAt يجب أن يكون تاريخ ISO صالح'),
  body('createdBy').optional().custom(isObjectId).withMessage('createdBy غير صالح'),
  body('lastMessageAt').optional().isISO8601().withMessage('lastMessageAt يجب أن يكون تاريخ ISO صالح'),
  body('lastMessagePreview').optional().trim().isString().withMessage('lastMessagePreview يجب أن يكون نصاً'),
  body('messageCount').optional().isFloat().withMessage('messageCount يجب أن يكون رقماً'),
  body('isEncrypted').optional().isBoolean().withMessage('isEncrypted يجب أن يكون قيمة منطقية'),
  body('tags').optional().isArray().withMessage('tags يجب أن يكون مصفوفة'),
  body('relatedEntityId').optional().custom(isObjectId).withMessage('relatedEntityId غير صالح'),
  body('relatedEntityType').optional().trim().isString().withMessage('relatedEntityType يجب أن يكون نصاً'),
];

/* ═══ Message: Create ═══ */
const createMessage = [
  body('conversationId').custom(isObjectId).withMessage('conversationId غير صالح'),
  body('senderId').custom(isObjectId).withMessage('senderId غير صالح'),
  body('type').optional().isIn(["text","image","file","audio","video","location","clinical_note","assessment_link","appointment_request","system","template","rich_text"]).withMessage('type غير صالح'),
  body('status').optional().isIn(["draft","sending","sent","delivered","read","failed","recalled","expired","flagged","deleted"]).withMessage('status غير صالح'),
  body('priority').optional().isIn(["low","normal","high","urgent","critical","emergency"]).withMessage('priority غير صالح'),
  body('content').trim().notEmpty().withMessage('content مطلوب'),
  body('subject').optional().trim().isString().withMessage('subject يجب أن يكون نصاً'),
  body('attachments').optional().isArray().withMessage('attachments يجب أن يكون مصفوفة'),
  body('replyTo').optional().custom(isObjectId).withMessage('replyTo غير صالح'),
  body('readBy').optional().isArray().withMessage('readBy يجب أن يكون مصفوفة'),
  body('reactions').optional().isArray().withMessage('reactions يجب أن يكون مصفوفة'),
  body('isEdited').optional().isBoolean().withMessage('isEdited يجب أن يكون قيمة منطقية'),
  body('editedAt').optional().isISO8601().withMessage('editedAt يجب أن يكون تاريخ ISO صالح'),
  body('deletedAt').optional().isISO8601().withMessage('deletedAt يجب أن يكون تاريخ ISO صالح'),
];

/* ═══ Message: Update ═══ */
const updateMessage = [
  body('conversationId').optional().custom(isObjectId).withMessage('conversationId غير صالح'),
  body('senderId').optional().custom(isObjectId).withMessage('senderId غير صالح'),
  body('type').optional().isIn(["text","image","file","audio","video","location","clinical_note","assessment_link","appointment_request","system","template","rich_text"]).withMessage('type غير صالح'),
  body('status').optional().isIn(["draft","sending","sent","delivered","read","failed","recalled","expired","flagged","deleted"]).withMessage('status غير صالح'),
  body('priority').optional().isIn(["low","normal","high","urgent","critical","emergency"]).withMessage('priority غير صالح'),
  body('content').optional().trim().isString().withMessage('content يجب أن يكون نصاً'),
  body('subject').optional().trim().isString().withMessage('subject يجب أن يكون نصاً'),
  body('attachments').optional().isArray().withMessage('attachments يجب أن يكون مصفوفة'),
  body('replyTo').optional().custom(isObjectId).withMessage('replyTo غير صالح'),
  body('readBy').optional().isArray().withMessage('readBy يجب أن يكون مصفوفة'),
  body('reactions').optional().isArray().withMessage('reactions يجب أن يكون مصفوفة'),
  body('isEdited').optional().isBoolean().withMessage('isEdited يجب أن يكون قيمة منطقية'),
  body('editedAt').optional().isISO8601().withMessage('editedAt يجب أن يكون تاريخ ISO صالح'),
  body('deletedAt').optional().isISO8601().withMessage('deletedAt يجب أن يكون تاريخ ISO صالح'),
];

/* ═══ MessageTemplate: Create ═══ */
const createMessageTemplate = [
  body('code').trim().notEmpty().withMessage('code مطلوب'),
  body('name').trim().notEmpty().withMessage('name مطلوب'),
  body('nameAr').optional().trim().isString().withMessage('nameAr يجب أن يكون نصاً'),
  body('category').isIn(["appointment_reminder","session_summary","progress_update","billing_notice","welcome_message","discharge_summary","follow_up","consent_request","feedback_request","emergency_alert","general","clinical"]).withMessage('category غير صالح'),
  body('subject').optional().trim().isString().withMessage('subject يجب أن يكون نصاً'),
  body('bodyTemplate').trim().notEmpty().withMessage('bodyTemplate مطلوب'),
  body('bodyTemplateAr').optional().trim().isString().withMessage('bodyTemplateAr يجب أن يكون نصاً'),
  body('variables').optional().isArray().withMessage('variables يجب أن يكون مصفوفة'),
  body('channels').optional().isArray().withMessage('channels يجب أن يكون مصفوفة'),
  body('isActive').optional().isBoolean().withMessage('isActive يجب أن يكون قيمة منطقية'),
];

/* ═══ MessageTemplate: Update ═══ */
const updateMessageTemplate = [
  body('code').optional().trim().isString().withMessage('code يجب أن يكون نصاً'),
  body('name').optional().trim().isString().withMessage('name يجب أن يكون نصاً'),
  body('nameAr').optional().trim().isString().withMessage('nameAr يجب أن يكون نصاً'),
  body('category').optional().isIn(["appointment_reminder","session_summary","progress_update","billing_notice","welcome_message","discharge_summary","follow_up","consent_request","feedback_request","emergency_alert","general","clinical"]).withMessage('category غير صالح'),
  body('subject').optional().trim().isString().withMessage('subject يجب أن يكون نصاً'),
  body('bodyTemplate').optional().trim().isString().withMessage('bodyTemplate يجب أن يكون نصاً'),
  body('bodyTemplateAr').optional().trim().isString().withMessage('bodyTemplateAr يجب أن يكون نصاً'),
  body('variables').optional().isArray().withMessage('variables يجب أن يكون مصفوفة'),
  body('channels').optional().isArray().withMessage('channels يجب أن يكون مصفوفة'),
  body('isActive').optional().isBoolean().withMessage('isActive يجب أن يكون قيمة منطقية'),
];

/* ═══ MessageDraft: Create ═══ */
const createMessageDraft = [
  body('userId').custom(isObjectId).withMessage('userId غير صالح'),
  body('conversationId').optional().custom(isObjectId).withMessage('conversationId غير صالح'),
  body('recipientIds').optional().isArray().withMessage('recipientIds يجب أن يكون مصفوفة'),
  body('subject').optional().trim().isString().withMessage('subject يجب أن يكون نصاً'),
  body('content').optional().trim().isString().withMessage('content يجب أن يكون نصاً'),
  body('attachments').optional().isArray().withMessage('attachments يجب أن يكون مصفوفة'),
  body('templateId').optional().custom(isObjectId).withMessage('templateId غير صالح'),
  body('scheduledAt').optional().isISO8601().withMessage('scheduledAt يجب أن يكون تاريخ ISO صالح'),
];

/* ═══ MessageDraft: Update ═══ */
const updateMessageDraft = [
  body('userId').optional().custom(isObjectId).withMessage('userId غير صالح'),
  body('conversationId').optional().custom(isObjectId).withMessage('conversationId غير صالح'),
  body('recipientIds').optional().isArray().withMessage('recipientIds يجب أن يكون مصفوفة'),
  body('subject').optional().trim().isString().withMessage('subject يجب أن يكون نصاً'),
  body('content').optional().trim().isString().withMessage('content يجب أن يكون نصاً'),
  body('attachments').optional().isArray().withMessage('attachments يجب أن يكون مصفوفة'),
  body('templateId').optional().custom(isObjectId).withMessage('templateId غير صالح'),
  body('scheduledAt').optional().isISO8601().withMessage('scheduledAt يجب أن يكون تاريخ ISO صالح'),
];

module.exports = {
  createConversation,
  updateConversation,
  createMessage,
  updateMessage,
  createMessageTemplate,
  updateMessageTemplate,
  createMessageDraft,
  updateMessageDraft,
};