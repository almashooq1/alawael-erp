'use strict';
/**
 * MetricsCollector — Auto-Generated Validation Schemas
 * Generated by: scripts/gen-validation-from-model.js
 */

const { body, param } = require('express-validator');

const isObjectId = (value) => /^[a-f\d]{24}$/i.test(value);

/* ═══ MetricEntry: Create ═══ */
const createMetricEntry = [
  body('name').trim().notEmpty().withMessage('name مطلوب'),
  body('type').isIn(["counter","gauge","histogram","summary","timer"]).withMessage('type غير صالح'),
  body('value').isFloat().withMessage('value يجب أن يكون رقماً'),
  body('domain').optional().trim().isString().withMessage('domain يجب أن يكون نصاً'),
  body('branchId').optional().custom(isObjectId).withMessage('branchId غير صالح'),
  body('route').optional().trim().isString().withMessage('route يجب أن يكون نصاً'),
  body('method').optional().trim().isString().withMessage('method يجب أن يكون نصاً'),
  body('statusCode').optional().isFloat().withMessage('statusCode يجب أن يكون رقماً'),
  body('durationMs').optional().isFloat().withMessage('durationMs يجب أن يكون رقماً'),
  body('bucket').optional().trim().isString().withMessage('bucket يجب أن يكون نصاً'),
  body('isDeleted').optional().isBoolean().withMessage('isDeleted يجب أن يكون قيمة منطقية'),
];

/* ═══ MetricEntry: Update ═══ */
const updateMetricEntry = [
  body('name').optional().trim().isString().withMessage('name يجب أن يكون نصاً'),
  body('type').optional().isIn(["counter","gauge","histogram","summary","timer"]).withMessage('type غير صالح'),
  body('value').optional().isFloat().withMessage('value يجب أن يكون رقماً'),
  body('domain').optional().trim().isString().withMessage('domain يجب أن يكون نصاً'),
  body('branchId').optional().custom(isObjectId).withMessage('branchId غير صالح'),
  body('route').optional().trim().isString().withMessage('route يجب أن يكون نصاً'),
  body('method').optional().trim().isString().withMessage('method يجب أن يكون نصاً'),
  body('statusCode').optional().isFloat().withMessage('statusCode يجب أن يكون رقماً'),
  body('durationMs').optional().isFloat().withMessage('durationMs يجب أن يكون رقماً'),
  body('bucket').optional().trim().isString().withMessage('bucket يجب أن يكون نصاً'),
  body('isDeleted').optional().isBoolean().withMessage('isDeleted يجب أن يكون قيمة منطقية'),
];

module.exports = {
  createMetricEntry,
  updateMetricEntry,
};