'use strict';
/**
 * PerformanceEvaluator — Auto-Generated Validation Schemas
 * Generated by: scripts/gen-validation-from-model.js
 */

const { body, param } = require('express-validator');

const isObjectId = (value) => /^[a-f\d]{24}$/i.test(value);

/* ═══ PerformanceReview: Create ═══ */
const createPerformanceReview = [
  body('reviewCode').trim().notEmpty().withMessage('reviewCode مطلوب'),
  body('staffId').custom(isObjectId).withMessage('staffId غير صالح'),
  body('reviewerId').custom(isObjectId).withMessage('reviewerId غير صالح'),
  body('type').isIn(["annual","semi_annual","quarterly","probation","project_based","peer_review","360_degree","self_assessment","promotion","corrective","mid_year","onboarding"]).withMessage('type غير صالح'),
  body('status').optional().isIn(["draft","self_assessment","peer_feedback","manager_review","calibration","approved","acknowledged","completed","disputed","reopened"]).withMessage('status غير صالح'),
  body('periodStart').isISO8601().withMessage('periodStart يجب أن يكون تاريخ ISO صالح'),
  body('periodEnd').isISO8601().withMessage('periodEnd يجب أن يكون تاريخ ISO صالح'),
  body('overallRating').optional().isIn(["exceeds_expectations","meets_expectations","partially_meets","needs_improvement","unsatisfactory","outstanding","on_track","off_track","not_rated","not_applicable"]).withMessage('overallRating غير صالح'),
  body('overallScore').optional().isFloat({ min: 0, max: 5 }).withMessage('overallScore يجب أن يكون بين 0 و 5'),
  body('strengths').optional().isArray().withMessage('strengths يجب أن يكون مصفوفة'),
  body('areasForImprovement').optional().isArray().withMessage('areasForImprovement يجب أن يكون مصفوفة'),
  body('managerComments').optional().trim().isString().withMessage('managerComments يجب أن يكون نصاً'),
  body('employeeComments').optional().trim().isString().withMessage('employeeComments يجب أن يكون نصاً'),
  body('developmentPlan').optional().trim().isString().withMessage('developmentPlan يجب أن يكون نصاً'),
  body('nextReviewDate').optional().isISO8601().withMessage('nextReviewDate يجب أن يكون تاريخ ISO صالح'),
];

/* ═══ PerformanceReview: Update ═══ */
const updatePerformanceReview = [
  body('reviewCode').optional().trim().isString().withMessage('reviewCode يجب أن يكون نصاً'),
  body('staffId').optional().custom(isObjectId).withMessage('staffId غير صالح'),
  body('reviewerId').optional().custom(isObjectId).withMessage('reviewerId غير صالح'),
  body('type').optional().isIn(["annual","semi_annual","quarterly","probation","project_based","peer_review","360_degree","self_assessment","promotion","corrective","mid_year","onboarding"]).withMessage('type غير صالح'),
  body('status').optional().isIn(["draft","self_assessment","peer_feedback","manager_review","calibration","approved","acknowledged","completed","disputed","reopened"]).withMessage('status غير صالح'),
  body('periodStart').optional().isISO8601().withMessage('periodStart يجب أن يكون تاريخ ISO صالح'),
  body('periodEnd').optional().isISO8601().withMessage('periodEnd يجب أن يكون تاريخ ISO صالح'),
  body('overallRating').optional().isIn(["exceeds_expectations","meets_expectations","partially_meets","needs_improvement","unsatisfactory","outstanding","on_track","off_track","not_rated","not_applicable"]).withMessage('overallRating غير صالح'),
  body('overallScore').optional().isFloat({ min: 0, max: 5 }).withMessage('overallScore يجب أن يكون بين 0 و 5'),
  body('strengths').optional().isArray().withMessage('strengths يجب أن يكون مصفوفة'),
  body('areasForImprovement').optional().isArray().withMessage('areasForImprovement يجب أن يكون مصفوفة'),
  body('managerComments').optional().trim().isString().withMessage('managerComments يجب أن يكون نصاً'),
  body('employeeComments').optional().trim().isString().withMessage('employeeComments يجب أن يكون نصاً'),
  body('developmentPlan').optional().trim().isString().withMessage('developmentPlan يجب أن يكون نصاً'),
  body('nextReviewDate').optional().isISO8601().withMessage('nextReviewDate يجب أن يكون تاريخ ISO صالح'),
];

/* ═══ PerformanceGoal: Create ═══ */
const createPerformanceGoal = [
  body('staffId').custom(isObjectId).withMessage('staffId غير صالح'),
  body('reviewId').optional().custom(isObjectId).withMessage('reviewId غير صالح'),
  body('title').trim().notEmpty().withMessage('title مطلوب'),
  body('description').optional().trim().isString().withMessage('description يجب أن يكون نصاً'),
  body('status').optional().isIn(["draft","active","in_progress","completed","exceeded","partially_met","not_met","deferred","cancelled","revised"]).withMessage('status غير صالح'),
  body('category').optional().isIn(["clinical_quality","patient_satisfaction","productivity","documentation","compliance","teamwork","innovation","leadership","communication","professional_development","attendance","safety"]).withMessage('category غير صالح'),
  body('targetValue').optional().isFloat().withMessage('targetValue يجب أن يكون رقماً'),
  body('unit').optional().trim().isString().withMessage('unit يجب أن يكون نصاً'),
  body('weight').optional().isFloat().withMessage('weight يجب أن يكون رقماً'),
  body('dueDate').optional().isISO8601().withMessage('dueDate يجب أن يكون تاريخ ISO صالح'),
  body('milestones').optional().isArray().withMessage('milestones يجب أن يكون مصفوفة'),
  body('notes').optional().trim().isString().withMessage('notes يجب أن يكون نصاً'),
];

/* ═══ PerformanceGoal: Update ═══ */
const updatePerformanceGoal = [
  body('staffId').optional().custom(isObjectId).withMessage('staffId غير صالح'),
  body('reviewId').optional().custom(isObjectId).withMessage('reviewId غير صالح'),
  body('title').optional().trim().isString().withMessage('title يجب أن يكون نصاً'),
  body('description').optional().trim().isString().withMessage('description يجب أن يكون نصاً'),
  body('status').optional().isIn(["draft","active","in_progress","completed","exceeded","partially_met","not_met","deferred","cancelled","revised"]).withMessage('status غير صالح'),
  body('category').optional().isIn(["clinical_quality","patient_satisfaction","productivity","documentation","compliance","teamwork","innovation","leadership","communication","professional_development","attendance","safety"]).withMessage('category غير صالح'),
  body('targetValue').optional().isFloat().withMessage('targetValue يجب أن يكون رقماً'),
  body('unit').optional().trim().isString().withMessage('unit يجب أن يكون نصاً'),
  body('weight').optional().isFloat().withMessage('weight يجب أن يكون رقماً'),
  body('dueDate').optional().isISO8601().withMessage('dueDate يجب أن يكون تاريخ ISO صالح'),
  body('milestones').optional().isArray().withMessage('milestones يجب أن يكون مصفوفة'),
  body('notes').optional().trim().isString().withMessage('notes يجب أن يكون نصاً'),
];

/* ═══ Feedback: Create ═══ */
const createFeedback = [
  body('staffId').custom(isObjectId).withMessage('staffId غير صالح'),
  body('reviewId').optional().custom(isObjectId).withMessage('reviewId غير صالح'),
  body('providerId').optional().custom(isObjectId).withMessage('providerId غير صالح'),
  body('type').isIn(["peer","manager","subordinate","self","client","cross_functional","mentor","external","patient","family"]).withMessage('type غير صالح'),
  body('isAnonymous').optional().isBoolean().withMessage('isAnonymous يجب أن يكون قيمة منطقية'),
  body('ratings').optional().isArray().withMessage('ratings يجب أن يكون مصفوفة'),
  body('overallScore').optional().isFloat({ min: 0, max: 5 }).withMessage('overallScore يجب أن يكون بين 0 و 5'),
  body('strengths').optional().trim().isString().withMessage('strengths يجب أن يكون نصاً'),
  body('improvements').optional().trim().isString().withMessage('improvements يجب أن يكون نصاً'),
  body('additionalComments').optional().trim().isString().withMessage('additionalComments يجب أن يكون نصاً'),
];

/* ═══ Feedback: Update ═══ */
const updateFeedback = [
  body('staffId').optional().custom(isObjectId).withMessage('staffId غير صالح'),
  body('reviewId').optional().custom(isObjectId).withMessage('reviewId غير صالح'),
  body('providerId').optional().custom(isObjectId).withMessage('providerId غير صالح'),
  body('type').optional().isIn(["peer","manager","subordinate","self","client","cross_functional","mentor","external","patient","family"]).withMessage('type غير صالح'),
  body('isAnonymous').optional().isBoolean().withMessage('isAnonymous يجب أن يكون قيمة منطقية'),
  body('ratings').optional().isArray().withMessage('ratings يجب أن يكون مصفوفة'),
  body('overallScore').optional().isFloat({ min: 0, max: 5 }).withMessage('overallScore يجب أن يكون بين 0 و 5'),
  body('strengths').optional().trim().isString().withMessage('strengths يجب أن يكون نصاً'),
  body('improvements').optional().trim().isString().withMessage('improvements يجب أن يكون نصاً'),
  body('additionalComments').optional().trim().isString().withMessage('additionalComments يجب أن يكون نصاً'),
];

/* ═══ PerformanceKPI: Create ═══ */
const createPerformanceKPI = [
  body('code').trim().notEmpty().withMessage('code مطلوب'),
  body('name').trim().notEmpty().withMessage('name مطلوب'),
  body('nameAr').optional().trim().isString().withMessage('nameAr يجب أن يكون نصاً'),
  body('category').isIn(["clinical_quality","patient_satisfaction","productivity","documentation","compliance","teamwork","innovation","leadership","communication","professional_development","attendance","safety"]).withMessage('category غير صالح'),
  body('targetValue').isFloat().withMessage('targetValue يجب أن يكون رقماً'),
  body('unit').trim().notEmpty().withMessage('unit مطلوب'),
  body('weight').optional().isFloat().withMessage('weight يجب أن يكون رقماً'),
  body('formula').optional().trim().isString().withMessage('formula يجب أن يكون نصاً'),
  body('dataSource').optional().trim().isString().withMessage('dataSource يجب أن يكون نصاً'),
  body('isActive').optional().isBoolean().withMessage('isActive يجب أن يكون قيمة منطقية'),
  body('applicableTo').optional().isArray().withMessage('applicableTo يجب أن يكون مصفوفة'),
];

/* ═══ PerformanceKPI: Update ═══ */
const updatePerformanceKPI = [
  body('code').optional().trim().isString().withMessage('code يجب أن يكون نصاً'),
  body('name').optional().trim().isString().withMessage('name يجب أن يكون نصاً'),
  body('nameAr').optional().trim().isString().withMessage('nameAr يجب أن يكون نصاً'),
  body('category').optional().isIn(["clinical_quality","patient_satisfaction","productivity","documentation","compliance","teamwork","innovation","leadership","communication","professional_development","attendance","safety"]).withMessage('category غير صالح'),
  body('targetValue').optional().isFloat().withMessage('targetValue يجب أن يكون رقماً'),
  body('unit').optional().trim().isString().withMessage('unit يجب أن يكون نصاً'),
  body('weight').optional().isFloat().withMessage('weight يجب أن يكون رقماً'),
  body('formula').optional().trim().isString().withMessage('formula يجب أن يكون نصاً'),
  body('dataSource').optional().trim().isString().withMessage('dataSource يجب أن يكون نصاً'),
  body('isActive').optional().isBoolean().withMessage('isActive يجب أن يكون قيمة منطقية'),
  body('applicableTo').optional().isArray().withMessage('applicableTo يجب أن يكون مصفوفة'),
];

module.exports = {
  createPerformanceReview,
  updatePerformanceReview,
  createPerformanceGoal,
  updatePerformanceGoal,
  createFeedback,
  updateFeedback,
  createPerformanceKPI,
  updatePerformanceKPI,
};