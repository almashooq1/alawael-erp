'use strict';
/**
 * SupplyChainTracker — Auto-Generated Validation Schemas
 * Generated by: scripts/gen-validation-from-model.js
 */

const { body, param } = require('express-validator');

const isObjectId = (value) => /^[a-f\d]{24}$/i.test(value);

/* ═══ LogisticsPartner: Create ═══ */
const createLogisticsPartner = [
  body('code').trim().notEmpty().withMessage('code مطلوب'),
  body('name').trim().notEmpty().withMessage('name مطلوب'),
  body('nameAr').optional().trim().isString().withMessage('nameAr يجب أن يكون نصاً'),
  body('type').isIn(["freight_forwarder","courier","warehouse_provider","customs_broker","packaging_supplier","cold_chain_specialist","last_mile","international_carrier","local_transport","medical_logistics"]).withMessage('type غير صالح'),
  body('country').optional().trim().isString().withMessage('country يجب أن يكون نصاً'),
  body('contactPerson').optional().trim().isString().withMessage('contactPerson يجب أن يكون نصاً'),
  body('email').optional().trim().isString().withMessage('email يجب أن يكون نصاً'),
  body('phone').optional().trim().isString().withMessage('phone يجب أن يكون نصاً'),
  body('website').optional().trim().isString().withMessage('website يجب أن يكون نصاً'),
  body('trackingUrlTemplate').optional().trim().isString().withMessage('trackingUrlTemplate يجب أن يكون نصاً'),
  body('apiKey').optional().trim().isString().withMessage('apiKey يجب أن يكون نصاً'),
  body('isActive').optional().isBoolean().withMessage('isActive يجب أن يكون قيمة منطقية'),
  body('rating').optional().isFloat({ min: 0, max: 5 }).withMessage('rating يجب أن يكون بين 0 و 5'),
];

/* ═══ LogisticsPartner: Update ═══ */
const updateLogisticsPartner = [
  body('code').optional().trim().isString().withMessage('code يجب أن يكون نصاً'),
  body('name').optional().trim().isString().withMessage('name يجب أن يكون نصاً'),
  body('nameAr').optional().trim().isString().withMessage('nameAr يجب أن يكون نصاً'),
  body('type').optional().isIn(["freight_forwarder","courier","warehouse_provider","customs_broker","packaging_supplier","cold_chain_specialist","last_mile","international_carrier","local_transport","medical_logistics"]).withMessage('type غير صالح'),
  body('country').optional().trim().isString().withMessage('country يجب أن يكون نصاً'),
  body('contactPerson').optional().trim().isString().withMessage('contactPerson يجب أن يكون نصاً'),
  body('email').optional().trim().isString().withMessage('email يجب أن يكون نصاً'),
  body('phone').optional().trim().isString().withMessage('phone يجب أن يكون نصاً'),
  body('website').optional().trim().isString().withMessage('website يجب أن يكون نصاً'),
  body('trackingUrlTemplate').optional().trim().isString().withMessage('trackingUrlTemplate يجب أن يكون نصاً'),
  body('apiKey').optional().trim().isString().withMessage('apiKey يجب أن يكون نصاً'),
  body('isActive').optional().isBoolean().withMessage('isActive يجب أن يكون قيمة منطقية'),
  body('rating').optional().isFloat({ min: 0, max: 5 }).withMessage('rating يجب أن يكون بين 0 و 5'),
];

/* ═══ Shipment: Create ═══ */
const createShipment = [
  body('trackingNumber').trim().notEmpty().withMessage('trackingNumber مطلوب'),
  body('type').isIn(["inbound_purchase","inbound_return","outbound_transfer","outbound_patient","inter_branch","emergency","maintenance_return","sample","hazardous","cold_chain","oversized","express"]).withMessage('type غير صالح'),
  body('status').optional().isIn(["created","label_generated","picked_up","in_transit","at_customs","out_for_delivery","delivered","failed_delivery","returned","cancelled","on_hold","lost"]).withMessage('status غير صالح'),
  body('priority').optional().isIn(["standard","expedited","express","same_day","next_day","critical_medical","emergency"]).withMessage('priority غير صالح'),
  body('transportMode').optional().isIn(["road","air","sea","rail","courier","express_courier","own_fleet","pickup","drone","multimodal"]).withMessage('transportMode غير صالح'),
  body('partnerId').optional().custom(isObjectId).withMessage('partnerId غير صالح'),
  body('purchaseOrderId').optional().custom(isObjectId).withMessage('purchaseOrderId غير صالح'),
  body('origin').optional().custom(isObjectId).withMessage('origin غير صالح'),
  body('destination').optional().custom(isObjectId).withMessage('destination غير صالح'),
  body('description').optional().trim().isString().withMessage('description يجب أن يكون نصاً'),
  body('quantity').optional().isFloat().withMessage('quantity يجب أن يكون رقماً'),
  body('weight').optional().isFloat().withMessage('weight يجب أن يكون رقماً'),
  body('dimensions').optional().trim().isString().withMessage('dimensions يجب أن يكون نصاً'),
  body('totalWeight').optional().isFloat().withMessage('totalWeight يجب أن يكون رقماً'),
  body('totalPackages').optional().isFloat().withMessage('totalPackages يجب أن يكون رقماً'),
  body('estimatedDeliveryDate').optional().isISO8601().withMessage('estimatedDeliveryDate يجب أن يكون تاريخ ISO صالح'),
  body('shippedAt').optional().isISO8601().withMessage('shippedAt يجب أن يكون تاريخ ISO صالح'),
  body('deliveredAt').optional().isISO8601().withMessage('deliveredAt يجب أن يكون تاريخ ISO صالح'),
  body('cost').optional().isFloat().withMessage('cost يجب أن يكون رقماً'),
  body('currency').optional().trim().isString().withMessage('currency يجب أن يكون نصاً'),
  body('insuranceValue').optional().isFloat().withMessage('insuranceValue يجب أن يكون رقماً'),
  body('specialInstructions').optional().trim().isString().withMessage('specialInstructions يجب أن يكون نصاً'),
  body('proofOfDelivery').optional().trim().isString().withMessage('proofOfDelivery يجب أن يكون نصاً'),
  body('createdBy').optional().custom(isObjectId).withMessage('createdBy غير صالح'),
];

/* ═══ Shipment: Update ═══ */
const updateShipment = [
  body('trackingNumber').optional().trim().isString().withMessage('trackingNumber يجب أن يكون نصاً'),
  body('type').optional().isIn(["inbound_purchase","inbound_return","outbound_transfer","outbound_patient","inter_branch","emergency","maintenance_return","sample","hazardous","cold_chain","oversized","express"]).withMessage('type غير صالح'),
  body('status').optional().isIn(["created","label_generated","picked_up","in_transit","at_customs","out_for_delivery","delivered","failed_delivery","returned","cancelled","on_hold","lost"]).withMessage('status غير صالح'),
  body('priority').optional().isIn(["standard","expedited","express","same_day","next_day","critical_medical","emergency"]).withMessage('priority غير صالح'),
  body('transportMode').optional().isIn(["road","air","sea","rail","courier","express_courier","own_fleet","pickup","drone","multimodal"]).withMessage('transportMode غير صالح'),
  body('partnerId').optional().custom(isObjectId).withMessage('partnerId غير صالح'),
  body('purchaseOrderId').optional().custom(isObjectId).withMessage('purchaseOrderId غير صالح'),
  body('origin').optional().custom(isObjectId).withMessage('origin غير صالح'),
  body('destination').optional().custom(isObjectId).withMessage('destination غير صالح'),
  body('description').optional().trim().isString().withMessage('description يجب أن يكون نصاً'),
  body('quantity').optional().isFloat().withMessage('quantity يجب أن يكون رقماً'),
  body('weight').optional().isFloat().withMessage('weight يجب أن يكون رقماً'),
  body('dimensions').optional().trim().isString().withMessage('dimensions يجب أن يكون نصاً'),
  body('totalWeight').optional().isFloat().withMessage('totalWeight يجب أن يكون رقماً'),
  body('totalPackages').optional().isFloat().withMessage('totalPackages يجب أن يكون رقماً'),
  body('estimatedDeliveryDate').optional().isISO8601().withMessage('estimatedDeliveryDate يجب أن يكون تاريخ ISO صالح'),
  body('shippedAt').optional().isISO8601().withMessage('shippedAt يجب أن يكون تاريخ ISO صالح'),
  body('deliveredAt').optional().isISO8601().withMessage('deliveredAt يجب أن يكون تاريخ ISO صالح'),
  body('cost').optional().isFloat().withMessage('cost يجب أن يكون رقماً'),
  body('currency').optional().trim().isString().withMessage('currency يجب أن يكون نصاً'),
  body('insuranceValue').optional().isFloat().withMessage('insuranceValue يجب أن يكون رقماً'),
  body('specialInstructions').optional().trim().isString().withMessage('specialInstructions يجب أن يكون نصاً'),
  body('proofOfDelivery').optional().trim().isString().withMessage('proofOfDelivery يجب أن يكون نصاً'),
  body('createdBy').optional().custom(isObjectId).withMessage('createdBy غير صالح'),
];

/* ═══ DeliveryRoute: Create ═══ */
const createDeliveryRoute = [
  body('routeCode').trim().notEmpty().withMessage('routeCode مطلوب'),
  body('name').trim().notEmpty().withMessage('name مطلوب'),
  body('nameAr').optional().trim().isString().withMessage('nameAr يجب أن يكون نصاً'),
  body('partnerId').optional().custom(isObjectId).withMessage('partnerId غير صالح'),
  body('driverId').optional().custom(isObjectId).withMessage('driverId غير صالح'),
  body('vehicleId').optional().trim().isString().withMessage('vehicleId يجب أن يكون نصاً'),
  body('order').optional().isFloat().withMessage('order يجب أن يكون رقماً'),
  body('locationId').optional().custom(isObjectId).withMessage('locationId غير صالح'),
  body('name').optional().trim().isString().withMessage('name يجب أن يكون نصاً'),
  body('address').optional().trim().isString().withMessage('address يجب أن يكون نصاً'),
  body('shipmentIds').optional().isArray().withMessage('shipmentIds يجب أن يكون مصفوفة'),
  body('estimatedArrival').optional().isISO8601().withMessage('estimatedArrival يجب أن يكون تاريخ ISO صالح'),
  body('actualArrival').optional().isISO8601().withMessage('actualArrival يجب أن يكون تاريخ ISO صالح'),
  body('status').optional().isIn(["pending","arrived","completed","skipped"]).withMessage('status غير صالح'),
  body('startTime').optional().isISO8601().withMessage('startTime يجب أن يكون تاريخ ISO صالح'),
  body('endTime').optional().isISO8601().withMessage('endTime يجب أن يكون تاريخ ISO صالح'),
  body('totalDistance').optional().isFloat().withMessage('totalDistance يجب أن يكون رقماً'),
  body('status').optional().isIn(["planned","in_progress","completed","cancelled"]).withMessage('status غير صالح'),
  body('isRecurring').optional().isBoolean().withMessage('isRecurring يجب أن يكون قيمة منطقية'),
  body('schedule').optional().trim().isString().withMessage('schedule يجب أن يكون نصاً'),
];

/* ═══ DeliveryRoute: Update ═══ */
const updateDeliveryRoute = [
  body('routeCode').optional().trim().isString().withMessage('routeCode يجب أن يكون نصاً'),
  body('name').optional().trim().isString().withMessage('name يجب أن يكون نصاً'),
  body('nameAr').optional().trim().isString().withMessage('nameAr يجب أن يكون نصاً'),
  body('partnerId').optional().custom(isObjectId).withMessage('partnerId غير صالح'),
  body('driverId').optional().custom(isObjectId).withMessage('driverId غير صالح'),
  body('vehicleId').optional().trim().isString().withMessage('vehicleId يجب أن يكون نصاً'),
  body('order').optional().isFloat().withMessage('order يجب أن يكون رقماً'),
  body('locationId').optional().custom(isObjectId).withMessage('locationId غير صالح'),
  body('name').optional().trim().isString().withMessage('name يجب أن يكون نصاً'),
  body('address').optional().trim().isString().withMessage('address يجب أن يكون نصاً'),
  body('shipmentIds').optional().isArray().withMessage('shipmentIds يجب أن يكون مصفوفة'),
  body('estimatedArrival').optional().isISO8601().withMessage('estimatedArrival يجب أن يكون تاريخ ISO صالح'),
  body('actualArrival').optional().isISO8601().withMessage('actualArrival يجب أن يكون تاريخ ISO صالح'),
  body('status').optional().isIn(["pending","arrived","completed","skipped"]).withMessage('status غير صالح'),
  body('startTime').optional().isISO8601().withMessage('startTime يجب أن يكون تاريخ ISO صالح'),
  body('endTime').optional().isISO8601().withMessage('endTime يجب أن يكون تاريخ ISO صالح'),
  body('totalDistance').optional().isFloat().withMessage('totalDistance يجب أن يكون رقماً'),
  body('status').optional().isIn(["planned","in_progress","completed","cancelled"]).withMessage('status غير صالح'),
  body('isRecurring').optional().isBoolean().withMessage('isRecurring يجب أن يكون قيمة منطقية'),
  body('schedule').optional().trim().isString().withMessage('schedule يجب أن يكون نصاً'),
];

/* ═══ SupplyChainEvent: Create ═══ */
const createSupplyChainEvent = [
  body('shipmentId').custom(isObjectId).withMessage('shipmentId غير صالح'),
  body('eventType').isIn(["order_placed","order_confirmed","picked","packed","shipped","in_transit_scan","customs_clearance","hub_arrival","hub_departure","out_for_delivery","delivery_attempted","delivered","signature_captured","exception","returned","damaged_reported"]).withMessage('eventType غير صالح'),
  body('timestamp').optional().isISO8601().withMessage('timestamp يجب أن يكون تاريخ ISO صالح'),
  body('location').optional().trim().isString().withMessage('location يجب أن يكون نصاً'),
  body('city').optional().trim().isString().withMessage('city يجب أن يكون نصاً'),
  body('country').optional().trim().isString().withMessage('country يجب أن يكون نصاً'),
  body('description').optional().trim().isString().withMessage('description يجب أن يكون نصاً'),
  body('performedBy').optional().trim().isString().withMessage('performedBy يجب أن يكون نصاً'),
  body('gpsCoordinates').optional().trim().isString().withMessage('gpsCoordinates يجب أن يكون نصاً'),
  body('attachments').optional().isArray().withMessage('attachments يجب أن يكون مصفوفة'),
];

/* ═══ SupplyChainEvent: Update ═══ */
const updateSupplyChainEvent = [
  body('shipmentId').optional().custom(isObjectId).withMessage('shipmentId غير صالح'),
  body('eventType').optional().isIn(["order_placed","order_confirmed","picked","packed","shipped","in_transit_scan","customs_clearance","hub_arrival","hub_departure","out_for_delivery","delivery_attempted","delivered","signature_captured","exception","returned","damaged_reported"]).withMessage('eventType غير صالح'),
  body('timestamp').optional().isISO8601().withMessage('timestamp يجب أن يكون تاريخ ISO صالح'),
  body('location').optional().trim().isString().withMessage('location يجب أن يكون نصاً'),
  body('city').optional().trim().isString().withMessage('city يجب أن يكون نصاً'),
  body('country').optional().trim().isString().withMessage('country يجب أن يكون نصاً'),
  body('description').optional().trim().isString().withMessage('description يجب أن يكون نصاً'),
  body('performedBy').optional().trim().isString().withMessage('performedBy يجب أن يكون نصاً'),
  body('gpsCoordinates').optional().trim().isString().withMessage('gpsCoordinates يجب أن يكون نصاً'),
  body('attachments').optional().isArray().withMessage('attachments يجب أن يكون مصفوفة'),
];

module.exports = {
  createLogisticsPartner,
  updateLogisticsPartner,
  createShipment,
  updateShipment,
  createDeliveryRoute,
  updateDeliveryRoute,
  createSupplyChainEvent,
  updateSupplyChainEvent,
};