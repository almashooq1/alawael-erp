'use strict';
/**
 * TaskQueue — Auto-Generated Validation Schemas
 * Generated by: scripts/gen-validation-from-model.js
 */

const { body, param } = require('express-validator');

const isObjectId = (value) => /^[a-f\d]{24}$/i.test(value);

/* ═══ Job: Create ═══ */
const createJob = [
  body('queue').trim().notEmpty().withMessage('queue مطلوب'),
  body('type').trim().notEmpty().withMessage('type مطلوب'),
  body('priority').optional().isIn(["critical","high","normal","low"]).withMessage('priority غير صالح'),
  body('priorityWeight').optional().isFloat().withMessage('priorityWeight يجب أن يكون رقماً'),
  body('status').optional().isIn(["pending","processing","completed","failed","dead","cancelled","delayed"]).withMessage('status غير صالح'),
  body('attempts').optional().isFloat().withMessage('attempts يجب أن يكون رقماً'),
  body('maxAttempts').optional().isFloat().withMessage('maxAttempts يجب أن يكون رقماً'),
  body('retryDelayMs').optional().isFloat().withMessage('retryDelayMs يجب أن يكون رقماً'),
  body('errors').optional().isArray().withMessage('errors يجب أن يكون مصفوفة'),
  body('scheduledAt').optional().isISO8601().withMessage('scheduledAt يجب أن يكون تاريخ ISO صالح'),
  body('startedAt').optional().isISO8601().withMessage('startedAt يجب أن يكون تاريخ ISO صالح'),
  body('duration').optional().isFloat().withMessage('duration يجب أن يكون رقماً'),
  body('progress').optional().isFloat({ min: 0, max: 100 }).withMessage('progress يجب أن يكون بين 0 و 100'),
  body('progressMessage').optional().trim().isString().withMessage('progressMessage يجب أن يكون نصاً'),
  body('domain').optional().trim().isString().withMessage('domain يجب أن يكون نصاً'),
  body('userId').optional().custom(isObjectId).withMessage('userId غير صالح'),
  body('branchId').optional().custom(isObjectId).withMessage('branchId غير صالح'),
  body('correlationId').optional().trim().isString().withMessage('correlationId يجب أن يكون نصاً'),
  body('isDeleted').optional().isBoolean().withMessage('isDeleted يجب أن يكون قيمة منطقية'),
];

/* ═══ Job: Update ═══ */
const updateJob = [
  body('queue').optional().trim().isString().withMessage('queue يجب أن يكون نصاً'),
  body('type').optional().trim().isString().withMessage('type يجب أن يكون نصاً'),
  body('priority').optional().isIn(["critical","high","normal","low"]).withMessage('priority غير صالح'),
  body('priorityWeight').optional().isFloat().withMessage('priorityWeight يجب أن يكون رقماً'),
  body('status').optional().isIn(["pending","processing","completed","failed","dead","cancelled","delayed"]).withMessage('status غير صالح'),
  body('attempts').optional().isFloat().withMessage('attempts يجب أن يكون رقماً'),
  body('maxAttempts').optional().isFloat().withMessage('maxAttempts يجب أن يكون رقماً'),
  body('retryDelayMs').optional().isFloat().withMessage('retryDelayMs يجب أن يكون رقماً'),
  body('errors').optional().isArray().withMessage('errors يجب أن يكون مصفوفة'),
  body('scheduledAt').optional().isISO8601().withMessage('scheduledAt يجب أن يكون تاريخ ISO صالح'),
  body('startedAt').optional().isISO8601().withMessage('startedAt يجب أن يكون تاريخ ISO صالح'),
  body('duration').optional().isFloat().withMessage('duration يجب أن يكون رقماً'),
  body('progress').optional().isFloat({ min: 0, max: 100 }).withMessage('progress يجب أن يكون بين 0 و 100'),
  body('progressMessage').optional().trim().isString().withMessage('progressMessage يجب أن يكون نصاً'),
  body('domain').optional().trim().isString().withMessage('domain يجب أن يكون نصاً'),
  body('userId').optional().custom(isObjectId).withMessage('userId غير صالح'),
  body('branchId').optional().custom(isObjectId).withMessage('branchId غير صالح'),
  body('correlationId').optional().trim().isString().withMessage('correlationId يجب أن يكون نصاً'),
  body('isDeleted').optional().isBoolean().withMessage('isDeleted يجب أن يكون قيمة منطقية'),
];

module.exports = {
  createJob,
  updateJob,
};