'use strict';
/**
 * TransportManager — Auto-Generated Validation Schemas
 * Generated by: scripts/gen-validation-from-model.js
 */

const { body, param } = require('express-validator');

const isObjectId = (value) => /^[a-f\d]{24}$/i.test(value);

/* ═══ Vehicle: Create ═══ */
const createVehicle = [
  body('vehicleCode').trim().notEmpty().withMessage('vehicleCode مطلوب'),
  body('plateNumber').trim().notEmpty().withMessage('plateNumber مطلوب'),
  body('type').isIn(["sedan","minivan","wheelchair_van","ambulance","bus","mini_bus","suv","cargo_van","specialized_medical","electric_vehicle","hybrid","stretcher_van"]).withMessage('type غير صالح'),
  body('status').optional().isIn(["available","in_service","en_route","maintenance","out_of_service","reserved","fueling","inspection","decommissioned","standby"]).withMessage('status غير صالح'),
  body('make').optional().trim().isString().withMessage('make يجب أن يكون نصاً'),
  body('model').optional().trim().isString().withMessage('model يجب أن يكون نصاً'),
  body('year').optional().isFloat().withMessage('year يجب أن يكون رقماً'),
  body('color').optional().trim().isString().withMessage('color يجب أن يكون نصاً'),
  body('capacity').optional().isFloat().withMessage('capacity يجب أن يكون رقماً'),
  body('wheelchairCapacity').optional().isFloat().withMessage('wheelchairCapacity يجب أن يكون رقماً'),
  body('fuelType').optional().isIn(["gasoline","diesel","electric","hybrid","cng"]).withMessage('fuelType غير صالح'),
  body('currentMileage').optional().isFloat().withMessage('currentMileage يجب أن يكون رقماً'),
  body('lastServiceDate').optional().isISO8601().withMessage('lastServiceDate يجب أن يكون تاريخ ISO صالح'),
  body('nextServiceDate').optional().isISO8601().withMessage('nextServiceDate يجب أن يكون تاريخ ISO صالح'),
  body('insuranceExpiry').optional().isISO8601().withMessage('insuranceExpiry يجب أن يكون تاريخ ISO صالح'),
  body('registrationExpiry').optional().isISO8601().withMessage('registrationExpiry يجب أن يكون تاريخ ISO صالح'),
  body('features').optional().isArray().withMessage('features يجب أن يكون مصفوفة'),
  body('gpsTrackerId').optional().trim().isString().withMessage('gpsTrackerId يجب أن يكون نصاً'),
  body('isActive').optional().isBoolean().withMessage('isActive يجب أن يكون قيمة منطقية'),
];

/* ═══ Vehicle: Update ═══ */
const updateVehicle = [
  body('vehicleCode').optional().trim().isString().withMessage('vehicleCode يجب أن يكون نصاً'),
  body('plateNumber').optional().trim().isString().withMessage('plateNumber يجب أن يكون نصاً'),
  body('type').optional().isIn(["sedan","minivan","wheelchair_van","ambulance","bus","mini_bus","suv","cargo_van","specialized_medical","electric_vehicle","hybrid","stretcher_van"]).withMessage('type غير صالح'),
  body('status').optional().isIn(["available","in_service","en_route","maintenance","out_of_service","reserved","fueling","inspection","decommissioned","standby"]).withMessage('status غير صالح'),
  body('make').optional().trim().isString().withMessage('make يجب أن يكون نصاً'),
  body('model').optional().trim().isString().withMessage('model يجب أن يكون نصاً'),
  body('year').optional().isFloat().withMessage('year يجب أن يكون رقماً'),
  body('color').optional().trim().isString().withMessage('color يجب أن يكون نصاً'),
  body('capacity').optional().isFloat().withMessage('capacity يجب أن يكون رقماً'),
  body('wheelchairCapacity').optional().isFloat().withMessage('wheelchairCapacity يجب أن يكون رقماً'),
  body('fuelType').optional().isIn(["gasoline","diesel","electric","hybrid","cng"]).withMessage('fuelType غير صالح'),
  body('currentMileage').optional().isFloat().withMessage('currentMileage يجب أن يكون رقماً'),
  body('lastServiceDate').optional().isISO8601().withMessage('lastServiceDate يجب أن يكون تاريخ ISO صالح'),
  body('nextServiceDate').optional().isISO8601().withMessage('nextServiceDate يجب أن يكون تاريخ ISO صالح'),
  body('insuranceExpiry').optional().isISO8601().withMessage('insuranceExpiry يجب أن يكون تاريخ ISO صالح'),
  body('registrationExpiry').optional().isISO8601().withMessage('registrationExpiry يجب أن يكون تاريخ ISO صالح'),
  body('features').optional().isArray().withMessage('features يجب أن يكون مصفوفة'),
  body('gpsTrackerId').optional().trim().isString().withMessage('gpsTrackerId يجب أن يكون نصاً'),
  body('isActive').optional().isBoolean().withMessage('isActive يجب أن يكون قيمة منطقية'),
];

/* ═══ Driver: Create ═══ */
const createDriver = [
  body('driverCode').trim().notEmpty().withMessage('driverCode مطلوب'),
  body('userId').optional().custom(isObjectId).withMessage('userId غير صالح'),
  body('name').trim().notEmpty().withMessage('name مطلوب'),
  body('nameAr').optional().trim().isString().withMessage('nameAr يجب أن يكون نصاً'),
  body('phone').optional().trim().isString().withMessage('phone يجب أن يكون نصاً'),
  body('licenseNumber').trim().notEmpty().withMessage('licenseNumber مطلوب'),
  body('licenseExpiry').optional().isISO8601().withMessage('licenseExpiry يجب أن يكون تاريخ ISO صالح'),
  body('status').optional().isIn(["available","on_duty","off_duty","on_break","on_trip","training","sick_leave","vacation","suspended","inactive"]).withMessage('status غير صالح'),
  body('certifications').optional().isArray().withMessage('certifications يجب أن يكون مصفوفة'),
  body('assignedVehicle').optional().custom(isObjectId).withMessage('assignedVehicle غير صالح'),
  body('rating').optional().isFloat({ min: 0, max: 5 }).withMessage('rating يجب أن يكون بين 0 و 5'),
  body('totalTrips').optional().isFloat().withMessage('totalTrips يجب أن يكون رقماً'),
  body('violations').optional().isArray().withMessage('violations يجب أن يكون مصفوفة'),
  body('emergencyContact').optional().trim().isString().withMessage('emergencyContact يجب أن يكون نصاً'),
  body('isActive').optional().isBoolean().withMessage('isActive يجب أن يكون قيمة منطقية'),
];

/* ═══ Driver: Update ═══ */
const updateDriver = [
  body('driverCode').optional().trim().isString().withMessage('driverCode يجب أن يكون نصاً'),
  body('userId').optional().custom(isObjectId).withMessage('userId غير صالح'),
  body('name').optional().trim().isString().withMessage('name يجب أن يكون نصاً'),
  body('nameAr').optional().trim().isString().withMessage('nameAr يجب أن يكون نصاً'),
  body('phone').optional().trim().isString().withMessage('phone يجب أن يكون نصاً'),
  body('licenseNumber').optional().trim().isString().withMessage('licenseNumber يجب أن يكون نصاً'),
  body('licenseExpiry').optional().isISO8601().withMessage('licenseExpiry يجب أن يكون تاريخ ISO صالح'),
  body('status').optional().isIn(["available","on_duty","off_duty","on_break","on_trip","training","sick_leave","vacation","suspended","inactive"]).withMessage('status غير صالح'),
  body('certifications').optional().isArray().withMessage('certifications يجب أن يكون مصفوفة'),
  body('assignedVehicle').optional().custom(isObjectId).withMessage('assignedVehicle غير صالح'),
  body('rating').optional().isFloat({ min: 0, max: 5 }).withMessage('rating يجب أن يكون بين 0 و 5'),
  body('totalTrips').optional().isFloat().withMessage('totalTrips يجب أن يكون رقماً'),
  body('violations').optional().isArray().withMessage('violations يجب أن يكون مصفوفة'),
  body('emergencyContact').optional().trim().isString().withMessage('emergencyContact يجب أن يكون نصاً'),
  body('isActive').optional().isBoolean().withMessage('isActive يجب أن يكون قيمة منطقية'),
];

/* ═══ TransportSchedule: Create ═══ */
const createTransportSchedule = [
  body('scheduleCode').trim().notEmpty().withMessage('scheduleCode مطلوب'),
  body('name').trim().notEmpty().withMessage('name مطلوب'),
  body('nameAr').optional().trim().isString().withMessage('nameAr يجب أن يكون نصاً'),
  body('type').isIn(["daily_route","weekly_recurring","monthly_route","on_demand","emergency","school_transport","clinic_shuttle","therapy_shuttle","staff_transport","special_event"]).withMessage('type غير صالح'),
  body('vehicleId').optional().custom(isObjectId).withMessage('vehicleId غير صالح'),
  body('driverId').optional().custom(isObjectId).withMessage('driverId غير صالح'),
  body('route').optional().trim().isString().withMessage('route يجب أن يكون نصاً'),
  body('recurrence').optional().trim().isString().withMessage('recurrence يجب أن يكون نصاً'),
  body('effectiveFrom').optional().isISO8601().withMessage('effectiveFrom يجب أن يكون تاريخ ISO صالح'),
  body('effectiveTo').optional().isISO8601().withMessage('effectiveTo يجب أن يكون تاريخ ISO صالح'),
  body('maxPassengers').optional().isFloat().withMessage('maxPassengers يجب أن يكون رقماً'),
  body('isActive').optional().isBoolean().withMessage('isActive يجب أن يكون قيمة منطقية'),
];

/* ═══ TransportSchedule: Update ═══ */
const updateTransportSchedule = [
  body('scheduleCode').optional().trim().isString().withMessage('scheduleCode يجب أن يكون نصاً'),
  body('name').optional().trim().isString().withMessage('name يجب أن يكون نصاً'),
  body('nameAr').optional().trim().isString().withMessage('nameAr يجب أن يكون نصاً'),
  body('type').optional().isIn(["daily_route","weekly_recurring","monthly_route","on_demand","emergency","school_transport","clinic_shuttle","therapy_shuttle","staff_transport","special_event"]).withMessage('type غير صالح'),
  body('vehicleId').optional().custom(isObjectId).withMessage('vehicleId غير صالح'),
  body('driverId').optional().custom(isObjectId).withMessage('driverId غير صالح'),
  body('route').optional().trim().isString().withMessage('route يجب أن يكون نصاً'),
  body('recurrence').optional().trim().isString().withMessage('recurrence يجب أن يكون نصاً'),
  body('effectiveFrom').optional().isISO8601().withMessage('effectiveFrom يجب أن يكون تاريخ ISO صالح'),
  body('effectiveTo').optional().isISO8601().withMessage('effectiveTo يجب أن يكون تاريخ ISO صالح'),
  body('maxPassengers').optional().isFloat().withMessage('maxPassengers يجب أن يكون رقماً'),
  body('isActive').optional().isBoolean().withMessage('isActive يجب أن يكون قيمة منطقية'),
];

/* ═══ TransportPolicy: Create ═══ */
const createTransportPolicy = [
  body('code').trim().notEmpty().withMessage('code مطلوب'),
  body('name').trim().notEmpty().withMessage('name مطلوب'),
  body('nameAr').optional().trim().isString().withMessage('nameAr يجب أن يكون نصاً'),
  body('category').isIn(["safety","accessibility","scheduling","maintenance","driver_conduct","passenger_rights","emergency_protocol","fuel_management","insurance","compliance","vehicle_assignment","route_planning"]).withMessage('category غير صالح'),
  body('description').optional().trim().isString().withMessage('description يجب أن يكون نصاً'),
  body('rules').optional().isArray().withMessage('rules يجب أن يكون مصفوفة'),
  body('approvedBy').optional().custom(isObjectId).withMessage('approvedBy غير صالح'),
  body('effectiveDate').optional().isISO8601().withMessage('effectiveDate يجب أن يكون تاريخ ISO صالح'),
  body('nextReviewDate').optional().isISO8601().withMessage('nextReviewDate يجب أن يكون تاريخ ISO صالح'),
  body('isActive').optional().isBoolean().withMessage('isActive يجب أن يكون قيمة منطقية'),
];

/* ═══ TransportPolicy: Update ═══ */
const updateTransportPolicy = [
  body('code').optional().trim().isString().withMessage('code يجب أن يكون نصاً'),
  body('name').optional().trim().isString().withMessage('name يجب أن يكون نصاً'),
  body('nameAr').optional().trim().isString().withMessage('nameAr يجب أن يكون نصاً'),
  body('category').optional().isIn(["safety","accessibility","scheduling","maintenance","driver_conduct","passenger_rights","emergency_protocol","fuel_management","insurance","compliance","vehicle_assignment","route_planning"]).withMessage('category غير صالح'),
  body('description').optional().trim().isString().withMessage('description يجب أن يكون نصاً'),
  body('rules').optional().isArray().withMessage('rules يجب أن يكون مصفوفة'),
  body('approvedBy').optional().custom(isObjectId).withMessage('approvedBy غير صالح'),
  body('effectiveDate').optional().isISO8601().withMessage('effectiveDate يجب أن يكون تاريخ ISO صالح'),
  body('nextReviewDate').optional().isISO8601().withMessage('nextReviewDate يجب أن يكون تاريخ ISO صالح'),
  body('isActive').optional().isBoolean().withMessage('isActive يجب أن يكون قيمة منطقية'),
];

module.exports = {
  createVehicle,
  updateVehicle,
  createDriver,
  updateDriver,
  createTransportSchedule,
  updateTransportSchedule,
  createTransportPolicy,
  updateTransportPolicy,
};