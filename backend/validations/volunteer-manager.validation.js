'use strict';
/**
 * VolunteerManager — Auto-Generated Validation Schemas
 * Generated by: scripts/gen-validation-from-model.js
 */

const { body, param } = require('express-validator');

const isObjectId = (value) => /^[a-f\d]{24}$/i.test(value);

/* ═══ Volunteer: Create ═══ */
const createVolunteer = [
  body('volunteerCode').trim().notEmpty().withMessage('volunteerCode مطلوب'),
  body('userId').optional().custom(isObjectId).withMessage('userId غير صالح'),
  body('name').trim().notEmpty().withMessage('name مطلوب'),
  body('nameAr').optional().trim().isString().withMessage('nameAr يجب أن يكون نصاً'),
  body('email').optional().trim().isString().withMessage('email يجب أن يكون نصاً'),
  body('phone').optional().trim().isString().withMessage('phone يجب أن يكون نصاً'),
  body('status').optional().isIn(["applicant","screening","approved","active","on_leave","inactive","suspended","alumni","pending_training","retired"]).withMessage('status غير صالح'),
  body('category').optional().isIn(["clinical_support","administrative","therapy_assistant","transport_escort","event_support","fundraising","community_outreach","mentoring","translation","recreational","educational","technical"]).withMessage('category غير صالح'),
  body('dateOfBirth').optional().isISO8601().withMessage('dateOfBirth يجب أن يكون تاريخ ISO صالح'),
  body('address').optional().trim().isString().withMessage('address يجب أن يكون نصاً'),
  body('availability').optional().isArray().withMessage('availability يجب أن يكون مصفوفة'),
  body('startDate').optional().isISO8601().withMessage('startDate يجب أن يكون تاريخ ISO صالح'),
  body('totalHours').optional().isFloat().withMessage('totalHours يجب أن يكون رقماً'),
  body('backgroundCheckDate').optional().isISO8601().withMessage('backgroundCheckDate يجب أن يكون تاريخ ISO صالح'),
  body('backgroundCheckStatus').optional().isIn(["pending","cleared","flagged","expired"]).withMessage('backgroundCheckStatus غير صالح'),
  body('emergencyContact').optional().trim().isString().withMessage('emergencyContact يجب أن يكون نصاً'),
  body('notes').optional().trim().isString().withMessage('notes يجب أن يكون نصاً'),
  body('isActive').optional().isBoolean().withMessage('isActive يجب أن يكون قيمة منطقية'),
];

/* ═══ Volunteer: Update ═══ */
const updateVolunteer = [
  body('volunteerCode').optional().trim().isString().withMessage('volunteerCode يجب أن يكون نصاً'),
  body('userId').optional().custom(isObjectId).withMessage('userId غير صالح'),
  body('name').optional().trim().isString().withMessage('name يجب أن يكون نصاً'),
  body('nameAr').optional().trim().isString().withMessage('nameAr يجب أن يكون نصاً'),
  body('email').optional().trim().isString().withMessage('email يجب أن يكون نصاً'),
  body('phone').optional().trim().isString().withMessage('phone يجب أن يكون نصاً'),
  body('status').optional().isIn(["applicant","screening","approved","active","on_leave","inactive","suspended","alumni","pending_training","retired"]).withMessage('status غير صالح'),
  body('category').optional().isIn(["clinical_support","administrative","therapy_assistant","transport_escort","event_support","fundraising","community_outreach","mentoring","translation","recreational","educational","technical"]).withMessage('category غير صالح'),
  body('dateOfBirth').optional().isISO8601().withMessage('dateOfBirth يجب أن يكون تاريخ ISO صالح'),
  body('address').optional().trim().isString().withMessage('address يجب أن يكون نصاً'),
  body('availability').optional().isArray().withMessage('availability يجب أن يكون مصفوفة'),
  body('startDate').optional().isISO8601().withMessage('startDate يجب أن يكون تاريخ ISO صالح'),
  body('totalHours').optional().isFloat().withMessage('totalHours يجب أن يكون رقماً'),
  body('backgroundCheckDate').optional().isISO8601().withMessage('backgroundCheckDate يجب أن يكون تاريخ ISO صالح'),
  body('backgroundCheckStatus').optional().isIn(["pending","cleared","flagged","expired"]).withMessage('backgroundCheckStatus غير صالح'),
  body('emergencyContact').optional().trim().isString().withMessage('emergencyContact يجب أن يكون نصاً'),
  body('notes').optional().trim().isString().withMessage('notes يجب أن يكون نصاً'),
  body('isActive').optional().isBoolean().withMessage('isActive يجب أن يكون قيمة منطقية'),
];

/* ═══ VolunteerShift: Create ═══ */
const createVolunteerShift = [
  body('shiftCode').trim().notEmpty().withMessage('shiftCode مطلوب'),
  body('volunteerId').custom(isObjectId).withMessage('volunteerId غير صالح'),
  body('title').trim().notEmpty().withMessage('title مطلوب'),
  body('status').optional().isIn(["scheduled","confirmed","checked_in","in_progress","completed","cancelled","no_show","rescheduled","pending_approval","swapped"]).withMessage('status غير صالح'),
  body('department').optional().trim().isString().withMessage('department يجب أن يكون نصاً'),
  body('location').optional().trim().isString().withMessage('location يجب أن يكون نصاً'),
  body('scheduledDate').isISO8601().withMessage('scheduledDate يجب أن يكون تاريخ ISO صالح'),
  body('startTime').optional().trim().isString().withMessage('startTime يجب أن يكون نصاً'),
  body('endTime').optional().trim().isString().withMessage('endTime يجب أن يكون نصاً'),
  body('hoursWorked').optional().isFloat().withMessage('hoursWorked يجب أن يكون رقماً'),
  body('supervisor').optional().custom(isObjectId).withMessage('supervisor غير صالح'),
  body('tasks').optional().isArray().withMessage('tasks يجب أن يكون مصفوفة'),
  body('rating').optional().isFloat({ min: 1, max: 5 }).withMessage('rating يجب أن يكون بين 1 و 5'),
  body('feedback').optional().trim().isString().withMessage('feedback يجب أن يكون نصاً'),
];

/* ═══ VolunteerShift: Update ═══ */
const updateVolunteerShift = [
  body('shiftCode').optional().trim().isString().withMessage('shiftCode يجب أن يكون نصاً'),
  body('volunteerId').optional().custom(isObjectId).withMessage('volunteerId غير صالح'),
  body('title').optional().trim().isString().withMessage('title يجب أن يكون نصاً'),
  body('status').optional().isIn(["scheduled","confirmed","checked_in","in_progress","completed","cancelled","no_show","rescheduled","pending_approval","swapped"]).withMessage('status غير صالح'),
  body('department').optional().trim().isString().withMessage('department يجب أن يكون نصاً'),
  body('location').optional().trim().isString().withMessage('location يجب أن يكون نصاً'),
  body('scheduledDate').optional().isISO8601().withMessage('scheduledDate يجب أن يكون تاريخ ISO صالح'),
  body('startTime').optional().trim().isString().withMessage('startTime يجب أن يكون نصاً'),
  body('endTime').optional().trim().isString().withMessage('endTime يجب أن يكون نصاً'),
  body('hoursWorked').optional().isFloat().withMessage('hoursWorked يجب أن يكون رقماً'),
  body('supervisor').optional().custom(isObjectId).withMessage('supervisor غير صالح'),
  body('tasks').optional().isArray().withMessage('tasks يجب أن يكون مصفوفة'),
  body('rating').optional().isFloat({ min: 1, max: 5 }).withMessage('rating يجب أن يكون بين 1 و 5'),
  body('feedback').optional().trim().isString().withMessage('feedback يجب أن يكون نصاً'),
];

/* ═══ VolunteerSkill: Create ═══ */
const createVolunteerSkill = [
  body('volunteerId').custom(isObjectId).withMessage('volunteerId غير صالح'),
  body('skillName').trim().notEmpty().withMessage('skillName مطلوب'),
  body('category').optional().trim().isString().withMessage('category يجب أن يكون نصاً'),
  body('level').optional().isIn(["beginner","intermediate","advanced","expert","certified","master","in_training","refresher_needed","expired","pending_verification"]).withMessage('level غير صالح'),
  body('certifiedDate').optional().isISO8601().withMessage('certifiedDate يجب أن يكون تاريخ ISO صالح'),
  body('expiryDate').optional().isISO8601().withMessage('expiryDate يجب أن يكون تاريخ ISO صالح'),
  body('verifiedBy').optional().custom(isObjectId).withMessage('verifiedBy غير صالح'),
  body('trainingHours').optional().isFloat().withMessage('trainingHours يجب أن يكون رقماً'),
  body('notes').optional().trim().isString().withMessage('notes يجب أن يكون نصاً'),
];

/* ═══ VolunteerSkill: Update ═══ */
const updateVolunteerSkill = [
  body('volunteerId').optional().custom(isObjectId).withMessage('volunteerId غير صالح'),
  body('skillName').optional().trim().isString().withMessage('skillName يجب أن يكون نصاً'),
  body('category').optional().trim().isString().withMessage('category يجب أن يكون نصاً'),
  body('level').optional().isIn(["beginner","intermediate","advanced","expert","certified","master","in_training","refresher_needed","expired","pending_verification"]).withMessage('level غير صالح'),
  body('certifiedDate').optional().isISO8601().withMessage('certifiedDate يجب أن يكون تاريخ ISO صالح'),
  body('expiryDate').optional().isISO8601().withMessage('expiryDate يجب أن يكون تاريخ ISO صالح'),
  body('verifiedBy').optional().custom(isObjectId).withMessage('verifiedBy غير صالح'),
  body('trainingHours').optional().isFloat().withMessage('trainingHours يجب أن يكون رقماً'),
  body('notes').optional().trim().isString().withMessage('notes يجب أن يكون نصاً'),
];

/* ═══ VolunteerRecognition: Create ═══ */
const createVolunteerRecognition = [
  body('recognitionCode').trim().notEmpty().withMessage('recognitionCode مطلوب'),
  body('volunteerId').custom(isObjectId).withMessage('volunteerId غير صالح'),
  body('type').isIn(["hours_milestone","excellence_award","innovation_award","service_anniversary","leadership_award","community_impact","patient_appreciation","team_player","mentor_award","outstanding_commitment","special_recognition","certificate"]).withMessage('type غير صالح'),
  body('title').trim().notEmpty().withMessage('title مطلوب'),
  body('description').optional().trim().isString().withMessage('description يجب أن يكون نصاً'),
  body('awardedDate').optional().isISO8601().withMessage('awardedDate يجب أن يكون تاريخ ISO صالح'),
  body('awardedBy').optional().custom(isObjectId).withMessage('awardedBy غير صالح'),
  body('hoursAtAward').optional().isFloat().withMessage('hoursAtAward يجب أن يكون رقماً'),
  body('certificateUrl').optional().trim().isString().withMessage('certificateUrl يجب أن يكون نصاً'),
  body('isPublic').optional().isBoolean().withMessage('isPublic يجب أن يكون قيمة منطقية'),
];

/* ═══ VolunteerRecognition: Update ═══ */
const updateVolunteerRecognition = [
  body('recognitionCode').optional().trim().isString().withMessage('recognitionCode يجب أن يكون نصاً'),
  body('volunteerId').optional().custom(isObjectId).withMessage('volunteerId غير صالح'),
  body('type').optional().isIn(["hours_milestone","excellence_award","innovation_award","service_anniversary","leadership_award","community_impact","patient_appreciation","team_player","mentor_award","outstanding_commitment","special_recognition","certificate"]).withMessage('type غير صالح'),
  body('title').optional().trim().isString().withMessage('title يجب أن يكون نصاً'),
  body('description').optional().trim().isString().withMessage('description يجب أن يكون نصاً'),
  body('awardedDate').optional().isISO8601().withMessage('awardedDate يجب أن يكون تاريخ ISO صالح'),
  body('awardedBy').optional().custom(isObjectId).withMessage('awardedBy غير صالح'),
  body('hoursAtAward').optional().isFloat().withMessage('hoursAtAward يجب أن يكون رقماً'),
  body('certificateUrl').optional().trim().isString().withMessage('certificateUrl يجب أن يكون نصاً'),
  body('isPublic').optional().isBoolean().withMessage('isPublic يجب أن يكون قيمة منطقية'),
];

module.exports = {
  createVolunteer,
  updateVolunteer,
  createVolunteerShift,
  updateVolunteerShift,
  createVolunteerSkill,
  updateVolunteerSkill,
  createVolunteerRecognition,
  updateVolunteerRecognition,
};