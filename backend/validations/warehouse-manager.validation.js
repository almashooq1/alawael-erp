'use strict';
/**
 * WarehouseManager — Auto-Generated Validation Schemas
 * Generated by: scripts/gen-validation-from-model.js
 */

const { body, param } = require('express-validator');

const isObjectId = (value) => /^[a-f\d]{24}$/i.test(value);

/* ═══ Warehouse: Create ═══ */
const createWarehouse = [
  body('code').trim().notEmpty().withMessage('code مطلوب'),
  body('name').trim().notEmpty().withMessage('name مطلوب'),
  body('nameAr').optional().trim().isString().withMessage('nameAr يجب أن يكون نصاً'),
  body('type').isIn(["central","satellite","cold_storage","hazardous_materials","sterile","general","distribution_centre","quarantine","returns_processing","archive","mobile_unit"]).withMessage('type غير صالح'),
  body('status').optional().isIn(["active","inactive","under_maintenance","full_capacity","restricted","decommissioned","planned","temporary"]).withMessage('status غير صالح'),
  body('locationId').optional().custom(isObjectId).withMessage('locationId غير صالح'),
  body('address').optional().trim().isString().withMessage('address يجب أن يكون نصاً'),
  body('capacity').optional().isFloat().withMessage('capacity يجب أن يكون رقماً'),
  body('usedCapacity').optional().isFloat().withMessage('usedCapacity يجب أن يكون رقماً'),
  body('code').optional().trim().isString().withMessage('code يجب أن يكون نصاً'),
  body('name').optional().trim().isString().withMessage('name يجب أن يكون نصاً'),
  body('type').optional().isIn(["receiving","storage","picking","packing","shipping","quarantine","returns","staging","cold_chain","hazardous","high_value","bulk"]).withMessage('type غير صالح'),
  body('capacity').optional().isFloat().withMessage('capacity يجب أن يكون رقماً'),
  body('managerId').optional().custom(isObjectId).withMessage('managerId غير صالح'),
  body('operatingHours').optional().trim().isString().withMessage('operatingHours يجب أن يكون نصاً'),
  body('contactPhone').optional().trim().isString().withMessage('contactPhone يجب أن يكون نصاً'),
  body('contactEmail').optional().trim().isString().withMessage('contactEmail يجب أن يكون نصاً'),
  body('isTemperatureControlled').optional().isBoolean().withMessage('isTemperatureControlled يجب أن يكون قيمة منطقية'),
  body('temperatureRange').optional().trim().isString().withMessage('temperatureRange يجب أن يكون نصاً'),
];

/* ═══ Warehouse: Update ═══ */
const updateWarehouse = [
  body('code').optional().trim().isString().withMessage('code يجب أن يكون نصاً'),
  body('name').optional().trim().isString().withMessage('name يجب أن يكون نصاً'),
  body('nameAr').optional().trim().isString().withMessage('nameAr يجب أن يكون نصاً'),
  body('type').optional().isIn(["central","satellite","cold_storage","hazardous_materials","sterile","general","distribution_centre","quarantine","returns_processing","archive","mobile_unit"]).withMessage('type غير صالح'),
  body('status').optional().isIn(["active","inactive","under_maintenance","full_capacity","restricted","decommissioned","planned","temporary"]).withMessage('status غير صالح'),
  body('locationId').optional().custom(isObjectId).withMessage('locationId غير صالح'),
  body('address').optional().trim().isString().withMessage('address يجب أن يكون نصاً'),
  body('capacity').optional().isFloat().withMessage('capacity يجب أن يكون رقماً'),
  body('usedCapacity').optional().isFloat().withMessage('usedCapacity يجب أن يكون رقماً'),
  body('code').optional().trim().isString().withMessage('code يجب أن يكون نصاً'),
  body('name').optional().trim().isString().withMessage('name يجب أن يكون نصاً'),
  body('type').optional().isIn(["receiving","storage","picking","packing","shipping","quarantine","returns","staging","cold_chain","hazardous","high_value","bulk"]).withMessage('type غير صالح'),
  body('capacity').optional().isFloat().withMessage('capacity يجب أن يكون رقماً'),
  body('managerId').optional().custom(isObjectId).withMessage('managerId غير صالح'),
  body('operatingHours').optional().trim().isString().withMessage('operatingHours يجب أن يكون نصاً'),
  body('contactPhone').optional().trim().isString().withMessage('contactPhone يجب أن يكون نصاً'),
  body('contactEmail').optional().trim().isString().withMessage('contactEmail يجب أن يكون نصاً'),
  body('isTemperatureControlled').optional().isBoolean().withMessage('isTemperatureControlled يجب أن يكون قيمة منطقية'),
  body('temperatureRange').optional().trim().isString().withMessage('temperatureRange يجب أن يكون نصاً'),
];

/* ═══ StorageBin: Create ═══ */
const createStorageBin = [
  body('warehouseId').custom(isObjectId).withMessage('warehouseId غير صالح'),
  body('binCode').trim().notEmpty().withMessage('binCode مطلوب'),
  body('type').optional().isIn(["shelf","rack","drawer","cabinet","floor_area","cold_room","freezer","safe","pallet_position","staging_area","receiving_dock","shipping_dock"]).withMessage('type غير صالح'),
  body('zone').optional().trim().isString().withMessage('zone يجب أن يكون نصاً'),
  body('aisle').optional().trim().isString().withMessage('aisle يجب أن يكون نصاً'),
  body('rack').optional().trim().isString().withMessage('rack يجب أن يكون نصاً'),
  body('level').optional().trim().isString().withMessage('level يجب أن يكون نصاً'),
  body('position').optional().trim().isString().withMessage('position يجب أن يكون نصاً'),
  body('capacity').optional().isFloat().withMessage('capacity يجب أن يكون رقماً'),
  body('usedCapacity').optional().isFloat().withMessage('usedCapacity يجب أن يكون رقماً'),
  body('isOccupied').optional().isBoolean().withMessage('isOccupied يجب أن يكون قيمة منطقية'),
  body('isLocked').optional().isBoolean().withMessage('isLocked يجب أن يكون قيمة منطقية'),
  body('itemId').optional().custom(isObjectId).withMessage('itemId غير صالح'),
  body('quantity').optional().isFloat().withMessage('quantity يجب أن يكون رقماً'),
  body('lotNumber').optional().trim().isString().withMessage('lotNumber يجب أن يكون نصاً'),
  body('restrictions').optional().isArray().withMessage('restrictions يجب أن يكون مصفوفة'),
];

/* ═══ StorageBin: Update ═══ */
const updateStorageBin = [
  body('warehouseId').optional().custom(isObjectId).withMessage('warehouseId غير صالح'),
  body('binCode').optional().trim().isString().withMessage('binCode يجب أن يكون نصاً'),
  body('type').optional().isIn(["shelf","rack","drawer","cabinet","floor_area","cold_room","freezer","safe","pallet_position","staging_area","receiving_dock","shipping_dock"]).withMessage('type غير صالح'),
  body('zone').optional().trim().isString().withMessage('zone يجب أن يكون نصاً'),
  body('aisle').optional().trim().isString().withMessage('aisle يجب أن يكون نصاً'),
  body('rack').optional().trim().isString().withMessage('rack يجب أن يكون نصاً'),
  body('level').optional().trim().isString().withMessage('level يجب أن يكون نصاً'),
  body('position').optional().trim().isString().withMessage('position يجب أن يكون نصاً'),
  body('capacity').optional().isFloat().withMessage('capacity يجب أن يكون رقماً'),
  body('usedCapacity').optional().isFloat().withMessage('usedCapacity يجب أن يكون رقماً'),
  body('isOccupied').optional().isBoolean().withMessage('isOccupied يجب أن يكون قيمة منطقية'),
  body('isLocked').optional().isBoolean().withMessage('isLocked يجب أن يكون قيمة منطقية'),
  body('itemId').optional().custom(isObjectId).withMessage('itemId غير صالح'),
  body('quantity').optional().isFloat().withMessage('quantity يجب أن يكون رقماً'),
  body('lotNumber').optional().trim().isString().withMessage('lotNumber يجب أن يكون نصاً'),
  body('restrictions').optional().isArray().withMessage('restrictions يجب أن يكون مصفوفة'),
];

/* ═══ PickList: Create ═══ */
const createPickList = [
  body('pickNumber').trim().notEmpty().withMessage('pickNumber مطلوب'),
  body('warehouseId').custom(isObjectId).withMessage('warehouseId غير صالح'),
  body('status').optional().isIn(["created","assigned","in_progress","partially_picked","picked","packed","shipped","cancelled","on_hold"]).withMessage('status غير صالح'),
  body('priority').optional().isIn(["low","medium","high","urgent"]).withMessage('priority غير صالح'),
  body('assignedTo').optional().custom(isObjectId).withMessage('assignedTo غير صالح'),
  body('itemId').custom(isObjectId).withMessage('itemId غير صالح'),
  body('binId').optional().custom(isObjectId).withMessage('binId غير صالح'),
  body('requestedQty').isFloat().withMessage('requestedQty يجب أن يكون رقماً'),
  body('pickedQty').optional().isFloat().withMessage('pickedQty يجب أن يكون رقماً'),
  body('lotNumber').optional().trim().isString().withMessage('lotNumber يجب أن يكون نصاً'),
  body('status').optional().isIn(["pending","picked","short","substituted"]).withMessage('status غير صالح'),
  body('referenceType').optional().trim().isString().withMessage('referenceType يجب أن يكون نصاً'),
  body('referenceId').optional().custom(isObjectId).withMessage('referenceId غير صالح'),
  body('startedAt').optional().isISO8601().withMessage('startedAt يجب أن يكون تاريخ ISO صالح'),
  body('notes').optional().trim().isString().withMessage('notes يجب أن يكون نصاً'),
  body('createdBy').optional().custom(isObjectId).withMessage('createdBy غير صالح'),
];

/* ═══ PickList: Update ═══ */
const updatePickList = [
  body('pickNumber').optional().trim().isString().withMessage('pickNumber يجب أن يكون نصاً'),
  body('warehouseId').optional().custom(isObjectId).withMessage('warehouseId غير صالح'),
  body('status').optional().isIn(["created","assigned","in_progress","partially_picked","picked","packed","shipped","cancelled","on_hold"]).withMessage('status غير صالح'),
  body('priority').optional().isIn(["low","medium","high","urgent"]).withMessage('priority غير صالح'),
  body('assignedTo').optional().custom(isObjectId).withMessage('assignedTo غير صالح'),
  body('itemId').optional().custom(isObjectId).withMessage('itemId غير صالح'),
  body('binId').optional().custom(isObjectId).withMessage('binId غير صالح'),
  body('requestedQty').optional().isFloat().withMessage('requestedQty يجب أن يكون رقماً'),
  body('pickedQty').optional().isFloat().withMessage('pickedQty يجب أن يكون رقماً'),
  body('lotNumber').optional().trim().isString().withMessage('lotNumber يجب أن يكون نصاً'),
  body('status').optional().isIn(["pending","picked","short","substituted"]).withMessage('status غير صالح'),
  body('referenceType').optional().trim().isString().withMessage('referenceType يجب أن يكون نصاً'),
  body('referenceId').optional().custom(isObjectId).withMessage('referenceId غير صالح'),
  body('startedAt').optional().isISO8601().withMessage('startedAt يجب أن يكون تاريخ ISO صالح'),
  body('notes').optional().trim().isString().withMessage('notes يجب أن يكون نصاً'),
  body('createdBy').optional().custom(isObjectId).withMessage('createdBy غير صالح'),
];

/* ═══ CycleCount: Create ═══ */
const createCycleCount = [
  body('countNumber').trim().notEmpty().withMessage('countNumber مطلوب'),
  body('warehouseId').custom(isObjectId).withMessage('warehouseId غير صالح'),
  body('status').optional().isIn(["scheduled","in_progress","counting","review","approved","adjusted","completed","cancelled"]).withMessage('status غير صالح'),
  body('countType').optional().isIn(["full","partial","abc_class","random_sample","zone"]).withMessage('countType غير صالح'),
  body('zone').optional().trim().isString().withMessage('zone يجب أن يكون نصاً'),
  body('scheduledDate').optional().isISO8601().withMessage('scheduledDate يجب أن يكون تاريخ ISO صالح'),
  body('startedAt').optional().isISO8601().withMessage('startedAt يجب أن يكون تاريخ ISO صالح'),
  body('itemId').custom(isObjectId).withMessage('itemId غير صالح'),
  body('binId').optional().custom(isObjectId).withMessage('binId غير صالح'),
  body('systemQty').optional().isFloat().withMessage('systemQty يجب أن يكون رقماً'),
  body('countedQty').optional().isFloat().withMessage('countedQty يجب أن يكون رقماً'),
  body('variance').optional().isFloat().withMessage('variance يجب أن يكون رقماً'),
  body('varianceValue').optional().isFloat().withMessage('varianceValue يجب أن يكون رقماً'),
  body('isReconciled').optional().isBoolean().withMessage('isReconciled يجب أن يكون قيمة منطقية'),
  body('notes').optional().trim().isString().withMessage('notes يجب أن يكون نصاً'),
  body('totalItems').optional().isFloat().withMessage('totalItems يجب أن يكون رقماً'),
  body('totalVariances').optional().isFloat().withMessage('totalVariances يجب أن يكون رقماً'),
  body('varianceRate').optional().isFloat().withMessage('varianceRate يجب أن يكون رقماً'),
  body('assignedTo').optional().custom(isObjectId).withMessage('assignedTo غير صالح'),
  body('approvedBy').optional().custom(isObjectId).withMessage('approvedBy غير صالح'),
  body('createdBy').optional().custom(isObjectId).withMessage('createdBy غير صالح'),
];

/* ═══ CycleCount: Update ═══ */
const updateCycleCount = [
  body('countNumber').optional().trim().isString().withMessage('countNumber يجب أن يكون نصاً'),
  body('warehouseId').optional().custom(isObjectId).withMessage('warehouseId غير صالح'),
  body('status').optional().isIn(["scheduled","in_progress","counting","review","approved","adjusted","completed","cancelled"]).withMessage('status غير صالح'),
  body('countType').optional().isIn(["full","partial","abc_class","random_sample","zone"]).withMessage('countType غير صالح'),
  body('zone').optional().trim().isString().withMessage('zone يجب أن يكون نصاً'),
  body('scheduledDate').optional().isISO8601().withMessage('scheduledDate يجب أن يكون تاريخ ISO صالح'),
  body('startedAt').optional().isISO8601().withMessage('startedAt يجب أن يكون تاريخ ISO صالح'),
  body('itemId').optional().custom(isObjectId).withMessage('itemId غير صالح'),
  body('binId').optional().custom(isObjectId).withMessage('binId غير صالح'),
  body('systemQty').optional().isFloat().withMessage('systemQty يجب أن يكون رقماً'),
  body('countedQty').optional().isFloat().withMessage('countedQty يجب أن يكون رقماً'),
  body('variance').optional().isFloat().withMessage('variance يجب أن يكون رقماً'),
  body('varianceValue').optional().isFloat().withMessage('varianceValue يجب أن يكون رقماً'),
  body('isReconciled').optional().isBoolean().withMessage('isReconciled يجب أن يكون قيمة منطقية'),
  body('notes').optional().trim().isString().withMessage('notes يجب أن يكون نصاً'),
  body('totalItems').optional().isFloat().withMessage('totalItems يجب أن يكون رقماً'),
  body('totalVariances').optional().isFloat().withMessage('totalVariances يجب أن يكون رقماً'),
  body('varianceRate').optional().isFloat().withMessage('varianceRate يجب أن يكون رقماً'),
  body('assignedTo').optional().custom(isObjectId).withMessage('assignedTo غير صالح'),
  body('approvedBy').optional().custom(isObjectId).withMessage('approvedBy غير صالح'),
  body('createdBy').optional().custom(isObjectId).withMessage('createdBy غير صالح'),
];

module.exports = {
  createWarehouse,
  updateWarehouse,
  createStorageBin,
  updateStorageBin,
  createPickList,
  updatePickList,
  createCycleCount,
  updateCycleCount,
};