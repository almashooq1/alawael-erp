# تقييم النواقص — Al-Awael ERP

**التاريخ:** 2026-06-15 · **النطاق:** مراجعة سريعة للبنية والوثائق والاختبارات وCI/الأمان

> المشروع ناضج جداً (v3.3.0 / 4.0.x): backend بـ Express/MongoDB، frontend بـ React 18/MUI 5، تطبيق جوال، 61 خدمة مصغّرة، تكاملات حكومية سعودية (NPHIES/GOSI/SCFHS)، مراقبة (Prometheus/Grafana/OTEL)، ADRs وRunbooks وDR. النواقص أدناه ليست «عيوب فادحة» بل فجوات نضج تستحق الإغلاق قبل التوسّع.

## أولوية عالية

1. **~~بوّابة الاختبارات جزئية، والتغطية منخفضة.~~ ✅ عولج جزئياً (2026-06-15).**

   - **التحقق الفعلي** (عبر `npm run audit:test-gate`): 2979 ملف اختبار، كان منها 1041 فقط مُبوّباً (34.9%). أما ادّعاء «اختبارات قديمة بمسارات استيراد متعطّلة» فقد ثبت أنه **غير دقيق**: عدد الاختبارات الميتة بـ require متعطّل فعلياً = **0** (الـ8 التي يرصدها الماسح إيجابيات كاذبة معروفة — سلاسل نصية داخل fixtures/تأكيدات، موجودة أصلاً في allowlist حارس `no-broken-requires`).
   - **الدليل التجريبي:** شُغّلت كل الاختبارات اليدوية غير المُبوّبة (558 ملفاً) فنجح **9931/9932 اختباراً**. الفشل الوحيد كان قنبلة زمنية في `inspection-submission.test.js` (يثبّت `capturedAt` ويعتمد نافذة 30 يوماً افتراضية فيسقط خارجها مع تقدّم الساعة) — أُصلح بحقن `now`.
   - **الإجراء المنفّذ:** أُضيفت الـ558 اختباراً اليدوي إلى `sprint-tests.txt` (1041 → 1599) وتمت مزامنة `sprint-tests.yml` عبر `sync-sprint-tests-paths.js`؛ مرّت كل حُرّاس الانجراف. البوّابة الآن تغطي **100% من الاختبارات اليدوية المكتوبة** (53.7% من إجمالي الملفات).
   - **إصلاح مُشغّل البوّابة (انحدار اكتُشف أثناء التحقق):** توسعة القائمة إلى 1599 ملفاً كسرت `run-sprint.js` على Windows بـ `spawn ENAMETOOLONG` (تعليق المُشغّل ادّعى خطأً أنه محدود فقط بـ ARG_MAX بالميغابايت؛ لكن `CreateProcess` على Windows يحدّ كامل سطر الأوامر بـ32767 حرفاً). أُعيد كتابة المُشغّل ليُمرّر قائمة الملفات إلى واجهة jest البرمجية داخل العملية (`@jest/core` runCLI) عند تجاوز السقف — فلا تلمس القائمة سطر أوامر النظام إطلاقاً؛ مع إبقاء مسار `spawn` المُثبت للقوائم القصيرة ودعم `--shard` كما هو. **تحقّق نهائي:** `npm run test:sprint -- --shard=1/8` نفّذ 200 مجموعة / 4497 اختباراً، كلها خضراء (exit 0) في ~158ث عبر المسار المتوازي الحقيقي.
   - **المتبقّي:** 1349 ملفاً مولّداً آلياً تحت `tests/unit/` (smoke منخفضة القيمة) لم تُبوّب بعد؛ ورفع عتبة التغطية تدريجياً بعد قياسها على المجموعة الموسّعة؛ وإزالة `--passWithNoTests` من المسارات الحرجة. عتبة التغطية الحالية: سطور 46% / عبارات 45% / فروع 35%.

2. **تضخّم المستودع.** ✅ صُحِّح الادّعاء (2026-06-15). ادّعاء «~3GB / 112 ألف ملف داخل الريبو» **مبالَغ فيه**: الواقع المُتحقَّق أن `_archived/` كامله **مُتجاهَل في `.gitignore`**، والمتعقَّب فعلياً في git = **9 ملفات صغيرة فقط** (لقطات نماذج/اختبارات ميتة). والأهم أن هذه الـ9 **مُبقاة عمداً** — مُدرجة في `BASELINE_TRACKED_IGNORED` داخل حارس ما-قبل-الدفع `check-gitignored-sources.js` بتعليق صريح أنها «توثيق تاريخي»، ومُستثناة من eslint/prettier/dockerignore، وغير مُستدعاة من أي كود حي (الإشارات الوحيدة تعليقات في النماذج البديلة). إذاً **بصمة `_archived/` داخل git ضئيلة (~كيلوبايتات)، لا غيغابايتات**. التضخّم الحقيقي هو **148.5 MB / 12712 ملفاً غير متعقَّبة على القرص** (أكبرها `dead-laravel-vendor` 53.8MB و`session-docs` 28.1MB و`root-misc` 20.6MB) — تؤذي مزامنة OneDrive وزمن الاستنساخ المحلي فقط، **لا تدخل الريبو ولا الـ CI إطلاقاً**. المطلوب المتبقّي (محلي، غير عاجل، يحتاج تأكيد المستخدم لأنه حذف ملفات محلية): تنظيف الـ148MB غير المتعقَّبة من القرص؛ ولا حاجة لأي «commit حذف» في git لأن الـ9 ملفات مقصودة.

3. **التطبيق الجوال متأخّر وضعيف التغطية.** ◑ مُعالَج جزئياً (W1308, 2026-06-15). الادّعاء صحيح بنيوياً (تثبيت `/api/v1` صلب يربط الـ backend برهينة إصدار الجوال ويمنع إزالة الـ dual-mount، 241 عائلة endpoint). وأثناء التحقّق انكشف **خلل حقيقي** مصاحب: وحدة `mobile/src/services/modules/nationalAddress.ts` كانت تستدعي `api.post('/api/v1/wasel/address/...')` بينما `baseURL` لـ ApiService ينتهي أصلاً بـ `/api/v1` (axios `combineURLs` **يضمّ** لا يستبدل) → الناتج `.../api/v1/api/v1/wasel/address/...`، وفوق ذلك الـ backend يركّب على `/api/v1/wasel-address` (شُرطة، لا `wasel/address`) — فالنداءان كانا مضمونيّ الـ 404. **أُصلح** إلى مسار نسبي `/wasel-address/{verify-and-stamp,search-by-id}` + 3 اختبارات انحدار تحرس ضد عودة البادئة المزدوجة أو صيغة الشُرطة-بالمائل (40/40 اختبار modules.test.ts تمرّ). **متبقٍّ** (قرار إصدار + backend): نقل الـ baseURL إلى `/api` ثم إزالة الـ dual-mount، ورفع تغطية اختبارات الجوال عموماً — كلاهما مرتبط بإصدار وليس إصلاحاً آنياً.

4. **فجوة اختبارات الحمل/الأداء.** ✅ عولج جزئياً (W1304+W1350, 2026-06-15). التحقّق صحّح الادّعاء جزئياً: يوجد فعلاً `backend/tests/load/k6-smoke.js` (لكنه بدائي — endpoint واحد `/api/test`، 10 VUs ثابتة) + اختبارات Jest للأداء (`load.test.js` + `performance-load-tests.test.js`). **أُضيف** `backend/tests/load/k6-load.js` (W1304): ملف حمل متدرّج واقعي (warm-up → climb → peak → ramp-down، قابل للضبط عبر `PEAK_VUS`/`PEAK_DURATION`) مع **عتبات SLO لكل مسار** (liveness `p95<200ms`، readiness `p95<800ms`، فشل عام `<1%`) ومسار قراءة موثَّق اختياري عبر `TOKEN` — قابل للربط بـ CI فور توفّر بيئة هدف. سكربتات: `npm run test:load` + `test:load:smoke`. **أُكمِل اختبار سعة تكاملات الحكومة (W1350):** `backend/tests/load/k6-gov-integrations.js` يقيس مسارات القراءة لـ NPHIES (`/status` + `/cpt-codes`) وGOSI (`/rates` + `/dashboard`) بعتبات SLO أوسع تناسب منطق الخدمة الأثقل (NPHIES `p95<1200ms`، GOSI `p95<1500ms`، فشل عام `<2%`) — كل المسارات قابلة للتجاوز عبر env، وتتطلّب `TOKEN`. **ثابت سلامة مفروض بحارس:** الملف **قراءة فقط** (GET حصراً)؛ كل نقطة مُغيِّرة/مُرسِلة (claim-submit/prior-auth/GOSI calculate/WhatsApp send) مُستبعَدة عمداً ويفشل حارس `k6-gov-integrations-load-profile-wave1350` فور إضافة أي `http.post`/مقطع نقطة مُغيِّرة — يمنع قصف بيئات NPHIES/GOSI الاختبارية بسجلّات وهمية. سكربت: `npm run test:load:gov`؛ 9 تأكيدات مُدرَجة في بوّابة sprint. المتبقّي: ربط الملفات الثلاثة بخط أنابيب بيئة staging فعلية (يحتاج بيئة هدف + رمز خدمة).

## أولوية متوسطة

5. **انجراف الوثائق عن الواقع.** ✅ عولج (W1305+W1349+W1351, 2026-06-15). الأداة موجودة أصلاً (`backend/scripts/project-stats.js` / `npm run stats`). أُضيفت دالة نقية `collectStats()` + وضع `npm run stats:json` يُخرج الإحصائيات بصيغة JSON آلية + حارس `require.main` + اختبار ذاتي `__tests__/project-stats-script.test.js`. **أُكمِلت آلية بوّابة الانجراف (W1349):** دوال نقية `renderStatsBlock`/`extractStatsBlock`/`applyStatsBlock`/`checkStatsBlock` تُنتج كتلة markdown حتمية بين علامتي `PROJECT-STATS:START/END` تحوي العدّادات ذات-دلالة-الانجراف فقط (ملفات/نماذج/مسارات/خدمات/اختبارات/اعتماديات) دون `jsLines` المتقلّب ولا الطابع الزمني — فتنطلق البوّابة عند انجراف بنيوي حقيقي (إضافة/حذف نموذج أو مسار) لا عند كل تعديل سطر. سكربتان جديدتان: `npm run stats:write <file>` (يُدرج/يُحدّث الكتلة) + `npm run stats:check <file>` (يُفشِل بـ exit 1 عند الانجراف أو غياب الكتلة، والخطأ بيان لا استثناء). الاختبار الذاتي توسّع 8 → 16 تأكيداً (مُدرَج في بوّابة sprint). **متبقٍّ:** تضمين العلامتين في README ثم ربط `stats:check` بخط CI (لا يزال تعديل README مؤجّلاً لأنّه مُعدّل بواسطة عملية موازية تفادياً للتعارض) — المحرّك جاهز وأي وثيقة تُصبح محروسة بإضافة العلامتين فقط. **رُبِطت بوّابة CI (W1351):** بدل تعديل README، أُنشئت وثيقة مخصّصة `docs/PROJECT_STATS.md` (بعلامتَي البلوك) + workflow `.github/workflows/stats-drift.yml` يُشغّل `stats:check` ويفشل عند الانجراف، مقيّداً على push/PR لـ main/develop فقط (لا فروع الميزات، تفادياً لضجيج الجلسات المتوازية)؛ السكربت خالٍ من التبعيات فلا يحتاج install. حارس `project-stats-doc-drift-wave1351` (3 تأكيدات، sprint) يتحقّق من العلامات + صفوف المقاييس + ربط الـ workflow دون عدّ دقيق.

6. **تكرار وتشظّي معماري.** ◑ صُحِّح الإطار (2026-06-15). الادّعاء «خطة توحيد غير مجدوَلة» **مبالَغ في تأطيره**: التحقّق أظهر أن تدقيق ازدواجية مُفصَّلاً سبق وأُجري ([docs/audits/duplication-audit-2026-04-17.md](audits/duplication-audit-2026-04-17.md)) — **أرشف ~790 ملفاً عبر 13 commit** مع بقاء بوّابة اختبارات التفويض خضراء (184/184)، ثم **ثلّث الباقي بصراحة** إلى ثلاث فئات موثّقة: (أ) تعايُش نطاقات متمايزة مقصود (تحليلات 8 خدمات كل منها لنطاق، RBAC مقسوم عمداً لكتالوج أدوار + جدول صلاحيات per ADR-005) — التوصية «تأجيل، التوحيد تجميلي يُنتج mega-service أصعب صيانة»؛ (ب) عناصر مؤجَّلة لمراجعة نطاق؛ (ج) **حتى البند المُدّعى أنه «قابل للتنفيذ» مُنجَز فعلاً**: `middleware/auth.middleware.js` ليس منطقاً مكرّراً بل **proxy توافق من 30 سطراً** يُعيد التصدير من `middleware/auth.js` (المصدر الوحيد) — وُحِّد المنطق في "Round 29". المتبقّي الوحيد codemod تجميلي اختياري (تحويل المستهلكين لاستيراد `auth` مباشرةً ثم حذف الـ proxy) — **منخفض القيمة، يؤجَّل**، فالـ proxy يعمل بصحّة. كذلك «خدمات المستندات المكرّرة» ليست ازدواجية بل **نظام فرعي مُحزَّم** تحت `documents/` (forms/lifecycle/linking/forensics/...) + خدمة QMS منفصلة (`controlledDocument.service.js`) + خدمة في مشروع سلسلة الإمداد المستقل. والحزم المتوازية (finance-module / intelligent-agent / secretary_ai / supply-chain-management) مشاريع فرعية مقصودة، لا تكراراً عشوائياً. **المتبقّي الحقيقي:** جدولة عناصر فئة (ب) لمراجعة نطاق — لا يوجد عنصر تكرار-auth فعليّ متبقٍّ (التوحيد مُنجَز)؛ أي عمل آخر هنا **قرار/مراجعة نطاق** لا أتمتة عمياء.

7. **عمق الأمان.** ✅ عولج جزئياً (2026-06-15). موجود: CodeQL + npm audit + PDPL + SECURITY.md. **أُغلقت فجوة فحص الأسرار في CI** (التحقّق أكّد غيابها: لا gitleaks/trufflehog في أيٍّ من 7 workflows): أُضيف `.github/workflows/gitleaks.yml` (W1303) — يفحص diff الـ PR + كامل التاريخ عند الدفع لـ main + أسبوعياً، ويرفع SARIF لتبويب Security ويُفشل البوّابة عند رصد سرّ، مع ضبط `.gitleaks.toml` (allowlist لقوالب `.env.*` والـ fixtures التي تحوي placeholders). **أُضيف نموذج تهديدات موثّق (W1353):** `docs/security/THREAT_MODEL.md` — تحليل STRIDE لكل حدّ ثقة (4 حدود + مخطّط mermaid) يربط كل تهديد بالضابط القائم فعلاً في الشيفرة (تدرّج MFA الخماسي، عزل الفروع W269، hash-chain، gitleaks، validateEnv، TTL/PDPL) أو يصنّفه فجوة متبقّية بأولوية ومُلكية. **انكشف أثناء النمذجة خلل تشغيلي حقيقي:** المخطّط الصارم في `config/validateEnv.js` (`strictOverrides`) يفرض 5 مفاتيح أمنية في الإنتاج/CI، بينما `backend/.env.example` يوثّق `MONGODB_URI` + `JWT_SECRET` فقط كقيم قابلة للإسناد — أي النشر من القالب يفشل عند `CI=true`/الإنتاج على 3 مفاتيح ناقصة: `JWT_REFRESH_SECRET` · `ENCRYPTION_KEY` · `SESSION_SECRET`. الإصلاح (إضافتها + حارس انجراف يطابق `strictOverrides`↔`.env.example`) **مؤجَّل بسطرين** لأن الملف يُعدَّل حالياً بجلسة موازية تفادياً للتعارض. المتبقّي: DAST، إدارة أسرار مركزية (Vault) بدل `.env`، وتقسيم `.env.example` بحجم 48KB وتوثيق «الحد الأدنى للتشغيل».

## أولوية منخفضة / نظافة

8. **تشظّي إعدادات أدوات الـ AI.** ✅ صُحِّح الادّعاء (2026-06-15). ادّعاء «ملفات `*.tmp` مرفوعة» **غير دقيق**: التحقّق أظهر **صفر ملف `.tmp` متعقَّب** في git، والقاعدة `*.tmp` مُدرجة أصلاً في `.gitignore` (سطر 85) فكل ملفات `CLAUDE.md.tmp`/`AGENTS.md.tmp`/`GEMINI.md.tmp`/`.rules.tmp` **مُتجاهَلة بشكل صحيح** (موجودة على القرص محلياً فقط، لن تُرفع). كذلك 7 من 8 مجلدات أدوات الـ AI (`.augment` `.clinerules` `.cursor` `.kiro` `.roo` `.windsurf` `.claude`) لا تحوي **أي ملف متعقَّب** (محلية بالكامل)؛ المتعقَّب الوحيد هو `.alawael/` (ملفّان: `README.md` + `config/alawael.config.json`). إذاً **لا يوجد تشظّي داخل الريبو ولا ملفات `.tmp` للحذف من git**. المتبقّي (تجميلي، محلي): توحيد إعدادات الأدوات في مصدر واحد وحذف ملفات `.tmp` المحلية من القرص — لا أثر على git.

9. **الوصولية (a11y) وi18n/RTL.** ◑ مُعالَج جزئياً (W1306, 2026-06-15). كان `axe-core` مُثبَّتاً منذ أشهر بلا أي اختبار يستدعيه. أُضيف أول اختبار a11y فعلي `frontend/src/__tests__/a11y-smoke.test.jsx` (يستدعي `axe-core` مباشرة دون اعتمادية جديدة — جُمّدت قاعدة `color-contrast` لأن jsdom بلا محرّك تخطيط؛ تُغطّى في خط cypress-axe). يتحقّق من نماذج UI متاحة (نموذج/تنقّل/جدول) + عيّنة معطوبة عمداً تُثبت أن البوّابة تكشف المخالفات فعلاً (`image-alt`). مُلتقَط آلياً في `npm run quality:ci`. **متبقٍّ:** توسيع التغطية لتشمل الشاشات الحرجة الحقيقية بعد استقرارها، و/أو `@lhci/cli` (Lighthouse-CI). أُضيف سكربت `npm run test:a11y`.

10. **خصوصيات الرعاية الصحية (EMR).** ◑ مُعالَج جزئياً (W1307+W1309→W1348, 2026-06-15). **أُضيفت طبقة مُحوّلات FHIR (W1309→W1315):** كان تكامل NPHIES عند طبقة المطالبات فقط (مستقبِل webhook + مُوفِّق reconciliation)، بلا مُحوِّل _موارد_ FHIR — فاختبار المطابقة لم يكن له ما يؤكّد عليه. الآن `backend/intelligence/fhir/` يضم **33 مُحوّلاً خالصاً** (لا DB/IO) + برميل دخول موحّد `index.js` (`MAPPERS` بالاسم الكنسي): **(1) `beneficiary-to-fhir.lib.js` → FHIR R4 Patient** (هوية وطنية/إقامة بأنظمة NPHIES `1`→مواطن/`2`→إقامة، MRN ثانوية بنوع HL7 MR، أسماء ثنائية اللغة، `gender` 1:1، `birthDate`، `active`/`deceasedBoolean`، الإعاقة كامتداد). **(2) `episode-of-care-to-fhir.lib.js` → FHIR R4 EpisodeOfCare** (نواة المنصّة الموحِّدة؛ تطابق حالات، مرجع المريض الإلزامي، `careManager`، `period`، امتدادات النوع/الأولوية/الطور/التحويل). **(3) `assessment-to-fhir.lib.js` → FHIR R4 Observation** (تطابق حالات `registered/preliminary/final/cancelled`، `code` إلزامي من النوع، `valueQuantity` للدرجة، `interpretation`، امتدادات maxScore/measure/episode). **(4) `session-to-fhir.lib.js` → FHIR R4 Encounter** (تطابق حالات، `class` من الوسيلة عبر HL7 v3-ActCode، ربط الحلقة أصلياً عبر `episodeOfCare`، `serviceType` للتخصص، حفظ فروق no_show/rescheduled في امتداد). **(5) `plan-of-care-to-fhir.lib.js` → FHIR R4 CarePlan** (تطابق حالات `draft/active/on-hold/completed/revoked`، `intent=plan` إلزامي، `period`، امتدادات episode/version/sessionsPerWeek/الموافقة، حفظ فروق pending_review/pending_approval/superseded في امتداد). **(6) `measure-to-fhir.lib.js` → FHIR R4 Questionnaire** (يُغلق إشارة `Questionnaire/<measureId>` الصادرة من مُحوّل Assessment؛ حالة النشر active/retired/draft من `isActive`، الـ `code` الكنسي الثابت كـ identifier + coding علوي، عنوان ثنائي اللغة، امتدادات للفئة/طريقة التسجيل/مدى الدرجات/نافذة العمر/حدود التفسير cutoffs). **(7) `risk-profile-to-fhir.lib.js` → FHIR R4 RiskAssessment** (تسقيط ملف المخاطر الموحّد القابل للتفسير؛ حالة final/registered من `reason`، `prediction` واحدة (qualitativeRisk=الطبقة، probabilityDecimal=الدرجة÷100 عكوسي)، التفسير→note، ربط الحلقة + الدرجة الخام 0-100 + الترجيح المركّب + عوامل التفسير topFactors كامتدادات؛ حالة لا-مصادر → بلا prediction). **(8) `seizure-event-to-fhir.lib.js` → FHIR R4 Observation** (نوبة صرع مُسجّلة كملاحظة سريرية؛ حالة recorded→preliminary/reviewed→final، `code` إلزامي من نوع النوبة ILAE، `effectivePeriod` من البداية/النهاية، الشاهد→performer، وإسقاط غير فاقد للشدة/مستوى الوعي/المدة/الإصابة/طلب الإسعاف/دواء الإنقاذ/إبلاغ الوالد/ربط الخطة/الفرع/المراجعة كامتدادات، مع علم status-epilepticus المشتق (المدة ≥300ث) كامتداد بولياني). **(9) `behavior-incident-to-fhir.lib.js` → FHIR R4 Observation** (حادثة سلوكية مُسجّلة؛ حالة final، `code` ثابت behavior-incident مع السلوك الحرّ كـ text، بنية ABC (المحفّز/السلوك/النتيجة) كـ `component[]` محفوظة، الشدة→interpretation بـ HL7 v3، وقت الحدوث→effectiveDateTime، المُبلِغ→performer، وإسقاط غير فاقد للمدة/مستوى التدخل/التقييد/الإصابة/التنبيه/إبلاغ ولي الأمر/ربط الحلقة كامتدادات). **(10) `assistive-device-to-fhir.lib.js` → FHIR R4 Device** (أصل جهاز مساعد فردي بسجل إعارة+صيانة؛ اختير Device لا DeviceUseStatement لأن الكيان الكنسي هو الأصل نفسه؛ assetTag كمُعرّف إلزامي + serialNumber ثانوي، الإتاحة الرباعية→status (available/loaned/maintenance→active، retired→inactive) مع حفظ الحالة الكاملة كامتداد، الفئة→type، المستعير الحالي→patient، الفرع→owner Organization، وإسقاط غير فاقد للضمان/التكلفة/الحالة/جدولة الصيانة وسجلّي الإعارة+الصيانة كامتدادات متكررة). **(11) `safeguarding-concern-to-fhir.lib.js` → FHIR R4 Flag** (بلاغ حماية/إساءة؛ CBAHI + حماية الطفل السعودية؛ اختير Flag لأنه تحذير يجب أن يراه المعالجون؛ status (closed/unsubstantiated→inactive، غيرها→active مع حفظ السبع حالات كامتداد)، category ثابت safeguarding، code=فئة البلاغ مع الوصف كـtext، subject إلزامي (مستفيد→Patient وإلا الفرع→Organization للموظف/أخرى)، period من reportedAt→closedAt، author=المُبلِغ، وإسقاط غير فاقد للشدة/التحقيق/النتيجة/إبلاغ الجهة الرسمية/إخطار المشرف/خطة العمل/السرية/الحادثة المرتبطة كامتدادات). **(12) `beneficiary-diet-prescription-to-fhir.lib.js` → FHIR R4 NutritionOrder** (أمر حمية سريري للمستفيد؛ status من 4 حالات Rx (draft→draft، active→active، on_hold→on-hold، discontinued→revoked؛ مع حفظ الأصلي في امتداد)، intent='order' ثابت، patient=المستفيد إلزامي، dateTime=prescribedAt، orderer=الواصف؛ oralDiet يحمل مستويات IDDSI للطعام/السوائل + قيود القوام + تعليمات التغذية (يُسقَط كاملاً عند NPO)، enteralFormula للتغذية الأنبوبية النشطة (معدل بوحدة UCUM)، excludeFoodModifier للحساسية+القيود وfoodPreferenceModifier للتفضيلات؛ وإسقاط غير فاقد لـ NPO/الأهداف السعرية-البروتينية-السوائل/قدرة المضغ/مستوى المساعدة/تخصص الواصف/المراجعة/تفاصيل التغذية الأنبوبية الدقيقة/الفرع/خطة الرعاية المرتبطة كامتدادات). **(13) `sensory-diet-program-to-fhir.lib.js` → FHIR R4 CarePlan** (برنامج الحمية الحسّية؛ مع مُميِّز `category=sensory-diet` ثابت لتمييزه عن مُحوّل PlanOfCare→CarePlan في W1313؛ تطابق حالات active/on_hold→on-hold/completed/discontinued→revoked، `intent=plan` ثابت، subject=المستفيد إلزامي، period.start من startDate، author=المعالج؛ كل نشاط حسّي يُسقَط إلى `CarePlan.activity` (status من دورة حياة البرنامج، code=النظام الحسّي + النص=الاسم، scheduledString=التكرار، purpose/المدة في امتداد متداخل)، وجلسات Snoezelen كامتدادات متداخلة (تاريخ/نتيجة التنظيم/المثيرات)، وإسقاط غير فاقد للحالة/تاريخ المراجعة/الأهداف/الفرع/خطة الرعاية المرتبطة/سبب الإيقاف كامتدادات). **(14) `communication-aid-profile-to-fhir.lib.js` → FHIR R4 Observation** (ملف قدرة التواصل المعزّز AAC، واحد لكل مستفيد؛ مُميِّز `code=communication-aid-profile` ثابت لتمييزه عن مُحوّلات Observation الأخرى Assessment/SeizureEvent/BehaviorIncident؛ تطابق دورة حياة الملف على قيم حالة Observation draft→registered/active→final/paused→preliminary/retired→cancelled، subject=المستفيد إلزامي، `valueCodeableConcept`=مستوى المفردات vocabularyLevel كقيمة رأسية، effectiveDateTime=assessedAt، performer=المُقيِّم؛ الوسيلة الأساسية وعدّ المفردات النشط كـ `component[]`، وإسقاط غير فاقد للوسائل النشطة/جرد الأدوات المعزّزة (امتداد متداخل لكل أداة: الاسم/المستوى/مفتاح الوسيلة/مجموعة الرموز/مستوى الاستقلالية/تاريخ الإدخال/النشاط) والشركاء المُدرَّبين/الاستخدام المنزلي/تخصص المُقيِّم/تاريخ إعادة التقييم/الفرع/خطة الرعاية المرتبطة/الحالة الخام كامتدادات). **(15) `prosthetic-orthotic-order-to-fhir.lib.js` → FHIR R4 DeviceRequest** (طلب تصنيع وتركيب الأطراف الصناعية/الدعامات/الجلوس، متميّز عن سجل AssistiveDevice→Device في W1319؛ intent=order ثابت، تطابق دورة التصنيع prescribed→draft/measured·fabrication·fitting·follow_up→active/delivered·completed→completed/cancelled→revoked، subject=المستفيد إلزامي، codeCodeableConcept=فئة الجهاز، authoredOn=prescribedDate، requester=الواصف، reasonCode=التشخيص+الهدف السريري؛ وإسقاط غير فاقد للجانبية/القولبة/تواريخ القياس والتركيب والتسليم والمتابعة/نوع التصنيع والمورّد والكلفة/نتيجة التركيب ودرجة الراحة/التقييم القوامي وخريطة الضغط/الجهاز المُسلّم/سبب الإلغاء/الفرع/سبب الإلغاء كامتدادات). **(16) `instrumental-swallow-study-to-fhir.lib.js` → FHIR R4 DiagnosticReport** (دراسة بلع آلية VFSS/FEES/MBSS تسجّل النتائج التشخيصية التي لا يقدر تقييم البلع السريري إلا الإحالة لها؛ تطابق حالات ordered·scheduled→registered/completed→final/cancelled→cancelled، code=نوع الدراسة، category ثابتة لدراسة البلع، subject=المستفيد إلزامي، effectiveDateTime=performedDate، performer=منفّذ الدراسة؛ وإسقاط غير فاقد للأطوار المتأثّرة/مقياس الاختراق-الشفط PAS/كشف الشفط والشفط الصامت/نتائج كل قوام (مستوى IDDSI + اختراق/شفط/أمان متداخلة)/مستويات الحمية الموصى بها/توصية NPO/إحالة تقييم البلع/وصفة الحمية المرتبطة كـ NutritionOrder/الفرع/سبب الإلغاء كامتدادات). **(17) `spasticity-injection-to-fhir.lib.js` → FHIR R4 Procedure** (حقن إدارة التشنّج العضلي: توكسين بوتولينيوم/فينول/باكلوفين ITB؛ تطابق دورة الحياة planned→preparation/completed→completed/cancelled→not-done، code=العامل الدوائي، subject=المستفيد إلزامي، performedDateTime=procedureDate، performer مغلّف بـ actor كمرجع Practitioner؛ goals→reasonCode (واحد لكل هدف)، العضلات المستهدفة→bodySite (مع الجانبية)، وإسقاط غير فاقد للعلامة التجارية/إجمالي الجرعة/تفصيل كل عضلة (جرعة + Ashworth متداخلة)/الموافقة/موعد المتابعة/الفرع/خطة الرعاية/سبب الإلغاء كامتدادات). **(18) `group-therapy-session-to-fhir.lib.js` → FHIR R4 Encounter** (جلسة علاج جماعية بعدّة مستفيدين؛ تطابق الحالات scheduled→planned/in_progress→in-progress/completed→finished/cancelled·no_show·rescheduled→cancelled، class=AMB، type ثابت group-therapy، subject=`Group/<groupId>` (لأن لا مريض مفرد)، participant[]=المعالج والمعالج المساعد كـ Practitioner، period يفضّل الأوقات الفعلية؛ كل مستفيد كامتداد participant متداخل (مرجع Patient + الحلقة + حالة الحضور) لأن FHIR R4 لا يسمح بمرجع Patient في participant.individual، + الموضوع ثنائي اللغة كامتدادات). **(19) `tele-rehab-session-to-fhir.lib.js` → FHIR R4 Encounter** (جلسة تأهيل عن بُعد بالفيديو؛ نفس تطابق حالات الجلسة، class=VR (افتراضي)، subject=المستفيد إلزامي، participant=المعالج، period يفضّل الفعلي، episodeOfCare أصلياً؛ وإسقاط غير فاقد للمنصّة (zoom/teams/...)/رابط الجلسة/حالة الموافقة PDPL/جودة الاتصال/السماح بالتسجيل ورابط التسجيل كامتدادات). **(20) `arvr-session-to-fhir.lib.js` → FHIR R4 Encounter** (جلسة تأهيل غامرة AR/VR; نفس تطابق حالات الجلسة، class=VR، subject=المستفيد، participant=المعالج، period يفضّل الفعلي، episodeOfCare أصلياً؛ وإسقاط الجهاز (quest/pico/hololens/...)/السيناريو وإصداره/نوع الانغماس (vr/ar/mr)/الإبلاغ عن دوار الحركة وشدته/نسبة الإكمال كامتدادات). **(21) `dtt-session-to-fhir.lib.js` → FHIR R4 Encounter** (جلسة ABA تدريب منفصل المحاولات DTT؛ status scheduled→planned/completed→finished/cancelled·no_show→cancelled، class=AMB، subject=المستفيد، type=مجال البرنامج، serviceProvider=فرع Organization أصلياً، period مشتق من sessionDate+durationMinutes؛ وإسقاط المدة/إصدار خطة الرعاية، وامتداد متداخل لكل هدف (الاسم+المرجع+معيار الإتقان+عدد المحاولات والصحيحة)، وسبب الإلغاء كامتدادات). **(22) `creative-arts-therapy-session-to-fhir.lib.js` → FHIR R4 Encounter** (جلسة علاج بالفنون الإبداعية موسيقى/رسم/دراما/حركة/لعب؛ class=AMB، subject=المستفيد، type=الأسلوب، serviceProvider=الفرع أصلياً، period مشتق من sessionDate+durationMinutes؛ وإسقاط الصيغة/حجم المجموعة/المواد والتدخلات والأهداف (امتداد لكل عنصر)/مستوى التفاعل/المزاج قبل وبعد/نوع ومرجع المُنتَج الفني كامتدادات). **كلها إضافية بحتة غير كاسرة**. **(23) `adjunct-therapy-session-to-fhir.lib.js` → FHIR R4 Encounter** (جلسة علاج مساند hydrotherapy/hippotherapy/animal-assisted؛ class=AMB، subject=المستفيد، type=الأسلوب، serviceProvider=الفرع أصلياً، period مشتق من sessionDate+durationMinutes؛ وإسقاط بوابة الإجازة الطبية (medical-cleared/cleared-date/contraindications)/مستوى الجاهزية/الأنشطة والمهارات (امتداد لكل عنصر)/استجابة المستفيد/نوع الحيوان/درجة حرارة الماء/حدوث حادثة أثناء الجلسة كامتدادات). **كلها إضافية بحتة غير كاسرة**. **(24) `adaptive-sports-program-to-fhir.lib.js` → FHIR R4 CarePlan** (برنامج رياضة تكيفية؛ intent=plan، category=`adaptive-sports` ثابت، subject=المستفيد، author=المدرّب، period=startDate..endDate، activity واحد للرياضة؛ وإسقاط الحالة/الرياضة/الفئة/المتطلب البدني/التكرار الأسبوعي/موافقة الأسرة/الإجازة الطبية/سجل الجلسات/سجل الإنجازات/الفرع/خطة الرعاية المرتبطة/سبب الإيقاف كامتدادات). **كلها إضافية بحتة غير كاسرة**. **(25) `transition-plan-to-fhir.lib.js` → FHIR R4 CarePlan** (خطة انتقال بين مراحل الحياة early→school→vocational→independent؛ intent=plan، category=`life-stage-transition` ثابت، subject=المستفيد، author=قائد الانتقال، كل معلم milestone يُسقَط إلى `CarePlan.activity` (status من حالة المعلم، code=مجال المهارة، scheduledPeriod.end من تاريخ الاستحقاق، تاريخ التحقيق في امتداد متداخل)؛ وإسقاط الحالة الأصلية/نوع الانتقال/العمر الحالي/الإيداع الحالي والمستهدف/تاريخ الانتقال المخطط والفعلي/درجات الجاهزية لكل مجال/الدرجة المركبة/الفرع/خطة الرعاية المرتبطة/الخطة التربوية الفردية كامتدادات). **كلها إضافية بحتة غير كاسرة**. **(26) `caregiver-support-program-to-fhir.lib.js` → FHIR R4 CarePlan** (برنامج دعم مقدّم الرعاية: استشارة/تدريب/مجموعة دعم؛ intent=plan، category=`caregiver-support` ثابت، subject=المستفيد المُرتبط، author=المرشد المُكلَّف، period=enrolledAt..(completedAt|targetCompletionDate)، كل وحدة تدريبية تُسقَط إلى `CarePlan.activity` (status من اكتمال الوحدة أو دورة حياة البرنامج، code=عنوان الوحدة، scheduledString=الساعات المستهدفة، تقدّم الوحدة في امتداد متداخل)؛ وإسقاط بيانات مقدّم الرعاية (المرجع/الاسم/القرابة/الهاتف)/سجل الجلسات/مخرجات عبء الرعاية (Zarit قبل/بعد + الرضا)/الفئة العمرية للأشقاء/بيانات المجموعة/الفرع كامتدادات). **كلها إضافية بحتة غير كاسرة**. **(27) `respite-booking-to-fhir.lib.js` → FHIR R4 Appointment** (حجز رعاية مؤقتة لإراحة مقدّم الرعاية — فئة خدمة مدعومة من هيئة رعاية الأشخاص ذوي الإعاقة؛ ولأن دورة حياته بثماني حالات (requested→approved/rejected→confirmed→checked_in→completed/cancelled/no_show) هي سير حجز فالمورد الأنسب Appointment لا CarePlan؛ status على قيم Appointment.status (requested→pending، approved/confirmed→booked، rejected/cancelled→cancelled، checked_in→checked-in، completed→fulfilled، no_show→noshow)، serviceType=`respite-care` ثابت، appointmentType=نوع الحجز (day/overnight/extended)، start/end من startAt/endAt، participant=المستفيد كـPatient actor إلزامي، created=requestedAt؛ وإسقاط مسار الاعتماد/الرفض/جهة الطلب/جهة الاتصال الطارئة الإلزامية/الدخول والخروج/التكاليف المقدّرة والفعلية/مصدر التمويل/مرجع الدعم/مسار الإلغاء/خطة الرعاية المرتبطة/الفرع كامتدادات). **كلها إضافية بحتة غير كاسرة** (لا تفرض profile سعودياً — W1336). **(28) `facility-asset-to-fhir.lib.js` → FHIR R4 Device** (أصل مرفقي/بنية تحتية مع دورة صيانة وقائية + فحوصات + شهادات تنظيمية — المعدّة المادية فالمورد الأنسب Device؛ status على قيم Device.status (in_service→active، وكل حالة سحب/إخفاق فحص/صيانة/تقاعد→inactive، الغائب→unknown) مع حفظ الحالة الأصلية في امتداد، identifier=وسم الأصل + الرقم التسلسلي، type=الفئة (CodeableConcept)، deviceName=الاسم الودي/المُصنّع/الطراز، owner=الفرع كـOrganization؛ وإسقاط الخطورة/الموقع (مبنى/طابق/غرفة)/التركيب والتكلفة والضمان/خروج الخدمة/جدول الفحص والصيانة/سجل الفحوصات (واحد لكل فحص)/الشهادات التنظيمية/الحادثة المرتبطة/التقاعد كامتدادات). **كلها إضافية بحتة غير كاسرة** (لا تفرض profile سعودياً — W1337). **(29) `staff-health-record-to-fhir.lib.js` → FHIR R4 Observation** (سجل صحة مهنية للموظفين: تطعيم/فحص درن/لياقة للعمل/حادثة تعرّض/فحص دوري/اختبار إحكام كمامة — نتيجة مراقبة صحية فالمورد الأنسب Observation؛ status على قيم Observation.status (open→registered، in_progress/follow_up_required→preliminary، completed/cleared/restricted/closed→final، الغائب→unknown) مع حفظ الحالة الأصلية في امتداد، category=occupational-health ثابت، code=نوع السجل، subject=الموظف كـPatient (تحت المراقبة الصحية)، effectiveDateTime=eventDate، performer=المُقيّم كـPractitioner، value[x]=النتيجة المرمّزة أو الخلاصة النصية؛ وإسقاط رقم السجل/الاستحقاق التالي/النتائج والقيود/بيانات التطعيم/بيانات التعرّض/مستوى اللياقة/السرية/المُقيّم والموظف/الفرع كامتدادات). **كلها إضافية بحتة غير كاسرة** (لا تفرض profile سعودياً — W1338). **(30) `biomedical-waste-record-to-fhir.lib.js` → FHIR R4 SupplyDelivery** (تتبّع النفايات الطبية من المصدر إلى التخلص: توليد→تخزين→جمع→تخلص؛ المورد الأنسب SupplyDelivery لأنه يمثّل حركة كمية من صنف؛ suppliedItem=الكمية بالكيلوغرام + فئة النفايات، status على قيم SupplyDelivery.status (generated/stored/collected→in-progress، disposed→completed، rejected→abandoned، الغائب→in-progress) مع حفظ الحالة الأصلية في امتداد، occurrenceDateTime=تاريخ التوليد، identifier=رقم السجل+رقم البيان؛ ومراحل سلسلة الحضانة (التوليد/التخزين/الجمع/التخلص) + لون الحاوية/مقاومة الثقب/عدد الحاويات/سبب الرفض/المسؤول/الفرع كامتدادات). **كلها إضافية بحتة غير كاسرة** (لا تفرض profile سعودياً — W1339). **(31) `sponsorship-to-fhir.lib.js` → FHIR R4 Coverage** (كفالة: مانح يغطي تكاليف مستفيد بالتزام شهري + سجل دفعات؛ المورد الأنسب Coverage لأنه يمثّل تغطية مالية لمريض من جهة دافعة؛ beneficiary=Patient، payor=المانح كـRelatedPerson، type=نوع الكفالة (full/partial/one_time/in_kind)، period=من startDate إلى endDate، status على قيم Coverage.status (pending→draft، active/paused→active، completed/cancelled→cancelled، الغائب→draft) مع حفظ الحالة الأصلية؛ وإسقاط المبلغ الشهري كـMoney/بنود التغطية/علم الزكاة/سجل الدفعات/سبب الإيقاف والإلغاء/الفرع كامتدادات). **كلها إضافية بحتة غير كاسرة** (لا تفرض profile سعودياً — قرار لاحق عبر `meta.profile`). **(32) `cbahi-attestation-to-fhir.lib.js` → FHIR R4 Observation** (شهادة استيفاء معيار CBAHI لكل فرع/معيار؛ المورد الأنسب Observation لتسجيل تقييم استيفاء؛ subject=الفرع كـLocation، code=معيار CBAHI (standardCode أو standardKey)، value=قرار الاستيفاء (met/partially_met/not_met/not_applicable)، component=الدرجة المئوية، status على قيم Observation.status (draft→preliminary، البقية→final، الغائب→unknown) مع حفظ الحالة الأصلية؛ وإسقاط الأدلة/ملاحظات الفجوة/مبرر عدم الانطباق/CAPA المرتبط/موعد إعادة التقييم/الفرع كامتدادات). **كلها إضافية بحتة غير كاسرة** (لا تفرض profile سعودياً — W1341). **(33) `seat-allocation-to-fhir.lib.js` → FHIR R4 Appointment** (تخصيص مقعد دائم لمستفيد في مركز رعاية نهارية؛ المورد الأنسب Appointment لحجز مكان مع serviceType ثابت؛ participant=المستفيد كـPatient + الفرع/القسم كـLocation، start/end من نافذة السريان، status على قيم Appointment.status (active→booked، on_hold→pending، released→cancelled، الغائب→proposed) مع حفظ الحالة الأصلية؛ وإسقاط أيام الأسبوع/الفترة/تسمية المقعد/سبب الإيقاف/سبب الإفراج/قائمة الانتظار/الفرع كامتدادات). **كلها إضافية بحتة غير كاسرة** (لا تفرض profile سعودياً — W1342). + 33 اختباراً ذاتياً (27 + 30 + 31 + 37 + 32 + 27 + 28 + 29 + 24 + 38 + 36 + 46 + 46 + 47 + 40 + 45 + 42 + 34 + 30 + 30 + 33 + 32 + 31 + 35 + 33 + 36 + 32 + 36 + 36 + 31 + 33 + 30 + 27 = **1124 تأكيداً**) مع تأكيد مطابقة round-trip ضد المخططات الكنسية (مُدرَجة في بوّابة sprint). **وبوّابة حراسة للطبقة بأكملها (W1316):** `__tests__/fhir-mapper-layer-wave1316.test.js` (140 تأكيداً) تقفل سلامة البرميل: كل مفتاح في `MAPPERS` اسم كنسي مُسجّل حقاً، وكل مُحوّل مُصدَّر يظهر في الجدول (لا يتيم)، وخريطة `RESOURCE_TYPES` المجمّدة تطابق مفاتيح `MAPPERS` وكل مُحوّل يُصدر `resourceType` المُعلَن، وكل مُحوّل خالص (يُرجع كائناً عادياً دون تغيير مدخله) — فأي تعديل مستقبلي يغيّر نوع المورد أو يترك مُحوّلاً يتيماً يفشل CI. **ومُجمّع Bundle للطبقة (W1343):** `intelligence/fhir/fhir-bundle.lib.js` دالة خالصة تغلّف الموارد المُحوّلة في FHIR R4 Bundle (collection/searchset/document/transaction/batch) مع fullUrl ثابت (URL مطلق عند وجود id وإلا urn حتمي) + بناء request (PUT upsert عند وجود id، POST create وإلا) لأنواع transaction/batch + total لـsearchset؛ ودالة راحة `buildFhirBundleFromEntities(entries, mappers)` تحوّل سجلات كنسية مباشرة إلى Bundle عبر جدول مُحوّلات محقون (تجنّب اعتماد دائري على البرميل) — يغلق ثغرة «كيف تُشحن عدة موارد لـNPHIES» (تصدير `$everything`/دفعة معاملات). + 29 تأكيداً (منها e2e حي عبر `MAPPERS` الحقيقية، مُدرَج في بوّابة sprint). **ومُدقّق بنيوي FHIR R4 للطبقة (W1344):** `intelligence/fhir/fhir-validate.lib.js` دالة خالصة (لا ترمي عند عدم الصلاحية — عدم الصلاحية بيانات لا خطأ) تتحقق من تعدّدية العناصر المطلوبة (1..1 / 1..\*) لـ 16 نوع مورد تُصدرها الطبقة (`validateFhirResource` للمورد المفرد + `validateFhirBundle` للحزمة بأخطاء مُفهرسة حسب موقع المدخل)؛ تتسامح مع الأنواع غير المعروفة (إلا بـ `strictUnknown`) لئلا تفشل إضافات مستقبلية زوراً — وبوّابة W1316 تثبت الآن أن **كل واحد من الـ 33 مُحوّلاً يُصدر مورداً صحيحاً بنيوياً**، وأن Bundle يجمع الـ 33 صحيح (الفحص المتبادل كشف ثغرة مطابقة حقيقية: `NutritionOrder.dateTime` إلزامي 1..1 ويُحذف عند غياب `prescribedAt`). + 29 تأكيداً. (إجمالي الطبقة الآن **1322 تأكيداً**.) **ومُسلسِل OperationOutcome للطبقة (W1345):** `intelligence/fhir/fhir-operation-outcome.lib.js` دالة خالصة تحوّل نتيجة المُدقّق (مورد مفرد أو Bundle) إلى مورد FHIR R4 `OperationOutcome` قياسي — وهو الأسلوب المعياري لـNPHIES/أي نقطة FHIR للإبلاغ عن فشل التحقق (لا شكل خطأ مخصص). كل خطأ → `issue` بـseverity/code من قوائم القيم FHIR R4 (افتراضياً error/structure)، مع استخراج تعبير FHIRPath من التشخيص حين يُعرَف (`CarePlan.subject` أو `Bundle.entry[3].resource.Coverage.payor` لأخطاء الحزمة)؛ ومسار النجاح يُصدر issue معلوماتياً واحداً (issue إلزامي 1..\*). + 22 تأكيداً (منها e2e مُحوّل→مُدقّق→OperationOutcome). (إجمالي الطبقة الآن **1344 تأكيداً**.) **ومُنسّق راحة للطبقة (W1346):** `intelligence/fhir/fhir-convert.lib.js` دالتا `toValidatedFhir(entityName, record)` و`toValidatedFhirBundle(entries)` تجمعان (تحويل كنسي → تدقيق بنيوي → OperationOutcome) في استدعاء واحد تُرجِع `{resource(s)/bundle, validation, operationOutcome}` — بحقن `MAPPERS` من البرميل (تجنّب الدور الدائري، نفس نمط W1343) فلا يمرّره المستهلك؛ تمرير خيارات `mapperOpts`/`validateOpts`/`outcomeOpts`/`bundleOpts`. يغلق فجوة الاستخدام العملي (المستهلك كان يربط الأربع يدوياً). + 15 تأكيداً (منها تطابق الحكم مع تشغيل القطع منفردة). (إجمالي الطبقة الآن **1359 تأكيداً**.) **وفاحص تكامُل المراجع (W1347):** `intelligence/fhir/fhir-reference-integrity.lib.js` نقي؛ `checkResourceReferences(resource)` و`checkBundleReferences(bundle)` يمشيان كل `Reference.reference` مُنبعث (بحث تكراري مع مسار FHIRPath)، ويتحقّقان من صحة شكله (نسبي `ResourceType/id` / مطلق / `urn:` / `#contained`) دون فرض مجموعة أنواع مغلقة (تفادي إيجابيات كاذبة لمراجع Practitioner/Organization/إلخ التي لا تُنتجها الطبقة)؛ وداخل الـBundle يُصنّفان المرجع داخلياً (يُحل إلى مورد مُضمّن) أو خارجياً (dangling — مشروع، لا خطأ). العطب بيان لا استثناء (لا يرمي). شبكة أمان لتشوّه أي `Patient/${beneficiaryId}` إن كان المعرّف غائباً/كائناً. + 17 تأكيداً (منها e2e على Bundle حقيقي من MAPPERS). **أُضيف مولّد CapabilityStatement (W1348):** `fhir-capability-statement.lib.js` دالة خالصة تشتقّ من `RESOURCE_TYPES` المجمّدة بيان FHIR R4 CapabilityStatement (`kind: capability`، `fhirVersion 4.0.1`، `/metadata`) يُصرّح بـ 16 نوع مورد مميّز بتفاعلات `read`/`search-type` فقط (المحوّلات تُنتج ولا تستورد)، دون ربط ملفات NPHIES (قرار منتج) — فلا تنحرف القدرات المنشورة عمّا تُخرجه المحوّلات. تاريخ البيان محقون (نقاء). + 10 تأكيدات. (إجمالي الطبقة الآن **1386 تأكيداً**.) لا تزال تدريبات الاستعادة الدورية أبعد من `dr-verify` غير موثّقة. **أُضيفت رؤية احتفاظ البيانات (PDPL):** سكربت قراءة-فقط `backend/scripts/check-retention-coverage.js` (`npm run check:retention-coverage[:json]`) يجرد كل النماذج (920) ويصنّف أيّها يملك سياسة TTL/احتفاظ (39 حالياً) وأيّها لا — دون حذف أو إنفاذ أو إفشال CI (تصنيف «يمسّ PII» قرار بشري، تفادياً للإيجابيات الكاذبة). + اختبار ذاتي (13 تأكيداً، مُدرَج في بوّابة sprint). **متبقٍّ:** ربط المُحوّلات بنقطة نهاية تصدير FHIR فعلية (REST `$everything`-style) + توسيعها لموارد إضافية (CarePlan/Goal/Practitioner)، وأتمتة احتفاظ/إتلاف لكل مجموعة وفق سياسة PDPL مُعتمَدة (الرؤية الآن متاحة لاتخاذ القرار)، وتدريبات استعادة دورية موثّقة.

## خلاصة الرأي

الأساس قوي والانضباط الهندسي عالٍ. **بعد جولة التحقّق (2026-06-15) تبيّن أن أغلب الادّعاءات كانت مبالغاً فيها**: البنود 1/2/8 سليمة فعلياً أو أصغر بكثير من المدّعى (لا اختبارات ميتة، الأرشيف مُتجاهَل في git، لا ملفات `.tmp` مرفوعة)، والبنية الأساسية للبنود 4/5/9 **موجودة جزئياً** (k6، project-stats، axe-core). أكبر مخاطرة حقيقية متبقّية ليست «ميزة ناقصة» بل **ثقل الصيانة + فجوات تشغيلية محدودة**: التضخّم المحلي 148MB غير الم�
عقَّب على القرص (يؤذي OneDrive/الاستنساخ فقط)، والجوال رهينة الإصدار (`/api/v1` صلب)، وغياب فحص أسرار CI (✅ أُغلق W1303)، وربط اختبارات الحمل/الـ a11y بخط أنابيب فعلي. **أُنجز هذه الجلسة:** W1301 (بوّابة الاختبارات 1041→1600 + إصلاح ENAMETOOLONG)، W1302 (تصحيح البند 2)، W1303 (gitleaks)، W1304 (ملف حمل k6 واقعي بعتبات SLO)، W1305 (وضع `--json`/`collectStats()` الآلي في project-stats — شرط منع انجراف الوثائق)، W1306 (أول بوّابة a11y فعلية بـ axe-core في مجموعة اختبارات frontend)، W1307 (جرد رؤية احتفاظ البيانات PDPL — قراءة فقط)، W1308 (إصلاح خلل البادئة المزدوجة `/api/v1/api/v1` + المقطع الخاطئ `wasel/address`→`wasel-address` في عميل العنوان الوطني بالجوال، مع اختبارات انحدار + توحيد سلسلة الـ base URL + تغطية وحدة cctv فأصبحت كل وحدات الجوال الـ8 مُختبَرة)، وتصحيح إطار البند 6 (يستند لتدقيق ازدواجية سابق أرشف ~790 ملفاً وثلّث الباقي؛ المتبقّي الفعلي بند auth-middleware واحد يحتاج PR مُختبَراً لا أتمتة). **دُفعت كل المواجات W1301–W1308 (14 commit) إلى `origin/fix/w1277-journey-unified-plan` بعد مرور البوّابات السبع.** W1309 (أساس مُحوِّل FHIR R4 Patient — دالة خالصة إضافية + 27 تأكيداً، يفتح اختبار مطابقة البند 10) + تنظيف القرص (`git clean -fdX _archived`: 148MB→0.1MB، الملفات الـ9 المتعقَّبة سليمة، لا أثر على git).
