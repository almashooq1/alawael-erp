# تفعيل وتشغيل طبقة واتساب المؤسسية — دليل الإطلاق (Activation & Cutover)

> **الحالة:** دليل تشغيل — 2026-06-24
> **النطاق:** تفعيل الموجات الست المشحونة من [`ENTERPRISE_LAYER_BLUEPRINT.md`](./ENTERPRISE_LAYER_BLUEPRINT.md) في الإنتاج.
> **وثائق شقيقة:** [`ENTERPRISE_LAYER_BLUEPRINT.md`](./ENTERPRISE_LAYER_BLUEPRINT.md) (التصميم) · [`MENU_BOT.md`](./MENU_BOT.md) · [`SETUP_AND_OTP.md`](./SETUP_AND_OTP.md) (إعداد Meta + OTP).

هذا الدليل يحوّل الكود المشحون إلى **منتج قابل للنشر**: ماذا تفعّل، بأي ترتيب، وكيف تتحقّق.

---

## 0) ما الذي شُحِن (11 موجة)

| الموجة   | القدرة                                       | الخلفية (alawael-erp)    | الواجهة (alawael-rehab-platform) |
| -------- | -------------------------------------------- | ------------------------ | -------------------------------- |
| 1 · M2   | شريط السياق التأهيلي في الـInbox             | W1491 (#627, **merged**) | W1492 (#154)                     |
| 2 · M2   | ملاحظات داخلية + سجل تحويل + ربط تذكرة/جلسة  | W1493 (#628, **merged**) | W1494 (#155)                     |
| 3 · M7   | الحملات (إنشاء/تشغيل/تتبّع)                  | W1495 (#629, **merged**) | W1496 (#156)                     |
| 4 · M3   | مزامنة المواعيد من الـInbox (إلغاء/تغيّب)    | W1497 (#630, **merged**) | W1498 (#157)                     |
| 5 · M8   | مؤشرات أثر التأهيل (KPIs)                    | W1499 (#633, **merged**) | W1500 (#158)                     |
| 6 · M7+  | مُجدوِل الحملات (إطلاق آلي)                  | W1501 (#635, **merged**) | — (لا واجهة)                     |
| 7 · M2   | واجهة التحويل + قائمة الموظفين القابلين      | W1505 (#637, **merged**) | W1506 (#159)                     |
| 8 · M10  | مركز الإعدادات + إدارة الموافقات             | — (لا حاجة)              | W1507 (#160)                     |
| 9 · M9   | تمييز صلاحيات السياق/الملاحظات السريرية      | W1509 (#638)             | — (لا حاجة)                      |
| 10 · M5  | أتمتة ملخّص ما بعد الجلسة (حدث→رسالة)        | W1511 (#639)             | — (لا واجهة)                     |
| 11 · M6  | إشعار حلّ الشكوى (حدث→رسالة)                 | W1513 (#640)             | — (لا واجهة)                     |
| —        | دليل التفعيل والتشغيل (هذا الملف)            | W1503 (#636, **merged**) | —                                |

> **الخلفية:** الموجات 1–8 (#627–#637) مدموجة في `main`؛ مفتوحة وقابلة للدمج: **#638 (M9)** · **#639 (M5)** · **#640 (M6)**. **الواجهة:** سلسلة مكدّسة **#154→#160** على `master`. راجع `gh pr list` لأحدث حالة.

---

## 1) ترتيب الدمج (مهم — سلسلة الواجهة مكدّسة)

**الخلفية (erp):** الـPRs مستقلّة أو مدموجة — ادمج #633 ثم #635 على `main` بأي ترتيب.

**الواجهة (rehab):** السلسلة **#154 → #155 → #156 → #157 → #158** مترابطة خطيًا (تتشارك `[id]/page.tsx` + `whatsapp-api.ts` + `types/whatsapp.ts` + `nav-items.v2.tsx`).

- ادمجها **بالترتيب أعلاه**.
- **يُفضّل «Merge commit» أو «Rebase and merge»** (لا **Squash**) — الـsquash يُنشئ commit جديدًا لا يشارك التاريخ، فيتباعد الفرع التالي ويظهر `CONFLICTING`.
- إن استخدمت Squash لقاعدةٍ ما، أعد قاعدة الفرع التالي قبل دمجه:
  ```bash
  git worktree add /tmp/fix <child-branch>
  git rebase --onto origin/master <old-base-sha> <child-branch>
  git push --force-with-lease ; gh pr edit <n> --base master
  ```

---

## 2) أعلام البيئة (Env Flags) — التفعيل التدريجي

كل شيء **يعمل بأمان دون أي علم جديد** عدا التالي. فعّل بالترتيب (الأقل خطرًا أولًا):

| العلم                                          | الموجة     | الافتراضي | الأثر عند التفعيل                                                             |
| ---------------------------------------------- | ---------- | --------- | ----------------------------------------------------------------------------- |
| (لا شيء)                                       | 1–5,7,8    | —         | السياق + الملاحظات + التحويل + المواعيد + KPIs + الإعدادات تعمل فور الدمج     |
| `ENABLE_WHATSAPP_CAMPAIGN_SCHEDULER=true`      | 6          | **OFF**   | إطلاق آلي ساعتيّ للحملات المجدوَلة المستحقّة                                  |
| `ENABLE_WHATSAPP_POST_SESSION_SUMMARY=true`    | 10 · M5    | **OFF**   | **رسالة آلية للولي عند اكتمال كل جلسة.** ⚠️ راجع التواتر للحضور اليومي        |
| `ENABLE_WHATSAPP_COMPLAINT_RESOLVED=true`      | 11 · M6    | **OFF**   | رسالة آلية للولي عند حلّ شكوى مرتبطة بمستفيد                                  |

**اعتماديات Meta المسبقة (موجودة من قبل — ليست من هذه الموجات):** `WHATSAPP_API_TOKEN` + `WHATSAPP_PHONE_ID` + `WHATSAPP_WEBHOOK_SECRET` + `WHATSAPP_VERIFY_TOKEN` (راجع `SETUP_AND_OTP.md`). بدونها تبقى عمليات الإرسال في وضع stub (آمن للاختبار).

> **سلامة المُجدوِل:** `runCampaign` يضبط `status='running'` ← التشغيل المزدوج آمن (لا إرسال مكرّر)؛ كل مستلم يمرّ بفلتر الموافقة + تحديد المعدّل. مُعطَّل افتراضيًا = أثر صفري.

> **⚠️ سلامة الأتمتة (الأعلام 10/11 — رسائل آلية للأسر):** كل الأحداث مُعطّلة افتراضيًا وخاملة تمامًا حتى التفعيل المتعمَّد. عند التفعيل: (1) كل رسالة تمرّ ببوابة موافقة `WhatsAppConsent.canMessage`؛ (2) تتحوّل stub تلقائيًا إن لم تُفعَّل اعتمادات Meta؛ (3) **اختبر أولًا برقم اختبار** قبل التفعيل في الإنتاج؛ (4) للحضور اليومي، راجع تواتر «ملخّص ما بعد الجلسة» (رسالة لكل جلسة قد تكون كثيفة) — فعّله انتقائيًا أو أضف تصفية قبل التوسّع. حلّ الولي عبر `whatsappGuardianResolver` (ولي شرعي → راعٍ رئيسي → أول برقم).

---

## 3) الأدوار والصلاحيات

الموجات تَستخدم نموذج المصادقة القائم (`authenticate` + عزل الفرع `effectiveBranchScope`). النقاط حسّاسة-المستأجر معزولة بالفرع تلقائيًا.

**M9 (مشحون — W1509 / #638): تمييز أقل-امتياز للمحتوى السريري.** أُضيف مفتاحا صلاحية (`whatsapp:beneficiary:context` + `whatsapp:notes:internal`) إلى `can()` يحاكيان سياسة `beneficiary:clinical:read`:

- **مُتاح:** أخصائي · مشرف · مدير فرع · مدير تنفيذي · مدقّق.
- **محظور (403 + تجريد الملاحظات):** استقبال · تحصيل · موارد بشرية · **مدير النظام/super_admin**.

> ⚠️ **تغيير سلوكي مقصود عند دمج #638:** اختبر السياق/الملاحظات بدور **أخصائي/مدير لا super_admin** (مدير النظام محظور بالتصميم — أقل امتياز). القيم قابلة للتعديل في `docs/architecture/role-permissions.seed.json` ثم `npm run gen:permissions-registry`. الواجهة تتدهور بلطف (بلا أعطال).

---

## 4) قائمة تحقّق الإطلاق (Cutover Checklist)

1. **ادمج الخلفية** (#633 ثم #635) في `main` → ينشر تلقائيًا (راجع runbook النشر).
2. **ادمج سلسلة الواجهة** بالترتيب #154→#158 (Merge commit، لا squash).
3. **تحقّق من القراءة-فقط فورًا** (لا أعلام لازمة):
   - افتح محادثة في الـInbox → يظهر **شريط السياق** (مستفيد/خطة/أهداف/جلسات/فواتير).
   - أضف **ملاحظة داخلية** → تظهر بالكاتب والوقت، وليست في خيط واتساب.
   - على جلسة قادمة: زرّا **«تغيّب»/«إلغاء»** → تتحدّث حالة الجلسة (حدث `session.no_show`/`session.cancelled` → الخط الزمني).
   - افتح **«مؤشرات الأثر»** → اعتماد + غياب (مع/بدون واتساب) + أهداف + NPS.
4. **أنشئ حملة تجريبية** (مسودة) → شغّلها يدويًا (`/run`) على مجموعة صغيرة → راجع المقاييس (مُرسَل/مؤهَّل/محجوب).
5. **فعّل المُجدوِل** أخيرًا: `ENABLE_WHATSAPP_CAMPAIGN_SCHEDULER=true` + أعد تشغيل pm2 (إعادة قراءة `.env`) → أنشئ حملة **مجدوَلة** بوقت قريب → تأكّد إطلاقها آليًا خلال الساعة.

---

## 5) التحقّق السريع عبر API (curl)

```bash
# سياق المستفيد لمحادثة
GET  /api/v1/whatsapp/conversations/:id/context
# ملاحظة داخلية
POST /api/v1/whatsapp/conversations/:id/notes        { "text": "…" }
# تحويل مُدقَّق + قائمة الموظفين القابلين (للمُنتقي)
POST /api/v1/whatsapp/conversations/:id/transfer     { "staffId": "…", "reason": "…" }
GET  /api/v1/whatsapp/assignable-staff               # موظفو الفرع النشطون
# مزامنة موعد
POST /api/v1/whatsapp/conversations/:id/appointment-action { "sessionId":"…", "action":"no_show" }
# حملات
GET/POST /api/v1/whatsapp/campaigns ; POST /api/v1/whatsapp/campaigns/:id/run
POST /api/v1/whatsapp/campaigns/run-due              # إطلاق المستحقّة (يدوي/خارجي)
# مؤشرات الأثر + الموافقات (الإعدادات)
GET  /api/v1/whatsapp/analytics/rehab-outcomes?windowDays=90
GET  /api/v1/whatsapp/consent/:phone ; POST .../consent/:phone/opt-in|/opt-out
```

كل النقاط معزولة بالفرع؛ معرّف من فرع آخر ← 404 نظيف.

---

## 6) ما هو **مؤجَّل** (متابعات مقصودة)

| البند                                                          | المرجع | السبب / الحالة                                                                            |
| ------------------------------------------------------------- | ------ | ---------------------------------------------------------------------------------------- |
| تسليم التقرير الفعلي عبر واتساب (مستند)                        | M4     | حمولة `generated_report.completed` بلا رابط مستند عام؛ المتاح بنفس النمط = إشعار «جاهز»  |
| باني event-bindings قابل للتهيئة من الإدارة (واجهة)            | M5+    | الأعلام الثابتة (10/11) تعمل؛ تحويلها لتهيئة بواجهة = مشروع منفصل فوق `whatsappEventBindings` |
| تأكيد الموعد (`confirm`) + تخزين سبب الغياب على الجلسة         | M3     | لا دالة facade نظيفة لـ confirm؛ السبب حاليًا في خيط واتساب                              |
| توحيد حلّال الولي (W1511 → `whatsappGuardianResolver` المشترك) | M5/M6  | تنظيف تافه بعد دمج #639/#640                                                              |
| واجهة فعل الربط (تذكرة/جلسة) + إشعارات اختيارية (موعد/فاتورة)   | M2/M5  | الـAPI/المُنتِجون جاهزون؛ نفس النمط المُثبَت — أضِف عند الحاجة                            |
| استهداف الشرائح حسب التشخيص في الحملات                         | M7     | الجمهور حاليًا = مجموعة جهات اتصال                                                        |
| «الحضور بعد التذكير» كمؤشر دقيق                                | M8     | يتطلب ربط كل تذكير بجلسته (غير متعقَّب)؛ المتاح = مقارنة ارتباطية                         |

> **نمط إضافة أتمتة جديدة (حدث→رسالة):** انسخ `services/whatsapp/whatsappComplaintResolvedSubscriber.js`، غيّر `EVENT` + `ENV_FLAG` + نص الرسالة، أعد استخدام `whatsappGuardianResolver`، اربطه في `startup/integrationBus.js`، وأضف اختبار 3-artifacts (نقيّ + سلوكي + تكامل عبر الناقل الحقيقي داخل-العملية بلا boot). **المُنتِجون الأحياء الجاهزون للاستهلاك:** `sessions.session.completed` (✅) · `complaint.complaint.resolved` (✅) · `invoices.invoice.paid` · `appointments.appointment.booked`/`.cancelled` · `generated-report.generated_report.completed` · `risk-snapshot.risk_snapshot.escalated` — كلها تحمل `beneficiaryId`.

---

## 7) ملاحظات الموثوقية والامتثال

- **العزل:** كل قراءة/إجراء عبر `effectiveBranchScope`/`branchFilter` (W1407/W1412)؛ لا `req.branchId` (W269h).
- **عدم التكرار:** الموجات تعيد استخدام قناة واتساب القائمة (whatsappService، بوت FSM، 7 نماذج) ونماذج النواة بـ `ref` فقط — لا نموذج محادثة/رسالة جديد.
- **مؤشرات M8 ارتباطية لا سببية** — مذكور صراحةً في الواجهة والكود.
- **التفويض النظيف (M3):** مزامنة المواعيد تمرّ عبر facade نطاق الجلسات (`cancelSession`/`markNoShow`) فتُصدر الأحداث القانونية ولا تتجاوز منطق النطاق.
- **الحوكمة:** كل موجة شُحنت بحارس انحراف (static + pure) + بوابات pre-push السبع خضراء + worktree معزول.
