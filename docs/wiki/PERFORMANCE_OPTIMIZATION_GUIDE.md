# 🎯 دليل تحسين الأداء الشامل

## 1️⃣ الأوامر الأساسية السريعة

```bash
# تنظيف سريع
npm cache clean --force

# إعادة تثبيت المكتبات
rm -rf node_modules && npm install

# تشغيل الاختبارات
npm test -- --passWithNoTests

# فحص الأكواد
npm run lint

# تنسيق الأكواد
npm run format
```

## 2️⃣ إصلاح المشاكل الشائعة

### ❌ المشكلة: VS Code بطيء جداً
**الحل:**
1. أغلق جميع المجلدات غير المستخدمة
2. أزل extensions غير المستخدمة
3. أعد تشغيل VS Code
4. تحقق من المساحة الحرة (يجب أن تكون أكثر من 5GB)

### ❌ المشكلة: npm install يفشل
**الحل:**
```bash
npm cache clean --force
del node_modules (Windows)
rm -rf node_modules (Linux/Mac)
npm install
```

### ❌ المشكلة: Jest tests فاشلة
**الحل:**
```bash
npm test -- --clearCache
npm test -- --testPathIgnorePatterns=/node_modules/
npm test -- --forceExit
```

### ❌ المشكلة: الاتصال بـ MongoDB فاشل
**الحل:**
1. تأكد من أن MongoDB قيد التشغيل: `mongod`
2. تحقق من عنوان URL في .env
3. تأكد من الإنترنت إذا كنت تستخدم MongoDB Atlas

## 3️⃣ نصائح الأداء المتقدمة

### 🚀 تحسين سرعة التطوير
1. **استخدم نسخة صغيرة من البيانات للاختبار**
   - لا تحمل قاعدة بيانات كاملة
   - استخدم mock data للاختبارات

2. **عطّل الميزات غير الضرورية**
   - Redis cache (إذا لم تكن بحاجة إليه)
   - جميع Socket.io connections إن أمكن

3. **استخدم nodemon بحذر**
   - قد يسبب استهلاك CPU عالي
   - استخدم `--ignore` لاستبعاد المجلدات غير الضرورية

4. **نظف logs بانتظام**
   - حذف ملفات .log القديمة
   - استخدم log rotation

## 4️⃣ إعدادات VS Code الموصى بها

### تقليل الاستهلاك
```json
{
  "files.watcherExclude": {
    "**/node_modules": true,
    "**/.git": true,
    "**/dist": true
  },
  "search.exclude": {
    "**/node_modules": true,
    "**/coverage": true
  },
  "telemetry.telemetryLevel": "off",
  "workbench.startupEditor": "none"
}
```

## 5️⃣ المهام المتكررة

### يومياً
- [ ] تشغيل `npm test`
- [ ] تشغيل `npm run lint`
- [ ] حذف السجلات القديمة

### أسبوعياً
- [ ] `npm cache clean --force`
- [ ] `npm update`
- [ ] تنظيف المجلدات غير المستخدمة

### شهرياً
- [ ] `rm -rf node_modules && npm install`
- [ ] تحديث VS Code
- [ ] تحديث المكتبات الكبرى

## 6️⃣ مراقبة الأداء

### استخدام Task Manager (Windows)
- اضغط `Ctrl + Shift + Esc`
- ابحث عن `node.exe` أو `npm.exe`
- تحقق من استهلاك الذاكرة والـ CPU

### استخدام Activity Monitor (Mac)
- افتح `Activity Monitor`
- ابحث عن `Node` أو `npm`

### استخدام htop (Linux)
```bash
htop
# بحث عن node process
```

## 7️⃣ روابط مفيدة

- [npm Documentation](https://docs.npmjs.com/)
- [Node.js Best Practices](https://nodejs.org/en/docs/guides/)
- [VS Code Performance](https://code.visualstudio.com/docs)

## ⚠️ تحذيرات هامة

❌ **لا تحذف:**
- package.json و package-lock.json
- الملفات المهمة في src أو controllers
- ملفات .env الخاصة

✅ **يمكنك حذف:**
- node_modules (سيتم إعادة تثبيته)
- ملفات .log
- مجلدات build و dist
- ملفات .cache

---

**تم التحديث:** فبراير 2026
