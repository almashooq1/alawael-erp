import React from 'react';
import { Box, Typography } from '@mui/material';

const ArchivingDashboard = () => {
    return (
        <Box sx={{ p: 3 }}>
            <Typography variant="h4">Archiving Dashboard</Typography>
            <Typography variant="body1">Content coming soon...</Typography>
        </Box>
    );
};

export default ArchivingDashboard;
