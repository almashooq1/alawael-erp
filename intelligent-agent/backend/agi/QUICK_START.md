# ⚡ دليل البدء السريع - نظام Rehab AGI

## 🚀 البدء في 5 دقائق

### 1. التثبيت السريع

```bash
cd intelligent-agent/backend/agi
npm install
```

### 2. الإعداد

```bash
# انسخ ملف البيئة
cp .env.example .env

# عدّل الإعدادات الأساسية
ERP_API_URL=https://your-erp.com/api
ERP_API_KEY=your-api-key
AGI_PORT=5001
```

### 3. التشغيل

```bash
# بناء المشروع
npm run build

# تشغيل الخادم
npm start

# أو للتطوير
npm run dev
```

### 4. التحقق

افتح المتصفح على:

```
http://localhost:5001
```

---

## 🎯 أول استدعاء API

### تحليل مستفيد

```bash
curl -X POST http://localhost:5001/api/rehab-agi/beneficiary/analyze \
  -H "Content-Type: application/json" \
  -d '{"beneficiaryId": "BEN-001"}'
```

### الحصول على القدرات

```bash
curl http://localhost:5001/api/rehab-agi/capabilities
```

### الحصول على الأمثلة

```bash
curl http://localhost:5001/api/rehab-agi/examples
```

---

## 📚 نقاط النهاية الرئيسية

| الطريقة | المسار                                        | الوصف                |
| ------- | --------------------------------------------- | -------------------- |
| POST    | `/api/rehab-agi/beneficiary/analyze`          | تحليل حالة مستفيد    |
| POST    | `/api/rehab-agi/beneficiary/suggest-program`  | اقتراح برامج تأهيلية |
| POST    | `/api/rehab-agi/beneficiary/predict-progress` | التنبؤ بالتقدم       |
| POST    | `/api/rehab-agi/erp/sync-beneficiary`         | مزامنة مع ERP        |
| POST    | `/api/rehab-agi/erp/create-invoice`           | إنشاء فاتورة         |
| GET     | `/api/rehab-agi/capabilities`                 | قدرات النظام         |
| GET     | `/api/rehab-agi/examples`                     | أمثلة الاستخدام      |

---

## 🔧 الإعدادات الأساسية

### ملف `.env`

```env
# الخادم
AGI_PORT=5001
NODE_ENV=production

# ERP
ERP_API_URL=https://your-erp.com/api
ERP_API_KEY=your-secret-key
ERP_SYNC_ENABLED=true

# قاعدة البيانات (اختياري)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=rehab_agi
```

---

## 🧪 الاختبار السريع

```bash
# تشغيل جميع الاختبارات
npm test

# اختبار ملف محدد
npm test -- disability-rehab-agi.test.ts

# مع تغطية الكود
npm run test:coverage
```

---

## 📖 الوثائق الكاملة

- 📘 [دليل شامل](REHAB_AGI_README.md)
- 💻 [أمثلة عملية](REHAB_AGI_EXAMPLES.md)
- 🔌 [تكامل ERP](ERP_INTEGRATION_GUIDE.md)
- 🧠 [AGI الرئيسي](README_AGI.md)

---

## 🎨 أنواع الإعاقات المدعومة

| النوع      | الكود      | الوصف           |
| ---------- | ---------- | --------------- |
| 🏃 الحركية | `physical` | صعوبات الحركة   |
| 👁️ البصرية | `visual`   | ضعف/فقدان البصر |
| 👂 السمعية | `hearing`  | ضعف/فقدان السمع |
| 🧠 الذهنية | `mental`   | صعوبات الإدراك  |
| 📖 التعلم  | `learning` | صعوبات التعلم   |
| 🗣️ النطق   | `speech`   | صعوبات النطق    |
| 🎭 التوحد  | `autism`   | طيف التوحد      |
| 🔀 متعددة  | `multiple` | إعاقات متعددة   |

---

## 💊 البرامج التأهيلية

| البرنامج          | الكود                   | الاستخدام         |
| ----------------- | ----------------------- | ----------------- |
| العلاج الطبيعي    | `physiotherapy`         | تحسين الحركة      |
| العلاج الوظيفي    | `occupational-therapy`  | مهارات الحياة     |
| علاج النطق        | `speech-therapy`        | تحسين النطق       |
| العلاج السلوكي    | `behavioral-therapy`    | تعديل السلوك      |
| التعليم الخاص     | `special-education`     | تعليم مخصص        |
| التأهيل المهني    | `vocational-training`   | مهارات العمل      |
| الإرشاد الاجتماعي | `social-guidance`       | التفاعل الاجتماعي |
| الدعم النفسي      | `psychological-support` | الصحة النفسية     |

---

## 🔥 أمثلة سريعة

### JavaScript/Node.js

```javascript
const axios = require('axios');

async function analyzeBeneficiary() {
  const response = await axios.post(
    'http://localhost:5001/api/rehab-agi/beneficiary/analyze',
    { beneficiaryId: 'BEN-001' }
  );
  console.log(response.data);
}

analyzeBeneficiary();
```

### Python

```python
import requests

response = requests.post(
    'http://localhost:5001/api/rehab-agi/beneficiary/analyze',
    json={'beneficiaryId': 'BEN-001'}
)
print(response.json())
```

### cURL

```bash
curl -X POST http://localhost:5001/api/rehab-agi/beneficiary/analyze \
  -H "Content-Type: application/json" \
  -d '{"beneficiaryId": "BEN-001"}'
```

---

## ⚠️ استكشاف الأخطاء

### الخادم لا يعمل

```bash
# تحقق من المنفذ
netstat -ano | findstr :5001

# أعد تشغيل الخادم
npm start
```

### خطأ في الاتصال بـ ERP

```bash
# تحقق من الإعدادات
echo $ERP_API_URL
echo $ERP_API_KEY

# اختبر الاتصال
curl $ERP_API_URL/health
```

### خطأ في قاعدة البيانات

```bash
# تحقق من الاتصال
psql -h $DB_HOST -U $DB_USER -d $DB_NAME
```

---

## 📞 الدعم السريع

- 🐛 مشكلة؟ افتح issue على GitHub
- 💬 سؤال؟ تواصل على: support@rehab-agi.com
- 📚 وثائق إضافية: في مجلد `docs/`

---

## ✅ قائمة التحقق قبل الإنتاج

- [ ] تحديث جميع المفاتيح السرية في `.env`
- [ ] تفعيل HTTPS
- [ ] إعداد قاعدة البيانات
- [ ] اختبار التكامل مع ERP
- [ ] تفعيل النسخ الاحتياطي
- [ ] إعداد المراقبة والتنبيهات
- [ ] مراجعة إعدادات الأمان
- [ ] اختبار الأداء تحت الضغط
- [ ] توثيق الإجراءات التشغيلية
- [ ] تدريب الفريق

---

## 🎯 الخطوات التالية

1. ✅ تشغيل النظام محلياً
2. 📝 قراءة الوثائق الكاملة
3. 🧪 تجربة الأمثلة
4. 🔌 إعداد تكامل ERP
5. 🚀 النشر في الإنتاج

---

_دليل البدء السريع - آخر تحديث: 30 يناير 2026_
