# 📘 دليل النظام المتكامل (المحاسبة + الذكاء الاصطناعي)

**التاريخ:** 30 يناير 2026  
**الإصدار:** 1.0.0  
**الحالة:** جاهز للتشغيل

---

## 1) نظرة عامة

هذا الدليل يشرح النظام المتكامل الذي يربط:
- **النظام المحاسبي المتقدم** (Accounting API)
- **منصة الذكاء الاصطناعي والـ ML** (Intelligent Agent)
- **Gateway موحد** لتوجيه الطلبات
- **لوحات تحكم وتوقعات مالية** مدعومة بالذكاء الاصطناعي

الهدف: توفير **تحليلات مالية لحظية** + **توصيات ذكية** + **تنبؤات** عبر واجهات موحدة.

---

## 2) البنية المعمارية

### 🧩 الخدمات الأساسية
- **Accounting API**: `http://localhost:5001/api/accounting`
- **Intelligent Agent API**: `http://localhost:3001/api`
- **Gateway**: `http://localhost:3001/api/gateway`

### 🔌 تدفق البيانات
1. Intelligent Agent يستدعي تقارير المحاسبة.
2. يحلل البيانات ويولد Insights + تنبؤات.
3. يعرض النتائج في لوحة موحدة.
4. Gateway يتيح الوصول الموحد للأنظمة.

---

## 3) إعدادات البيئة

### المتغيرات المطلوبة
```
ACCOUNTING_API_BASE_URL=http://localhost:5001/api/accounting
ACCOUNTING_API_TOKEN=YOUR_JWT_IF_REQUIRED
AI_API_BASE_URL=http://localhost:3001/api/ai
ML_API_BASE_URL=http://localhost:3001/api/ml
```

> ملاحظة: `ACCOUNTING_API_TOKEN` اختياري حسب إعدادات المصادقة.

---

## 4) نقاط النهاية الأساسية

### ✅ تكامل المحاسبة + الذكاء الاصطناعي
- GET `/api/ai/accounting/health`
- GET `/api/ai/accounting/summary`
- GET `/api/ai/accounting/insights`
- GET `/api/ai/accounting/reports/advanced`
- GET `/api/ai/accounting/forecast`

### ✅ Gateway موحد
- GET `/api/gateway/health`
- Proxy `/api/gateway/accounting/*`
- Proxy `/api/gateway/ml/*`
- Proxy `/api/gateway/ai/*`

---

## 5) المخرجات الذكية (AI Outputs)

### 📊 تقرير AI المالي المتقدم
يتضمن:
- **ملخص تنفيذي**
- **إشارات السيولة والرافعة والربحية**
- **تنبيهات المخاطر**
- **توصيات تشغيلية**

### 🔮 التنبؤات المالية
- متوسط النمو الشهري (Revenue & Expense)
- توقعات 3 أشهر
- درجة ثقة متوقعة بناءً على التقلبات

---

## 6) لوحة التحكم الموحدة

### موقع الواجهة
`/لوحة موحدة` داخل واجهة Intelligent Agent

### ما يتم عرضه؟
- إجمالي الأصول / الخصوم / حقوق الملكية
- صافي الربح والإيرادات والمصروفات
- مستوى المخاطر
- توصيات الذكاء الاصطناعي
- التنبؤات المالية القادمة

---

## 7) الاختبار السريع

### اختبار صحة التكامل
```
GET http://localhost:3001/api/ai/accounting/health
```

### تقرير الذكاء الاصطناعي
```
GET http://localhost:3001/api/ai/accounting/reports/advanced
```

### توقعات مالية
```
GET http://localhost:3001/api/ai/accounting/forecast
```

---

## 8) الملفات المهمة

### Backend
- `backend/services/accountingAi.service.ts`
- `backend/routes/ai.accounting.routes.ts`
- `backend/routes/gateway.routes.ts`
- `backend/services/gatewayProxy.service.ts`

### Frontend
- `frontend/src/components/UnifiedAccountingAIDashboard.tsx`
- `frontend/src/components/AIProcessReports.tsx`
- `frontend/src/services/AIClient.ts`

---

## 9) ملاحظات تشغيلية

- تأكد من تشغيل خادم المحاسبة أولًا.
- ثم تشغيل خادم Intelligent Agent.
- Gateway يعمل تلقائيًا ضمن نفس الخادم.

---

## 10) الحالة الحالية

✅ تم تنفيذ:
- تكامل Accounting + AI
- تقارير ذكية متقدمة
- تنبؤات مالية
- لوحة موحدة
- Gateway موحد

⏳ المتبقي:
- لا يوجد متطلبات تقنية إضافية حالياً.

---

## 11) دعم وصيانة

عند ظهور أخطاء:
- راجع `/api/ai/accounting/health`
- تحقق من متغيرات البيئة
- تأكد من اتصال قاعدة البيانات وخادم المحاسبة

---

**النظام جاهز للاستخدام الإنتاجي.**
