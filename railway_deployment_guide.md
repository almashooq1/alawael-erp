# دليل النشر على Railway - خطوة بخطوة

## 🚀 الخطوات الكاملة للنشر

### الخطوة 1: رفع الكود إلى GitHub ✅

```bash
# في مجلد المشروع
git init
git add .
git commit -m "نظام ERP مراكز الأوائل - جاهز للنشر"
git branch -M main
git remote add origin https://github.com/yourusername/alawael-erp.git
git push -u origin main
```

### الخطوة 2: إنشاء حساب Railway ✅

1. اذهب إلى: **https://railway.app**
2. انقر "Sign up" أو "Login with GitHub"
3. اختر "Continue with GitHub"
4. وافق على الصلاحيات

### الخطوة 3: إنشاء مشروع جديد ✅

1. في Railway Dashboard انقر **"New Project"**
2. اختر **"GitHub Repo"** ✅
3. ستظهر قائمة بمستودعاتك

### الخطوة 4: اختيار المستودع 🔄

**ابحث عن مستودع المشروع:**
- الاسم: `splitwise` أو `alawael-erp`
- يحتوي على ملف `app.py`
- انقر على اسم المستودع

### الخطوة 5: انتظار النشر الأولي ⏳

**ما سيحدث:**
- Railway سيكتشف أنه مشروع Python
- سيقرأ ملف `requirements.txt`
- سيبدأ تثبيت المكتبات (2-3 دقائق)
- سيحاول تشغيل التطبيق

**النتيجة المتوقعة:**
- ✅ النشر سينجح جزئياً
- ❌ التطبيق لن يعمل (لأننا نحتاج قاعدة بيانات)

### الخطوة 6: إضافة قاعدة البيانات

**في Railway Dashboard:**
1. انقر **"Add Service"** أو **"+"**
2. اختر **"PostgreSQL"**
3. انتظر 1-2 دقيقة للإعداد
4. ستحصل على `DATABASE_URL` تلقائياً

### الخطوة 7: تكوين متغيرات البيئة

**انقر على خدمة التطبيق (Web Service):**
1. اذهب إلى تبويب **"Variables"**
2. أضف المتغيرات التالية:

```bash
SECRET_KEY=your-32-character-secret-key-here
JWT_SECRET_KEY=your-32-character-jwt-secret-here
FLASK_ENV=production
DATABASE_URI=postgresql://... (سيتم إنشاؤها تلقائياً)
```

**لإنشاء مفاتيح آمنة:**
```python
import secrets
print(secrets.token_urlsafe(32))  # انسخ النتيجة للـ SECRET_KEY
print(secrets.token_urlsafe(32))  # انسخ النتيجة للـ JWT_SECRET_KEY
```

### الخطوة 8: إعادة النشر

**بعد إضافة المتغيرات:**
- Railway سيعيد النشر تلقائياً
- انتظر 2-3 دقائق
- ستحصل على رابط يعمل

### الخطوة 9: اختبار النظام

**الرابط النهائي:**
```
https://your-project-name.railway.app
```

**اختبر:**
- ✅ فتح الصفحة الرئيسية
- ✅ تسجيل الدخول
- ✅ الوصول لصفحة الذكاء الاصطناعي

## 🔧 استكشاف الأخطاء

### مشكلة: "Application Error"
**الحل:**
- تحقق من Variables
- تأكد من وجود DATABASE_URL
- راجع Logs في Railway

### مشكلة: "Build Failed"
**الحل:**
- تحقق من ملف `requirements.txt`
- تأكد من وجود ملف `Procfile`

### مشكلة: "Database Connection Error"
**الحل:**
- تأكد من إضافة PostgreSQL Service
- تحقق من DATABASE_URL في Variables

## ⏱️ الوقت المتوقع

- **الإعداد الأولي**: 5-10 دقائق
- **النشر الأول**: 3-5 دقائق
- **إضافة قاعدة البيانات**: 2-3 دقائق
- **التكوين النهائي**: 2-3 دقائق

**المجموع: 15-20 دقيقة لنظام يعمل بالكامل**

## 🎯 الخطوة الحالية

**أنت الآن في الخطوة 4:**
- اختيار مستودع المشروع من قائمة GitHub
- ابحث عن `splitwise` أو اسم مشروعك
- انقر عليه لبدء النشر
