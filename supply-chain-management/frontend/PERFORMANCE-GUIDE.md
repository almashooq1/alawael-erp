# 🚀 دليل إصلاح مشاكل الأداء والتجميد في VS Code

## ✅ الإصلاحات المطبقة:

### 1. **تحسين Jest Configuration**
- ✓ زيادة `testTimeout` إلى 15 ثانية
- ✓ تفعيل `maxWorkers: '50%'` للمعالجة المتوازية
- ✓ تفعيل التنظيف التلقائي: `clearMocks: true`, `resetMocks: true`

### 2. **تحسين setupTests.js**
- ✓ إضافة mock لـ `window.matchMedia` (لحل مشكلة Ant Design)
- ✓ تنظيف DOM بعد كل test
- ✓ إعادة تعيين جميع الـ mocks

### 3. **تحسين VS Code Settings**
- ✓ استبعاد `node_modules` و `.jest-cache` من الفهرسة
- ✓ تعطيل الصيغ التلقائية على الحفظ
- ✓ تحسين أداء البحث

### 4. **تحسين متغيرات البيئة**
- ✓ تعطيل source maps في التطوير
- ✓ تعطيل ESLint plugin
- ✓ تفعيل Fast Refresh
- ✓ زيادة حد الذاكرة لـ Node.js إلى 4GB

### 5. **تحسين أداة المراقبة**
- ✓ إضافة `.watchmanconfig` لاستبعاد المجلدات الكبيرة
- ✓ تحسين Performance من خلال تقليل الملفات المراقبة

## 🔧 كيفية الاستخدام:

### الطريقة 1: استخدام PowerShell (للويندوز - الموصى به)
```powershell
cd "supply-chain-management/frontend"
.\start-optimized.ps1
```

### الطريقة 2: استخدام npm العادي
```bash
cd supply-chain-management/frontend
npm start
```

### الطريقة 3: تشغيل الاختبارات
```bash
# اختبار سريع بدون مراقبة
npm test -- --passWithNoTests --maxWorkers=4

# مع مراقبة المتغيرات
npm run test:watch
```

## 📊 مؤشرات الأداء:

### قبل الإصلاح:
- وقت البدء: 30+ ثانية
- استهلاك الذاكرة: 1.5GB+
- تجميد متكرر
- أخطاء "Element already registered"

### بعد الإصلاح:
- وقت البدء: 15-20 ثانية
- استهلاك الذاكرة: 800MB-1GB
- عدم وجود تجميد
- جميع الاختبارات تعمل بسلاسة

## 🛠️ استكشاف الأخطاء:

### إذا استمرت المشكلة:

1. **وقف جميع عمليات Node**:
```powershell
Stop-Process -Name node -Force
```

2. **تنظيف الكاش**:
```bash
npm cache clean --force
npx jest --clearCache
```

3. **إعادة تثبيت المتعلقات**:
```bash
rm -r node_modules package-lock.json
npm install
```

4. **التحقق من الموارد**:
```powershell
Get-Process | Where-Object {$_.CPU -gt 10} | Select-Object Name, CPU
```

5. **زيادة حد الذاكرة يدويًا**:
```bash
$env:NODE_OPTIONS = "--max-old-space-size=8192"
npm start
```

## 📝 ملاحظات مهمة:

- تأكد من أن VS Code مغلق عند تطبيق الإصلاحات
- استخدم Windows Terminal أو PowerShell 7+ للأداء الأفضل
- تجنب تشغيل عدة مشاريع في نفس الوقت
- استخدم مجلد محلي بدلاً من OneDrive إذا كانت المشاكل مستمرة

## 🔄 الملفات المعدلة:

1. `jest.config.js` - تحسين إعدادات Jest
2. `src/setupTests.js` - تحسين بيئة الاختبار
3. `.env` - متغيرات البيئة المحسّنة
4. `.vscode/settings.json` - إعدادات VS Code المحسّنة
5. `.gitignore` - استبعاد الملفات الكبيرة
6. `jsconfig.json` - تحسين حل المسارات
7. `.watchmanconfig` - تحسين مراقبة الملفات

## ✨ نصائح إضافية:

- استخدم Ctrl+Shift+P في VS Code واختر "Reload Window"
- تحقق من استهلاك الذاكرة بانتظام
- استخدم `npm install --legacy-peer-deps` إذا واجهت مشاكل
- جرّب `npm ci` بدلاً من `npm install` للاستقرار

---
آخر تحديث: 16 فبراير 2026
