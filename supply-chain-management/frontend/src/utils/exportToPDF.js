/* eslint-disable no-unused-vars */
export function exportToPDF(data, columns, _fileName = 'data.pdf') {
  // يمكن لاحقاً استخدام مكتبة jsPDF أو pdfmake
  alert('ميزة التصدير إلى PDF قيد التطوير. حالياً يدعم التصدير إلى Excel فقط.');
}
