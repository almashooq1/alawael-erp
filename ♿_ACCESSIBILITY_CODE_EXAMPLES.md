# ♿ **Accessibility Features - Complete Usage Examples**\n\n**Version:** 1.0.0  \n**Last Updated:** February 18, 2026  \n\n---\n\n## 🎯 **Quick Start**\n\n### **1. Basic Integration (5 minutes)**\n\n```jsx\n// App.jsx\nimport React from 'react';\nimport AccessibilityPanel from './components/AccessibilityPanel';\nimport AccessibilityService from './services/accessibility.service';\nimport I18nService from './services/i18n.service';\n\nfunction App() {\n  return (\n    <div>\n      <Navigation />\n      <MainContent />\n      {/* Add this one line */}\n      <AccessibilityPanel />\n    </div>\n  );\n}\n\nexport default App;\n```\n\n---\n\n## 🎨 **Theme Examples**\n\n### **Programmatically Change Theme**\n\n```javascript\n// Change to dark theme\nwindow.accessibilityService.setTheme('dark');\n\n// Change to high contrast\nwindow.accessibilityService.setTheme('hc-dark');\n\n// Get list of available themes\nconst themes = window.accessibilityService.getThemes();\nconsole.log(themes);\n// Output:\n// [\n//   { name: 'light', label: 'Light' },\n//   { name: 'dark', label: 'Dark' },\n//   { name: 'hc-dark', label: 'High Contrast Dark' },\n//   { name: 'hc-light', label: 'High Contrast Light' },\n//   { name: 'dyslexia', label: 'Dyslexia-Friendly' }\n// ]\n```\n\n### **React Theme Switcher Component**\n\n```jsx\nfunction ThemeSwitcher() {\n  const [currentTheme, setCurrentTheme] = React.useState('light');\n  \n  const themes = window.accessibilityService.getThemes();\n  \n  const handleThemeChange = (themeName) => {\n    window.accessibilityService.setTheme(themeName);\n    setCurrentTheme(themeName);\n  };\n  \n  return (\n    <div>\n      <label htmlFor=\"theme-select\">Theme:</label>\n      <select \n        id=\"theme-select\"\n        value={currentTheme} \n        onChange={(e) => handleThemeChange(e.target.value)}\n      >\n        {themes.map(theme => (\n          <option key={theme.name} value={theme.name}>\n            {theme.label}\n          </option>\n        ))}\n      </select>\n    </div>\n  );\n}\n```\n\n---\n\n## 📝 **Font & Size Examples**\n\n### **Adjust Font Size Programmatically**\n\n```javascript\n// Set font size to 18px\nwindow.accessibilityService.setFontSize(18);\n\n// Increase by 2px\nconst currentSize = localStorage.getItem('a11y-fontSize') || 16;\nwindow.accessibilityService.setFontSize(parseInt(currentSize) + 2);\n\n// Set to maximum\nwindow.accessibilityService.setFontSize(32);\n\n// Get all font options\nconst fonts = window.accessibilityService.getFonts();\nconsole.log(fonts);\n// Output:\n// [\n//   { name: 'standard', label: 'Standard' },\n//   { name: 'dyslexia', label: 'Dyslexia-Friendly' },\n//   { name: 'serif', label: 'Serif' },\n//   { name: 'sans', label: 'Sans-Serif' }\n// ]\n```\n\n### **React Font Size Adjuster**\n\n```jsx\nfunction FontSizeAdjuster() {\n  const [fontSize, setFontSize] = React.useState(16);\n  \n  const handleIncrease = () => {\n    const newSize = Math.min(fontSize + 2, 32);\n    window.accessibilityService.setFontSize(newSize);\n    setFontSize(newSize);\n  };\n  \n  const handleDecrease = () => {\n    const newSize = Math.max(fontSize - 2, 12);\n    window.accessibilityService.setFontSize(newSize);\n    setFontSize(newSize);\n  };\n  \n  return (\n    <div>\n      <button onClick={handleDecrease}>🔤 -</button>\n      <span>{fontSize}px</span>\n      <button onClick={handleIncrease}>🔤 +</button>\n    </div>\n  );\n}\n```\n\n### **Apply Multiple Font Settings**\n\n```javascript\n// For users with dyslexia\nwindow.accessibilityService.setFontFamily('dyslexia');\nwindow.accessibilityService.setFontSize(16);\nwindow.accessibilityService.setLineHeight(2.0);\nwindow.accessibilityService.setLetterSpacing(0.18);\nwindow.accessibilityService.setTheme('dark');\nconsole.log('Dyslexia-friendly settings applied!');\n```\n\n---\n\n## 🌍 **Language & i18n Examples**\n\n### **Change Language Programmatically**\n\n```javascript\n// Switch to Arabic\nwindow.i18n.setLanguage('ar');\n\n// Switch to German\nwindow.i18n.setLanguage('de');\n\n// Get current language\nconst currentLang = window.i18n.getCurrentLanguage();\nconsole.log(currentLang); // 'ar' or 'en', etc.\n\n// Get all supported languages\nconst languages = window.i18n.getSupportedLanguages();\nconsole.log(languages);\n// Output:\n// [\n//   { code: 'ar', name: 'العربية', flag: '🇸🇦' },\n//   { code: 'en', name: 'English', flag: '🇬🇧' },\n//   { code: 'fr', name: 'Français', flag: '🇫🇷' },\n//   { code: 'es', name: 'Español', flag: '🇪🇸' },\n//   { code: 'de', name: 'Deutsch', flag: '🇩🇪' }\n// ]\n```\n\n### **Use Translations in Components**\n\n```jsx\nfunction NavigationBar() {\n  const t = (key) => window.i18n.t(key);\n  const currentLang = window.i18n.getCurrentLanguage();\n  const isArabic = currentLang === 'ar';\n  \n  return (\n    <nav dir={isArabic ? 'rtl' : 'ltr'}>\n      <ul>\n        <li><a href=\"/\">{t('nav.dashboard')}</a></li>\n        <li><a href=\"/employees\">{t('nav.employees')}</a></li>\n        <li><a href=\"/attendance\">{t('nav.attendance')}</a></li>\n        <li><a href=\"/payroll\">{t('nav.payroll')}</a></li>\n        <li><a href=\"/leave\">{t('nav.leave')}</a></li>\n        <li><a href=\"/files\">{t('nav.files')}</a></li>\n      </ul>\n    </nav>\n  );\n}\n```\n\n### **Listen for Language Changes**\n\n```jsx\nfunction MyComponentWithLanguageDetection() {\n  const [language, setLanguage] = React.useState('en');\n  const [translations, setTranslations] = React.useState({});\n  \n  React.useEffect(() => {\n    // Set initial language\n    const currentLang = window.i18n.getCurrentLanguage();\n    setLanguage(currentLang);\n    setTranslations(window.i18n.getTranslations());\n    \n    // Listen for language changes\n    const handleLanguageChange = (event) => {\n      setLanguage(event.detail.language);\n      setTranslations(window.i18n.getTranslations());\n    };\n    \n    window.addEventListener('languageChanged', handleLanguageChange);\n    \n    return () => {\n      window.removeEventListener('languageChanged', handleLanguageChange);\n    };\n  }, []);\n  \n  return (\n    <div dir={language === 'ar' ? 'rtl' : 'ltr'}>\n      <h1>{window.i18n.t('nav.dashboard')}</h1>\n      <button>{window.i18n.t('action.save')}</button>\n    </div>\n  );\n}\n```\n\n### **Language Selector Component**\n\n```jsx\nfunction LanguageSelector() {\n  const [language, setLanguage] = React.useState(\n    window.i18n.getCurrentLanguage()\n  );\n  const languages = window.i18n.getSupportedLanguages();\n  \n  const handleLanguageChange = (newLang) => {\n    window.i18n.setLanguage(newLang);\n    setLanguage(newLang);\n    document.documentElement.dir = newLang === 'ar' ? 'rtl' : 'ltr';\n  };\n  \n  return (\n    <div className=\"language-selector\">\n      {languages.map((lang) => (\n        <button\n          key={lang.code}\n          onClick={() => handleLanguageChange(lang.code)}\n          className={language === lang.code ? 'active' : ''}\n          title={lang.name}\n        >\n          <span>{lang.flag} {lang.code.toUpperCase()}</span>\n        </button>\n      ))}\n    </div>\n  );\n}\n```\n\n---\n\n## 🔊 **Text-to-Speech Examples**\n\n### **Basic TTS Usage**\n\n```javascript\n// Enable Text-to-Speech\nwindow.accessibilityService.toggleTextToSpeech();\n\n// Speak specific content\nconst element = document.querySelector('h1');\nwindow.accessibilityService.speakElementContent(element);\n\n// Get TTS status\nconst isTTSEnabled = localStorage.getItem('a11y-tts') === 'true';\nconsole.log('TTS enabled:', isTTSEnabled);\n```\n\n### **Advanced TTS Control in React**\n\n```jsx\nfunction TextToSpeechButton() {\n  const [isSpeaking, setIsSpeaking] = React.useState(false);\n  const [speed, setSpeed] = React.useState(1.0);\n  \n  const handleSpeak = (element) => {\n    if (isSpeaking) {\n      window.speechSynthesis.cancel();\n      setIsSpeaking(false);\n    } else {\n      window.speechSynthesis.cancel();\n      \n      const text = element.innerText;\n      const language = window.i18n.getCurrentLanguage();\n      const utterance = new SpeechSynthesisUtterance(text);\n      \n      // Set language\n      const langMap = {\n        'ar': 'ar-SA',\n        'en': 'en-US',\n        'fr': 'fr-FR',\n        'es': 'es-ES',\n        'de': 'de-DE'\n      };\n      utterance.lang = langMap[language] || 'en-US';\n      utterance.rate = speed;\n      \n      utterance.onend = () => setIsSpeaking(false);\n      \n      window.speechSynthesis.speak(utterance);\n      setIsSpeaking(true);\n    }\n  };\n  \n  return (\n    <div>\n      <button \n        onClick={() => handleSpeak(document.querySelector('main'))}\n        aria-pressed={isSpeaking}\n      >\n        {isSpeaking ? '⏸️ Stop' : '▶️ Speak Page'}\n      </button>\n      \n      <label>\n        Speed: \n        <input \n          type=\"range\" \n          min=\"0.5\" \n          max=\"2.0\" \n          step=\"0.1\" \n          value={speed}\n          onChange={(e) => setSpeed(parseFloat(e.target.value))}\n        />\n        {speed}x\n      </label>\n    </div>\n  );\n}\n```\n\n---\n\n## ⌨️ **Keyboard Navigation Examples**\n\n### **Global Keyboard Shortcuts**\n\n```javascript\n// These are already built-in:\n// Alt+A  - Toggle Accessibility Panel\n// Alt+D  - Dashboard\n// Alt+F  - Files\n// Alt+T  - Text-to-Speech\n// Alt+H  - Help\n// Alt++  - Increase Font\n// Alt+-  - Decrease Font\n\n// Listen for specific key combinations\ndocument.addEventListener('keydown', (e) => {\n  if (e.altKey && e.key === 'e') {\n    // Alt+E for custom action\n    e.preventDefault();\n    console.log('Custom Alt+E pressed');\n  }\n});\n```\n\n### **Make Custom Component Keyboard Accessible**\n\n```jsx\nfunction AccessibleDropdown() {\n  const [isOpen, setIsOpen] = React.useState(false);\n  const [selectedIndex, setSelectedIndex] = React.useState(0);\n  const options = ['Option 1', 'Option 2', 'Option 3', 'Option 4'];\n  \n  const handleKeyDown = (e) => {\n    switch (e.key) {\n      case 'ArrowDown':\n        e.preventDefault();\n        setSelectedIndex((i) => (i + 1) % options.length);\n        break;\n      case 'ArrowUp':\n        e.preventDefault():\n        setSelectedIndex((i) => (i - 1 + options.length) % options.length);\n        break;\n      case 'Enter':\n      case ' ':\n        e.preventDefault();\n        setIsOpen(!isOpen);\n        break;\n      case 'Escape':\n        setIsOpen(false);\n        break;\n      default:\n        break;\n    }\n  };\n  \n  return (\n    <div className=\"dropdown\">\n      <button\n        onClick={() => setIsOpen(!isOpen)}\n        onKeyDown={handleKeyDown}\n        aria-expanded={isOpen}\n        aria-haspopup=\"listbox\"\n      >\n        {options[selectedIndex]}\n      </button>\n      \n      {isOpen && (\n        <ul role=\"listbox\">\n          {options.map((option, index) => (\n            <li\n              key={index}\n              role=\"option\"\n              aria-selected={index === selectedIndex}\n              className={index === selectedIndex ? 'selected' : ''}\n              onClick={() => setSelectedIndex(index)}\n            >\n              {option}\n            </li>\n          ))}\n        </ul>\n      )}\n    </div>\n  );\n}\n```\n\n---\n\n## ♿ **ARIA & Semantic HTML Examples**\n\n### **Proper Form Structure**\n\n```jsx\nfunction AccessibleForm() {\n  const t = (key) => window.i18n.t(key);\n  \n  return (\n    <form aria-label={t('form.label')}>\n      <div>\n        <label htmlFor=\"name\">{t('form.name')}</label>\n        <input\n          id=\"name\"\n          type=\"text\"\n          required\n          aria-required=\"true\"\n          aria-describedby=\"name-hint\"\n        />\n        <small id=\"name-hint\">{t('form.name-hint')}</small>\n      </div>\n      \n      <div>\n        <label htmlFor=\"email\">{t('form.email')}</label>\n        <input\n          id=\"email\"\n          type=\"email\"\n          required\n          aria-required=\"true\"\n          aria-describedby=\"email-error\"\n        />\n      </div>\n      \n      <button type=\"submit\" aria-label={t('action.submit-form')}>\n        {t('action.save')}\n      </button>\n    </form>\n  );\n}\n```\n\n### **Use ARIA Live Regions for Dynamic Updates**\n\n```jsx\nfunction DynamicContent() {\n  const [message, setMessage] = React.useState('');\n  const [count, setCount] = React.useState(0);\n  \n  const handleAction = () => {\n    setCount(count + 1);\n    setMessage(`Action completed! Count: ${count + 1}`);\n  };\n  \n  return (\n    <div>\n      {/* This will be announced to screen readers */}\n      <div\n        aria-live=\"polite\"\n        aria-atomic=\"true\"\n        role=\"status\"\n      >\n        {message}\n      </div>\n      \n      <button onClick={handleAction}>\n        Click me\n      </button>\n    </div>\n  );\n}\n```\n\n---\n\n## 💾 **Preference Management Examples**\n\n### **Export User Preferences**\n\n```javascript\n// Get all current preferences as JSON\nconst preferences = window.accessibilityService.exportPreferences();\nconsole.log(preferences);\n// Output:\n// {\n//   theme: 'dark',\n//   fontSize: 18,\n//   fontFamily: 'dyslexia',\n//   lineHeight: 1.8,\n//   letterSpacing: 0.15,\n//   language: 'ar',\n//   textToSpeech: true,\n//   reduceMotion: false,\n//   reduceTransparency: false,\n//   underlineLinks: true,\n//   boldText: false\n// }\n\n// Save to file\nfunction downloadPreferences() {\n  const prefs = window.accessibilityService.exportPreferences();\n  const dataStr = JSON.stringify(prefs, null, 2);\n  const dataBlob = new Blob([dataStr], { type: 'application/json' });\n  const url = URL.createObjectURL(dataBlob);\n  const link = document.createElement('a');\n  link.href = url;\n  link.download = 'accessibility-preferences.json';\n  link.click();\n}\n```\n\n### **Import Preferences from File**\n\n```jsx\nfunction ImportPreferencesButton() {\n  const inputRef = React.useRef();\n  \n  const handleFileSelect = (e) => {\n    const file = e.target.files[0];\n    if (!file) return;\n    \n    const reader = new FileReader();\n    reader.onload = (event) => {\n      try {\n        const prefs = JSON.parse(event.target.result);\n        window.accessibilityService.importPreferences(prefs);\n        alert('Preferences imported successfully!');\n      } catch (error) {\n        alert('Error importing preferences: ' + error.message);\n      }\n    };\n    reader.readAsText(file);\n  };\n  \n  return (\n    <div>\n      <input\n        ref={inputRef}\n        type=\"file\"\n        accept=\".json\"\n        onChange={handleFileSelect}\n        style={{ display: 'none' }}\n      />\n      <button onClick={() => inputRef.current?.click()}>\n        📥 Import Preferences\n      </button>\n    </div>\n  );\n}\n```\n\n---\n\n## 🎯 **Complete Feature Enablement Example**\n\n### **Enable Everything for Maximum Accessibility**\n\n```javascript\nfunction enableMaximumAccessibility() {\n  // Theme for high contrast\n  window.accessibilityService.setTheme('hc-dark');\n  \n  // Large, readable font\n  window.accessibilityService.setFontSize(24);\n  window.accessibilityService.setFontFamily('dyslexia');\n  window.accessibilityService.setLineHeight(2.0);\n  window.accessibilityService.setLetterSpacing(0.18);\n  \n  // Text options\n  window.accessibilityService.toggleBoldText(true);\n  window.accessibilityService.underlineAllLinks(true);\n  \n  // Motion reduction\n  window.accessibilityService.reduceMotion(true);\n  window.accessibilityService.reduceTransparency(true);\n  \n  // Enable TTS\n  window.accessibilityService.toggleTextToSpeech();\n  \n  // Use simple language (e.g., English)\n  window.i18n.setLanguage('en');\n  \n  console.log('✅ Maximum accessibility enabled!');\n  \n  // Log all settings\n  const prefs = window.accessibilityService.exportPreferences();\n  console.table(prefs);\n}\n\n// Call it\nenableMaximumAccessibility();\n```\n\n---\n\n## 🧪 **Testing Examples**\n\n### **Verify All Services Are Working**\n\n```javascript\nfunction verifyAccessibilityServices() {\n  console.group('🧪 Accessibility Services Verification');\n  \n  // Check services exist\n  console.assert(\n    window.accessibilityService,\n    '❌ AccessibilityService not found'\n  );\n  console.assert(\n    window.i18n,\n    '❌ I18nService not found'\n  );\n  \n  if (!window.accessibilityService || !window.i18n) {\n    console.groupEnd();\n    return false;\n  }\n  \n  // Check themes\n  const themes = window.accessibilityService.getThemes();\n  console.log(`✅ ${themes.length} themes available:`, themes);\n  \n  // Check fonts\n  const fonts = window.accessibilityService.getFonts();\n  console.log(`✅ ${fonts.length} fonts available:`, fonts);\n  \n  // Check languages\n  const languages = window.i18n.getSupportedLanguages();\n  console.log(`✅ ${languages.length} languages available:`, languages);\n  \n  // Check current settings\n  const prefs = window.accessibilityService.exportPreferences();\n  console.log('✅ Current preferences:', prefs);\n  \n  // Test theme change\n  window.accessibilityService.setTheme('dark');\n  const bgColor = getComputedStyle(document.documentElement)\n    .getPropertyValue('--bg-color');\n  console.log('✅ Theme change test - bg-color:', bgColor);\n  \n  // Test language change\n  window.i18n.setLanguage('es');\n  const title = window.i18n.t('nav.dashboard');\n  console.log('✅ Language change test - dashboard:', title);\n  \n  console.log('✅ All services working correctly!');\n  console.groupEnd();\n  \n  return true;\n}\n\n// Run verification\nverifyAccessibilityServices();\n```\n\n---\n\n## 🚀 **Production Deployment Example**\n\n### **Complete Setup in Main App**\n\n```jsx\n// App.jsx - Complete setup\nimport React, { useEffect } from 'react';\nimport AccessibilityPanel from './components/AccessibilityPanel';\nimport AccessibilityService from './services/accessibility.service';\nimport I18nService from './services/i18n.service';\n\nfunction App() {\n  useEffect(() => {\n    // Services auto-initialize\n    \n    // Verify they're ready\n    console.log('✅ Accessibility services initialized');\n    console.log('Current language:', window.i18n.getCurrentLanguage());\n    \n    // Apply saved preferences\n    const savedTheme = localStorage.getItem('a11y-theme');\n    if (savedTheme) {\n      window.accessibilityService.setTheme(savedTheme);\n    }\n    \n    // Listen for language changes for RTL support\n    const handleLanguageChange = (event) => {\n      const lang = event.detail.language;\n      document.documentElement.dir = lang === 'ar' ? 'rtl' : 'ltr';\n      document.documentElement.lang = lang;\n    };\n    \n    window.addEventListener('languageChanged', handleLanguageChange);\n    \n    return () => {\n      window.removeEventListener('languageChanged', handleLanguageChange);\n    };\n  }, []);\n  \n  return (\n    <div className=\"app\">\n      {/* Main app content */}\n      <Header />\n      <Navigation />\n      <MainContent />\n      <Footer />\n      \n      {/* Add accessibility panel - always available */}\n      <AccessibilityPanel />\n    </div>\n  );\n}\n\nexport default App;\n```\n\n---\n\n**These examples cover all major use cases. Customize as needed for your specific application!**\n