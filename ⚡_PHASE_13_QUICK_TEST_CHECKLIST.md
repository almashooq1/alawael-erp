# 🧪 قائمة الاختبار السريعة - Phase 13

## Quick Testing Checklist

---

## ✅ اختبار API بـ cURL

### 1. اختبار البرامج:

```bash
# جلب جميع البرامج
curl http://localhost:5000/api/programs

# جلب برنامج واحد
curl http://localhost:5000/api/programs/{programId}

# إنشاء برنامج جديد
curl -X POST http://localhost:5000/api/programs \
  -H "Content-Type: application/json" \
  -d '{
    "name": "برنامج اختبار",
    "code": "TEST-001",
    "disabilityType": "MOTOR"
  }'

# جلب برامج حسب النوع
curl http://localhost:5000/api/programs/by-disability/MOTOR

# جلب إحصائيات برنامج
curl http://localhost:5000/api/programs/{programId}/statistics
```

### 2. اختبار الجلسات:

```bash
# جلب جميع الجلسات
curl http://localhost:5000/api/sessions

# إنشاء جلسة جديدة
curl -X POST http://localhost:5000/api/sessions \
  -H "Content-Type: application/json" \
  -d '{
    "beneficiaryId": "userId",
    "programId": "programId",
    "specialistId": "specialistId",
    "title": "جلسة اختبار",
    "scheduledDateTime": "2026-01-25T10:00:00Z",
    "scheduledDuration": 60
  }'

# بدء جلسة
curl -X POST http://localhost:5000/api/sessions/{sessionId}/start

# إكمال جلسة
curl -X POST http://localhost:5000/api/sessions/{sessionId}/complete \
  -H "Content-Type: application/json" \
  -d '{
    "beneficiaryAttendance": {
      "status": "present",
      "remarks": "حضر بنجاح"
    }
  }'

# جلب تقرير الجلسة
curl http://localhost:5000/api/sessions/{sessionId}/report
```

### 3. اختبار الجدولة الذكية:

```bash
# إنشاء جدولة ذكية
curl -X POST http://localhost:5000/api/scheduler/create-schedule \
  -H "Content-Type: application/json" \
  -d '{
    "beneficiaryId": "userId",
    "programId": "programId",
    "frequency": "weekly",
    "sessionsPerWeek": 2,
    "planDuration": 90
  }'

# توليد المقترحات
curl -X POST http://localhost:5000/api/scheduler/{schedulerId}/generate-suggestions

# الموافقة على الجدولة
curl -X POST http://localhost:5000/api/scheduler/{schedulerId}/approve-schedule \
  -H "Content-Type: application/json" \
  -d '{
    "approvedBy": "specialist"
  }'

# تفعيل الجدولة
curl -X POST http://localhost:5000/api/scheduler/{schedulerId}/activate-schedule

# جلب التحليلات
curl http://localhost:5000/api/scheduler/{schedulerId}/analytics
```

---

## 🖥️ اختبار الواجهة الرسومية

### 1. اختبار صفحة البرامج:

```
الخطوات:
1. افتح http://localhost:3000/programs
2. تحقق من ظهور قائمة البرامج
3. جرب فلترة حسب نوع الإعاقة
4. انقر "إضافة برنامج جديد"
5. ملأ النموذج وانقر إنشاء
6. تحقق من ظهور البرنامج الجديد

النتيجة المتوقعة: ✅ جميع العمليات تعمل بنجاح
```

### 2. اختبار صفحة الجلسات:

```
الخطوات:
1. افتح http://localhost:3000/sessions
2. تحقق من ظهور قائمة الجلسات
3. جرب فلترة حسب الحالة
4. انقر "إضافة جلسة جديدة"
5. ملأ النموذج وانقر إنشاء
6. انقر على جلسة لعرض التفاصيل
7. جرب بدء الجلسة
8. جرب إكمال الجلسة

النتيجة المتوقعة: ✅ جميع العمليات تعمل بنجاح
```

### 3. اختبار صفحة الجدولة الذكية:

```
الخطوات:
1. افتح http://localhost:3000/scheduler
2. ملأ بيانات الإعدادات
3. انقر "توليد المقترحات"
4. تحقق من ظهور المقترحات
5. انقر "الموافقة" على مقترح
6. انقر "تفعيل الجدولة"
7. عرض التحليلات

النتيجة المتوقعة: ✅ جميع العمليات تعمل بنجاح
```

---

## 📋 قائمة الفحص الشاملة

### الجودة الأساسية:

- [ ] لا توجد أخطاء JavaScript في Console
- [ ] لا توجد تحذيرات في Console
- [ ] جميع الصور تحمل بشكل صحيح
- [ ] الخطوط تعرض بشكل صحيح
- [ ] لا توجد أخطاء في الشبكة

### الأداء:

- [ ] صفحة البرامج تحمل في < 1 ثانية
- [ ] صفحة الجلسات تحمل في < 1 ثانية
- [ ] صفحة الجدولة تحمل في < 2 ثانية
- [ ] الاستجابة للتفاعل فورية

### الاستجابة (Responsiveness):

- [ ] يعمل بشكل صحيح على الهاتف المحمول
- [ ] يعمل بشكل صحيح على التابلت
- [ ] يعمل بشكل صحيح على الحاسوب
- [ ] الأزرار قابلة للضغط بسهولة

### الوظائف:

- [ ] CRUD العمليات تعمل
- [ ] الفلترة تعمل
- [ ] الفرز يعمل
- [ ] البحث يعمل
- [ ] التنقل يعمل

### الأمان:

- [ ] الرموز (Tokens) تُرسل بشكل آمن
- [ ] البيانات الحساسة محمية
- [ ] لا توجد ثغرات XSS واضحة
- [ ] منع حقن SQL فعال

---

## 🔍 اختبارات الحالات الحدية (Edge Cases)

### 1. الحقول الفارغة:

```
✓ محاولة إنشاء برنامج بدون اسم → يجب عرض خطأ
✓ محاولة إنشاء جلسة بدون تاريخ → يجب عرض خطأ
✓ محاولة إنشاء جدولة بدون مستفيد → يجب عرض خطأ
```

### 2. البيانات الكبيرة:

```
✓ قائمة بـ 1000+ برنامج → يجب التحميل بسرعة
✓ قائمة بـ 10000+ جلسة → يجب التحميل بسرعة
✓ تقرير بـ 100 صفحة → يجب التحميل بسرعة
```

### 3. الأخطاء:

```
✓ الاتصال ينقطع → يجب عرض رسالة خطأ
✓ انتهاء الجلسة → يجب عرض رسالة خطأ
✓ بيانات غير صحيحة → يجب عرض رسالة خطأ
```

---

## 📊 المقاييس التي يجب قياسها

### وقت التحميل (Load Time):

```
صفحة البرامج:      < 1000ms   ✓
صفحة الجلسات:      < 1000ms   ✓
صفحة الجدولة:      < 2000ms   ✓
```

### الاستجابة (Response Time):

```
جلب البرامج:       < 200ms    ✓
إنشاء برنامج:      < 100ms    ✓
بدء جلسة:          < 100ms    ✓
```

### استخدام الذاكرة:

```
الصفحة الواحدة:     < 50MB     ✓
التطبيق الكامل:    < 150MB    ✓
```

---

## 🎯 سيناريوهات الاختبار الشاملة

### السيناريو 1: إنشاء برنامج كامل

```
1. انتقل إلى صفحة البرامج
2. انقر "إضافة برنامج جديد"
3. ملأ جميع الحقول:
   - الاسم: "برنامج علاج شامل"
   - النوع: "MOTOR"
   - المدة: 60
   - التكرار: 2 مرات أسبوعياً
4. انقر "إنشاء"
5. تحقق من ظهور البرنامج

النتيجة: ✅ البرنامج ينشأ بنجاح
```

### السيناريو 2: جدولة كاملة للجلسات

```
1. انتقل إلى صفحة الجدولة
2. اختر برنامج ومستفيد
3. اختر التكرار والمدة
4. انقر "توليد المقترحات"
5. اختر مقترح وانقر "الموافقة"
6. انقر "تفعيل الجدولة"
7. تحقق من ظهور الجلسات المجدولة

النتيجة: ✅ جدولة ذكية كاملة
```

### السيناريو 3: إجراء جلسة وتسجيل الأداء

```
1. انتقل إلى صفحة الجلسات
2. اختر جلسة "مجدولة"
3. انقر "بدء الجلسة"
4. سجّل الحضور
5. سجّل الأنشطة المنفذة
6. أدخل تقييم الأداء
7. انقر "إكمال الجلسة"
8. عرض التقرير

النتيجة: ✅ جلسة كاملة مسجلة
```

---

## 🐛 اختبار الأخطاء

### أخطاء متوقعة:

```
1. خطأ 404 - المورد غير موجود
   الحل: تحقق من المعرف

2. خطأ 401 - غير مخول
   الحل: تحقق من الرمز

3. خطأ 500 - خطأ الخادم
   الحل: افحص سجلات الخادم

4. خطأ CORS
   الحل: تحقق من إعدادات CORS
```

---

## 📋 قالب تقرير الاختبار

```markdown
# تقرير الاختبار - Phase 13

التاريخ: ********\_\_\_\_******** المختبر: ********\_\_\_\_********

## 1. البرامج المتخصصة

- [ ] الإنشاء يعمل
- [ ] التحديث يعمل
- [ ] الحذف يعمل
- [ ] الفلترة تعمل
- [ ] الإحصائيات تعمل

## 2. الجلسات المتقدمة

- [ ] الإنشاء يعمل
- [ ] التعديل يعمل
- [ ] البدء يعمل
- [ ] الإكمال يعمل
- [ ] التقرير يعمل

## 3. الجدولة الذكية

- [ ] الإنشاء يعمل
- [ ] المقترحات تعمل
- [ ] الموافقة تعمل
- [ ] التفعيل يعمل
- [ ] التحليلات تعمل

## 4. الأداء

- [ ] سرعة القائمة مقبولة
- [ ] سرعة الإنشاء مقبولة
- [ ] سرعة التحديث مقبولة

## 5. الأمان

- [ ] المصادقة تعمل
- [ ] الصلاحيات تعمل
- [ ] البيانات محمية

## ملاحظات إضافية:

---

## التوصيات:

---

النتيجة النهائية: ☐ نجح ☐ فشل جزئي ☐ فشل كامل
```

---

## ✨ نصائح الاختبار

### 1. اختبر بتركيز:

```
✓ فهم ما تختبره
✓ اتبع الخطوات بدقة
✓ سجّل النتائج
✓ أبلغ عن المشاكل
```

### 2. استخدم أدوات الفحص:

```
✓ DevTools (لفحص الـ DOM والشبكة)
✓ Postman (لاختبار API)
✓ Lighthouse (لاختبار الأداء)
✓ WAVE (لاختبار الوصولية)
```

### 3. اختبر على أجهزة مختلفة:

```
✓ هاتف ذكي
✓ تابلت
✓ حاسوب مكتبي
✓ متصفحات مختلفة
```

---

## 📞 الإبلاغ عن الأخطاء

### عند العثور على خطأ:

1. اوثق الخطأ:

   ```
   - الخطوات المؤدية للخطأ
   - رسالة الخطأ كاملة
   - لقطة شاشة
   - معلومات النظام
   ```

2. أبلغ عنه:

   ```
   - البريد: bugs@alawael.com
   - الهاتف: +966-XXXX-XXXX
   - الموقع: bugs.alawael.com
   ```

3. انتظر الرد:
   ```
   - بناء الجسر عادة ضمن 24 ساعة
   - التصحيح ضمن 48 ساعة
   - الإطلاق ضمن أسبوع
   ```

---

**آخر تحديث: 22 يناير 2026** **الإصدار: 1.0.0** **الحالة: جاهز للاختبار ✅**
