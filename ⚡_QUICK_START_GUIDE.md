# 🚀 دليل التشغيل السريع - نظام إدارة مراكز التأهيل

## ⚡ البدء السريع (5 دقائق)

### 1. تجهيز Backend

```bash
# انتقل إلى مجلد Backend
cd backend

# أنشئ بيئة افتراضية
python -m venv venv

# فعّل البيئة الافتراضية
# على Windows:
venv\Scripts\activate
# على Linux/Mac:
source venv/bin/activate

# ثبّت المتطلبات
pip install -r requirements.txt

# شغّل الخادم
python app.py
# سيبدأ على: http://localhost:5000
```

### 2. تجهيز Frontend

```bash
# افتح نافذة Terminal جديدة
cd frontend

# ثبّت المكتبات
npm install

# شغّل تطبيق React
npm start
# سيفتح تلقائياً على: http://localhost:3000
```

---

## 📋 المسار السريع للعمل

### الخطوة 1: التسجيل

1. اذهب إلى `http://localhost:3000`
2. اضغط "تسجيل حساب جديد"
3. أدخل البيانات المطلوبة
4. اضغط "تسجيل"

### الخطوة 2: تسجيل الدخول

1. أدخل البريد الإلكتروني والكلمة المرورية
2. اضغط "دخول"

### الخطوة 3: إضافة مستفيد

1. من القائمة الجانبية، اختر "المستفيدون"
2. اضغط "مستفيد جديد"
3. ملء البيانات في الخطوات الثلاث
4. اضغط "إنشاء"

### الخطوة 4: إضافة جلسة

1. من القائمة، اختر "الجلسات"
2. اضغط "جلسة جديدة"
3. اختر المستفيد والمعالج
4. حدد التاريخ والوقت
5. اضغط "إنشاء"

---

## 🔗 الـ URLs الأساسية

| الصفحة         | URL                                   |
| -------------- | ------------------------------------- |
| تسجيل الدخول   | `http://localhost:3000/login`         |
| لوحة المعلومات | `http://localhost:3000/dashboard`     |
| المستفيدون     | `http://localhost:3000/beneficiaries` |
| التقارير       | `http://localhost:3000/reports`       |
| الجلسات        | `http://localhost:3000/sessions`      |
| التقييمات      | `http://localhost:3000/assessments`   |
| البرامج        | `http://localhost:3000/programs`      |
| الأهداف        | `http://localhost:3000/goals`         |

---

## 🔐 بيانات الاختبار (Test Data)

### حساب اختبار

- **البريد الإلكتروني:** admin@rehab.com
- **الكلمة المرورية:** Admin@123456

### أو أنشئ حسابك الخاص

- اختر "تسجيل حساب جديد" من صفحة Login
- أدخل البيانات والبريد الإلكتروني
- ستحصل على الوصول الفوري

---

## ⚙️ متطلبات التشغيل

### البرامج المطلوبة

- **Python 3.8+** - [Download](https://www.python.org/downloads/)
- **Node.js 14+** - [Download](https://nodejs.org/)
- **PostgreSQL 12+** (اختياري) - [Download](https://www.postgresql.org/download/)

### للـ Database (اختياري)

إذا لم تكن تريد استخدام PostgreSQL:

```bash
# يمكن استخدام SQLite بدلاً منها
# تعديل في backend/config.py
SQLALCHEMY_DATABASE_URI = 'sqlite:///rehabilitation.db'
```

---

## 🐛 استكشاف الأخطاء

### الخطأ: "Port 5000 مشغول"

```bash
# على Windows:
netstat -ano | findstr :5000
taskkill /PID [PID] /F

# على Linux/Mac:
lsof -ti:5000 | xargs kill -9
```

### الخطأ: "Module not found"

```bash
# في backend:
pip install -r requirements.txt --upgrade

# في frontend:
npm install --legacy-peer-deps
```

### الخطأ: "Connection refused"

```bash
# تأكد أن Backend يعمل:
http://localhost:5000/api/health

# إذا لم يستجب، أعد تشغيل Backend
```

### الخطأ: الـ Login لا يعمل

```bash
# تأكد من أن قاعدة البيانات تحتوي على مستخدمين
# أو أنشئ حساب جديد من صفحة التسجيل
```

---

## 📚 الملفات الأساسية

### Backend

```
backend/
├── app.py               # البرنامج الرئيسي
├── config.py            # الإعدادات
├── requirements.txt     # المكتبات المطلوبة
├── models/              # نماذج قاعدة البيانات
└── routes/              # المسارات والـ APIs
```

### Frontend

```
frontend/
├── src/
│   ├── App.jsx          # البرنامج الرئيسي
│   ├── pages/           # الصفحات
│   ├── store/           # Redux Store
│   ├── services/        # خدمات API
│   ├── layouts/         # التخطيطات
│   └── routes/          # المسارات
├── package.json         # المكتبات
└── .env.local          # الإعدادات المحلية
```

---

## 🎯 سيناريو الاختبار الكامل

### 1. إضافة مستفيد

```
المستفيدون → مستفيد جديد → ملء البيانات → إنشاء
✅ يجب أن يظهر في القائمة
```

### 2. إضافة جلسة

```
الجلسات → جلسة جديدة → اختيار المستفيد → تحديد الوقت → إنشاء
✅ يجب أن تظهر في القائمة بحالة "مجدولة"
```

### 3. إكمال جلسة

```
الجلسات → اختيار جلسة → إكمال الجلسة → إضافة ملاحظات → تأكيد
✅ يجب أن تتغير حالتها إلى "مكتملة"
```

### 4. إنشاء تقرير

```
التقارير → تقرير جديد → اختيار نوع التقرير → اختيار مستفيد → ملء البيانات → إنشاء
✅ يجب أن يظهر التقرير في قائمة التقارير
```

### 5. إضافة تقييم

```
التقييمات → تقييم جديد → اختيار نوع التقييم → إدخال النتائج → حفظ
✅ يجب أن يظهر التقييم مع الدرجة
```

---

## 🔧 تخصيص الإعدادات

### تغيير الـ Port

```python
# في backend/app.py
if __name__ == '__main__':
    app.run(debug=True, port=8000)  # تغيير 5000 إلى 8000
```

### تغيير Database

```python
# في backend/config.py
class Config:
    SQLALCHEMY_DATABASE_URI = 'postgresql://user:password@localhost/rehab'
```

### تغيير الـ Theme

```javascript
// في frontend/src/App.jsx
const theme = createTheme({
  palette: {
    primary: { main: '#your-color' },
  },
});
```

---

## 📊 الإحصائيات المتوقعة

بعد الاختبار الكامل، يجب أن تكون لديك:

- ✅ 3-5 مستفيدين
- ✅ 5-10 جلسات
- ✅ 2-3 تقارير
- ✅ 3-5 تقييمات
- ✅ 1-2 برامج
- ✅ 3-5 أهداف

---

## 🌐 ربط الـ API

### في حالة تشغيل Backend على منفذ مختلف

```javascript
// في frontend/src/services/api.js
const API_BASE_URL = 'http://localhost:8000/api';
```

### في حالة تشغيل على Server مختلف

```javascript
const API_BASE_URL = 'http://your-server.com/api';
```

---

## 💾 نسخ احتياطية

### قاعدة البيانات

```bash
# PostgreSQL
pg_dump -U username -d rehab > backup.sql

# SQLite
cp rehabilitation.db backup.db
```

### استعادة النسخة

```bash
# PostgreSQL
psql -U username -d rehab < backup.sql

# SQLite
cp backup.db rehabilitation.db
```

---

## ✨ نصائح مهمة

1. **احفظ كلمة المرور** - لا توجد طريقة لاستعادة النسخة الاختبارية
2. **تفعيل Notifications** - حتى تتمكن من متابعة التحديثات
3. **استخدم البحث** - للعثور على البيانات بسرعة
4. **قم بالـ Export** - احفظ التقارير بصيغة PDF
5. **تتبع الإحصائيات** - استخدم لوحة المعلومات للمراقبة

---

## 🎓 الخطوات التالية

بعد التأكد من عمل كل شيء:

1. ✅ اختبر جميع الصفحات
2. ✅ أضف بيانات اختبارية
3. ✅ جرّب جميع العمليات
4. ✅ تحقق من الأخطاء
5. ✅ احفظ بيانات النسخة الاختبارية

---

## 📞 الدعم الفني

### في حالة المشاكل:

1. **تحقق من الـ Console** (F12 في المتصفح)
2. **اقرأ الخطأ بعناية**
3. **ابحث عن الحل في الأعلى**
4. **أعد تشغيل البرنامج**

---

## 🎉 اللتاليين!

**النظام جاهز للعمل!** 🚀

استمتع باستخدام نظام إدارة مراكز التأهيل!

---

_آخر تحديث: الآن | إصدار: 1.0-Quick Start_
