# ⚡ إعداد SQLite للاختبار السريع (بدون تثبيت)

## ميزات SQLite:

- ✅ مدمج في Python (لا يحتاج تثبيت)
- ✅ سريع للتطوير والاختبار
- ✅ ملف واحد فقط
- ⚠️ لا يُنصح به للإنتاج الثقيل

---

## الإعداد (30 ثانية):

### 1. حدّث .env.production:

```env
# SQLite (للاختبار السريع)
DATABASE_URL=sqlite:///./alawael_erp.db

# أو مع مسار كامل:
# DATABASE_URL=sqlite:///C:/Users/x-be/OneDrive/المستندات/04-10-2025/66666/backend/alawael_erp.db
```

### 2. لا يوجد خطوة 2! جاهز! 🎉

---

## شغّل التطبيق:

```powershell
# سيتم إنشاء الملف تلقائياً
.\deploy.ps1 -Environment production

# أو اختبار سريع:
cd backend
npm run dev
```

---

## الملف سيُنشأ في:

```
backend/alawael_erp.db
```

---

## ⚠️ ملاحظة:

SQLite مناسب للاختبار فقط. للإنتاج استخدم:

- MongoDB Atlas (موصى به)
- PostgreSQL
- MySQL

---

## التحويل للإنتاج لاحقاً:

فقط غيّر `DATABASE_URL` في `.env.production` وشغّل:

```powershell
npm run migrate
```

---

**التوصية:** استخدم SQLite الآن للاختبار، ثم انتقل لـ MongoDB Atlas للإنتاج.
