# ⚡ ابدأ الاختبار الآن

## ✅ النظام يعمل!

### 🌐 روابط الاختبار

1. **Frontend (الواجهة الأمامية)**
   ```
   http://localhost:3000
   ```
2. **Backend Health Check**

   ```
   http://localhost:5000/api/health
   ```

3. **Backend API**
   ```
   http://localhost:5000/api
   ```

---

## 🔐 بيانات تسجيل الدخول للاختبار

```
البريد الإلكتروني: admin@example.com
كلمة المرور: Admin@123
```

---

## 📝 خطوات الاختبار

### الخطوة 1: افتح المتصفح

اذهب إلى: **http://localhost:3000**

### الخطوة 2: سجل الدخول

- استخدم البيانات أعلاه
- اضغط "تسجيل دخول"

### الخطوة 3: اختبر الصفحات

- Dashboard
- إدارة المستخدمين
- التقارير
- الإعدادات

---

## 🔍 التحقق من Backend

افتح PowerShell جديد واكتب:

```powershell
# اختبار Health Endpoint
Invoke-WebRequest http://localhost:5000/api/health

# اختبار Login API مباشرة
$body = @{
    email = "admin@example.com"
    password = "Admin@123"
} | ConvertTo-Json

Invoke-RestMethod -Uri "http://localhost:5000/api/auth/login" `
    -Method POST `
    -Body $body `
    -ContentType "application/json"
```

---

## ⚠️ في حالة المشاكل

### Backend لا يستجيب؟

```powershell
# أوقف العمليات القديمة
Get-Process -Name node | Stop-Process -Force

# أعد تشغيل Backend
cd backend
npm start
```

### Frontend لا يفتح؟

```powershell
# أوقف العمليات القديمة
Get-Process -Name node | Stop-Process -Force

# أعد تشغيل Frontend
cd frontend
npm start
```

### خطأ في قاعدة البيانات؟

Backend مضبوط على **SQLite** - لا حاجة لـ MongoDB!

الملف موجود في: `backend/alawael_erp.db`

---

## 🚀 نصائح مهمة

1. **النوافذ المنفصلة**
   - Backend في نافذة PowerShell منفصلة
   - Frontend في نافذة PowerShell منفصلة
   - **لا تغلقهم!** دعهم يعملون

2. **وقت التحميل**
   - Backend يحتاج ~10 ثوانٍ للتشغيل
   - Frontend يحتاج ~30 ثانية للتحميل الأول

3. **المتصفح**
   - سيفتح تلقائياً على http://localhost:3000
   - إذا لم يفتح، افتحه يدوياً

---

## 📊 حالة النظام

| المكون   | الحالة    | المنفذ |
| -------- | --------- | ------ |
| Backend  | ✅ يعمل   | 5000   |
| Frontend | ✅ يعمل   | 3000   |
| Database | ✅ SQLite | -      |
| Auth     | ✅ جاهز   | -      |

---

## 🎯 ماذا بعد؟

بعد اختبار النظام محلياً:

1. ✅ تأكد أن كل الصفحات تعمل
2. ✅ جرب تسجيل الدخول/الخروج
3. ✅ اختبر العمليات الأساسية
4. 🚀 استعد للنشر على الإنتاج!

---

## 📞 دعم سريع

إذا واجهت أي مشكلة، تحقق من:

- ✅ النوافذ المنفصلة للـ Backend والـ Frontend
- ✅ لا توجد أخطاء في نوافذ PowerShell
- ✅ المنافذ 3000 و 5000 ليست مشغولة
- ✅ ملف .env موجود في backend/

---

**🎉 استمتع بالاختبار!**
