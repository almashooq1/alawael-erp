# 📚 نظام الإمداد والمساندة بين الفروع - دليل شامل
# Supply & Support System Between Branches - Complete Guide

**الإصدار:** 4.0.0  
**التاريخ:** 22 يناير 2026  
**الحالة:** ✅ جاهز للإنتاج

---

## 📖 جدول المحتويات

1. [نظرة عامة](#نظرة-عامة)
2. [البنية المعمارية](#البنية-المعمارية)
3. [الفروع والمخزون](#الفروع-والمخزون)
4. [نظام الطلبات](#نظام-الطلبات)
5. [إدارة التحويلات](#إدارة-التحويلات)
6. [نظام تذاكر الدعم](#نظام-تذاكر-الدعم)
7. [التحليلات والتقارير](#التحليلات-والتقارير)
8. [API الشامل](#api-الشامل)
9. [أمثلة عملية](#أمثلة-عملية)
10. [الخطأ والاستكشاف](#الخطأ-والاستكشاف)

---

## نظرة عامة

### الهدف الرئيسي
تطوير نظام متقدم لإدارة الإمدادات والمساندة بين فروع المؤسسة، يتيح:
- نقل الموارد بين الفروع بكفاءة
- تتبع الشحنات في الوقت الفعلي
- إدارة تذاكر الدعم والمشاكل
- تحليل المخزون والتنبؤ بالاحتياجات

### المميزات الرئيسية

#### 1. 🏢 إدارة الفروع
```
✓ 4 فروع رئيسية (الرياض، الدمام، جدة، أبها)
✓ معلومات كاملة لكل فرع (الموقع، المدير، الاتصال)
✓ سعة التخزين لكل فرع
✓ تتبع الأداء والمقاييس
```

#### 2. 📦 نظام الطلبات الذكي
```
✓ إنشاء طلبات بين الفروع
✓ ثلاث مستويات أولوية (عادي، عاجل، طوارئ)
✓ موافقة تلقائية للطلبات الموثوقة
✓ رفض الطلبات ذات المخزون الناقص
```

#### 3. 🚚 إدارة التحويلات
```
✓ تحويل تلقائي للطلبات المعتمدة
✓ أرقام تتبع فريدة (TRK-XXXXXX)
✓ ثلاث حالات: معلق → جاهز للشحن → مُسلّم
✓ تحديث المخزون التلقائي عند الاستلام
```

#### 4. 🎫 نظام تذاكر الدعم
```
✓ تسجيل المشاكل والطلبات
✓ 4 فئات: تقني، إمدادات، معدات، أخرى
✓ 4 مستويات أولوية: منخفض، عادي، عالي، حرج
✓ نظام التعليقات والحل
```

#### 5. 📊 التحليلات المتقدمة
```
✓ تنبؤ بالمخزون الناقص
✓ تحديد الفائض والمخزون
✓ توصيات إعادة الطلب
✓ مقاييس الأداء (معدل التسليم، الدقة، وقت الاستجابة)
```

---

## البنية المعمارية

### الطبقات الثلاث

```
┌─────────────────────────────────────┐
│     Frontend / API Clients          │
└─────────────────────────────────────┘
           ↓↑
┌─────────────────────────────────────┐
│   Express.js REST API Layer         │
│  (supply_support_routes.js)         │
└─────────────────────────────────────┘
           ↓↑
┌─────────────────────────────────────┐
│   Core Business Logic Layer         │
│ (SupplySupportSystem class)         │
└─────────────────────────────────────┘
           ↓↑
┌─────────────────────────────────────┐
│   In-Memory Data Management         │
│  (Maps for branches, requests, etc) │
└─────────────────────────────────────┘
```

### مكونات النظام

```javascript
SupplySupportSystem
├── Branch Management (إدارة الفروع)
├── Supply Request System (نظام الطلبات)
├── Transfer Management (إدارة التحويلات)
├── Support Ticket System (نظام التذاكر)
├── Inventory Analytics (تحليل المخزون)
├── Performance Metrics (مقاييس الأداء)
└── Reporting Engine (محرك التقارير)
```

---

## الفروع والمخزون

### الفروع الأربعة

```javascript
BR001 - الفرع الرئيسي
  الموقع: الرياض
  المدير: أحمد الراشد
  السعة: 500 وحدة
  الاتصال: +966-11-XXXX

BR002 - الفرع الشمالي
  الموقع: الدمام
  المدير: فاطمة الزهراء
  السعة: 400 وحدة
  الاتصال: +966-13-XXXX

BR003 - الفرع الغربي
  الموقع: جدة
  المدير: محمد العتيبي
  السعة: 450 وحدة
  الاتصال: +966-12-XXXX

BR004 - الفرع الجنوبي
  الموقع: أبها
  المدير: نور العجمي
  السعة: 350 وحدة
  الاتصال: +966-17-XXXX
```

### فئات المخزون

#### 1️⃣ المجهزات الطبية (Medical Supplies)
```
- الضمادات (Bandages): 500 وحدة
- حقن (Syringes): 1000 وحدة
- قفازات (Gloves): 800 وحدة
- أقنعة (Masks): 2000 وحدة
```

#### 2️⃣ المعدات (Equipment)
```
- كراسي متحركة (Wheelchairs): 15 وحدة
- عكازات (Crutches): 50 وحدة
- مشايات (Walkers): 30 وحدة
```

#### 3️⃣ الأدوية (Medications)
```
- مضادات حيوية (Antibiotics): 1000 وحدة
- مسكنات (Painkillers): 1500 وحدة
- فيتامينات (Vitamins): 800 وحدة
```

#### 4️⃣ مستلزمات المكتب (Office Supplies)
```
- أوراق (Paper): 200 وحدة
- أقلام (Pens): 500 وحدة
- ملفات (Folders): 300 وحدة
```

### عرض المخزون

```bash
GET /api/supply/branches/BR001

النتيجة:
{
  "id": "BR001",
  "name": "الفرع الرئيسي",
  "location": "الرياض",
  "inventory": {
    "medical_supplies": {
      "bandages": 500,
      "syringes": 1000,
      "gloves": 800,
      "masks": 2000
    },
    "equipment": {
      "wheelchairs": 15,
      "crutches": 50,
      "walkers": 30
    },
    "medications": {
      "antibiotics": 1000,
      "painkillers": 1500,
      "vitamins": 800
    },
    "office_supplies": {
      "paper": 200,
      "pens": 500,
      "folders": 300
    }
  },
  "total_items": 10500,
  "capacity": 500,
  "capacity_used": 85
}
```

---

## نظام الطلبات

### مستويات الأولوية

```
🟢 Normal (عادي)
   الوقت: 7 أيام
   الوصف: طلبات روتينية

🟡 Urgent (عاجل)
   الوقت: 3 أيام
   الوصف: طلبات تحتاج سرعة

🔴 Emergency (طوارئ)
   الوقت: 1 يوم
   الوصف: حالات حرجة
```

### دورة حياة الطلب

```
1. الإنشاء
   ↓
2. قيد الانتظار (Pending)
   ↓
3. الموافقة ✓ أو الرفض ✗
   ↓
4. إنشاء تحويل (Transfer)
   ↓
5. النقل والتسليم
```

### إنشاء طلب

```javascript
POST /api/supply/requests

البيانات المرسلة:
{
  "fromBranch": "BR001",    // الفرع المرسل
  "toBranch": "BR002",      // الفرع المستقبل
  "items": [                 // قائمة الأصناف
    {
      "item_name": "bandages",    // اسم الصنف
      "quantity": 100,            // الكمية
      "unit_price": 10            // السعر للوحدة
    },
    {
      "item_name": "gloves",
      "quantity": 500,
      "unit_price": 5
    }
  ],
  "priority": "normal"      // المستوى: normal/urgent/emergency
}

النتيجة:
{
  "id": "REQ-1",
  "status": "pending",
  "from_branch": "BR001",
  "to_branch": "BR002",
  "items": [...],
  "total_amount": 3500,
  "priority": "normal",
  "estimated_delivery": "2026-01-29",
  "created_at": "2026-01-22T10:30:00Z"
}
```

### الموافقة على الطلب

```javascript
POST /api/supply/requests/REQ-1/approve

النتيجة:
{
  "request_id": "REQ-1",
  "status": "approved",
  "transfer": {
    "id": "TRN-1",
    "status": "pending",
    "tracking_code": "TRK-XXXXXXXXXXXX"
  },
  "message": "تم الموافقة على الطلب وإنشاء تحويل"
}
```

---

## إدارة التحويلات

### دورة حياة التحويل

```
Pending (معلق)
  ↓ [تحضير الشحنة]
In Transit (جاهز للشحن)
  ↓ [النقل والتسليم]
Delivered (مُسلّم)
```

### تحديث حالة التحويل

```javascript
PUT /api/supply/transfers/TRN-1

// تحديث إلى "جاهز للشحن"
{
  "status": "in_transit",
  "notes": "الشحنة في الطريق الآن"
}

// تحديث إلى "مُسلّم"
{
  "status": "delivered",
  "notes": "تم التسليم بنجاح"
}

النتيجة:
{
  "transfer_id": "TRN-1",
  "status": "delivered",
  "from_branch": "BR001",
  "to_branch": "BR002",
  "items_count": 2,
  "total_amount": 3500,
  "tracking_code": "TRK-XXXXXXXXXXXX",
  "updated_at": "2026-01-24T15:00:00Z",
  "message": "تم التحديث بنجاح"
}
```

### تتبع التحويل

```javascript
GET /api/supply/branches/BR002/transfers

النتيجة:
{
  "branch_id": "BR002",
  "transfers": [
    {
      "id": "TRN-1",
      "tracking_code": "TRK-ABC123XYZ",
      "from_branch": "BR001",
      "status": "delivered",
      "delivery_date": "2026-01-24",
      "items_count": 2
    }
  ]
}
```

---

## نظام تذاكر الدعم

### فئات التذاكر

```
🔧 Technical (تقني)
   أمثلة: عطل في النظام، مشاكل في البرامج

📦 Supply (إمدادات)
   أمثلة: نقص المخزون، تأخر الشحنة

🏥 Equipment (معدات)
   أمثلة: معدات معطلة، تحتاج إلى صيانة

❓ Other (أخرى)
   أمثلة: استفسارات عامة، اقتراحات
```

### مستويات الأولوية

```
🟢 Low (منخفض) - وقت الحل: أسبوع
🟡 Normal (عادي) - وقت الحل: 3 أيام
🟠 High (عالي) - وقت الحل: يوم واحد
🔴 Critical (حرج) - وقت الحل: ساعات
```

### إنشاء تذكرة

```javascript
POST /api/supply/tickets

{
  "fromBranch": "BR002",
  "category": "supply",
  "description": "نفاد المجهزات الطبية - نحتاج إمدادات عاجلة",
  "priority": "urgent"
}

النتيجة:
{
  "id": "TKT-1",
  "status": "open",
  "from_branch": "BR002",
  "category": "supply",
  "priority": "urgent",
  "created_at": "2026-01-22T10:30:00Z"
}
```

### إضافة تعليق

```javascript
POST /api/supply/tickets/TKT-1/comments

{
  "author": "Ahmed Al-Rashid",
  "comment": "نحتاج إلى 500 وحدة من الضمادات والقفازات"
}

النتيجة:
{
  "comment_id": "COMM-1",
  "ticket_id": "TKT-1",
  "author": "Ahmed Al-Rashid",
  "comment": "نحتاج إلى 500 وحدة من الضمادات والقفازات",
  "added_at": "2026-01-22T10:35:00Z"
}
```

### حل التذكرة

```javascript
POST /api/supply/tickets/TKT-1/resolve

{
  "resolution": "تم توفير 600 وحدة من الفرع الرئيسي والشحنة في الطريق"
}

النتيجة:
{
  "ticket_id": "TKT-1",
  "status": "resolved",
  "resolution": "تم توفير 600 وحدة من الفرع الرئيسي",
  "resolved_at": "2026-01-22T11:00:00Z"
}
```

---

## التحليلات والتقارير

### مؤشرات الأداء (KPIs)

```javascript
GET /api/supply/branches/BR001/metrics

النتيجة:
{
  "branch_id": "BR001",
  "metrics": {
    "pending_requests": 3,
    "total_transfers": 12,
    "delivery_rate": "95.0%",        // معدل التسليم
    "order_accuracy": "98.0%",       // دقة الطلبات
    "avg_response_time": "4.5 hours", // وقت الاستجابة
    "satisfaction_rating": 4.7        // رضا العملاء
  }
}
```

### التنبؤ والتوصيات

```javascript
GET /api/supply/branches/BR001/predictions

النتيجة:
{
  "branch_id": "BR001",
  "low_stock_items": [
    {
      "item": "bandages",
      "current_quantity": 50,
      "recommended_quantity": 500,
      "action": "طلب فوري من الفرع الرئيسي"
    }
  ],
  "surplus_items": [
    {
      "item": "masks",
      "quantity": 3000,
      "recommendation": "نقل الفائض إلى الفروع الأخرى"
    }
  ],
  "reorder_items": [
    {
      "item": "gloves",
      "recommended_quantity": 1000,
      "estimated_cost": 5000
    }
  ]
}
```

### التقرير الشامل للفرع

```javascript
GET /api/supply/branches/BR001/report

النتيجة:
{
  "branch_id": "BR001",
  "branch_name": "الفرع الرئيسي",
  "summary": {
    "total_inventory_value": 525000,
    "items_in_stock": 10500,
    "pending_requests": 3,
    "active_transfers": 5,
    "open_tickets": 2
  },
  "performance": {
    "delivery_rate": "95.0%",
    "order_accuracy": "98.0%",
    "avg_response_time": "4.5 hours",
    "satisfaction_rating": 4.7
  },
  "recent_activity": [
    {
      "type": "transfer",
      "reference": "TRN-1",
      "status": "delivered",
      "date": "2026-01-21"
    }
  ]
}
```

---

## API الشامل

### نقاط النهاية (Endpoints)

#### 🏥 نقاط الصحة والحالة
```
GET /api/supply/health                 - فحص صحة النظام
GET /api/supply/system-status          - حالة النظام الكاملة
```

#### 🏢 إدارة الفروع
```
GET /api/supply/branches               - جميع الفروع
GET /api/supply/branches/:branchId     - فرع محدد
GET /api/supply/branches/:branchId/metrics    - مقاييس الأداء
GET /api/supply/branches/:branchId/report     - التقرير الشامل
GET /api/supply/branches/:branchId/transfers  - سجل التحويلات
GET /api/supply/branches/:branchId/predictions - التنبؤات
```

#### 📦 إدارة الطلبات
```
POST /api/supply/requests              - إنشاء طلب جديد
POST /api/supply/requests/:requestId/approve  - الموافقة
```

#### 🚚 إدارة التحويلات
```
POST /api/supply/transfers             - إنشاء تحويل
PUT /api/supply/transfers/:transferId  - تحديث التحويل
```

#### 🎫 نظام التذاكر
```
POST /api/supply/tickets               - إنشاء تذكرة
POST /api/supply/tickets/:ticketId/comments   - إضافة تعليق
POST /api/supply/tickets/:ticketId/resolve    - حل التذكرة
```

---

## أمثلة عملية

### مثال 1: سير عمل كامل للطلب

```bash
# 1. إنشاء طلب من BR001 إلى BR002
curl -X POST http://localhost:3001/api/supply/requests \
  -H "Content-Type: application/json" \
  -d '{
    "fromBranch": "BR001",
    "toBranch": "BR002",
    "items": [
      {"item_name": "bandages", "quantity": 100, "unit_price": 10}
    ],
    "priority": "normal"
  }'

# النتيجة: {"id": "REQ-1", "status": "pending", ...}

# 2. الموافقة على الطلب
curl -X POST http://localhost:3001/api/supply/requests/REQ-1/approve

# النتيجة: {"status": "approved", "transfer": {"id": "TRN-1", ...}}

# 3. تحديث التحويل إلى "جاهز للشحن"
curl -X PUT http://localhost:3001/api/supply/transfers/TRN-1 \
  -H "Content-Type: application/json" \
  -d '{"status": "in_transit"}'

# 4. تحديث التحويل إلى "مُسلّم"
curl -X PUT http://localhost:3001/api/supply/transfers/TRN-1 \
  -H "Content-Type: application/json" \
  -d '{"status": "delivered"}'

# 5. التحقق من تحديث المخزون
curl http://localhost:3001/api/supply/branches/BR002
```

### مثال 2: حل مشكلة دعم

```bash
# 1. إنشاء تذكرة دعم
curl -X POST http://localhost:3001/api/supply/tickets \
  -H "Content-Type: application/json" \
  -d '{
    "fromBranch": "BR003",
    "category": "supply",
    "description": "نفاد المجهزات الطبية - حالة طوارئ",
    "priority": "urgent"
  }'

# النتيجة: {"id": "TKT-1", "status": "open"}

# 2. إضافة تعليق بالحل المقترح
curl -X POST http://localhost:3001/api/supply/tickets/TKT-1/comments \
  -H "Content-Type: application/json" \
  -d '{
    "author": "نور العجمي",
    "comment": "نحتاج إلى 500 وحدة من الضمادات بشكل فوري"
  }'

# 3. إضافة تعليق بالحل
curl -X POST http://localhost:3001/api/supply/tickets/TKT-1/comments \
  -H "Content-Type: application/json" \
  -d '{
    "author": "أحمد الراشد",
    "comment": "تم إرسال 600 وحدة من الفرع الرئيسي - تحويل TRN-2"
  }'

# 4. حل التذكرة
curl -X POST http://localhost:3001/api/supply/tickets/TKT-1/resolve \
  -H "Content-Type: application/json" \
  -d '{
    "resolution": "تم توفير الإمدادات المطلوبة - الشحنة قيد الطريق"
  }'
```

---

## الخطأ والاستكشاف

### المشاكل الشائعة والحلول

#### ❌ مشكلة: "فرع غير موجود"

```
السبب: معرّف فرع خاطئ
الحل: تأكد من استخدام BR001, BR002, BR003, أو BR004

curl http://localhost:3001/api/supply/branches
```

#### ❌ مشكلة: "مخزون غير كافي"

```
السبب: الكمية المطلوبة أكثر من المتاح
الحل: 
1. تحقق من المخزون الحالي
2. اطلب كمية أقل
3. اطلب من فرع آخر

curl http://localhost:3001/api/supply/branches/BR001
```

#### ❌ مشكلة: "طلب قيد الانتظار"

```
السبب: الطلب لم يتم الموافقة عليه بعد
الحل: استخدم POST .../approve لتحويل الطلب

curl -X POST http://localhost:3001/api/supply/requests/REQ-1/approve
```

#### ❌ مشكلة: "تحويل غير موجود"

```
السبب: معرّف التحويل خاطئ
الحل: تحقق من معرّف التحويل من الطلب المعتمد

curl http://localhost:3001/api/supply/system-status
```

---

## 📈 إحصائيات النظام

### البيانات الأولية

```
عدد الفروع: 4
إجمالي سعة التخزين: 1700 وحدة
إجمالي المخزون: ~41,500 وحدة
فئات المخزون: 4 (طبية، معدات، أدوية، مكتب)
عدد أصناف المخزون: 14

مقاييس الأداء:
- معدل التسليم: 95%
- دقة الطلبات: 98%
- وقت الاستجابة: 4.5 ساعة
- رضا العملاء: 4.7/5.0
```

---

## ✅ قائمة المراجعة النهائية

```
المتطلبات الأساسية:
- [ ] Node.js 14+ مثبت
- [ ] ملفات النظام الأساسية موجودة
- [ ] الاختبارات تعمل بنجاح (19/19)

التكامل:
- [ ] المسارات مضافة في app.js
- [ ] المنافذ معرّفة بشكل صحيح
- [ ] معالجات الأخطاء جاهزة

الاختبار:
- [ ] جميع نقاط النهاية تعمل
- [ ] البيانات تحفظ بشكل صحيح
- [ ] الأخطاء تُتعامل معها بشكل سليم

الإنتاج:
- [ ] النظام مستقر
- [ ] الأداء مقبول
- [ ] التوثيق محدث
```

---

## 🎯 الخطوات التالية

1. **التكامل الكامل:** دمج النظام مع التطبيق الرئيسي
2. **إضافة قاعدة البيانات:** من الذاكرة إلى MongoDB/PostgreSQL
3. **التحسينات المتقدمة:** نظام الإشعارات، الأتمتة الذكية
4. **الواجهة الأمامية:** تصميم واجهة استخدام شاملة
5. **الأمان:** إضافة المصادقة والتفويض

---

**Version:** 4.0.0  
**Status:** ✅ Production Ready  
**Last Updated:** January 22, 2026
