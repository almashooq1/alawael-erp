# 🚀 دليل النشر الشامل - Railway/Hostinger/Vercel

## 📋 جدول المحتويات

1. [النشر على Railway](#النشر-على-railway)
2. [النشر على Hostinger VPS](#النشر-على-hostinger-vps)
3. [النشر على Vercel (Frontend)](#النشر-على-vercel)
4. [إعداد HTTPS و CDN](#إعداد-https-و-cdn)
5. [استكشاف الأخطاء](#استكشاف-الأخطاء)

---

## النشر على Railway

### المتطلبات:

- حساب GitHub
- حساب Railway

### الخطوات:

#### 1️⃣ إنشاء حساب على Railway

```
1. اذهب إلى: https://railway.app
2. اضغط "Deploy Now"
3. سجل باستخدام GitHub
4. اسمح بالوصول إلى المستودعات
```

#### 2️⃣ إنشاء Project

```bash
# عند الدخول إلى لوحة التحكم:
1. اضغط "+ New Project"
2. اختر "Deploy from GitHub repo"
3. اختر مستودعك
4. اضغط "Deploy"
```

#### 3️⃣ إعداد المتغيرات البيئية

في لوحة التحكم Railway:

```
✏️ اضغط على السهم > Variables
```

أضف المتغيرات التالية:

```env
NODE_ENV=production
PORT=3001
MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net/appdb
JWT_SECRET=your-super-secret-key-here
CORS_ORIGIN=https://yourdomain.com
REDIS_URL=redis://yourdomain.redis.com:6379
```

#### 4️⃣ ربط قاعدة البيانات MongoDB

```
1. في Railway > اضغط "+ New"
2. اختر "MongoDB"
3. سيتم ربط تلقائياً
4. انسخ MONGODB_URI
```

#### 5️⃣ النشر التلقائي

```
1. جعل Railway يراقب GitHub
2. كل push إلى main → نشر تلقائي
3. عرض السجلات: Build > Logs
```

**مثال على التثبيت الكامل:**

```bash
# في repository local
git add .
git commit -m "Deploy to Railway"
git push origin main

# Railway سيكتشف التغييرات تلقائياً
# اذهب إلى: https://yourrailwayapp.up.railway.app
```

---

## النشر على Hostinger VPS

### المتطلبات:

- خادم VPS (Hostinger/Linode/DigitalOcean)
- SSH Access
- Node.js 22+
- PM2

### الخطوات التفصيلية:

#### 1️⃣ إعداد السيرفر

```bash
# اتصل بـ SSH
ssh root@your-server-ip

# تحديث النظام
apt update && apt upgrade -y

# تثبيت Node.js
curl -fsSL https://deb.nodesource.com/setup_22.x | sudo -E bash -
apt install -y nodejs

# تثبيت PM2
npm install -g pm2

# تثبيت Nginx (بروكسي عكسي)
apt install -y nginx

# تثبيت Certbot (SSL)
apt install -y certbot python3-certbot-nginx

# تثبيت MongoDB (اختياري)
apt install -y mongodb

# تشغيل MongoDB
systemctl enable mongodb
systemctl start mongodb
```

#### 2️⃣ إنشاء مجلد التطبيق

```bash
# إنشاء المجلد
mkdir -p /var/www/app
cd /var/www/app

# نسخ المشروع
git clone https://github.com/yourname/yourrepo.git .

# تثبيت الـ Dependencies
npm install
cd backend && npm install && cd ..
cd frontend && npm install && cd ..

# بناء Frontend
cd frontend
npm run build
cd ..
```

#### 3️⃣ إعداد متغيرات البيئة

```bash
# إنشاء .env
nano /var/www/app/.env.production
```

```env
NODE_ENV=production
PORT=3001
MONGODB_URI=mongodb://localhost:27017/app
JWT_SECRET=your-super-secret-key-generate-this
CORS_ORIGIN=https://yourdomain.com
HTTPS_ENABLED=true
```

#### 4️⃣ إعداد PM2

```bash
# انسخ ecosystem.config.js
cp ecosystem.config.js /var/www/app/

# شغل التطبيق
pm2 start ecosystem.config.js

# احفظ الإعدادات
pm2 save
pm2 startup

# عرض السجلات
pm2 logs

# قائمة التطبيقات
pm2 list
```

#### 5️⃣ إعداد Nginx (البروكسي العكسي)

```bash
# إنشاء ملف Config جديد
nano /etc/nginx/sites-available/yourapp
```

```nginx
server {
    listen 80;
    server_name yourdomain.com www.yourdomain.com;

    # البروكسي للـ Backend
    location /api {
        proxy_pass http://localhost:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # البروكسي للـ Frontend
    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }

    # WebSocket
    location /socket.io {
        proxy_pass http://localhost:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
    }
}
```

#### 6️⃣ فعّل الموقع

```bash
# فعّل الموقع
ln -s /etc/nginx/sites-available/yourapp /etc/nginx/sites-enabled/

# اختبر الإعدادات
nginx -t

# أعد تشغيل Nginx
systemctl restart nginx
```

#### 7️⃣ إعداد SSL/HTTPS

```bash
# احصل على شهادة SSL مجانية
certbot --nginx -d yourdomain.com -d www.yourdomain.com

# سيقوم بـ:
# - التحقق من المالكية
# - تثبيت الشهادة
# - تحديث Nginx تلقائياً
# - تحديد التجديد التلقائي
```

#### 8️⃣ التحديثات التلقائية

```bash
# إضافة webhook في GitHub

# 1. اذهب: Settings > Webhooks
# 2. Add webhook
# 3. Payload URL: https://yourdomain.com/webhook
# 4. اختر: application/json
# 5. اختر: Push events
# 6. اضغط Add webhook

# اختر script التحديث:
nano /var/www/app/webhook.sh
```

```bash
#!/bin/bash
cd /var/www/app
git pull origin main
npm install
cd backend && npm install && cd ..
cd frontend && npm install && npm run build && cd ..
pm2 restart ecosystem.config.js
```

```bash
chmod +x /var/www/app/webhook.sh
```

---

## النشر على Vercel

### للـ Frontend فقط

#### الخطوات:

```
1. اذهب إلى: https://vercel.com
2. اضغط "Import Project"
3. اختر GitHub repo
4. في Framework: اختر "React"
5. في Root Directory: اختر "frontend"
6. في Build Command: npm run build
7. في Output Directory: build
8. أضف Environment Variables:
   - REACT_APP_API_URL=https://api.yourdomain.com
9. اضغط "Deploy"
```

**الأوامر:**

```bash
# المجلد frontend فقط
npm run build

# يتم النشر على CDN عالمي
# أداء سريع جداً
# شهادات SSL تلقائية
```

---

## إعداد HTTPS و CDN

### 1️⃣ HTTPS مع Let's Encrypt

```bash
# تثبيت Certbot
apt install certbot python3-certbot-nginx

# الحصول على شهادة
certbot certonly --nginx -d yourdomain.com

# التجديد التلقائي
certbot renew --dry-run

# ملف Cron للتجديد
0 12 * * * /usr/bin/certbot renew --quiet
```

### 2️⃣ Cloudflare CDN

```
1. اذهب: https://cloudflare.com
2. اضغط "Add Site"
3. أدخل: yourdomain.com
4. اختر Plan: Free
5. انسخ Nameservers
6. غيّر nameservers في Registrar
7. انتظر التفعيل (24 ساعة)
```

**الفوائد:**

- ✅ CDN عالمي
- ✅ DDoS Protection
- ✅ SSL مجاني
- ✅ Caching
- ✅ WAF

### 3️⃣ تحسين الأداء

```nginx
# في Nginx Config
gzip on;
gzip_types text/plain text/css text/js text/xml text/javascript
           application/javascript application/xml+rss application/json;
gzip_min_length 1000;

# Cache Headers
add_header Cache-Control "public, max-age=3600";
add_header X-Content-Type-Options "nosniff";
add_header X-Frame-Options "SAMEORIGIN";
add_header X-XSS-Protection "1; mode=block";
```

---

## استكشاف الأخطاء

### 502 Bad Gateway

```bash
# تحقق من خدمات PM2
pm2 list
pm2 logs

# أعد تشغيل التطبيق
pm2 restart ecosystem.config.js

# اختبر الـ Backend
curl http://localhost:3001/api/health
```

### عدم وجود قاعدة البيانات

```bash
# اختبر MongoDB
mongo --eval "db.adminCommand('ping')"

# أعد تشغيل MongoDB
systemctl restart mongodb

# اختبر الاتصال
mongosh "mongodb://localhost:27017/app"
```

### مشاكل CORS

```bash
# تأكد من CORS_ORIGIN
echo $CORS_ORIGIN

# اختبر الـ Origin
curl -H "Origin: https://yourdomain.com" http://localhost:3001/api
```

### SSL Errors

```bash
# تحقق من الشهادة
openssl x509 -in /etc/ssl/certs/yourdomain.crt -text -noout

# اختبر HTTPS
curl https://yourdomain.com

# تجديد يدوي
certbot renew --force-renewal
```

---

## ✅ قائمة التحقق النهائية

```
✅ قاعدة البيانات متصلة
✅ متغيرات البيئة مضبوطة
✅ HTTPS يعمل
✅ CDN يخدم الملفات الثابتة
✅ WebSocket متصل
✅ البريد الإلكتروني يعمل
✅ السجلات تُحفظ
✅ النسخ الاحتياطية مفعلة
✅ المراقبة نشطة
✅ الأداء محسّن
```

---

## 🎊 تم! نظامك جاهز للإنتاج!

**اتصل بك على:**

- Frontend: https://yourdomain.com
- API: https://api.yourdomain.com
- Admin: https://yourdomain.com/admin
