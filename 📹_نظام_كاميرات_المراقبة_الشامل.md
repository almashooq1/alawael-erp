# 📹 نظام إدارة كاميرات المراقبة - دليل شامل

**آخر تحديث:** 16 يناير 2026  
**الإصدار:** 1.0.0 - Final  
**الحالة:** 🟢 جاهز للإنتاج

---

## 📋 جدول المحتويات

1. [نظرة عامة](#نظرة-عامة)
2. [المميزات](#المميزات)
3. [البنية المعمارية](#البنية-المعمارية)
4. [التثبيت والإعداد](#التثبيت-والإعداد)
5. [دليل الاستخدام](#دليل-الاستخدام)
6. [APIs](#apis)
7. [استكشاف الأخطاء](#استكشاف-الأخطاء)

---

## 🎯 نظرة عامة

نظام متقدم لإدارة ومراقبة كاميرات Hikvision عبر عدة فروع مع:

- ✅ عدد فروع قابل للإضافة والتحديث
- ✅ عدد كاميرات مفتوح لكل فرع
- ✅ ربط كامل مع Hikvision API
- ✅ حفظ الفيديو تلقائياً على AWS S3
- ✅ لوحة تحكم مركزية موحدة
- ✅ كشف الحركة والتنبيهات
- ✅ تقارير وإحصائيات شاملة

---

## 🚀 المميزات

### 1. إدارة الفروع

```
✓ إنشاء/تحديث/حذف الفروع
✓ إدارة المستخدمين والأذونات
✓ إحصائيات مفصلة لكل فرع
✓ إعدادات التخزين والحفظ
```

### 2. إدارة الكاميرات

```
✓ إضافة عدد غير محدود من الكاميرات
✓ ربط Hikvision تلقائي
✓ اختبار الاتصال الفوري
✓ الحصول على معلومات الجهاز
✓ التحكم بجودة البث
```

### 3. البث المباشر

```
✓ بث RTSP مباشر
✓ دعم HTTP و WebSocket
✓ تشفير البث
✓ تنبيهات الحركة الفورية
```

### 4. التسجيل والحفظ

```
✓ تسجيل تلقائي 24/7
✓ جدولة التسجيل حسب الأوقات
✓ رفع تلقائي على السحابة
✓ ضغط الفيديو
```

### 5. التخزين السحابي

```
✓ AWS S3 متعدد الأجزاء
✓ Google Cloud Storage
✓ حفظ آمن مشفر
✓ إدارة النسخ الاحتياطية
```

### 6. الكشف عن الحركة

```
✓ تفعيل/تعطيل فوري
✓ ضبط الحساسية
✓ تنبيهات فورية
✓ تسجيل الأحداث
```

---

## 🏗️ البنية المعمارية

```
Backend (Node.js/Express)
├── Models
│   ├── Branch.js (الفروع)
│   ├── Camera.js (الكاميرات)
│   └── Recording.js (التسجيلات)
├── Services
│   ├── hikvisionService.js (تكامل Hikvision)
│   └── cloudStorageService.js (السحابة)
└── Routes
    ├── branchRoutes.js (APIs الفروع)
    └── cameraRoutes.js (APIs الكاميرات)

Frontend (React)
└── Components
    └── CameraDashboard.js (لوحة التحكم)

Database
└── MongoDB
    ├── branches collection
    ├── cameras collection
    └── recordings collection
```

---

## 🔧 التثبيت والإعداد

### 1. متطلبات النظام

```bash
✓ Node.js 14+
✓ MongoDB 4.4+
✓ AWS S3 Account (للتخزين السحابي)
✓ كاميرات Hikvision مع الوصول للشبكة
```

### 2. تثبيت الحزم

```bash
# Backend
cd backend
npm install axios aws-sdk @google-cloud/storage mongoose

# Frontend
cd frontend
npm install @mui/material @mui/icons-material
```

### 3. إعداد متغيرات البيئة

```bash
# .env.production

# Database
MONGODB_URI=mongodb+srv://user:pass@cluster.mongodb.net/cameras

# AWS S3
AWS_ACCESS_KEY_ID=your_access_key
AWS_SECRET_ACCESS_KEY=your_secret_key
AWS_REGION=us-east-1
S3_BUCKET_NAME=your-bucket-name

# Google Cloud (اختياري)
GOOGLE_PROJECT_ID=your_project
GOOGLE_KEY_PATH=/path/to/key.json

# Server
PORT=3001
NODE_ENV=production
```

### 4. تشغيل البرنامج

```bash
# تطوير
npm run dev

# إنتاج
npm start

# اختبارات
npm test
```

---

## 📖 دليل الاستخدام

### إضافة فرع جديد

```javascript
POST /api/branches
{
  "name": "الفرع الرئيسي",
  "code": "HQ",
  "location": {
    "address": "شارع النيل",
    "city": "الرياض"
  },
  "contact": {
    "phone": "+966501234567",
    "manager": "محمد أحمد"
  }
}
```

### إضافة كاميرا Hikvision

```javascript
POST /api/cameras
{
  "name": "كاميرا المدخل",
  "branchId": "60d5ec49c1234567890abcd",
  "hikvision": {
    "ipAddress": "192.168.1.100",
    "port": 8000,
    "username": "admin",
    "password": "12345",
    "cameraIndex": 1,
    "resolution": "1080p"
  },
  "recording": {
    "enabled": true,
    "quality": "high"
  },
  "cloudSettings": {
    "uploadEnabled": true,
    "cloudProvider": "aws-s3"
  }
}
```

### اختبار الاتصال

```javascript
POST / api / cameras / { cameraId } / test - connection;
```

### الحصول على البث المباشر

```javascript
GET /api/cameras/{cameraId}/stream
// النتيجة:
{
  "url": "rtsp://admin:pass@192.168.1.100:8000/Streaming/Channels/1/",
  "protocol": "rtsp"
}
```

### تفعيل كشف الحركة

```javascript
PUT /api/cameras/{cameraId}/motion-detection
{
  "enabled": true,
  "sensitivity": 50
}
```

---

## 🔌 APIs المتاحة

### Branches APIs

| الطريقة | المسار                            | الوصف                  |
| ------- | --------------------------------- | ---------------------- |
| GET     | `/api/branches`                   | الحصول على جميع الفروع |
| GET     | `/api/branches/:id`               | الحصول على فرع محدد    |
| POST    | `/api/branches`                   | إنشاء فرع جديد         |
| PUT     | `/api/branches/:id`               | تحديث فرع              |
| DELETE  | `/api/branches/:id`               | حذف فرع                |
| POST    | `/api/branches/:id/users`         | إضافة مستخدم           |
| DELETE  | `/api/branches/:id/users/:userId` | إزالة مستخدم           |
| GET     | `/api/branches/:id/statistics`    | إحصائيات الفرع         |

### Cameras APIs

| الطريقة | المسار                              | الوصف          |
| ------- | ----------------------------------- | -------------- |
| GET     | `/api/cameras`                      | جميع الكاميرات |
| GET     | `/api/cameras/:id`                  | كاميرا محددة   |
| POST    | `/api/cameras`                      | إنشاء كاميرا   |
| PUT     | `/api/cameras/:id`                  | تحديث الكاميرا |
| DELETE  | `/api/cameras/:id`                  | حذف الكاميرا   |
| POST    | `/api/cameras/:id/test-connection`  | اختبار الاتصال |
| GET     | `/api/cameras/:id/device-info`      | معلومات الجهاز |
| GET     | `/api/cameras/:id/stream`           | رابط البث      |
| PUT     | `/api/cameras/:id/motion-detection` | إعدادات الحركة |
| POST    | `/api/cameras/:id/capture-snapshot` | حفظ لقطة       |

---

## 📊 أمثلة عملية

### مثال 1: عرض جميع كاميرات الفرع

```javascript
const getCamerasOfBranch = async branchId => {
  const response = await fetch(`/api/cameras?branchId=${branchId}`, {
    headers: { Authorization: `Bearer ${token}` },
  });
  const data = await response.json();
  console.log(`عدد الكاميرات: ${data.data.length}`);
  return data.data;
};
```

### مثال 2: مراقبة حالة الاتصال

```javascript
const monitorConnections = async cameras => {
  for (const camera of cameras) {
    const response = await fetch(`/api/cameras/${camera._id}/test-connection`, {
      method: 'POST',
    });
    const result = await response.json();
    console.log(`${camera.name}: ${result.success ? '✅ متصل' : '❌ غير متصل'}`);
  }
};
```

### مثال 3: رفع التسجيلات على السحابة

```javascript
const uploadToCloud = async recording => {
  const cloudService = require('../services/cloudStorageService');
  const result = await cloudService.uploadToS3(recording.filePath, recording.filename, {
    cameraId: recording.cameraId,
    branchId: recording.branchId,
  });
  if (result.success) {
    console.log(`✅ تم الرفع: ${result.url}`);
  }
};
```

---

## 🐛 استكشاف الأخطاء

### المشكلة: فشل الاتصال بالكاميرا

```
الحل:
1. تحقق من IP والبورت
2. تأكد من اسم المستخدم وكلمة المرور
3. تحقق من وجود جدار الحماية
4. اختبر الاتصال من الجهاز مباشرة: ping 192.168.1.100
```

### المشكلة: الفيديو لا يتم رفعه للسحابة

```
الحل:
1. تحقق من بيانات AWS S3
2. تأكد من صلاحيات الدلو (Bucket Permissions)
3. تحقق من حجم الملف والاتصال الإنترنت
4. راجع السجلات: npm test
```

### المشكلة: استهلاك عالي للبيانات

```
الحل:
1. قلل جودة البث (720p بدلاً من 4K)
2. قلل عدد الاتصالات المتزامنة
3. فعّل الضغط (compression)
4. استخدم جدولة التسجيل بدلاً من 24/7
```

---

## 🔒 الأمان

### أفضل الممارسات

```
✅ استخدم HTTPS دائماً
✅ شفّر كلمات المرور في قاعدة البيانات
✅ استخدم JWT للمصادقة
✅ حدّد صلاحيات الوصول للأدوار
✅ راقب سجلات الوصول
✅ حدّث Hikvision Firmware باستمرار
```

---

## 📈 الأداء

### التحسينات المطبقة

```
✓ تخزين مؤقت للبيانات (Caching)
✓ ضغط الفيديو التلقائي
✓ الرفع المتعدد الأجزاء
✓ معالجة غير متزامنة
✓ فهارس MongoDB
```

### معايير الأداء المتوقعة

```
- وقت البث المباشر: < 1 ثانية
- وقت رفع الملف: يعتمد على الحجم والإنترنت
- استهلاك الذاكرة: 500MB - 2GB
- CPU Usage: 15-30%
```

---

## 📞 الدعم والمساعدة

### للمزيد من المعلومات:

1. الدليل الكامل: `README.md`
2. أمثلة العمل: `examples/`
3. اختبارات: `tests/`

### التواصل:

- 📧 البريد: support@example.com
- 💬 الدعم: support.example.com
- 📱 الهاتف: +966501234567

---

**آخر تحديث: 16 يناير 2026**  
**النسخة: 1.0.0 Final**  
🎉 **نظام متكامل وجاهز للإنتاج!**
