# 🕐 نظام الحضور والانصراف الذكي - دليل الاستخدام والتطوير

> نظام متكامل احترافي لإدارة الحضور والانصراف للموظفين مع ميزات ذكية متقدمة

---

## 📋 جدول المحتويات

1. [نظرة عامة](#نظرة-عامة)
2. [المميزات الرئيسية](#المميزات-الرئيسية)
3. [البنية التقنية](#البنية-التقنية)
4. [دليل التثبيت](#دليل-التثبيت)
5. [دليل الاستخدام](#دليل-الاستخدام)
6. [API Documentation](#api-documentation)
7. [أمثلة عملية](#أمثلة-عملية)
8. [استكشاف الأخطاء](#استكشاف-الأخطاء)

---

## 🎯 نظرة عامة

نظام الحضور والانصراف الذكي هو حل متكامل يوفر:

- **تسجيل الحضور والانصراف**: بتقنيات متعددة (GPS، صور، بصمة، PIN)
- **إدارة الإجازات**: طلب الإجازات والموافقات والرصيد
- **الساعات الإضافية**: حساب وتتبع الإضافي تلقائياً
- **التقارير الذكية**: تقارير شهرية وسنوية وشاملة
- **الإحصائيات والتحليلات**: معدلات الحضور والالتزام
- **التنبيهات**: إشعارات التأخير والتنبيهات المهمة

---

## ✨ المميزات الرئيسية

### 1. الحضور والانصراف الذكي
```
✓ تسجيل فوري مع توقيت دقيق
✓ تحديد الموقع الجغرافي (GPS)
✓ التقاط الصور للتحقق من الهوية
✓ كشف التأخير تلقائياً
✓ حساب ساعات العمل الفعلية
✓ رصد الانصراف المبكر
```

### 2. إدارة الإجازات المتقدمة
```
✓ أنواع إجازات متعددة:
  - إجازة سنوية (30 يوم)
  - إجازة مرضية (15 يوم)
  - إجازة استثنائية (5 أيام)
  - إجازة أمومة/أبوة
✓ نظام الموافقات المتسلسل
✓ تحديث الرصيد تلقائياً
✓ نقل الأيام غير المستخدمة
```

### 3. حسابات الرواتب الذكية
```
✓ حساب الساعات الإضافية
✓ تطبيق معدلات الإضافي
✓ خصومات التأخير التلقائية
✓ حسابات أيام الإجازة المدفوعة
✓ تقارير الرواتب الشهرية
```

### 4. التقارير والتحليلات
```
✓ تقارير شهرية مفصلة
✓ تقارير سنوية شاملة
✓ إحصائيات حسب القسم
✓ معدلات الحضور والالتزام
✓ تحليل أنماط الحضور
✓ تنبيهات التأخير المتكرر
```

---

## 🏗️ البنية التقنية

### النماذج (Models)

#### 1. AttendanceRecord - سجل الحضور
```javascript
{
  employeeId,        // معرف الموظف
  checkInTime,       // وقت الحضور
  checkOutTime,      // وقت الانصراف
  checkInLocation,   // موقع الحضور (GPS)
  checkOutLocation,  // موقع الانصراف
  status,            // حاضر / غياب / متأخر / إجازة
  latenessMinutes,   // دقائق التأخير
  workDuration,      // ساعات العمل الفعلية
  overtimeMinutes,   // دقائق الإضافي
  verificationMethod, // طريقة التحقق
  createdAt,         // تاريخ الإنشاء
}
```

#### 2. Leave - الإجازات
```javascript
{
  employeeId,     // معرف الموظف
  leaveType,      // نوع الإجازة
  startDate,      // تاريخ البداية
  endDate,        // تاريخ النهاية
  duration,       // عدد الأيام
  status,         // مرسل / قيد المراجعة / موافق / مرفوض
  approvedBy,     // الموظف الذي وافق
  isPaidLeave,    // هل الإجازة مدفوعة
}
```

#### 3. LeaveBalance - رصيد الإجازات
```javascript
{
  employeeId,
  year,
  annualLeaveAllocation,    // 30 يوم
  annualLeaveUsed,          // أيام مستخدمة
  annualLeaveRemaining,     // أيام متبقية
  sickLeaveAllocation,      // 15 يوم
  exceptionalLeaveAllocation, // 5 أيام
}
```

#### 4. MonthlyReport - التقرير الشهري
```javascript
{
  employeeId,
  year,
  month,
  totalDaysPresent,
  totalDaysAbsent,
  totalDaysLate,
  totalWorkHours,
  totalOvertimeHours,
  baseSalary,
  overtimeAmount,
  deductions,
  totalSalary,
}
```

---

## 📦 دليل التثبيت

### 1. تثبيت المكتبات المطلوبة

```bash
# في مجلد Backend
cd erp_new_system/backend
npm install

# المكتبات المضافة:
# - mongoose (قاعدة البيانات)
# - express (الخادم)
# - axios (الطلبات HTTP)
```

### 2. تسجيل المسارات في الخادم

```javascript
// في app.js أو server.js
const attendanceRoutes = require('./routes/attendance');
app.use('/api/attendance', attendanceRoutes);
app.use('/api/leave', attendanceRoutes);
app.use('/api/reports', attendanceRoutes);
```

### 3. تشغيل قاعدة البيانات

```bash
# تأكد من أن MongoDB يعمل
mongod

# أو استخدم MongoDB Atlas
# اضبط MONGO_URI في .env
```

### 4. إضافة مكون React

```bash
# نسخ المكون إلى مجلد components
cp AttendanceSystem.jsx src/components/
cp AttendanceSystem.css src/components/

# استيراد المكون
import AttendanceSystem from './components/AttendanceSystem';
```

---

## 👥 دليل الاستخدام

### للموظفين

#### تسجيل الحضور
1. افتح التطبيق في الصباح
2. اضغط على "تسجيل الحضور"
3. ستظهر الكاميرا - سيتم التقاط صورتك تلقائياً
4. سيتم حفظ الموقع الجغرافي
5. سترى رسالة تأكيد بنجاح التسجيل

```
✓ تم تسجيل الحضور بنجاح
الوقت: 09:15 AM
الموقع: تم تحديده بدقة
```

#### تسجيل الانصراف
1. في نهاية يوم العمل، اضغط على "الانصراف"
2. سيتم التقاط صورة أخرى
3. حفظ الموقع الجديد
4. سترى ملخص ساعات العمل

```
✓ تم تسجيل الانصراف بنجاح
ساعات العمل: 8.5 ساعة
إضافي: 0.5 ساعة
```

#### طلب إجازة
1. انتقل إلى تبويب "الإجازات"
2. اضغط على "طلب إجازة جديدة"
3. اختر نوع الإجازة من القائمة
4. حدد التاريخ من والتاريخ إلى
5. اكتب السبب (اختياري)
6. اضغط على "إرسال الطلب"

```
نوع الإجازة: إجازة سنوية
من: 15/3/2026
إلى: 17/3/2026
السبب: أغراض شخصية
```

#### عرض السجلات
1. اذهب إلى تبويب "السجلات"
2. اختر نطاق التاريخ
3. سترى جميع سجلاتك بالتفاصيل

```
14/03/2026 - حاضر
  الحضور: 09:00 AM
  الانصراف: 05:30 PM
  ساعات: 8.5
  ملاحظة: في الوقت
```

#### عرض التقارير
1. اذهب إلى تبويب "التقارير"
2. اختر الفترة الزمنية
3. سترى إحصائيات مفصلة:
   - نسبة الحضور
   - إجمالي ساعات العمل
   - الإضافي
   - معدل الالتزام بالموعد

### للمديرين والمشرفين

#### المراجعة والموافقة
```bash
GET /api/leave/pending
# الحصول على جميع طلبات الإجازات المعلقة

PUT /api/leave/approve/:leaveId
# الموافقة أو الرفض
```

#### الإدخال اليدوي
```javascript
POST /api/attendance/manual-entry
{
  employeeId: "...",
  date: "2026-03-15",
  checkInTime: "09:00:00",
  checkOutTime: "17:30:00",
  reason: "حضور استثنائي",
  notes: "ملاحظات إضافية"
}
```

#### التقارير الإدارية
```bash
GET /api/reports/department-daily?date=2026-03-15
# تقرير يومي لجميع موظفي القسم

GET /api/reports/annual/:employeeId?year=2026
# التقرير السنوي لموظف معين
```

---

## 📡 API Documentation

### 1. الحضور والانصراف

#### تسجيل الحضور
```
POST /api/attendance/check-in
Content-Type: application/json

{
  "employeeId": "507f1f77bcf86cd799439011",
  "location": {
    "latitude": 24.7136,
    "longitude": 46.6753,
    "accuracy": 25
  },
  "photo": "data:image/jpeg;base64,...",
  "verificationMethod": "تطبيق الجوال",
  "deviceId": "WEB_DEVICE_001"
}

Response (200):
{
  "success": true,
  "message": "تم تسجيل الحضور بنجاح",
  "data": { ... },
  "isLate": false
}
```

#### تسجيل الانصراف
```
POST /api/attendance/check-out
Content-Type: application/json

{
  "employeeId": "507f1f77bcf86cd799439011",
  "location": { ... },
  "photo": "data:image/jpeg;base64,..."
}

Response (200):
{
  "success": true,
  "message": "تم تسجيل الانصراف بنجاح",
  "workDuration": 8.5,
  "overtime": 30
}
```

#### الحصول على السجلات
```
GET /api/attendance/records/:employeeId?startDate=2026-03-01&endDate=2026-03-31&status=حاضر

Response (200):
{
  "success": true,
  "count": 20,
  "data": [ ... ]
}
```

#### حالة الحضور اليومية
```
GET /api/attendance/daily-status/:employeeId

Response (200):
{
  "success": true,
  "data": {
    "checkedIn": true,
    "checkedOut": false,
    "checkInTime": "2026-03-15T09:00:00Z",
    "status": "حاضر",
    "isLate": false,
    "latenessMinutes": 0
  }
}
```

### 2. الإجازات

#### طلب إجازة
```
POST /api/leave/request
Content-Type: application/json

{
  "employeeId": "507f1f77bcf86cd799439011",
  "leaveType": "إجازة سنوية",
  "startDate": "2026-04-01",
  "endDate": "2026-04-05",
  "reason": "أغراض شخصية",
  "isPaidLeave": true
}

Response (201):
{
  "success": true,
  "message": "تم إرسال طلب الإجازة بنجاح",
  "data": { ... }
}
```

#### الموافقة على الإجازة
```
PUT /api/leave/approve/:leaveId
Content-Type: application/json

{
  "approvedBy": "507f1f77bcf86cd799439012",
  "reject": false
}

Response (200):
{
  "success": true,
  "message": "تم تحديث حالة الإجازة",
  "data": { ... }
}
```

#### الحصول على رصيد الإجازات
```
GET /api/leave/balance/:employeeId

Response (200):
{
  "success": true,
  "data": {
    "year": 2026,
    "annualLeaveAllocation": 30,
    "annualLeaveUsed": 5,
    "annualLeaveRemaining": 25,
    "sickLeaveRemaining": 15,
    ...
  }
}
```

### 3. التقارير

#### التقرير الشهري
```
POST /api/reports/monthly
Content-Type: application/json

{
  "employeeId": "507f1f77bcf86cd799439011",
  "year": 2026,
  "month": 3
}

Response (201):
{
  "success": true,
  "data": {
    "totalDaysPresent": 20,
    "totalDaysAbsent": 1,
    "totalWorkHours": 160,
    "totalOvertimeHours": 5,
    ...
  }
}
```

#### الإحصائيات
```
GET /api/attendance/statistics/:employeeId?months=3

Response (200):
{
  "success": true,
  "data": {
    "period": "آخر 3 أشهر",
    "attendance": {
      "present": 60,
      "absent": 2,
      "late": 3
    },
    "quality": {
      "attendanceRate": "96.77",
      "punctualityRate": "95.16"
    }
  }
}
```

---

## 💡 أمثلة عملية

### مثال 1: تسجيل الحضور من التطبيق

```javascript
import axios from 'axios';

const registerCheckIn = async () => {
  try {
    // الحصول على الموقع
    const position = await new Promise((resolve, reject) => {
      navigator.geolocation.getCurrentPosition(resolve, reject);
    });

    // التقاط الصورة
    const photo = await capturePhoto();

    // إرسال الطلب
    const response = await axios.post('/api/attendance/check-in', {
      employeeId: localStorage.getItem('employeeId'),
      location: {
        latitude: position.coords.latitude,
        longitude: position.coords.longitude,
        accuracy: position.coords.accuracy
      },
      photo,
      verificationMethod: 'تطبيق الجوال'
    });

    console.log('✓ تم تسجيل الحضور:', response.data);
  } catch (error) {
    console.error('✗ خطأ:', error.message);
  }
};
```

### مثال 2: طلب إجازة من الواجهة

```javascript
const requestLeave = async (formData) => {
  try {
    const response = await axios.post('/api/leave/request', {
      employeeId: localStorage.getItem('employeeId'),
      leaveType: formData.type,
      startDate: formData.start,
      endDate: formData.end,
      reason: formData.reason
    });

    alert('✓ تم إرسال الطلب بنجاح');
  } catch (error) {
    alert('✗ ' + error.response.data.message);
  }
};
```

### مثال 3: الحصول على التقرير الشهري

```python
import requests

def get_monthly_report(employee_id, year, month):
    response = requests.get(
        f'/api/reports/monthly/{employee_id}',
        params={'year': year}
    )
    
    data = response.json()
    
    print(f"تقرير شهر {month}/{year}")
    print(f"أيام الحضور: {data['data'][month]['total_days_present']}")
    print(f"ساعات العمل: {data['data'][month]['total_work_hours']}")
    print(f"الإضافي: {data['data'][month]['total_overtime_hours']}")
```

---

## 🔧 استكشاف الأخطاء

### المشكلة: "لم يتم الحصول على الموقع"

**الحل:**
1. تأكد من تفعيل خدمة الموقع في الهاتف
2. اسمح للتطبيق بالوصول إلى الموقع
3. استخدم اتصال إنترنت قوي

### المشكلة: "خطأ في التقاط الصورة"

**الحل:**
1. تأكد من وجود كاميرا في الجهاز
2. اسمح للتطبيق بالوصول للكاميرا
3. تحقق من إضاءة الغرفة

### المشكلة: "لا يوجد رصيد إجازات"

**الحل:**
1. تحقق من السنة الحالية
2. تأكد من تحديث البيانات
3. اتصل بمدير الموارد البشرية

### المشكلة: "عدم ظهور السجلات"

**الحل:**
1. تحقق من تواريخ الفترة المحددة
2. تأكد من وجود سجلات للموظف
3. تحديث الصفحة (F5)

---

## 📈 النصائح والأفضليات

### ✅ أفضل الممارسات

1. **تسجيل يومي منتظم**: سجل الحضور في نفس الوقت يومياً
2. **استخدام الكاميرا**: التقط صورة واضحة في إضاءة جيدة
3. **موقع دقيق**: قف في موقع ثابت عند التسجيل
4. **طلبات الإجازة المبكرة**: قدم الطلب قبل الموعد بوقت كافي
5. **متابعة الرصيد**: راقب رصيد إجازاتك بانتظام

### ⚠️ ملاحظات مهمة

- **لا يمكن تسجيل الحضور مرتين**: كل يوم تسجيل واحد فقط
- **التأخير يؤثر على الراتب**: تأخير أكثر من 5 دقائق يُسجل كتأخير
- **الإجازة المرفوضة**: لا تؤثر على الرصيد
- **الإجازات المدفوعة**: تخصم من الرصيد السنوي
- **الانصراف المبكر**: قد يؤثر على حسابات الرواتب

---

## 📞 الدعم والمساعدة

للحصول على المساعدة:
- 📧 البريد الإلكتروني: support@company.com
- 📱 الهاتف: +966-12-3456789
- 💬 شات مباشر في التطبيق
- 📖 المرجع الكامل: [الروابط]

---

## 📄 الترخيص والشروط

جميع الحقوق محفوظة © 2026

الاستخدام محصور على الموظفين المصرح لهم فقط.
