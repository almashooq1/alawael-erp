# 🧪 اختبار الميزات الذكية - Quick Test

## ✅ تشغيل الاختبارات

جميع النقاط أدناه يمكن اختبارها عبر Postman أو curl

---

## 🎯 مجموعة الاختبارات 1: التنبؤات

### 1️⃣ توقع احتمالية الترقية

```bash
curl -X POST http://localhost:3001/api/organization/ai/predict-promotion \
  -H "Content-Type: application/json" \
  -d '{
    "position": "مدير مشروع",
    "experience": 5,
    "performance": 4.5
  }'
```

**النتيجة المتوقعة:** احتمالية ترقية 90%+ مع توصية "جاهز للترقية"

---

### 2️⃣ توقع مخاطر الاستقالة

```bash
curl -X POST http://localhost:3001/api/organization/ai/predict-turnover \
  -H "Content-Type: application/json" \
  -d '{
    "salary": 3500,
    "satisfaction": 3.2,
    "experience": 2
  }'
```

**النتيجة المتوقعة:** مستوى خطر عالي مع إجراءات فورية

---

### 3️⃣ تحليل تأثير التدريب

```bash
curl -X POST http://localhost:3001/api/organization/ai/training-impact \
  -H "Content-Type: application/json" \
  -d '{
    "position": "مهندس برمجيات",
    "currentSkills": 60,
    "targetSkills": 90
  }'
```

**النتيجة المتوقعة:** تحسن متوقع 85% مع ROI عالي

---

### 4️⃣ توقعات الميزانية

```bash
curl -X POST http://localhost:3001/api/organization/ai/forecast-budget \
  -H "Content-Type: application/json" \
  -d '{
    "positions": 150,
    "avgSalary": 4000,
    "growthRate": 0.1
  }'
```

**النتيجة المتوقعة:** إجمالي الميزانية حوالي 8.75 مليون ريال

---

## 🎯 مجموعة الاختبارات 2: المطابقة والتحليل

### 5️⃣ مطابقة المرشح بالوظيفة

```bash
curl -X POST http://localhost:3001/api/organization/ai/match-candidate \
  -H "Content-Type: application/json" \
  -d '{
    "employee": {
      "name": "فاطمة احمد",
      "skills": ["قيادة", "تدريب", "إدارة"],
      "experience": 6
    },
    "job": {
      "title": "مديرة إدارة",
      "requiredSkills": ["قيادة", "إدارة مشاريع"],
      "minExperience": 5
    }
  }'
```

**النتيجة المتوقعة:** نسبة ملاءمة 88%+

---

### 6️⃣ كفاءة الفريق

```bash
curl -X POST http://localhost:3001/api/organization/ai/team-efficiency \
  -H "Content-Type: application/json" \
  -d '{
    "team": [
      { "name": "أحمد", "tasksCompleted": 25, "quality": 4.2 },
      { "name": "فاطمة", "tasksCompleted": 28, "quality": 4.5 }
    ]
  }'
```

**النتيجة المتوقعة:** كفاءة الفريق 85%+

---

### 7️⃣ تحليل فجوات المهارات

```bash
curl -X POST http://localhost:3001/api/organization/ai/skill-gaps \
  -H "Content-Type: application/json" \
  -d '{
    "department": "تقنية المعلومات",
    "requiredSkills": ["Python", "JavaScript", "DevOps", "AI/ML"]
  }'
```

**النتيجة المتوقعة:** قائمة بالمهارات الناقصة حسب الأولوية

---

### 8️⃣ رؤى القسم الذكية

```bash
curl -X POST http://localhost:3001/api/organization/ai/department-insights \
  -H "Content-Type: application/json" \
  -d '{
    "deptId": "DEPT_004",
    "metrics": {
      "productivity": 85,
      "satisfaction": 80,
      "turnover": 5
    }
  }'
```

**النتيجة المتوقعة:** رؤى شاملة عن أداء القسم

---

## 🎯 مجموعة الاختبارات 3: التوصيات والتطوير

### 9️⃣ خطة التوظيف

```bash
curl -X POST http://localhost:3001/api/organization/ai/hiring-plan \
  -H "Content-Type: application/json" \
  -d '{
    "department": "قسم التأهيل",
    "currentStaff": 45,
    "projectedGrowth": 60
  }'
```

**النتيجة المتوقعة:** خطة توظيف مع أعداد ووقت التنفيذ

---

### 🔟 تخطيط الخلافة

```bash
curl -X POST http://localhost:3001/api/organization/ai/succession-plan \
  -H "Content-Type: application/json" \
  -d '{
    "position": { "title": "مدير عام" },
    "team": [
      { "name": "علي", "readiness": 0.85 },
      { "name": "نور", "readiness": 0.70 }
    ]
  }'
```

**النتيجة المتوقعة:** قائمة بالخلفاء المحتملين مع تقييم الجاهزية

---

### 1️⃣1️⃣ خطة التطوير الشخصية

```bash
curl -X POST http://localhost:3001/api/organization/ai/development-plan \
  -H "Content-Type: application/json" \
  -d '{
    "employee": "أحمد محمد",
    "currentPosition": "مهندس",
    "targetPosition": "مدير فني",
    "timeline": 12
  }'
```

**النتيجة المتوقعة:** خطة متعددة المراحل مع أهداف محددة

---

## 🎯 مجموعة الاختبارات 4: تقييم الأداء والرواتب

### 1️⃣2️⃣ نظام تقييم الأداء

```bash
curl -X POST http://localhost:3001/api/organization/ai/performance-score \
  -H "Content-Type: application/json" \
  -d '{
    "employee": {
      "name": "أحمد علي",
      "tasksCompleted": 25,
      "qualityRating": 4.5,
      "teamworkRating": 4.2,
      "attendance": 96,
      "trainingsCompleted": 4
    }
  }'
```

**النتيجة المتوقعة:** نقاط أداء 92 من 100 مع تقدير "ممتاز"

---

### 1️⃣3️⃣ تحليل الرواتب

```bash
curl -X POST http://localhost:3001/api/organization/ai/salary-analysis \
  -H "Content-Type: application/json" \
  -d '{
    "position": "محلل نظم",
    "currentSalary": 4500,
    "experience": 5,
    "performance": 4
  }'
```

**النتيجة المتوقعة:** راتب مقترح مع نسبة تعديل 25-35%

---

## 🎯 مجموعة الاختبارات 5: التحسين

### 1️⃣4️⃣ توزيع الموارد الذكي

```bash
curl -X POST http://localhost:3001/api/organization/ai/resource-allocation \
  -H "Content-Type: application/json" \
  -d '{
    "departments": [
      { "id": "DEPT_001", "staff": 30, "budget": 500000 },
      { "id": "DEPT_002", "staff": 25, "budget": 450000 }
    ]
  }'
```

**النتيجة المتوقعة:** تحليل التوزيع مع توصيات التحسين

---

### 1️⃣5️⃣ اقتراحات توفير التكاليف

```bash
curl -X POST http://localhost:3001/api/organization/ai/cost-savings \
  -H "Content-Type: application/json" \
  -d '{
    "currentSpending": 2000000,
    "category": "operations"
  }'
```

**النتيجة المتوقعة:** فرص توفير مع نسبة توفير مرتفعة

---

## 🎯 مجموعة الاختبارات 6: لوحات المعلومات

### 1️⃣6️⃣ لوحة المعلومات الشاملة

```bash
curl -X GET http://localhost:3001/api/organization/ai/dashboard
```

**النتيجة المتوقعة:** بيانات شاملة عن صحة المنظمة

---

### 1️⃣7️⃣ تقييم صحة المنظمة

```bash
curl -X GET http://localhost:3001/api/organization/ai/organization-health
```

**النتيجة المتوقعة:** تقييم 360 درجة للمنظمة

---

### 1️⃣8️⃣ تدفق الأحداث الحي

```bash
curl -X GET http://localhost:3001/api/organization/ai/events-stream
```

**النتيجة المتوقعة:** آخر أحداث المنظمة مرتبة بالوقت

---

## 📊 معايير النجاح

✅ **جميع الطلبات تعود 200 OK**
✅ **البيانات منطقية ومتسقة**
✅ **الرسائل العربية تظهر بشكل صحيح**
✅ **أوقات الاستجابة أقل من 500ms**
✅ **معالجة الأخطاء تعمل بشكل صحيح**

---

## 🐛 استكشاف الأخطاء

### خطأ "Cannot POST /api/organization/ai/..."

- تأكد من تشغيل الخادم: `npm start`
- تحقق من المنفذ 3001

### خطأ "Invalid JSON"

- تأكد من صحة صيغة JSON
- استخدم `Content-Type: application/json`

### نتائج غير متوقعة

- تحقق من قيم المعاملات
- اطلع على سجل الخادم للأخطاء

---

## 📋 قائمة الاختبارات الموصى بها

| الترتيب | الاختبار        | الأولوية | الوقت    |
| ------- | --------------- | -------- | -------- |
| 1       | لوحة المعلومات  | عالية    | 1 دقيقة  |
| 2       | توقع الترقية    | عالية    | 2 دقيقة  |
| 3       | تقييم الأداء    | عالية    | 2 دقيقة  |
| 4       | مطابقة المرشح   | متوسطة   | 2 دقيقة  |
| 5       | خطة التطوير     | متوسطة   | 3 دقائق  |
| 6       | جميع الاختبارات | منخفضة   | 20 دقيقة |

---

**آخر تحديث:** 17 يناير 2026
**الحالة:** ✅ جاهز للاختبار
