# 🧪 دليل اختبار نظام الموارد البشرية

## ✅ قائمة الاختبار الشاملة

### المرحلة 1: الاختبارات الأساسية (30 دقيقة)

#### 1.1 اختبار التكامل الأساسي

```
✓ تثبيت المكتبات
  npm install (في backend و frontend)

✓ تشغيل قاعدة البيانات
  MongoDB يجب أن يكون يعمل

✓ تشغيل الخوادم
  Backend: npm start (localhost:5000)
  Frontend: npm start (localhost:3000)

✓ لا توجد أخطاء في Console
```

---

#### 1.2 اختبار واجهات برمجية الموظفين (5 دقائق)

استخدم Postman أو curl للاختبار:

```bash
# 1. إنشاء موظف جديد
POST http://localhost:5000/api/hr/employees/create
Header: Authorization: Bearer YOUR_TOKEN
Body:
{
  "userId": "5f7b1c2d3e4f5g6h7i8j9k0l",
  "personalInfo": {
    "firstName": "محمد",
    "lastName": "أحمد",
    "email": "ahmed@test.com",
    "phone": "+966501234567"
  },
  "jobInfo": {
    "department": "IT",
    "position": "Software Engineer",
    "employmentType": "permanent",
    "joinDate": "2024-01-15",
    "salary": 5000
  }
}

# النتيجة المتوقعة:
# Status: 201
# {
#   "success": true,
#   "message": "Employee profile created successfully",
#   "data": { /* profile data */ }
# }
```

```bash
# 2. البحث عن الموظفين
GET http://localhost:5000/api/hr/employees/search/profiles?department=IT&status=active
Header: Authorization: Bearer YOUR_TOKEN

# النتيجة المتوقعة:
# Status: 200
# {
#   "success": true,
#   "count": 5,
#   "data": [...]
# }
```

```bash
# 3. إضافة مؤهل علمي
POST http://localhost:5000/api/hr/employees/{employeeId}/add-qualification
Header: Authorization: Bearer YOUR_TOKEN
Body:
{
  "degree": "بكالوريوس",
  "field": "علوم الحاسوب",
  "institution": "جامعة الملك سعود",
  "graduationDate": "2020-06-15",
  "gpa": 3.8
}

# النتيجة المتوقعة:
# Status: 200
# {
#   "success": true,
#   "message": "Qualification added successfully",
#   "data": { /* profile data */ }
# }
```

---

#### 1.3 اختبار واجهات برمجية التقييمات (5 دقائق)

```bash
# 1. إنشاء دورة تقييم
POST http://localhost:5000/api/performance/create
Header: Authorization: Bearer YOUR_TOKEN
Body:
{
  "employeeId": "{employeeId}",
  "evaluationPeriod": {
    "startDate": "2024-01-01",
    "endDate": "2024-03-31"
  }
}

# النتيجة المتوقعة:
# Status: 201
# {
#   "success": true,
#   "message": "Evaluation created successfully",
#   "data": { /* evaluation data */ }
# }
```

```bash
# 2. إضافة تقييم من الإدارة
POST http://localhost:5000/api/performance/{evaluationId}/management-evaluation
Header: Authorization: Bearer YOUR_TOKEN
Body:
{
  "score": 4.5,
  "scores": [
    { "criteriaName": "المهارات التقنية", "score": 5 },
    { "criteriaName": "القيادة", "score": 4 }
  ],
  "comments": "أداء جيد جداً",
  "strengths": ["قيادة فعالة"],
  "areasForImprovement": ["التواصل"],
  "recommendations": "جاهز للترقية"
}

# النتيجة المتوقعة:
# Status: 200
```

```bash
# 3. إكمال التقييم
PUT http://localhost:5000/api/performance/{evaluationId}/finalize
Header: Authorization: Bearer YOUR_TOKEN
Body:
{
  "executiveSummary": "موظف متميز",
  "keyAchievements": ["إنجاز المشاريع في الموقت"],
  "mainChallenges": ["ضغط العمل العالي"],
  "trainingNeeds": ["مهارات القيادة"],
  "promotionRecommended": true,
  "salaryAdjustmentPercentage": 10
}

# النتيجة المتوقعة:
# Status: 200
# يجب أن تظهر النتائج المحسوبة تلقائياً
```

---

#### 1.4 اختبار واجهات برمجية التعاقب (5 دقائق)

```bash
# 1. إنشاء خطة تعاقب
POST http://localhost:5000/api/succession/create
Header: Authorization: Bearer YOUR_TOKEN
Body:
{
  "positionId": "IT-001",
  "positionTitle": "مدير تقنية المعلومات",
  "department": "IT",
  "currentHolder": "{currentEmployeeId}",
  "requiredCompetencies": [
    {
      "competency": "إدارة الفريق",
      "proficiencyLevel": "advanced",
      "criticality": "critical"
    }
  ]
}

# النتيجة المتوقعة:
# Status: 201
```

```bash
# 2. إضافة مرشح خلافة
POST http://localhost:5000/api/succession/{planId}/add-successor
Header: Authorization: Bearer YOUR_TOKEN
Body:
{
  "candidateId": "{candidateEmployeeId}",
  "readinessLevel": "ready_1_year",
  "readinessPercentage": 75,
  "keyStrengths": ["قيادة فعالة"],
  "developmentNeeds": ["خبرة إدارية"],
  "assessmentComments": "جيد جداً"
}

# النتيجة المتوقعة:
# Status: 200
```

---

### المرحلة 2: اختبار المكونات (الواجهة الأمامية)

#### 2.1 اختبار HRDashboard

```
الخطوات:
1. افتح http://localhost:3000/hr/dashboard
2. تحقق من الرسائل التالية:

✓ يجب أن تظهر الإحصائيات:
  - إجمالي الموظفين: عدد صحيح
  - التقييمات النشطة: عدد صحيح
  - متوسط الأداء: رقم بين 0-5
  - الدورات المعلقة: عدد صحيح
  - المراكز الحرجة: عدد صحيح

✓ يجب أن يظهر جدول الموظفين:
  - الأسماء تظهر بشكل صحيح
  - يمكن البحث عن الموظفين
  - يمكن تصفية حسب القسم
  - يمكن فتح ملف الموظف

✓ يجب أن يكون لا توجد أخطاء في Console
```

---

#### 2.2 اختبار EmployeeProfileManager

```
الخطوات:
1. افتح http://localhost:3000/hr/profiles
2. اختر موظفاً موجوداً

✓ تحقق من التبويبات:
  - تبويب المؤهلات (جدول به المؤهلات)
  - تبويب الشهادات (جدول به الشهادات)
  - تبويب الدورات (جدول بالدورات)
  - تبويب الخبرات (جدول بالخبرات)

✓ اختبر الوظائف:
  - اضغط "إضافة مؤهل جديد"
  - ملأ النموذج
  - اضغط حفظ
  - يجب أن يظهر المؤهل في الجدول

✓ اختبر الحذف:
  - اختر مؤهلاً
  - اضغط حذف
  - تأكد الحذف
  - يجب أن يختفي من الجدول

✓ لا توجد أخطاء في Console
```

---

#### 2.3 اختبار PerformanceEvaluation

```
الخطوات:
1. افتح http://localhost:3000/hr/evaluations
2. اختر تقييماً موجوداً

✓ تحقق من البيانات:
  - تبويب الإدارة يظهر التقييم بشكل صحيح
  - درجة الإدارة تظهر
  - التعليقات والنقاط القوية تظهر

✓ تبويب الزملاء:
  - جدول بجميع تقييمات الزملاء
  - متوسط درجة الزملاء صحيح
  - يمكن إضافة تقييم جديد

✓ تبويب الملخص:
  - الدرجة الإجمالية صحيحة (حسب الأوزان)
  - التصنيف صحيح (ممتاز/جيد/إلخ)
  - التوصيات تظهر بشكل صحيح

✓ لا توجد أخطاء في Console
```

---

#### 2.4 اختبار SuccessionPlanning

```
الخطوات:
1. افتح http://localhost:3000/hr/succession
2. اختر خطة تعاقب

✓ تحقق من المعلومات:
  - مستوى المخاطر يظهر بشكل صحيح
  - نسبة الجاهزية الإجمالية صحيحة
  - قائمة المرشحين تظهر

✓ اختبر الجدول:
  - كل مرشح يظهر مع جاهزيته
  - يمكن عرض خطة التطوير
  - يمكن إضافة مرشح جديد

✓ تحقق من الكفاءات:
  - الكفاءات المطلوبة تظهر
  - مستوى الأهمية صحيح

✓ لا توجد أخطاء في Console
```

---

### المرحلة 3: اختبارات متقدمة (30 دقيقة)

#### 3.1 اختبار الحسابات التلقائية

```bash
# اختبر حساب درجة التقييم الموزونة
# الصيغة: (إدارة×0.4) + (زملاء×0.3) + (مستفيدين×0.2) + (ذاتي×0.1)

مثال:
- تقييم الإدارة: 5 × 0.4 = 2.0
- تقييم الزملاء: 4 × 0.3 = 1.2
- تقييم المستفيدين: 4 × 0.2 = 0.8
- التقييم الذاتي: 4.5 × 0.1 = 0.45
- الإجمالي: 2.0 + 1.2 + 0.8 + 0.45 = 4.45 ✓

التصنيف يجب أن يكون "جيد جداً"
```

```bash
# اختبر حساب الجاهزية
# إذا كان هناك:
# - 2 مرشح متقدم (90%)
# - 1 مرشح متوسط (75%)
# - 0 مرشح ضعيف
# المخاطر: منخفضة ✓

# إذا كان هناك:
# - 0 مرشح جاهز
# - 1 مرشح فقط
# المخاطر: حرجة ⚠️
```

---

#### 3.2 اختبار البحث والتصفية

```bash
# اختبر البحث عن الموظفين
GET /api/hr/employees/search/profiles?keyword=محمد
# يجب أن يعيد جميع الموظفين اسمهم يحتوي على "محمد"

# اختبر التصفية حسب القسم
GET /api/hr/employees/search/profiles?department=IT
# يجب أن يعيد فقط موظفي قسم IT

# اختبر التصفية حسب الحالة
GET /api/hr/employees/search/profiles?status=active
# يجب أن يعيد فقط الموظفين النشيطين
```

---

#### 3.3 اختبار التقارير

```bash
# اختبر تقرير إحصائيات التقييمات
GET /api/performance/reports/statistics?period=Q1-2024

النتيجة المتوقعة:
{
  "totalEvaluations": 45,
  "averageScore": 4.1,
  "ratingDistribution": {
    "excellent": 15,    // ممتاز
    "veryGood": 20,     // جيد جداً
    "good": 8,          // جيد
    "acceptable": 2,    // مقبول
    "poor": 0           // ضعيف
  },
  "promotionRecommendations": 12,
  "trainingNeeded": 18
}
```

```bash
# اختبر تقرير المخاطر
GET /api/succession/reports/risk-assessment

النتيجة المتوقعة:
{
  "totalPositions": 25,
  "criticalRisk": 2,        // مراكز حرجة
  "highRisk": 5,            // مخاطر عالية
  "mediumRisk": 10,         // مخاطر متوسطة
  "lowRisk": 8,             // مخاطر منخفضة
  "noSuccessors": 3,        // لا يوجد خلفاء
  "readySuccessors": 15     // خلفاء جاهزون
}
```

---

### المرحلة 4: اختبار الأخطاء (15 دقيقة)

#### 4.1 اختبار رسائل الخطأ

```bash
# 1. محاولة إنشاء موظف بدون بيانات مطلوبة
POST /api/hr/employees/create
Body: {}

النتيجة المتوقعة:
Status: 400
{
  "success": false,
  "message": "Required fields missing",
  "errors": ["userId is required", "personalInfo is required"]
}
```

```bash
# 2. محاولة الوصول لموظف غير موجود
GET /api/hr/employees/invalid-id

النتيجة المتوقعة:
Status: 404
{
  "success": false,
  "message": "Employee not found"
}
```

```bash
# 3. محاولة الوصول بدون تفويض
GET /api/hr/employees/search/profiles

النتيجة المتوقعة:
Status: 401
{
  "success": false,
  "message": "Unauthorized"
}
```

---

#### 4.2 اختبار التحقق من البيانات

```bash
# اختبر البريد الإلكتروني غير الصحيح
POST /api/hr/employees/create
Body: {
  "userId": "...",
  "personalInfo": {
    "email": "invalid-email"
  }
}

النتيجة المتوقعة:
Status: 400
{
  "success": false,
  "message": "Invalid email format"
}
```

---

### المرحلة 5: اختبار الأداء (20 دقيقة)

#### 5.1 اختبار السرعة

```
اختبر أوقات الاستجابة:

✓ GET /api/hr/employees/search/profiles
  الوقت المتوقع: < 200ms
  مع 1000 موظف: < 500ms

✓ GET /api/performance/reports/statistics
  الوقت المتوقع: < 300ms
  مع 1000 تقييم: < 1000ms

✓ تحميل Dashboard
  الوقت المتوقع: < 1 ثانية
```

---

#### 5.2 اختبار الحمل

```
# استخدم Apache Bench أو JMeter

# اختبر مع 100 طلب متزامن
ab -n 100 -c 10 http://localhost:5000/api/hr/employees/search/profiles

النتيجة المتوقعة:
- معدل النجاح: 100%
- متوسط الوقت: < 500ms
- لا توجد أخطاء 500
```

---

## 📋 نموذج التقرير

```
اختبار نظام الموارد البشرية
التاريخ: [التاريخ]
المختبر: [الاسم]

النتائج:
[ ] واجهات برمجية الموظفين ✓/✗
[ ] واجهات برمجية التقييمات ✓/✗
[ ] واجهات برمجية التعاقب ✓/✗
[ ] مكون Dashboard ✓/✗
[ ] مكون إدارة الملفات ✓/✗
[ ] مكون التقييمات ✓/✗
[ ] مكون التعاقب ✓/✗
[ ] البحث والتصفية ✓/✗
[ ] التقارير ✓/✗
[ ] معالجة الأخطاء ✓/✗

المشاكل المكتشفة:
1. [المشكلة]
2. [المشكلة]

الملاحظات الإضافية:
[الملاحظات]

التوقيع: __________________
التاريخ: __________________
```

---

## ✅ قائمة التحقق قبل الإطلاق

```
[ ] جميع الـ APIs تعمل بدون أخطاء
[ ] جميع المكونات تحمل بدون أخطاء
[ ] البيانات تعرض بشكل صحيح
[ ] الحسابات التلقائية صحيحة
[ ] رسائل الخطأ واضحة
[ ] الوقت استجابة سريع
[ ] لا توجد تحذيرات في Console
[ ] الأمان مفعل (التفويض)
[ ] قاعدة البيانات تعمل بشكل صحيح
[ ] التوثيق مكتمل
```

---

**نصائح الاختبار:**

1. اختبر في أوقات مختلفة من اليوم
2. اختبر مع كميات مختلفة من البيانات
3. اختبر جميع الحالات الحدية
4. وثق جميع المشاكل المكتشفة
5. أعد اختبار بعد كل إصلاح

**تم الإنشاء:** يناير 2025 **الإصدار:** 1.0
